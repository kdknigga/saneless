---
phase: 26-worker-and-web-robustness
plan: 10
type: tdd
wave: 4
depends_on: ["26-01", "26-04", "26-05", "26-06"]
files_modified:
  - src/saneless/web/routes.py
  - src/saneless/web/cache.py
  - tests/test_web.py
  - tests/test_cache.py
  - tests/test_web_errors.py
autonomous: true
requirements: [ROBU-05, ROBU-02, ROBU-08]
decisions: [D-05, D-06, D-10, D-11, D-19]

must_haves:
  truths:
    - "Every route handler is a plain def, so blocking I/O runs on the threadpool; /health answers in under a second while another request is blocked in its I/O (ROBU-05, M-01)"
    - "/health returns 200 {\"status\": \"ok\"} when healthy, and 503 with detail 'job store failing' when degraded or 'worker thread is down' when the thread is dead (D-10)"
    - "Concurrent requests for the same metadata key trigger one Paperless fetch, not a stampede (ROBU-05)"
    - "POST /api/scan with an unknown profile (checked under the profile lock) or a title over 256 characters returns 422 and creates no job row; POST /api/cache/invalidate with a resource other than tags/correspondents returns 422 (ROBU-08, D-19)"
    - "A submit refused because the queue is full returns 429 with Retry-After and records an ERROR row 'Not started: the scan queue was full' marked REJECTED (ROBU-02, D-05)"
    - "A submit refused because the worker is down or degraded returns 503 and records the matching REJECTED row only if the store accepts the write (D-05, D-11)"
    - "After a rejected submit, once the running job ends, the status area shows that job, not the rejection (D-06)"
  artifacts:
    - path: "src/saneless/web/routes.py"
      provides: "def handlers, router comment, MetadataResource Literal, validation, rejection rows, latest_run_job fallback, WorkerHealth /health"
      contains: "latest_run_job()"
    - path: "src/saneless/web/cache.py"
      provides: "MetadataCache.get_or_fetch single-flight"
      contains: "def get_or_fetch("
  key_links:
    - from: "routes.start_scan"
      to: "ScanWorker.has_profile"
      via: "unknown profile -> RequestRejected(UNKNOWN_PROFILE) before create_job"
      pattern: "has_profile\\(profile\\)"
    - from: "routes.start_scan"
      to: "RequestRejected"
      via: "match on SubmitResult -> 429/503"
      pattern: "SubmitResult\\.QUEUE_FULL"
    - from: "routes._current_or_recent_job"
      to: "JobStore.latest_run_job"
      via: "D-17 fallback skipping REJECTED rows"
      pattern: "job_store\\.latest_run_job\\(\\)"
    - from: "routes._get_cached_or_fetch"
      to: "MetadataCache.get_or_fetch"
      via: "single-flight fetch"
      pattern: "get_or_fetch\\("
---

<objective>
Bring the routes up to the phase's contracts: `def` handlers with an honest `/health`, a
single-flight metadata cache, input validation before any row exists, visible 429/503 rejections
recorded in history, and the D-06 status lookup (ROBU-02, ROBU-05, ROBU-08, D-05, D-06, D-11, D-19).

Purpose: M-01 (every `async def` route blocks the event loop on sync httpx/sqlite/worker calls),
N-20 (no input validation), and C-09's user-visible half (queue full). Routes only RAISE
`RequestRejected`; plan 26-05's handlers render. The OOB Scan button and `status_response.html`
are plan 26-11 -- this plan keeps rendering `partials/status.html`.

Output: rewritten `routes.py`, `MetadataCache.get_or_fetch`, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/26-worker-and-web-robustness/26-CONTEXT.md
@.planning/phases/26-worker-and-web-robustness/26-RESEARCH.md
@.planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md
@.planning/phases/26-worker-and-web-robustness/26-01-SUMMARY.md
@.planning/phases/26-worker-and-web-robustness/26-05-SUMMARY.md
@.planning/phases/26-worker-and-web-robustness/26-06-SUMMARY.md
@CLAUDE.md

<interfaces>
From src/saneless/vocabulary.py (26-01): `SubmitResult` (ACCEPTED, QUEUE_FULL, DOWN, DEGRADED); `WorkerHealth`; `worker_health_detail(health) -> str`; `RequestRejection` (QUEUE_FULL, WORKER_DOWN, WORKER_DEGRADED, UNKNOWN_PROFILE, ...); `TITLE_MAX_LENGTH = 256`; `QUEUE_FULL_JOB_ERROR`, `WORKER_DOWN_JOB_ERROR`, `WORKER_DEGRADED_JOB_ERROR`; `ErrorCategory.REJECTED`.
From src/saneless/job.py (26-01): `JobStore.latest_run_job() -> Job | None`; `create_job(profile, title, tags=None, correspondent=None) -> Job`; `finish_job(job_id, state, result=None, error=None, error_category=None)`.
From src/saneless/worker.py (26-04/26-06): `submit(job) -> SubmitResult`; `health -> WorkerHealth`; `profile_names() -> list[str]`; `has_profile(name) -> bool`.
From src/saneless/web/errors.py (26-05): `class RequestRejected(HTTPException)` with `__init__(self, rejection, *, refresh_history=False)`; `RETRY_AFTER_SECONDS = 30`; handlers turn `RequestValidationError` on `("body", "title")` + `string_too_long` into TITLE_TOO_LONG and other validation errors into INVALID_REQUEST.
Current routes.py (read it): 11 `async def` handlers (`index`, `health`, `paperless_test`, `start_scan`, `current_job_status`, `get_tags`, `get_correspondents`, `invalidate_cache`, `job_history`, `continue_flip`, `abort_flip`); `_get_cached_or_fetch(cache, paperless, resource: str)`; `_current_or_recent_job` uses `job_store.list_recent(limit=1)`; `index` reads `state.settings.profiles.keys()`; `start_scan(profile: str = Form(...), title: str = Form(default=""), tags: list[int] = _TAGS_FORM_DEFAULT, correspondent: int | None = Form(default=None))`.
Current cache.py: `MetadataCache(ttl)` with `get`, `set`, `invalidate` over `self._store` (no locking).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: def handlers, an honest /health, and a single-flight cache (ROBU-05)</name>
  <files>src/saneless/web/routes.py, src/saneless/web/cache.py, tests/test_web.py, tests/test_cache.py</files>
  <read_first>
    - src/saneless/web/routes.py (whole file, 410 lines)
    - src/saneless/web/cache.py (65 lines)
    - tests/test_web.py lines 1-160 and 485-642 (fixtures, health tests, paperless tests)
    - tests/test_cache.py (whole file)
    - .planning/phases/26-worker-and-web-robustness/26-RESEARCH.md § Pattern 6 (health-during-block test, no-coroutine test, single-flight code) and § Code Examples "/health with a total enum"
  </read_first>
  <behavior>
    - Structural: no `APIRoute` in `app.routes` has an endpoint for which `inspect.iscoroutinefunction` is True
    - `/api/tags` blocked inside `paperless.get_tags` on a `threading.Event` (cache invalidated first) in a background thread; after 0.2 s, `GET /health` returns 200 in under 1.0 s; the gate is released and the thread joined in `finally`
    - `/health` healthy: 200 `{"status": "ok"}`; with the app's worker `health` forced to DEGRADED (monkeypatch the property on the class for the test, or drive `_degraded` Event): 503 `{"status": "error", "detail": "job store failing"}`; DOWN (worker stopped): 503 `{"status": "error", "detail": "worker thread is down"}`
    - `MetadataCache.get_or_fetch`: 8 threads behind a `threading.Barrier(8)` call `get_or_fetch("tags", fetch)` where `fetch` sleeps 0.1 s and increments a counter: counter == 1 and all 8 results are the same list object
    - A fetch that raises propagates to that caller, caches nothing, and the next `get_or_fetch` calls `fetch` again
    - A fresh cached value is returned without calling `fetch`; after TTL expiry `fetch` is called again
    - `_get_cached_or_fetch` still returns `[]` and logs a WARNING when Paperless raises (unchanged fallback)
  </behavior>
  <action>
    RED (commit `test(26-10): ...`): add `test_no_route_handler_is_a_coroutine`, `test_health_answers_while_a_request_blocks`, `test_health_reports_degraded_worker`, `test_health_reports_down_worker` to tests/test_web.py; add `test_get_or_fetch_single_flight`, `test_get_or_fetch_failure_is_not_cached`, `test_get_or_fetch_returns_fresh_value_without_fetching` to tests/test_cache.py (docstrings "(ROBU-05)", matching the module's one-behaviour-per-function style).

    GREEN (commit `feat(26-10): ...`):
    - src/saneless/web/cache.py: add `self._locks_guard = threading.Lock()` and `self._key_locks: dict[str, threading.Lock] = {}`; `def get_or_fetch(self, key: str, fetch: Callable[[], list[dict[str, object]]]) -> list[dict[str, object]]`: return a fresh `get(key)` if present; else take (setdefault under `_locks_guard`) the per-key lock, re-check `get(key)`, else `data = fetch()`, `set(key, data)`, return. Docstring: single-flight so concurrent threadpool requests do not stampede Paperless (ROBU-05); a raising fetch caches nothing; known limitation: while Paperless is unreachable, waiting threads retry one after another, each paying the connect timeout -- the separate connect timeout and caching the unreachable outcome are deferred (M-01 second half, noted in 26-CONTEXT Deferred Ideas); stale-on-error is SWP-04. `Callable` from `collections.abc` under TYPE_CHECKING.
    - src/saneless/web/routes.py: convert all 11 handlers from `async def` to `def`. Directly above `router = APIRouter()` add a comment: every handler is a plain `def` on purpose -- each calls blocking code (sync httpx to Paperless, sqlite through the job store, the worker), and FastAPI runs `def` handlers on its threadpool, so a slow Paperless call cannot stall `/health` or the status poll (ROBU-05, M-01); shared state they touch is locked (JobStore RLock, worker profile lock, cache per-key locks). `_get_cached_or_fetch` delegates to `cache.get_or_fetch(resource, <fetch callable>)` inside its existing `try/except Exception` WARNING + `[]` fallback.
    - `/health`: `health = request.app.state.worker.health`; HEALTHY -> `{"status": "ok"}`; otherwise `JSONResponse(status_code=503, content={"status": "error", "detail": worker_health_detail(health)})`. Keep `response_model=None`. Docstring: 503 distinguishes a failing store from a dead thread (D-10); Docker's HEALTHCHECK marks the container unhealthy but does not restart it (documented in 26-14).
  </action>
  <verify>
    <automated>uv run pytest tests/test_web.py tests/test_cache.py -q -x && uv run pytest tests/test_web_state_rendering.py tests/test_web_errors.py tests/test_cross_origin.py -q && uv run ruff check src tests && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^async def" src/saneless/web/routes.py` returns 0
    - `grep -n "def get_or_fetch(" src/saneless/web/cache.py` matches once
    - `grep -n "worker_health_detail(" src/saneless/web/routes.py` matches once
    - `uv run pytest tests/test_web.py -q -k "coroutine or health"` selects at least 5 tests and passes
    - `uv run pytest tests/test_cache.py -q -k get_or_fetch` selects 3 tests and passes
    - Gate commands pass; `test(26-10):` precedes `feat(26-10):`
  </acceptance_criteria>
  <done>Routes run off the event loop, /health tells the truth, and the cache fetches once per key.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validation before any row, 429/503 with rejection rows, and the D-06 lookup</name>
  <files>src/saneless/web/routes.py, tests/test_web.py, tests/test_web_errors.py</files>
  <read_first>
    - src/saneless/web/routes.py (as left by Task 1)
    - src/saneless/web/errors.py (26-05)
    - tests/test_web_errors.py (26-05; fixture chain and HX assertions to reuse)
    - tests/test_web.py lines 160-300 (`test_scan_form_submit`, status polling, adopt-as-current idiom at ~181-184, `test_cache_invalidate`)
    - .planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md § S2 `refresh_history`, § S3, § Verification Contract T5, T6, T8
    - .planning/phases/26-worker-and-web-robustness/26-RESEARCH.md § Pattern 2 "Writing the row (D-05)"
  </read_first>
  <behavior>
    - `POST /api/scan` with `profile=zz-marker-<script>`: 422; HX branch body is the UNKNOWN_PROFILE partial with HX-Retarget; JSON branch detail is the UNKNOWN_PROFILE message; neither body contains `zz-marker`; `job_store.list_recent(50)` is unchanged (no row)
    - `POST /api/scan` with a valid profile and a 257-character title (`zz-marker` repeated, truncated to 257): 422 TITLE_TOO_LONG on both branches, body without `zz-marker`, no row; a 256-character title is accepted (200)
    - `POST /api/cache/invalidate?resource=bogus`: 422 INVALID_REQUEST; the cache entry for `tags` is untouched (still present after the call); `?resource=tags` still 200
    - Queue full (monkeypatch the app's `worker.submit` to return `SubmitResult.QUEUE_FULL`), HX request: 429, `Retry-After: 30`, the QUEUE_FULL partial INCLUDING the hidden `/api/jobs/history` loader; the newest row in `list_recent(1)` is ERROR with `error == QUEUE_FULL_JOB_ERROR` and `error_category is ErrorCategory.REJECTED`; non-HX: 429 JSON with Retry-After
    - `submit` returning DOWN: 503 WORKER_DOWN, row `WORKER_DOWN_JOB_ERROR`; DEGRADED: 503 WORKER_DEGRADED, row `WORKER_DEGRADED_JOB_ERROR`
    - Worker `health` already DEGRADED before the request and `job_store.create_job` raising `sqlite3.OperationalError`: still 503 WORKER_DEGRADED, no loader in the partial (row not written), a WARNING with exc_info logged, `submit` never called
    - D-06 (T8): a job adopted as current and SCANNING; a scan rejected with QUEUE_FULL creates a newer REJECTED row; then the running job is finished DONE and `worker._current_job_id` cleared: `GET /api/jobs/current/status` renders that job's `Done:` line, not the queue-full text; `GET /` status area likewise
    - With only REJECTED rows in the store and no current job, the status area renders "Ready to scan."
    - Index profile dropdown lists `worker.profile_names()` (test replaces profiles via `worker._set_profiles` and sees the new option)
  </behavior>
  <action>
    RED (commit `test(26-10): ...`): add the 422/429/503 route tests to tests/test_web_errors.py (names containing `422`, `429`/`queue_full`, `503`) and the D-06/profile/invalidate tests to tests/test_web.py (names containing `rejected`). Docstrings cite ROBU-02, ROBU-08, D-05, D-06, D-11, D-19.

    GREEN (commit `feat(26-10): ...`) in src/saneless/web/routes.py:
    - Runtime imports (FastAPI reads them): `from typing import Annotated, Literal`; `MetadataResource = Literal["tags", "correspondents"]` (module-level alias with a comment: N-20).
    - `index`: `profiles = state.worker.profile_names()` (D-19).
    - `start_scan` signature: `profile: Annotated[str, Form()]`, `title: Annotated[str, Form(max_length=TITLE_MAX_LENGTH)] = ""`, `tags: list[int] = _TAGS_FORM_DEFAULT`, `correspondent: Annotated[int | None, Form()] = None`. Body order:
      1. `if not state.worker.has_profile(profile): raise RequestRejected(RequestRejection.UNKNOWN_PROFILE)` -- before any store call (ROBU-08).
      2. Default the title as today.
      3. If `state.worker.health is not WorkerHealth.HEALTHY`: pick WORKER_DEGRADED/WORKER_DEGRADED_JOB_ERROR or WORKER_DOWN/WORKER_DOWN_JOB_ERROR from the health value (total match), `written = _record_rejected_submit(state.job_store, profile=..., title=..., tags=..., correspondent=..., job_id=None, error=...)`, then `raise RequestRejected(rejection, refresh_history=written)`.
      4. `job = state.job_store.create_job(...)` (a raise here is a genuine 500).
      5. `result = state.worker.submit(job)`; `match result:` ACCEPTED -> render as today; QUEUE_FULL -> QUEUE_FULL + QUEUE_FULL_JOB_ERROR; DOWN -> WORKER_DOWN + WORKER_DOWN_JOB_ERROR; DEGRADED -> WORKER_DEGRADED + WORKER_DEGRADED_JOB_ERROR; `case _: assert_never(result)`. For the three rejections, `written = _record_rejected_submit(state.job_store, job_id=job.id, error=...)` then `raise RequestRejected(rejection, refresh_history=written)`. Use single assignment per arm and one raise after the match to respect PLR0911/PLR0912; extract helpers if ruff still reports complexity.
    - `_record_rejected_submit(job_store, *, job_id: str | None, profile, title, tags, correspondent, error: str) -> bool`: inside `try`, create the row when `job_id is None`, then `finish_job(id, JobState.ERROR, error=error, error_category=ErrorCategory.REJECTED)`; return True; `except Exception: logger.warning("Could not record the rejected scan in job history", exc_info=True); return False`. Docstring: D-05 departs from C-09's "create the row only after enqueue" because the user wants the attempt visible; the row must exist before `put_nowait` anyway, or the worker could dequeue an id with no row; the REJECTED marker is what keeps it out of the status area (D-06).
    - `_current_or_recent_job`: replace `list_recent(limit=1)` with `job_store.latest_run_job()`; extend the docstring with D-06 (a row rejected at submit never ran, so it cannot be "the job that just ended"; D-17's contract is unchanged). `index` and `job_history` keep `list_recent(limit=50)` so rejections appear in history.
    - `invalidate_cache(request: Request, resource: MetadataResource)`; `_get_cached_or_fetch(..., resource: MetadataResource)`.
    - Update the `start_scan` docstring: 422 before any row; 429 with Retry-After, 503 when down or degraded; the response no longer claims "returns immediately after queuing" semantics beyond what is true (it returns once the job is queued or refused).
  </action>
  <verify>
    <automated>uv run pytest tests/test_web_errors.py tests/test_web.py -q -x && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check src tests && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "job_store.latest_run_job()" src/saneless/web/routes.py` matches once and `grep -n "list_recent(limit=1)" src/saneless/web/routes.py` returns nothing
    - `grep -n "Form(max_length=TITLE_MAX_LENGTH)" src/saneless/web/routes.py` matches once
    - `grep -n 'MetadataResource = Literal\["tags", "correspondents"\]' src/saneless/web/routes.py` matches once
    - `grep -n "state.settings.profiles" src/saneless/web/routes.py` returns nothing
    - `grep -n "assert_never(result)" src/saneless/web/routes.py` matches once
    - `uv run pytest tests/test_web_errors.py -q -k "422 or 429 or queue_full or 503"` selects at least 8 tests and passes; `uv run pytest tests/test_web.py -q -k rejected` selects at least 2 tests and passes
    - Gate commands pass; `test(26-10):` precedes `feat(26-10):`
  </acceptance_criteria>
  <done>Bad input never creates a job, refused submits are visible rejections recorded in history, and the status area still reports the job that actually ran.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated LAN client -> POST /api/scan, /api/cache/invalidate | untrusted form and query values |
| request threads -> worker, store, Paperless | blocking calls under concurrency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-40 | Denial of service | async routes blocking the event loop (M-01) | mitigate | All handlers `def`; structural coroutine test; /health-during-block test |
| T-26-41 | Denial of service | cache stampede against Paperless | mitigate | Per-key single-flight lock; 8-thread barrier test |
| T-26-42 | Tampering / DoS | unbounded title, arbitrary profile, arbitrary resource (N-20) | mitigate | `Form(max_length=256)`, profile allow-list under the D-19 lock, `Literal` resource; 422 before any row; no-row tests |
| T-26-43 | Tampering (XSS) / Info disclosure | echoing a submitted profile/title in the 422 | mitigate | Constant messages via the 26-05 renderer; `zz-marker` absence tests on both branches |
| T-26-44 | Denial of service | queue exhaustion by a LAN client | accept | Bounded queue returns 429 with Retry-After; trusted-LAN model (no auth) is a project decision; each rejection is recorded for visibility |
| T-26-45 | Repudiation | refused submits leave no trace | mitigate | REJECTED ERROR row written (D-05), loader refreshes history |
| T-26-SC | Tampering | package installs | accept | None |
</threat_model>

<verification>
- `uv run pytest tests/test_web.py tests/test_web_errors.py tests/test_cache.py -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- gate commands
</verification>

<success_criteria>
- Roadmap success criterion 3 (health during scan, no stampede, no mid-iteration mutation) holds by test
- ROBU-08 holds by test; ROBU-02's server half holds by test (browser visibility is 26-13)
</success_criteria>

<output>
Create `.planning/phases/26-worker-and-web-robustness/26-10-SUMMARY.md` when done
</output>
