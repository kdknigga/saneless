---
phase: 26-worker-and-web-robustness
plan: 09
type: tdd
wave: 4
depends_on: ["26-01", "26-04", "26-06", "26-07"]
files_modified:
  - src/saneless/web/app.py
  - src/saneless/job.py
  - tests/test_app_lifespan.py
autonomous: true
requirements: [ROBU-06, ROBU-02]
decisions: [D-07, D-09, D-13]

must_haves:
  truths:
    - "At startup every job left in an active state (PENDING included) is ERROR with 'The server restarted before this scan finished' before worker.start() runs (ROBU-06, D-13)"
    - "Startup order is validate dirs -> fail_active_jobs -> prune -> worker.start(); a prune failure is logged and never blocks startup (D-13)"
    - "If crash recovery itself cannot write, the app still starts, the worker starts degraded with recovery pending, and /health reports the store failing (D-13, research Open Question 1)"
    - "At shutdown the worker is stopped first; the Paperless client and job store are closed only if the worker confirmed it stopped (ROBU-06, D-09)"
    - "If the 5 s join expires, a WARNING names the running job id and the store and Paperless client are left open for process exit; no shutdown-time state write is attempted for the abandoned job (D-07, D-09)"
    - "job.py's module docstring and fail_active_jobs's docstring describe crash recovery as it now actually works (doc row 33)"
  artifacts:
    - path: "src/saneless/web/app.py"
      provides: "reordered lifespan with guarded recovery, guarded prune, and guarded close"
      contains: "fail_active_jobs(RESTART_REASON)"
    - path: "tests/test_app_lifespan.py"
      provides: "recovery ordering, recovery failure, prune failure, guarded close tests"
  key_links:
    - from: "lifespan startup"
      to: "JobStore.fail_active_jobs"
      via: "called before worker.start()"
      pattern: "fail_active_jobs\\(RESTART_REASON\\)"
    - from: "lifespan shutdown"
      to: "ScanWorker.stop"
      via: "return value gates paperless.close() and job_store.close()"
      pattern: "if not worker\\.stop\\(\\)"
---

<objective>
Give crash recovery its production caller and make shutdown safe against a live worker (ROBU-06,
M-03, D-07, D-09, D-13).

Purpose: `fail_active_jobs` has no caller, so a crash leaves rows polling "Scanning..." forever;
shutdown closes the store under a possibly-live worker thread ("Cannot operate on a closed
database"). This plan also corrects doc row 33 (`job.py` claimed crash recovery that did not exist).

Output: reordered lifespan in `web/app.py`, docstring corrections in `job.py`,
`tests/test_app_lifespan.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/26-worker-and-web-robustness/26-CONTEXT.md
@.planning/phases/26-worker-and-web-robustness/26-RESEARCH.md
@.planning/phases/26-worker-and-web-robustness/26-06-SUMMARY.md
@CLAUDE.md

<interfaces>
Current lifespan (src/saneless/web/app.py lines 70-84): `validate_settings_dirs(settings)`; `worker.start()`; `job_store.prune(settings.output.history_retention_days, settings.output.history_max_rows)`; `logger.info("App started, old jobs pruned")`; `yield`; `worker.stop()`; `paperless.close()`; `job_store.close()`; `logger.info("App shutdown complete")`.
After 26-05/26-07, `create_app` also calls `install_error_handlers(app)` and `app.add_middleware(CrossOriginGuard)` -- leave those untouched.
From src/saneless/worker.py (26-04/26-06): `STOP_JOIN_SECONDS = 5.0`; `stop() -> bool`; `current_job_id -> str | None`; `mark_recovery_pending() -> None`; `health -> WorkerHealth`.
From src/saneless/vocabulary.py (26-01): `RESTART_REASON`, `WorkerHealth`.
From src/saneless/job.py: module docstring lines 1-7 (says "for crash recovery"); `fail_active_jobs` docstring lines ~820-824 ("This method has NO production caller in this phase ... belongs to Phase 26").
Test idioms: tests/test_web.py fixtures (lines 45-129); a file-backed store via `OutputConfig(tmp_dir=str(tmp_path), data_dir=str(tmp_path))` -- `create_app` opens `settings.output.db_path`, so rows can be seeded by opening `JobStore(db_path=str(settings.output.db_path))`, writing, closing, then building the app.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Startup recovery before the worker; guarded prune; recovery failure starts degraded</name>
  <files>src/saneless/web/app.py, tests/test_app_lifespan.py</files>
  <read_first>
    - src/saneless/web/app.py (whole file, as left by 26-07)
    - src/saneless/worker.py `start`, `stop`, `mark_recovery_pending`, `health` (26-04/26-06)
    - src/saneless/job.py `fail_active_jobs`, `prune`
    - tests/test_web.py lines 45-129 (fixtures to copy)
    - .planning/phases/26-worker-and-web-robustness/26-RESEARCH.md § Pattern 7 and Open Question 1
  </read_first>
  <behavior>
    - Seed a file-backed store with one SCANNING row, one PENDING row, one AWAITING_FLIP row and one DONE row; build the app; enter `TestClient(app)`: the three active rows are ERROR with `error == RESTART_REASON`; the DONE row is unchanged
    - T9: in that seeded case, with the SCANNING row created last (newest), `GET /` renders `#status-area` with `&#10007; Error: The server restarted before this scan finished` (the existing ERROR branch; the status area is not polling), proving the recovered row is what the user sees
    - Ordering proof: monkeypatch `ScanWorker.start` on the app's worker instance with a wrapper that records `job_store.get_job(<seeded SCANNING id>).state` and then calls the original: the recorded state is ERROR
    - A WARNING record says how many interrupted jobs were marked failed (count 3 in the seeded case); with no active rows, no such WARNING is logged
    - `job_store.prune` raising `sqlite3.OperationalError`: the app still starts, `GET /health` answers, and a WARNING with exc_info is logged
    - `job_store.fail_active_jobs` raising `sqlite3.OperationalError`: the app still starts; `worker.health is WorkerHealth.DEGRADED` immediately after startup; an ERROR record with exc_info says crash recovery could not write
    - The prune call happens after `fail_active_jobs` and before `worker.start` (record call order with wrappers)
  </behavior>
  <action>
    RED (commit `test(26-09): ...`): create tests/test_app_lifespan.py (module docstring citing ROBU-06, M-03, D-13, D-09; copies the needed fixtures) with the behaviours above.

    GREEN (commit `feat(26-09): ...`) in src/saneless/web/app.py `lifespan` (startup half):
    - `validate_settings_dirs(settings)`.
    - `try: failed = job_store.fail_active_jobs(RESTART_REASON)` / `except Exception: logger.error("Crash recovery could not update the job store; starting degraded until it accepts writes", exc_info=True); worker.mark_recovery_pending()` / `else: if failed: logger.warning("Marked %d interrupted job(s) as failed at startup", failed)`.
    - `try: job_store.prune(settings.output.history_retention_days, settings.output.history_max_rows)` / `except Exception: logger.warning("Startup history prune failed", exc_info=True)`.
    - `worker.start()`; `logger.info("App started")`.
    - Docstring: D-13 order and why (the worker must never see an orphan as live work; a queued job lost from memory must not scan whatever is in the feeder after a restart); recovery failure starts degraded rather than refusing to start, so `/health` answers a truthful 503 (the worker's first successful probe runs the recovery). Import `RESTART_REASON` from `saneless.vocabulary`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_app_lifespan.py -q -x && uv run pytest tests/test_web.py tests/test_web_state_rendering.py tests/test_web_errors.py tests/test_cross_origin.py -q && uv run ruff check src tests && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - In src/saneless/web/app.py, `grep -n "fail_active_jobs(RESTART_REASON)\|job_store.prune(\|worker.start()" src/saneless/web/app.py` shows the three line numbers in increasing order
    - `grep -n "worker.mark_recovery_pending()" src/saneless/web/app.py` matches once
    - `uv run pytest tests/test_app_lifespan.py -q` passes at least 5 tests
    - Gate commands pass; `test(26-09):` precedes `feat(26-09):`
  </acceptance_criteria>
  <done>Orphans are failed with the restart reason before the worker exists, and startup survives a failing store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guarded close at shutdown, and the job.py docstrings (doc row 33)</name>
  <files>src/saneless/web/app.py, src/saneless/job.py, tests/test_app_lifespan.py</files>
  <read_first>
    - src/saneless/web/app.py (as left by Task 1)
    - src/saneless/job.py lines 1-40 and 792-841
    - .planning/reviews/2026-09-09-code-review.md -- search "doc row 33" / § 8 row 33 and § M-03
  </read_first>
  <behavior>
    - Normal exit: `paperless.close` and `job_store.close` are each called exactly once after `worker.stop` (wrappers record order: stop, then paperless close, then store close); INFO "App shutdown complete"
    - Worker does not stop (monkeypatch the app's `worker.stop` to return False and set `worker._current_job_id = "job-xyz"`): after leaving the `TestClient` context, `paperless.close` and `job_store.close` were NOT called; `job_store.get_job(<an existing id>)` still works; a WARNING record contains `job-xyz` and the number 5 (STOP_JOIN_SECONDS); no store write happened during shutdown (spy on `finish_job`/`update_state` records no call after stop)
    - Real worker, idle: leaving the context closes the store (a later `job_store.get_job` raises `sqlite3.ProgrammingError`)
  </behavior>
  <action>
    RED (commit `test(26-09): ...`): add the three behaviours to tests/test_app_lifespan.py. For the stuck case, close the store manually in the test's own `finally` so the test leaks nothing.

    GREEN (commit `feat(26-09): ...`):
    - src/saneless/web/app.py shutdown half: `if not worker.stop():` -> `logger.warning("Scan worker did not stop within %s s (job %s still running); leaving the job store and Paperless client open for process exit", STOP_JOIN_SECONDS, worker.current_job_id)` and `return`; else `paperless.close()`, `job_store.close()`, `logger.info("App shutdown complete")`. Comment: D-09 -- the store closes only after a confirmed stop, so a stuck thread never hits "Cannot operate on a closed database"; D-07 -- the abandoned job is not written here (that would race its own final write); the next startup's recovery records it. `worker.stop()` blocks the event loop for at most STOP_JOIN_SECONDS during lifespan shutdown, after uvicorn has stopped serving (bounded, D-08). Import `STOP_JOIN_SECONDS` from `saneless.worker`.
    - src/saneless/job.py: rewrite the module docstring lines that promise crash recovery so they describe the fact: the store persists jobs in SQLite, and on startup the web app fails every job still in an active state through `fail_active_jobs` before the worker starts (ROBU-06). Rewrite `fail_active_jobs`'s "NO production caller" paragraph: its caller is the web lifespan at startup (and the worker's recovery after a failed startup recovery, 26-06), passing `RESTART_REASON`. Keep every other sentence. Docstring-only change to job.py.
  </action>
  <verify>
    <automated>uv run pytest tests/test_app_lifespan.py tests/test_job.py -q && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check src tests && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "if not worker.stop():" src/saneless/web/app.py` matches once, and `paperless.close()` / `job_store.close()` appear on later lines
    - `grep -n "NO production caller in this phase" src/saneless/job.py` matches only inside `list_pending` (the `fail_active_jobs` occurrence is gone)
    - `git diff HEAD~1 -- src/saneless/job.py` (for the feat commit) touches only docstring lines
    - `uv run pytest tests/test_app_lifespan.py -q` passes at least 8 tests
    - Gate commands pass; `test(26-09):` precedes `feat(26-09):`
  </acceptance_criteria>
  <done>Shutdown never closes resources under a live worker, and job.py's documentation matches the code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| previous process -> this process | rows left active by a crash |
| lifespan -> worker thread | resources shared across shutdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-36 | Tampering (integrity) | orphaned PENDING job resumed after restart, scanning whatever paper is in the feeder | mitigate | `fail_active_jobs(RESTART_REASON)` before `worker.start()`; ordering test |
| T-26-37 | Denial of service | UI polling an orphan forever | mitigate | Same recovery; status area shows the restart reason |
| T-26-38 | Denial of service | app refuses to start when the store is failing | mitigate | Recovery failure logs and starts degraded; `/health` 503; probe completes recovery |
| T-26-39 | Tampering (integrity) | store closed under a live worker thread | mitigate | Close only when `stop()` returns True (D-09); stuck-worker test |
| T-26-SC | Tampering | package installs | accept | None |
</threat_model>

<verification>
- `uv run pytest tests/test_app_lifespan.py -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- gate commands
</verification>

<success_criteria>
- ROBU-06 both clauses proven by tests
- Doc row 33 corrected
</success_criteria>

<output>
Create `.planning/phases/26-worker-and-web-robustness/26-09-SUMMARY.md` when done
</output>
