---
phase: 26-worker-and-web-robustness
plan: 12
type: execute
wave: 6
depends_on: ["26-03", "26-05", "26-07", "26-09", "26-10", "26-11"]
files_modified:
  - tests/test_browser.py
  - .github/workflows/ci.yml
  - CONTRIBUTING.md
autonomous: true
requirements: [ROBU-11, ROBU-04, ROBU-09]
decisions: []

must_haves:
  truths:
    - "Every browser test runs behind a Playwright route that aborts and records any request not addressed to the test server, and fails if anything was recorded -- offline behaviour is proven in-test, locally and in CI (ROBU-09, ROBU-11)"
    - "A browser test clicks Scan, waits for the terminal status, and asserts #scan-btn is enabled again, reads 'Scan', has no aria-busy, and is the only id=scan-btn in the DOM (ROBU-11, ROBU-04, C-10)"
    - "In the browser, the button is disabled with 'Scanning...' while a job is held in SCANNING, and a page loaded during an active job keeps it disabled after the tags/correspondents load requests finish (hx-disinherit trap)"
    - "app.js is neither referenced by the page nor served (404)"
    - "A CI job named browser installs Chromium with its system dependencies and runs uv run pytest -m browser, so the DARK-01/DARK-02 tests run in CI too"
  artifacts:
    - path: "tests/test_browser.py"
      provides: "context egress override, egress_allowlist fixture, gated _BrowserTestScanner, B2/B3/B4/B5/B6/B7/B14, rewritten fallback-swap test"
      contains: "def context("
    - path: ".github/workflows/ci.yml"
      provides: "browser job"
      contains: "playwright install --with-deps chromium"
    - path: "CONTRIBUTING.md"
      provides: "CI job table and local commands including the browser job"
  key_links:
    - from: "tests/test_browser.py::context"
      to: "every page fixture in the module"
      via: "pytest-playwright page derives from the overridden context"
      pattern: "context\\.route\\(\"\\*\\*/\\*\""
    - from: ".github/workflows/ci.yml browser job"
      to: "tests/test_browser.py"
      via: "uv run pytest -m browser"
      pattern: "pytest -m browser"
---

<objective>
Prove ROBU-04 and ROBU-09 in a real browser with no internet, and run that proof in CI (ROBU-11,
M-33, 23.1 DARK coupling).

Purpose: C-10 shipped because its fix was proven by reading. The browser tests were excluded from
CI because they needed CDN egress; the assets are local now (26-03), so the no-egress guarantee can be
asserted inside every browser test rather than assumed from CI's network. The message-slot browser
tests (B8-B13) and the LAN-IP cross-origin proof are plan 26-13.

Output: browser test module changes, a `browser` CI job, CONTRIBUTING.md updated with it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md
@.planning/phases/26-worker-and-web-robustness/26-RESEARCH.md
@.planning/phases/26-worker-and-web-robustness/26-11-SUMMARY.md
@CLAUDE.md

<interfaces>
tests/test_browser.py (795 lines) today:
- module docstring lines 1-17 and the `_PICO_SURFACE` comment lines 57-69 describe the CDN dependency and "held for phase 26"
- `_BrowserTestScanner(ScannerBackend)` lines 77-105 (`scan_pages` returns one white image)
- `_BrowserServer(NamedTuple)`: `url`, `app`; session fixture `browser_server` (uvicorn on 127.0.0.1:0; profiles `default` and `duplex`) and `browser_server_url`
- load canaries `TestBrowserRendering.test_pico_css_applied` (192-212), `TestHTMXPolling.test_htmx_loaded` (252-257) -- keep
- `_DISABLE_SCAN_BUTTON` (350-357, mimics app.js) and `TestFallbackStatusRendering.test_scan_button_re_enables_after_a_fallback_swap` (661-684, asserts `aria-busy == "false"`) -- rewrite
- `fallback_page` fixture teardown discipline (clear `worker._current_job_id`, `delete_job`) -- copy for new stateful tests
- `TestDarkModeEngagement` comment at 751-753 notes pytest-playwright owns a fixture named `context`
From src/saneless/paperless.py: `class UploadResult` (line ~146; read its fields), `PaperlessClient.upload_document(...)` (~226), `poll_task(task_id, timeout=300)` (~385). The worker holds the same `app.state.paperless` instance, so patching its attributes affects real scans (tests/test_web.py `mock_paperless` does the same).
Playwright python: `BrowserContext.route(url, handler)`, `Route.continue_()`, `Route.abort()`, `route.request.url`; `page.wait_for_response(predicate)`; `expect(locator).to_be_visible(timeout=...)`, `.to_be_enabled()`, `.to_be_disabled()`, `.to_have_attribute(...)`.
ci.yml `test` job (lines 34-50) with pinned SHAs: `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1`, `astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1`, SANE headers step, `uv sync --locked`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The no-egress gate for every browser test, plus the page-structure checks (B0, B2, B3, B4, B14)</name>
  <files>tests/test_browser.py</files>
  <read_first>
    - tests/test_browser.py (whole file)
    - .planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md § Verification Contract B0, B2, B3, B4, B14
    - .planning/phases/26-worker-and-web-robustness/26-RESEARCH.md § Pattern 11 and assumption A3
  </read_first>
  <action>
    - Add a function-scoped fixture `egress_allowlist(browser_server) -> list[str]` returning `[browser_server.url]` (a list later tests may append a URL prefix to).
    - Override pytest-playwright's `context` fixture in this module: `def context(context: BrowserContext, egress_allowlist: list[str]) -> Iterator[BrowserContext]` registering `context.route("**/*", _gate)` where `_gate` continues a request whose URL equals an allowlisted base or starts with `base + "/"`, otherwise appends the URL to `blocked` and aborts; after `yield`, `assert blocked == [], f"the page tried to reach the network: {blocked}"`. If `data:` URLs turn out to reach the handler (assumption A3), continue them explicitly and say so in a comment; do not count them. Docstring: proves the UI needs no internet (ROBU-09) and that every browser test, DARK-01/DARK-02 included, runs offline in CI (ROBU-11).
    - Rewrite the module docstring (lines 1-17) and the `_PICO_SURFACE` comment (57-69): assets are vendored and integrity-pinned (`tests/test_vendor_assets.py` pins the bytes); every test runs behind the egress gate; the CI `browser` job runs this module; a Pico bump is a vendoring change plus these literals. Keep the "read test_pico_css_applied first" advice, re-worded for an SRI failure rather than a CDN outage.
    - New class `TestOfflinePage` (`@pytest.mark.browser`):
      - B2 `test_vendored_htmx_is_the_pinned_version_with_error_swapping`: `page.evaluate("htmx.version") == "2.0.8"`; `htmx.config.responseHandling.length == 3` and the entry with code `[45]..` has `swap === true`; `document.documentElement.hasAttribute("data-theme") === false`; `document.querySelectorAll("link[href*='pico.colors']").length === 0`.
      - B3 `test_app_js_is_gone`: `page.locator("script[src*='app.js']").count() == 0`; `page.request.get(browser_server.url + "/static/app.js").status == 404`.
      - B4 `test_status_message_slot_is_empty_and_zero_height` parametrised over light/dark (`emulate_media` before `goto`): `#status-message` count 1, `role` attribute `alert`, `childNodes.length === 0`, `getBoundingClientRect().height === 0`, `document.querySelectorAll('[id="status-message"]').length === 1`, and the slot precedes `#status-area` in document order (`compareDocumentPosition` has `DOCUMENT_POSITION_FOLLOWING`).
      - B14 `test_title_input_caps_at_256`: `#title-input` has `maxlength="256"`; `fill` with 300 characters then `input_value()` length is 256 (if Playwright's `fill` turns out to bypass `maxlength`, use `press_sequentially`, which types like a user, and note it in a comment).
    - Every new test has a docstring naming its UI-SPEC id and requirement.
    - Commit `test(26-12): ...`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_browser.py -m browser -q && uv run ruff check tests && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def context(" tests/test_browser.py` and `grep -n 'context.route("\*\*/\*"' tests/test_browser.py` each match once
    - `grep -n "cdn.jsdelivr\|held for phase 26" tests/test_browser.py` returns nothing
    - `uv run pytest tests/test_browser.py -m browser -q` passes (the whole module, including the DARK tests, behind the gate) -- except `test_scan_button_re_enables_after_a_fallback_swap`, which Task 2 rewrites; if it fails here, record that and proceed
    - Executor self-check recorded in the SUMMARY (not committed): temporarily restoring the CDN `<script>` tag in base.html makes the gate report the jsdelivr URL
    - Gate commands pass
  </acceptance_criteria>
  <done>No browser test can pass while the page reaches the network.</done>
</task>

<task type="auto">
  <name>Task 2: The Scan button in Chromium -- terminal re-enable, in-flight state, the disinherit trap (B5, B6, B7, ROBU-11)</name>
  <files>tests/test_browser.py</files>
  <read_first>
    - tests/test_browser.py (as left by Task 1), especially `fallback_page` and `test_flip_continue_click_answers_the_waiting_job` (teardown discipline)
    - src/saneless/paperless.py lines 140-260 and 380-400 (`UploadResult`, `upload_document`, `poll_task`)
    - src/saneless/web/templates/partials/scan_button.html and index.html (26-11)
    - .planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md § S4 and § Verification Contract B5, B6, B7
    - .planning/phases/26-worker-and-web-robustness/26-RESEARCH.md § Pattern 11 "ROBU-11 test outline" and Pitfall 7
  </read_first>
  <action>
    - `_BrowserTestScanner`: add `self.gate = threading.Event()` set in `__init__` (open by default) and make `scan_pages` wait on it with a bounded timeout (for example 30 s) before returning; docstring: tests close it to hold a job in SCANNING. Keep the session fixture holding the scanner instance reachable (for example store it on the `_BrowserServer` tuple as a new `scanner` field, or on `app.state` -- whichever keeps ty/pyrefly clean without suppressions).
    - A function-scoped fixture `delivering_paperless(browser_server)` that replaces `app.state.paperless.upload_document` with a callable returning a successful `UploadResult` (fields per paperless.py, delivered to the API with a task id) and `poll_task` with a callable returning None, restoring both originals in `finally`; and a helper that, in teardown, waits for every job the test created to reach a terminal state (poll the store with a bounded loop) then deletes those rows and clears `worker._current_job_id` (identify rows by diffing `list_recent(100)` ids before and after).
    - New class `TestServerOwnedScanButton` (`@pytest.mark.browser`):
      - B5 `test_scan_button_re_enables_after_a_real_scan` (ROBU-11): `page.goto`; assert no `script[src*='app.js']`; click `#scan-btn`; `expect(page.locator("#status-area .status-done")).to_be_visible(timeout=15_000)`; then `expect(#scan-btn).to_be_enabled()`, trimmed text `Scan`, `get_attribute("aria-busy") is None`, and `document.querySelectorAll('[id="scan-btn"]').length === 1`. Docstring cites C-10, ROBU-11 and the roadmap success criterion 5 wording.
      - B6 `test_scan_button_shows_busy_while_the_job_is_held`: close the gate, click Scan; within 3 s `#scan-btn` is disabled with `aria-busy="true"` and text `Scanning…` and there is exactly one `#scan-btn`; open the gate; then B5's terminal assertions hold.
      - B7 `test_page_loaded_during_an_active_job_keeps_the_button_disabled` (hx-disinherit trap, T-26-48): close the gate, submit a scan over HTTP from the test thread (`httpx.post(url + "/api/scan", data={"profile": "default"})`, 200), wait until the store shows SCANNING; then `with page.expect_response(lambda r: "/api/tags" in r.url)` and the same for `/api/correspondents` around `page.goto`; after both responses, `expect(page.locator("#scan-btn")).to_be_disabled()`. Open the gate in `finally`.
    - Rewrite `TestFallbackStatusRendering.test_scan_button_re_enables_after_a_fallback_swap`: delete `_DISABLE_SCAN_BUTTON`; force a stale disabled button with a one-line evaluate (`document.getElementById('scan-btn').disabled = true`), assert disabled, run `_SWAP_STATUS_AREA`, wait for `#scan-btn:not([disabled])`, and assert text `Scan` and `get_attribute("aria-busy") is None`. Docstring: the server's out-of-band button, not JavaScript, releases it (ROBU-04, T-23-23).
    - Executor self-checks recorded in the SUMMARY (UI-SPEC "Required mutation checks", run locally, not committed): removing `hx-disinherit` from index.html makes B7 fail; an htmx-config holding only the `[45]..` entry makes B5 fail.
    - Commit `test(26-12): ...`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_browser.py -m browser -q && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check tests && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_DISABLE_SCAN_BUTTON\|aria-busy\") == \"false\"" tests/test_browser.py` returns nothing
    - `grep -n "def test_scan_button_re_enables_after_a_real_scan\|def test_scan_button_shows_busy_while_the_job_is_held\|def test_page_loaded_during_an_active_job_keeps_the_button_disabled" tests/test_browser.py` returns 3 lines
    - `uv run pytest tests/test_browser.py -m browser -q` passes in full, twice in a row (no order dependence from session state)
    - SUMMARY records both mutation self-checks failing as expected
    - Gate commands pass
  </acceptance_criteria>
  <done>C-10 is proven fixed in Chromium, including the 2.0.8 inheritance trap.</done>
</task>

<task type="auto">
  <name>Task 3: The browser CI job and CONTRIBUTING.md</name>
  <files>.github/workflows/ci.yml, CONTRIBUTING.md</files>
  <read_first>
    - .github/workflows/ci.yml (50 lines)
    - CONTRIBUTING.md lines 50-100 (the six checks table, local commands, the "browser marker deselects" paragraph)
    - .planning/phases/26-worker-and-web-robustness/26-RESEARCH.md § Pattern 11 "CI job"
  </read_first>
  <action>
    - ci.yml: add a `browser` job after `test`, copying the `test` job's header and first four steps verbatim (same pinned SHAs and version comments, `timeout-minutes: 15`, SANE headers step because `uv sync --locked` compiles python-sane), then `- run: uv run playwright install --with-deps chromium` and `- run: uv run pytest -m browser`. Add a short comment: the browser tests run offline behind an in-test egress gate, so this job needs no CDN (ROBU-11); the Chromium download itself is the only network use. The `test` job keeps `-m "not browser and not sane_hardware"`.
    - CONTRIBUTING.md: the section becomes "The seven checks" (update the count in prose), the job table gains `| browser | uv run pytest -m browser |`, the reproduce-locally block gains `uv run playwright install chromium` (once) and `uv run pytest -m browser`, and the paragraph saying browser tests are deselected and run locally only is rewritten: the `test` job deselects them because the `browser` job runs them with Chromium installed; they need no internet. Keep the GitHub-markdown dialect.
    - Do not change repository rulesets or required status checks (a repository-settings decision for the user); note in the SUMMARY that `browser` is not yet a required check.
    - Commit `ci(26-12): ...`.
  </action>
  <verify>
    <automated>uv run python -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']['browser']; runs=[s.get('run','') for s in j['steps']]; assert 'uv run playwright install --with-deps chromium' in runs and 'uv run pytest -m browser' in runs; assert d['jobs']['test']['steps'][-2]['run'].startswith('uv run pytest -m \"not browser')" && uv run prek run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "playwright install --with-deps chromium" .github/workflows/ci.yml` matches once
    - `grep -c "actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1" .github/workflows/ci.yml` returns 3
    - `grep -n "pytest -m browser" CONTRIBUTING.md` matches at least twice
    - `grep -n "Run those\s*locally with .uv run pytest -m browser." CONTRIBUTING.md` returns nothing (old "locally only" text gone)
    - `uv run prek run --all-files` passes (includes any YAML/actions hooks configured)
  </acceptance_criteria>
  <done>CI runs the browser suite on every push and PR; contributors know it does.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser under test -> network | any non-test-server request is an offline-behaviour failure |
| CI runner -> third-party actions / Chromium download | supply chain of the CI job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-50 | Tampering | a regression reintroducing a CDN asset | mitigate | Egress gate asserts zero blocked requests in every browser test |
| T-26-51 | Tampering | CI action supply chain | mitigate | Same full-SHA pins with version comments as existing jobs (dependabot watches github-actions) |
| T-26-52 | Tampering | Chromium binary download in CI | accept | Official `playwright install` from the locked playwright version; same trust as the dev dependency itself |
| T-26-53 | Denial of service (UX) | Scan button stuck disabled (C-10) | mitigate | B5/B6/B7 in CI |
| T-26-SC | Tampering | package installs | accept | No new package; Playwright already a locked dev dependency |
</threat_model>

<verification>
- `uv run pytest -m browser -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- gate commands and `uv run prek run --all-files`
</verification>

<success_criteria>
- Roadmap success criterion 5 holds locally by test and is wired into CI (green CI on the pushed branch is observed at the phase gate)
</success_criteria>

<output>
Create `.planning/phases/26-worker-and-web-robustness/26-12-SUMMARY.md` when done
</output>
