---
phase: 26-worker-and-web-robustness
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/saneless/config.py
  - src/saneless/auto_profiles.py
  - src/saneless/cli.py
  - tests/test_config.py
  - tests/test_auto_profiles.py
autonomous: true
requirements: [ROBU-07]
decisions: [D-16]

must_haves:
  truths:
    - "load_settings records the config file it actually loaded on the returned Settings (`settings.config_path`), or None when no file was found (D-16)"
    - "The config path cannot be set from an environment variable or a TOML key: it is a private attribute, not a field (D-16)"
    - "The three-path config search list exists once, in config.py; auto_profiles no longer carries its own copy (D-16)"
    - "`saneless auto-profiles` writes to the file that was loaded, else ./saneless.toml exactly as before"
    - "is_bare_default recognises every untouched shape of the default profile (no [profiles] section, empty [profiles.default], env-only, env var set to a default value, title spelled via its alias) and rejects the legacy manual-duplex default (ROBU-07 clause 2)"
  artifacts:
    - path: "src/saneless/config.py"
      provides: "config_search_paths(), Settings._config_path PrivateAttr + config_path property, load_settings recording the path"
      contains: "def config_search_paths() -> tuple[Path, ...]"
    - path: "src/saneless/cli.py"
      provides: "auto-profiles writes to settings.config_path"
      contains: "settings.config_path"
    - path: "tests/test_auto_profiles.py"
      provides: "parametrised is_bare_default shapes test"
  key_links:
    - from: "src/saneless/auto_profiles.py::resolve_config_path"
      to: "src/saneless/config.py::config_search_paths"
      via: "iterates the single search list"
      pattern: "config_search_paths\\(\\)"
---

<objective>
Make the config file that was actually loaded a fact carried by `Settings` (D-16), collapse the
duplicated search list to one, and prove `is_bare_default` covers every untouched default shape
(ROBU-07 clause 2, verified by test, not rebuilt).

Purpose: M-04. The worker today re-derives a path (`resolve_config_path()` with no argument) that
ignores `--config`. Plan 26-08 makes startup generation write to `settings.config_path`; this plan
provides that fact. `auto_profiles.resolve_config_path` keeps exactly one production caller after
this plan (the worker's lazy generation), and 26-08 deletes the function together with that caller
and its test class.

Output: `config_search_paths()`, `Settings.config_path`, CLI switched over, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/26-worker-and-web-robustness/26-CONTEXT.md
@.planning/phases/26-worker-and-web-robustness/26-RESEARCH.md
@CLAUDE.md

<interfaces>
From src/saneless/config.py:
- `class Settings(BaseSettings)`: `model_config = SettingsConfigDict(env_prefix="SANELESS_", env_nested_delimiter="__")`;
  fields `scanner`, `paperless`, `output`, `profiles: dict[str, ProfileConfig] = {"default": ProfileConfig()}`
- `def _build_settings(toml_file: Path | None = None) -> Settings` (converts extra_forbidden to ConfigError)
- `def load_settings(config_path: str | None = None) -> Settings` (lines 408-438): explicit path, else
  first existing of `./saneless.toml`, `~/.config/saneless/config.toml`, `/etc/saneless/config.toml`, else no file
- Legacy `# noqa: ARG003` at lines 229-230 predates the no-suppression rule: do not copy it, do not touch it here

From src/saneless/auto_profiles.py:
- `def is_bare_default(settings: Settings) -> bool` (197-223): exactly one profile, named default, `== ProfileConfig()`
- `def resolve_config_path(config_path: str | None = None) -> Path` (425-448): second copy of the search list, falls back to `./saneless.toml`
- `def write_profiles_to_config(config_path, profiles, *, force=False)`

From src/saneless/cli.py (auto-profiles, 469-497): `config_path = resolve_config_path(config_path_str)` where
`config_path_str` comes from `ctx.parent.params.get("config_path")`; `resolve_config_path` imported at line 24.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings.config_path and one search list</name>
  <files>src/saneless/config.py, src/saneless/auto_profiles.py, src/saneless/cli.py, tests/test_config.py</files>
  <read_first>
    - src/saneless/config.py lines 1-60 and 200-438 (imports, Settings, _build_settings, load_settings)
    - src/saneless/auto_profiles.py lines 1-45 and 425-448
    - src/saneless/cli.py lines 1-40 and 469-505
    - tests/test_config.py lines 100-150 (`TestConfigFileSearch`, `monkeypatch.chdir(tmp_path)` idiom)
    - .planning/phases/26-worker-and-web-robustness/26-RESEARCH.md § Pattern 8
  </read_first>
  <behavior>
    - `load_settings(str(tmp_path / "x.toml"))` with that file present: `settings.config_path == tmp_path / "x.toml"`
    - Explicit path that does not exist: `settings.config_path == Path(that string)` (recorded as given; CFG-02 in Phase 27 owns making that an error)
    - `monkeypatch.chdir(tmp_path)` with `./saneless.toml` present and no explicit path: `settings.config_path == Path("saneless.toml")` (the relative search entry as found)
    - `monkeypatch.chdir(tmp_path)`, `monkeypatch.setenv("HOME", str(tmp_path / "home"))` and no files: `settings.config_path is None`
    - `monkeypatch.setenv("SANELESS_CONFIG_PATH", "/tmp/evil.toml")` then `load_settings()` in an empty CWD with HOME redirected: `settings.config_path is None`
    - A TOML file containing a top-level `config_path = "x"` key is rejected exactly as any other unknown top-level key is today (whatever `_build_settings` does now) and never populates `config_path`
    - `Settings()` constructed directly has `config_path is None`
    - `config_search_paths()` returns `(Path("./saneless.toml"), Path.home() / ".config" / "saneless" / "config.toml", Path("/etc/saneless/config.toml"))`, evaluating `Path.home()` at call time (a HOME change after import is honoured)
  </behavior>
  <action>
    RED (commit `test(26-02): ...`): add the behaviours above to tests/test_config.py (new class `TestLoadedConfigPath`, docstrings citing D-16 / M-04). Redirect HOME in every test that relies on "no file found" so a developer's real `~/.config/saneless/config.toml` cannot leak in.

    GREEN (commit `feat(26-02): ...`):
    - src/saneless/config.py: add `def config_search_paths() -> tuple[Path, ...]` (public, in `__all__` if the module has one) returning the three paths in order, docstring: the single search list for both loading and the CLI's write target (D-16); a function rather than a module constant so `Path.home()` is read when called. `load_settings` uses it (`next((p for p in config_search_paths() if p.exists()), None)` or an equivalent loop), builds settings with `_build_settings(toml_file=path)` (None when nothing found), then records the path: `settings._config_path = path` for an explicit path (as `Path(config_path)`) or the found path, None otherwise. Add to `Settings`: `_config_path: Path | None = PrivateAttr(default=None)` (import `PrivateAttr` from pydantic) and a read-only `@property def config_path(self) -> Path | None` with a docstring: the TOML file these settings were loaded from, None when only defaults and environment were used; a PrivateAttr and not a field because a field would be settable from `SANELESS_CONFIG_PATH` and from TOML (research Pattern 8). Update `load_settings`'s docstring (Returns: settings carrying `config_path`).
    - src/saneless/auto_profiles.py: `resolve_config_path` iterates `config_search_paths()` instead of its own list (import it from `.config`/`saneless.config`, matching the module's existing import style); keep its signature and `./saneless.toml` fallback. Add a docstring sentence: its last production caller is the worker's lazy generation, removed in plan 26-08.
    - src/saneless/cli.py `auto_profiles`: delete `config_path_str` and the `resolve_config_path` import; use `config_path = settings.config_path or Path("./saneless.toml")` with a comment: the file that was loaded (including an explicit `--config`), else today's documented `./saneless.toml` default (research Open Question 4; XDG placement is CFG-03 in Phase 27). Import `Path` if not already imported.
    - If pyrefly or ty object to assigning a PrivateAttr from outside the class, add a small `Settings._record_config_path(self, path: Path | None) -> None` method used by `load_settings` instead -- no suppression.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py tests/test_auto_profiles.py tests/test_cli.py -q -x && uv run ruff check src tests && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def config_search_paths() -> tuple\[Path, ...\]" src/saneless/config.py` matches once
    - `grep -n "PrivateAttr" src/saneless/config.py` matches at least twice (import and attribute)
    - `grep -n "def config_path(self) -> Path | None" src/saneless/config.py` matches once
    - `grep -c 'Path("/etc/saneless/config.toml")' src/saneless/config.py src/saneless/auto_profiles.py` totals 1 (only config.py)
    - `grep -n "resolve_config_path" src/saneless/cli.py` returns nothing
    - `uv run pytest tests/test_config.py -q -k "LoadedConfigPath or config_path"` selects at least 7 tests and passes
    - Gate commands pass; `test(26-02):` precedes `feat(26-02):`
  </acceptance_criteria>
  <done>Settings knows which file it came from; nothing can forge that fact; one search list remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: is_bare_default shapes table (ROBU-07 clause 2)</name>
  <files>tests/test_auto_profiles.py</files>
  <read_first>
    - tests/test_auto_profiles.py lines 1-40 and 332-411 (`TestIsBareDefault`, `TestResolveConfigPath`)
    - src/saneless/auto_profiles.py lines 197-223 (`is_bare_default`)
    - src/saneless/config.py lines 95-146 (`ProfileConfig`, `title` alias, legacy manual-duplex translation)
    - .planning/phases/26-worker-and-web-robustness/26-RESEARCH.md § Pattern 8 "is_bare_default shapes to cover"
  </read_first>
  <behavior>
    - TOML file with no `[profiles]` section at all, loaded through `load_settings(path)`: True
    - TOML with an empty `[profiles.default]` table: True
    - Env-only, no file (`load_settings()` in empty CWD with HOME redirected): True
    - `SANELESS_PROFILES__DEFAULT__RESOLUTION` set to the `DEFAULT_RESOLUTION` value, no file: True (research assumption A1; if pydantic-settings yields an unequal model, the test records the observed False with a comment explaining why, and the SUMMARY reports the assumption was wrong -- the production function is not changed to accommodate it)
    - `[profiles.default]` with `title = ""` written via the alias: True
    - `[profiles.default]` with legacy `source = "Manual Duplex"`: False (translated to `duplex = "manual"`)
  </behavior>
  <action>
    This is a verification test over existing behaviour (CONTEXT: "verified by a test covering the shapes, not rebuilt"), so RED may pass on first run. Write one `@pytest.mark.parametrize` test `test_untouched_default_shapes` in `TestIsBareDefault`, ids naming each shape, each case writing its TOML (or env) into `tmp_path` and loading through `load_settings` so the real loading path produces the shape. Docstring cites ROBU-07 and WR-04. If every case passes on first run, commit once as `test(26-02): pin is_bare_default untouched shapes` and state in the SUMMARY that no production change was needed. If a case fails, stop and fix `is_bare_default` only if the failure is a genuine untouched shape it rejects (commit `fix(26-02): ...`), keeping full-model equality.
    Leave `TestResolveConfigPath` in place (26-08 deletes it with the function).
  </action>
  <verify>
    <automated>uv run pytest tests/test_auto_profiles.py -q -k "bare" && uv run ruff check tests && uv run pyrefly check src tests && uv run ty check</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def test_untouched_default_shapes" tests/test_auto_profiles.py` matches once
    - `uv run pytest tests/test_auto_profiles.py -q -k untouched_default_shapes` reports 6 passed
    - `uv run pytest -m "not browser and not sane_hardware" -q` passes
  </acceptance_criteria>
  <done>Every untouched default shape is pinned by a parametrised test through the real loader.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment / TOML -> Settings | operator-controlled input shapes settings |
| Settings.config_path -> filesystem write (26-08, CLI) | the recorded path becomes a write target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05 | Tampering | `config_path` populated from `SANELESS_CONFIG_PATH` or TOML, redirecting profile writes | mitigate | PrivateAttr + read-only property; test sets the env var and asserts `config_path is None` |
| T-26-06 | Tampering | writes landing in the daemon CWD (`./saneless.toml`) | mitigate | The daemon path stops re-deriving (26-08 consumes `settings.config_path`); CLI fallback unchanged and documented |
| T-26-SC | Tampering | package installs | accept | None |
</threat_model>

<verification>
- `uv run pytest tests/test_config.py tests/test_auto_profiles.py tests/test_cli.py -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- gate commands
</verification>

<success_criteria>
- `settings.config_path` exists, is correct on all three branches, and is unforgeable
- One search list
- is_bare_default shapes table pinned
</success_criteria>

<output>
Create `.planning/phases/26-worker-and-web-robustness/26-02-SUMMARY.md` when done
</output>
