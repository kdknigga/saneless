---
phase: 21-vocabulary-and-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/saneless/vocabulary.py
  - src/saneless/job.py
  - tests/test_vocabulary.py
autonomous: true
requirements: [CTR-01, CTR-05]
must_haves:
  truths:
    - "A developer who adds an eighth JobState member and forgets its label gets an error from BOTH ty and pyrefly at edit time, not a silent fallthrough"
    - "Every JobState member is classified as active or terminal, never both, never neither"
    - "BUSY_STATES cannot drift from ACTIVE_STATES because it is computed from it"
    - "Every existing `from saneless.job import JobState` and `from saneless.job import ErrorCategory` in the codebase still resolves"
    - "A Job object can answer `is_active` and `is_busy` without the caller knowing any state names"
  artifacts:
    - path: "src/saneless/vocabulary.py"
      provides: "JobState, ErrorCategory, ScanOutcome, ACTIVE_STATES, TERMINAL_STATES, BUSY_STATES, state_label, progress_label, error_message, classify_error"
      contains: "assert_never"
    - path: "tests/test_vocabulary.py"
      provides: "D-09 parametrised completeness tests and the D-10 raise test"
      contains: "pytest.mark.parametrize"
    - path: "src/saneless/job.py"
      provides: "re-exported JobState/ErrorCategory plus Job.is_active / Job.is_busy"
      contains: "from saneless.vocabulary import"
  key_links:
    - from: "src/saneless/job.py"
      to: "src/saneless/vocabulary.py"
      via: "runtime re-export import kept alive by the existing __all__"
      pattern: "from saneless.vocabulary import ErrorCategory, JobState"
    - from: "src/saneless/job.py"
      to: "ACTIVE_STATES / BUSY_STATES"
      via: "Job.is_active / Job.is_busy properties"
      pattern: "self.state in (ACTIVE_STATES|BUSY_STATES)"
---

<objective>
Create `src/saneless/vocabulary.py` — the single leaf module that owns every word the
system uses about itself — and re-point `job.py` at it without breaking a single existing
import.

Purpose: this is the foundation the other four plans in Phase 21 import. Nothing else in
the phase can start until the enums, the three state classifications, and the four total
lookup functions exist and are proven exhaustive by `ty` and `pyrefly`.

Output: `src/saneless/vocabulary.py`, `tests/test_vocabulary.py`, and a `job.py` that
defines no enums but still exports them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-vocabulary-and-contracts/21-CONTEXT.md
@.planning/phases/21-vocabulary-and-contracts/21-RESEARCH.md
@.planning/phases/21-vocabulary-and-contracts/21-PATTERNS.md
@CLAUDE.md

<decisions_implemented>
D-02 (vocabulary.py is a new leaf module; `job.py` re-exports),
D-04 (three classifications; `BUSY_STATES` derived),
D-06 (no new `JobState` member),
D-07 (`ScanOutcome` = SUCCESS + FALLBACK only),
D-08 (`match` + `assert_never`, never `dict[JobState, str]`),
D-09 (parametrised completeness tests over `list(...)`),
D-10 (unrecognised value raises),
D-12 (`ErrorCategory` gets its message map but is NOT wired to the UI).
</decisions_implemented>

<interfaces>
Existing contracts the executor must preserve exactly. Read the files themselves for the
rest; these are the load-bearing ones.

`src/saneless/job.py:19` — DO NOT EDIT this line:
    __all__ = ["ErrorCategory", "Job", "JobState", "JobStore"]
Ruff treats `__all__` membership as usage, so a plain re-export import satisfies `F401`
with no `# noqa` and no `import X as X`. This was executed against the real project config
(21-PATTERNS.md § job.py) — do not add a suppression.

`JobState` members, in lifecycle order, values identical to names:
    PENDING, SCANNING, AWAITING_FLIP, ASSEMBLING, UPLOADING, DONE, ERROR

`ErrorCategory` members, values identical to names:
    FEEDER, CONFIG, SCANNER, UPLOAD, UNKNOWN

`Job` dataclass fields (`src/saneless/job.py`), unchanged by this plan:
    id, profile, title, state, error, error_category, created_at, tags,
    correspondent, thumbnail
</interfaces>

<hard_constraints>
1. `vocabulary.py` MUST NOT import from `job.py`, `pipeline.py`, `worker.py`, `cli.py`, or
   anything under `web/`. Importing `saneless.exceptions` IS permitted and required —
   it is itself a leaf module with no intra-package imports.
2. NO `# type: ignore`, NO `# noqa`, NO rule disabling, NO raising `pylint.max-returns`.
   CLAUDE.md forbids all four. Every finding is fixed structurally.
3. `case _: raise ValueError(...)` and `case _: pass` are FORBIDDEN as the fall-through of
   any total function — measured against this repo's `ty 0.0.80` and `pyrefly 1.2.0`, they
   produce NO diagnostic from either checker and silently destroy the enforcement.
4. Match arms use BARE MEMBER PATTERNS only (`case JobState.DONE:`). Raw string patterns
   (`case "DONE":`), `.value` patterns, and guarded arms (`case X if cond:`) are all
   forbidden — `ty` and `pyrefly` disagree about every one of them.
5. `ruff PLR0911` caps returns at 6 and `JobState` has 7 members. Every total function uses
   ASSIGN-IN-ARM plus a SINGLE TRAILING `return`. Use this form uniformly even for
   functions currently under the limit.
</hard_constraints>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create vocabulary.py with the three enums and the three state classifications</name>

  <files>src/saneless/vocabulary.py, tests/test_vocabulary.py</files>

  <read_first>
    - src/saneless/job.py (lines 1-45: module docstring style, import block, `__all__`, the `JobState` and `ErrorCategory` definitions being copied)
    - src/saneless/paper_sizes.py (the closest leaf-module analog: `from __future__ import annotations` first, sorted `__all__` after imports, module-level constants documented by a string literal directly beneath them)
    - src/saneless/exceptions.py (lines 1-14: the multi-line module docstring + multi-line sorted `__all__` form)
    - tests/test_paper_sizes.py (lines 1-50: test module header, `Covers requirements:` convention, class-per-subject grouping, `-> None` on every test)
    - .planning/phases/21-vocabulary-and-contracts/21-PATTERNS.md § `src/saneless/vocabulary.py`
  </read_first>

  <behavior>
    Write `tests/test_vocabulary.py` FIRST, confirm it fails (module does not exist), then
    implement. Both land in ONE commit — see `<green_at_every_commit>` below.

    - `list(JobState)` has exactly 7 members and none of them is `FALLBACK` or `SCANNING_REVERSE` (D-06)
    - `list(ScanOutcome)` is exactly `[SUCCESS, FALLBACK]` — `FAILED` is absent (D-07)
    - For every `state` in `list(JobState)`: `(state in ACTIVE_STATES) ^ (state in TERMINAL_STATES)` is True (D-09)
    - `ACTIVE_STATES | TERMINAL_STATES == set(JobState)`
    - `BUSY_STATES == ACTIVE_STATES - {JobState.AWAITING_FLIP}`
    - `JobState.AWAITING_FLIP` is in `ACTIVE_STATES` and NOT in `BUSY_STATES`
    - Every member's `.value` equals its name
  </behavior>

  <action>
    Create `src/saneless/vocabulary.py` as a leaf module. Multi-line module docstring
    (opening `"""` alone on its own line — `D212` is ignored project-wide) stating that the
    module must not import from `job.py`, `pipeline.py`, or `web/`, and that every consumer
    imports from here and never the reverse. Then `from __future__ import annotations`,
    then `from enum import StrEnum`, then a sorted `__all__`.

    For this task `__all__` is exactly:
    `["ACTIVE_STATES", "BUSY_STATES", "TERMINAL_STATES", "ErrorCategory", "JobState", "ScanOutcome"]`
    (Task 2 appends the four function names. `RUF022` enforces this sort — uppercase names
    sort before lowercase.)

    Define three `StrEnum`s copying `job.py`'s exact style: one-line class docstring, blank
    line after it, value string identical to the member name, members in lifecycle/semantic
    order (NOT alphabetical), no per-member comments.
    - `JobState`: PENDING, SCANNING, AWAITING_FLIP, ASSEMBLING, UPLOADING, DONE, ERROR.
      Copy verbatim from `job.py`. Add NO member — `FALLBACK` is Phase 23's (D-06) and
      `SCANNING_REVERSE` is Phase 25's.
    - `ErrorCategory`: FEEDER, CONFIG, SCANNER, UPLOAD, UNKNOWN. Copy verbatim from `job.py`.
    - `ScanOutcome`: SUCCESS, FALLBACK. Exactly two members. Add a comment on the line where
      `FAILED` would go recording that it is Phase 23's, added with the code path that
      produces it (D-07).

    Define the three classifications as module-level `frozenset[JobState]` constants, each
    documented by a string literal directly beneath it (the `paper_sizes.py:22-25` idiom):
    - `ACTIVE_STATES` — hand-written: PENDING, SCANNING, AWAITING_FLIP, ASSEMBLING, UPLOADING
    - `TERMINAL_STATES` — hand-written: DONE, ERROR
    - `BUSY_STATES` — DERIVED as `ACTIVE_STATES - {JobState.AWAITING_FLIP}`. Never write out
      its members. Its docstring explains the distinction: "the machine is working" versus
      "we are waiting for the human", and that the button text and `aria-busy` in the web UI
      depend on the difference (D-04).

    Create `tests/test_vocabulary.py` with a one-line-per-idea module docstring carrying
    `Covers requirements: CTR-01, CTR-02, CTR-05.`, `from __future__ import annotations`,
    and class-per-subject grouping following `tests/test_paper_sizes.py`. Use
    `@pytest.mark.parametrize("state", list(JobState))` — parametrise over `list(JobState)`,
    NEVER over a hand-written list of names; that is the whole point of D-09. Note that
    `@pytest.mark.parametrize` appears ZERO times elsewhere in this test suite, so there is
    no in-tree analog for the decorator itself; `parametrize` is a built-in mark and needs
    no `markers=` entry under `--strict-markers`.

    Put the requirement ID at the END of each test docstring in parentheses, matching the
    live convention (`(CTR-01)`, `(CTR-05)`) — see `tests/test_web.py:304`, `tests/test_cache.py:13`.
  </action>

  <green_at_every_commit>
    Write the tests, run them, SEE THEM FAIL, then write `vocabulary.py`, then commit BOTH
    in one commit. Do not commit a red test file on its own: Phase 21 is a refactor and the
    suite must be green at every commit boundary. The TDD discipline is honoured inside the
    task (test written and observed failing before implementation), not by splitting the
    commit.
  </green_at_every_commit>

  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check &amp;&amp; uv run pyrefly check &amp;&amp; uv run pytest -q tests/test_vocabulary.py</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q tests/test_vocabulary.py` passes with at least 7 parametrised cases for the XOR partition test
    - `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check` all exit 0
    - `uv run pytest -q -m "not browser"` reports at least 332 passed, 0 failed
    - `command grep -c 'BUSY_STATES = ACTIVE_STATES - {JobState.AWAITING_FLIP}' src/saneless/vocabulary.py` returns 1
    - `command grep -E 'FALLBACK|SCANNING_REVERSE' src/saneless/vocabulary.py | command grep -v '^ *#'` shows `FALLBACK` ONLY inside `ScanOutcome`, and shows no `SCANNING_REVERSE` at all
    - `command grep -nE '^from (saneless\.)?(job|pipeline|worker|cli|web)' src/saneless/vocabulary.py` returns nothing (exit status 1)
    - `command grep -c 'parametrize' tests/test_vocabulary.py` returns at least 1
    - `command grep -n 'noqa\|type: ignore' src/saneless/vocabulary.py tests/test_vocabulary.py` returns nothing
    - `python -c "from saneless.vocabulary import ACTIVE_STATES, TERMINAL_STATES, BUSY_STATES, JobState; assert ACTIVE_STATES | TERMINAL_STATES == set(JobState); assert not (ACTIVE_STATES &amp; TERMINAL_STATES); assert BUSY_STATES == ACTIVE_STATES - {JobState.AWAITING_FLIP}"` exits 0 (run via `uv run python -c`)
  </acceptance_criteria>

  <done>`saneless.vocabulary` defines all three enums and all three classifications, `BUSY_STATES` is provably derived, the parametrised partition test passes for all 7 members, and all four quality gates are clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the four total lookup functions behind match + assert_never</name>

  <files>src/saneless/vocabulary.py, tests/test_vocabulary.py</files>

  <read_first>
    - src/saneless/vocabulary.py (as written by Task 1)
    - src/saneless/web/app.py (lines 35-48: `_STATE_LABELS` — the exact seven label strings that must be preserved byte for byte)
    - src/saneless/web/templates/partials/status.html (lines 8-25: the progress prose strings, with their literal three-dot spelling)
    - src/saneless/cli.py (lines 110-122: `_event_labels` — the other progress prose vocabulary)
    - src/saneless/worker.py (lines 130-149: `_categorize_error`, which moves here verbatim minus `self`)
    - src/saneless/exceptions.py (the exception hierarchy `classify_error` isinstance-checks, and the order they must be checked in)
    - src/saneless/paper_sizes.py (lines 33-48: the Google-style Args/Returns docstring form with a blank line before the closing `"""`)
    - .planning/phases/21-vocabulary-and-contracts/21-RESEARCH.md § "Pattern 1: The Exhaustiveness Pattern" (the exact function form, and the measured pattern matrix)
  </read_first>

  <behavior>
    Write the tests first, confirm failure, then implement, then commit once.

    - `state_label(JobState.PENDING) == "Pending"`, `SCANNING -> "Scanning"`,
      `AWAITING_FLIP -> "Waiting for flip"`, `ASSEMBLING -> "Assembling"`,
      `UPLOADING -> "Uploading"`, `DONE -> "Complete"`, `ERROR -> "Failed"`
    - `progress_label(JobState.PENDING) == "Starting scan..."`, `SCANNING -> "Scanning..."`,
      `AWAITING_FLIP -> "Awaiting flip..."`, `ASSEMBLING -> "Assembling PDF..."`,
      `UPLOADING -> "Uploading to paperless-ngx..."`
    - For every `state` in `list(JobState)`: `state_label(state)` and `progress_label(state)`
      are both non-empty AND not equal to `state.value` (D-09)
    - For every `category` in `list(ErrorCategory)`: `error_message(category)` is non-empty
    - `state_label(cast("JobState", "UNKNOWN"))` raises `AssertionError` (D-10) — NOT `ValueError`
    - `classify_error(FeederEmptyError("x")) is ErrorCategory.FEEDER`,
      `ConfigError -> CONFIG`, `ScanError -> SCANNER`, `PaperlessError -> UPLOAD`,
      `ValueError -> UNKNOWN`
  </behavior>

  <action>
    Append four public functions to `vocabulary.py` and extend `__all__` to the full sorted
    list: `["ACTIVE_STATES", "BUSY_STATES", "TERMINAL_STATES", "ErrorCategory", "JobState",
    "ScanOutcome", "classify_error", "error_message", "progress_label", "state_label"]`.
    Add `from typing import assert_never` to the stdlib import block.

    ALL THREE label/message functions use the identical structural form: a `match` over the
    parameter, ONE ASSIGNMENT PER ARM to a single local variable, `case _: assert_never(x)`
    as the last arm, and ONE trailing `return`. This is not a style preference — the
    seven-return form trips `ruff PLR0911` (7 > 6), and `# noqa` / raising `max-returns`
    are both forbidden by CLAUDE.md. Use the single-return form even for `error_message`
    (5 arms) so the shape is uniform and survives Phase 25 adding members.

    `state_label(state: JobState) -> str` — the SHORT labels rendered in the history table.
    The seven strings are copied verbatim from `web/app.py:36-42` and MUST NOT change:
    "Pending", "Scanning", "Waiting for flip", "Assembling", "Uploading", "Complete", "Failed".

    `progress_label(state: JobState) -> str` — the PROGRESS PROSE rendered in the status
    area and echoed by the CLI. Five strings are user-visible today and MUST be byte
    identical including their literal three-dot spelling (they are three ASCII periods, not
    a Unicode ellipsis): PENDING -> "Starting scan...", SCANNING -> "Scanning...",
    AWAITING_FLIP -> "Awaiting flip...", ASSEMBLING -> "Assembling PDF...",
    UPLOADING -> "Uploading to paperless-ngx...". `DONE` and `ERROR` have no progress prose
    in production today — the templates and the CLI branch structurally for those two — but
    both arms are required for totality: return "Complete" and "Failed" respectively, and
    note in the docstring that these two arms exist for exhaustiveness and have no
    production caller in this phase.

    `error_message(category: ErrorCategory) -> str` — the single user-message map CTR-05
    calls for. Use plain language with no exception text interpolated:
    FEEDER -> "The document feeder is empty or jammed.",
    CONFIG -> "The saneless configuration is invalid.",
    SCANNER -> "The scanner could not complete the scan.",
    UPLOAD -> "The document could not be sent to paperless-ngx.",
    UNKNOWN -> "Something went wrong.".
    D-12 is explicit that this function is NOT wired to any template, route, or CLI output
    in this phase — the status partial keeps rendering `job.error` verbatim. Record that in
    the docstring: swapping the specific error text for a generic category message before
    Phase 24 makes the underlying messages truthful would be a user-visible regression, and
    the plain-language display is Phase 30's (U-05). Its parametrised completeness test is
    a sufficient consumer for now.

    `classify_error(exc: Exception) -> ErrorCategory` — move `worker.py:130-149` here
    VERBATIM, dropping only the `self` parameter and keeping the docstring. It is an
    `isinstance` chain, not a `match`, and it stays that way: it dispatches on exception
    type, not on an enum, so `assert_never` does not apply and the trailing
    `return ErrorCategory.UNKNOWN` is the correct total fallback. PRESERVE THE CHECK ORDER
    EXACTLY as it appears in `worker.py` — the ordering is load-bearing where the exception
    classes are related by inheritance. Import the four exception types from
    `saneless.exceptions` in the first-party import block; that import is permitted because
    `exceptions.py` is itself a leaf with no intra-package imports.

    Every function gets a Google-style docstring with `Args:` and `Returns:`. `state_label`,
    `progress_label`, and `error_message` additionally get a `Raises: AssertionError:` line
    documenting the `assert_never` fall-through — `Raises:` sections already exist in this
    codebase at `pipeline.py:327-331` and `paperless.py:90-92`.

    Extend `tests/test_vocabulary.py` with the parametrised completeness tests for all three
    lookup functions and the D-10 raise test. For the raise test, construct the bad value
    with `typing.cast` — `bad = cast("JobState", "UNKNOWN")` — which is idiomatic for
    "I am deliberately violating this contract to test the guard" and is NOT a suppression
    comment. Do NOT write `# type: ignore`. Confirm `cast` passes both `ty` and `pyrefly`
    here before committing; if it does not, find another suppression-free construction, do
    not add a comment. Assert `pytest.raises(AssertionError)`, not `ValueError`:
    `typing.assert_never` raises `AssertionError` via a real `raise` statement, so `python -O`
    does not remove it.

    The raise test's docstring MUST record WHY raising is safe, so a future reader does not
    "helpfully" restore a silent fallback: `Job.state` is only ever constructed through
    `JobState(row[3])` at `job.py:187` and `job.py:262`, which already rejects any value the
    enum does not name, so an unrecognised value cannot reach a template. This matters
    because a raising filter inside a Jinja render propagates out of `TemplateResponse` as a
    bare HTTP 500, and htmx 2 does not swap non-2xx bodies — the observable failure would be
    the status area freezing silently while the 1-second poll hammers the server.
  </action>

  <green_at_every_commit>
    Same discipline as Task 1: tests first, observe RED, implement, observe GREEN, one commit.
  </green_at_every_commit>

  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ty check &amp;&amp; uv run pyrefly check &amp;&amp; uv run pytest -q tests/test_vocabulary.py</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q tests/test_vocabulary.py` passes; the run includes a parametrised label test with 7 cases and a parametrised message test with 5 cases
    - `uv run ruff check .` exits 0 — specifically no `PLR0911` finding
    - `uv run ty check` and `uv run pyrefly check` both exit 0
    - `command grep -c 'assert_never' src/saneless/vocabulary.py` returns at least 3
    - `command grep -nE 'case _: *(raise|pass)' src/saneless/vocabulary.py` returns nothing
    - `command grep -nE 'case "[A-Z_]+"|case [A-Za-z]+\.[A-Z_]+\.value' src/saneless/vocabulary.py` returns nothing (no raw-string and no `.value` patterns)
    - `command grep -n 'noqa\|type: ignore' src/saneless/vocabulary.py tests/test_vocabulary.py` returns nothing
    - `uv run python -c "from saneless.vocabulary import state_label, progress_label, JobState; assert state_label(JobState.AWAITING_FLIP) == 'Waiting for flip'; assert progress_label(JobState.UPLOADING) == 'Uploading to paperless-ngx...'; assert progress_label(JobState.PENDING) == 'Starting scan...'"` exits 0
    - `uv run pytest -q -m "not browser"` reports at least 332 passed, 0 failed
  </acceptance_criteria>

  <done>All four lookup functions exist, are total under both type checkers, produce byte-identical user-facing strings, and a deliberately-removed match arm would fail `ty check` and `pyrefly check`.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point job.py at vocabulary.py and give Job its is_active / is_busy properties</name>

  <files>src/saneless/job.py, tests/test_vocabulary.py</files>

  <read_first>
    - src/saneless/job.py (the whole file: the import block at lines 9-17, `__all__` at line 19, the two enum definitions at lines 24-43, the `Job` dataclass at lines 46-74, and the `JobState(row[3])` construction sites)
    - src/saneless/vocabulary.py (as written by Tasks 1-2)
    - src/saneless/worker.py (lines 119-128: `is_alive` / `current_job_id` — the exact property docstring and annotation form to copy)
    - .planning/phases/21-vocabulary-and-contracts/21-PATTERNS.md § `src/saneless/job.py` (the executed proof that a plain re-export satisfies `F401` under this project's config)
  </read_first>

  <action>
    Delete the `JobState` and `ErrorCategory` class definitions from `job.py` (currently
    lines 24-43) and replace them with a single runtime import,
    `from saneless.vocabulary import ErrorCategory, JobState`, placed in a first-party import
    block after the stdlib block and separated from it by a blank line (see `pipeline.py:19-22`
    and `web/app.py:14-17` for the shape). Remove `from enum import StrEnum` from the stdlib
    block if nothing else in `job.py` still uses it.

    DO NOT EDIT `__all__` at line 19 — it already lists both names, and that membership is
    exactly what makes ruff treat the import as used. Do NOT write `import X as X`. Do NOT
    add `# noqa: F401`. This exact configuration was executed against the project's real
    ruff config and reported "All checks passed!".

    The re-export import MUST be a runtime import, NOT inside `if TYPE_CHECKING:` — the
    `TC`/`TCH` rules are enabled in this project, but a re-exported name is a runtime export
    and must not move into the type-only block.

    Also import `ACTIVE_STATES` and `BUSY_STATES` from `saneless.vocabulary` (same import
    line or a second one from the same module, whichever ruff's isort produces) and add two
    properties to the `Job` dataclass, placed AFTER the field block and BEFORE
    `class JobStore`:
    - `is_active` — "Whether this job is still in flight (not DONE or ERROR)." Returns
      `self.state in ACTIVE_STATES`.
    - `is_busy` — "Whether the machine is working (active, but not waiting for a human)."
      Returns `self.state in BUSY_STATES`.
    Copy `worker.py:119-128` exactly for form: `@property`, explicit `-> bool` annotation,
    ONE-LINE docstring beginning "Whether", single-expression body. A `@property` on a
    `@dataclass` is not a field, so it does not appear in `__init__` or `__eq__` and the
    dataclass decorator needs no change. Do NOT add `frozen=`, `slots=`, `kw_only=`, or `eq=`
    to the decorator — no dataclass in this codebase uses any of them.

    These two properties are the seam the templates will use in plan 21-03. They exist so
    the templates can ask the `Job` object a question instead of re-implementing the rule;
    do not expose the frozensets to the template namespace.

    Add tests to `tests/test_vocabulary.py` covering: (a) `from saneless.job import
    ErrorCategory, JobState` still resolves and `saneless.job.JobState is
    saneless.vocabulary.JobState` (the SAME object, not a copy); (b) a `Job` in each of the
    7 states reports the correct `is_active` / `is_busy` pair — in particular
    `AWAITING_FLIP` is active but NOT busy, and `DONE` is neither.
  </action>

  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ty check &amp;&amp; uv run pyrefly check &amp;&amp; uv run pytest -q -m "not browser"</automated>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q -m "not browser"` reports at least 332 passed, 0 failed (the existing `tests/test_job.py` imports at line 14 must pass unedited)
    - `uv run ruff check .` exits 0 with no `F401`
    - `uv run python -c "import saneless.job, saneless.vocabulary; assert saneless.job.JobState is saneless.vocabulary.JobState; assert saneless.job.ErrorCategory is saneless.vocabulary.ErrorCategory"` exits 0
    - `command grep -nE '^class (JobState|ErrorCategory)\(StrEnum\)' src/saneless/job.py` returns nothing
    - `command grep -c 'from saneless.vocabulary import' src/saneless/job.py` returns at least 1
    - `command grep -n 'noqa' src/saneless/job.py` returns nothing
    - `command grep -c '__all__ = \["ErrorCategory", "Job", "JobState", "JobStore"\]' src/saneless/job.py` returns 1 (line unchanged)
    - `uv run python -c "from saneless.job import Job, JobState; j = Job(id='x', profile='p', title='t', state=JobState.AWAITING_FLIP); assert j.is_active and not j.is_busy; j.state = JobState.DONE; assert not j.is_active and not j.is_busy"` exits 0
  </acceptance_criteria>

  <done>`job.py` defines no enum, re-exports both, and `Job` answers `is_active`/`is_busy` from the derived vocabulary; the whole existing suite passes with no edits to any file outside this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite row -> `Job` object | Persisted `TEXT` state values re-enter the type system at `job.py:187` / `:262` via `JobState(row[3])` |
| Python value -> Jinja render | Label strings produced here are interpolated into HTML by later plans in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | `state_label` / `progress_label` receiving a value that is not a `JobState` | mitigate | `case _: assert_never(state)` raises rather than echoing an attacker- or corruption-supplied string back into the page. The existing `JobState(row[3])` guard at the store boundary is what makes raising safe rather than a self-DoS; Task 2 records that argument in the test docstring |
| T-21-02 | Information disclosure | `error_message()` leaking raw exception text | mitigate | Every message is a developer-authored constant with no exception text interpolated. D-12 additionally keeps the function out of every template, route, and CLI path in this phase |
| T-21-03 | Tampering | XSS via a label string reaching a template | accept | Labels are developer-authored constants, never user input, and Jinja2 autoescape is in force for `.html`. Standing control for the whole phase: DO NOT apply the `\| safe` filter to any label |
| T-21-SC | Tampering | Supply chain — package installs | mitigate | This plan installs NOTHING. `uv.lock` and `pyproject.toml` dependencies are untouched; every construct is `enum`, `dataclasses`, or `typing` from the standard library. RESEARCH.md § Package Legitimacy Audit records zero packages, so no legitimacy checkpoint is required |
</threat_model>

<verification>
```bash
uv run ruff check .
uv run ruff format --check .
uv run ty check
uv run pyrefly check
uv run pytest -m "not browser"        # must be >= 332 passed, 0 failed
```

Exhaustiveness spot-check (the phase's actual enforcement mechanism — pytest cannot see a
missing enum arm). Temporarily delete ONE `case` arm from `state_label`, confirm BOTH
`uv run ty check` and `uv run pyrefly check` report an error naming the dropped member,
then restore the arm. If either checker stays silent, the function was written with a
forbidden pattern form (raw string, `.value`, guard, or a `raise`/`pass` wildcard) and must
be rewritten before this plan is considered done.
</verification>

<success_criteria>
- `src/saneless/vocabulary.py` exists, imports nothing from `job.py` / `pipeline.py` / `web/`, and owns `JobState`, `ErrorCategory`, `ScanOutcome`, `ACTIVE_STATES`, `TERMINAL_STATES`, `BUSY_STATES`, `state_label`, `progress_label`, `error_message`, `classify_error`
- `BUSY_STATES` is computed from `ACTIVE_STATES`, never written out
- Every total function uses `match` with bare member patterns, assign-in-arm, one trailing `return`, and `case _: assert_never(x)`
- Deleting any single match arm fails BOTH `ty check` and `pyrefly check`
- `tests/test_vocabulary.py` parametrises over `list(JobState)` and `list(ErrorCategory)`, never over a hand-written name list
- Every user-facing string is byte identical to today's
- `from saneless.job import JobState` / `ErrorCategory` still resolve to the SAME objects
- `Job.is_active` and `Job.is_busy` exist and read from the derived vocabulary
- No `# noqa`, no `# type: ignore`, no rule disabled, no `max-returns` change
- All five CONTRIBUTING checks green; suite at >= 332 passed
</success_criteria>

<output>
Create `.planning/phases/21-vocabulary-and-contracts/21-01-SUMMARY.md` when done.
Record: the exact label strings shipped, the `frozen=`/`slots=` decision context, the
result of the exhaustiveness spot-check, and confirmation that `typing.cast` passed both
checkers in the D-10 negative test.
</output>
