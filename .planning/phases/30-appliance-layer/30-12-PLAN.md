---
phase: 30-appliance-layer
plan: 12
type: tdd
wave: 7
depends_on: [30-11]
files_modified:
  - src/saneless/web/templates/partials/status.html
  - src/saneless/web/templates/partials/error.html
  - src/saneless/web/templates/partials/history.html
  - src/saneless/web/static/app.css
  - tests/test_web_state_rendering.py
  - tests/test_web_errors.py
  - tests/test_cross_origin.py
autonomous: true
requirements: [APPL-03, APPL-04, APPL-12]
decisions: [D-13, D-32, D-34, D-35]

must_haves:
  truths:
    - "The status area's ERROR branch shows the category sentence and the next step inside ONE wrapping role='alert' div, with job.error, the category and the job id inside a collapsed Technical details disclosure outside the alert (D-13, UI-SPEC S2)"
    - "The specific message is relocated, not removed: job.error is still rendered, inside the disclosure, and job.error storage is unchanged"
    - "A row with a NULL error_category renders today's shape verbatim, with no next step and no disclosure -- substituting UNKNOWN would print 'Something went wrong.' over a row that still holds a truthful specific message"
    - "No template under web/templates references a log file path (D-13: it is a host filesystem path on a LAN-visible page)"
    - "Page counts render as one muted line under the outcome in the status area and as a second line in the history Title cell; a NULL count renders no element at all, while a measured 0 renders as 0 (D-32, Pitfall 4)"
    - "The history table keeps four columns and colspan=4 -- Pitfall 11 is designed out, not guarded against"
    - "The history Time cell renders through the shared local_time filter, so it names the zone (D-34, D-35)"
  artifacts:
    - path: "src/saneless/web/templates/partials/status.html"
      provides: "the rewritten ERROR branch and the counts line"
      contains: "details class=\"tech-details\""
    - path: "src/saneless/web/templates/partials/history.html"
      provides: "local_time Time cell and the counts second line"
      contains: "local_time"
  key_links:
    - from: "src/saneless/web/templates/partials/status.html"
      to: "vocabulary.error_advice"
      via: "the error_message and error_next_step filters"
      pattern: "error_next_step"
    - from: "src/saneless/web/templates/partials/history.html"
      to: "vocabulary.page_counts"
      via: "the page_counts filter"
      pattern: "page_counts"
---

<objective>
Render the plain-language error and the page counts on the two surfaces a household member reads:
the status area and the history table.

Purpose: APPL-04 and APPL-03 are both display requirements over data that already exists. The care
is in what is *not* done -- the specific message is not deleted, a NULL count is not printed as 0,
the log path never reaches the page, and the table does not grow a fifth column.
Output: the rewritten ERROR branch, the two disclosures, the counts line, and the local Time cell.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-appliance-layer/30-CONTEXT.md
@.planning/phases/30-appliance-layer/30-RESEARCH.md
@.planning/phases/30-appliance-layer/30-PATTERNS.md
@.planning/phases/30-appliance-layer/30-UI-SPEC.md
@CLAUDE.md

<interfaces>
From `src/saneless/web/templates/partials/status.html` (41 lines, read it whole):
- line 9 `<p aria-busy="true">{{ job.state | progress_label }}</p>` — the busy branch
- lines 18-20 the DONE branch: `<p class="status-done">&#10003; Done: {{ job.title }}</p>` then the
  terminal reload include (plan 30-11 replaced the literal loader)
- lines 21-26 the FALLBACK branch, with its optional `job.warning` line
- lines 31-33 the ERROR branch: `<p role="alert" class="status-error">&#10007; Error: {{ job.error }}</p>`
- lines 35-37 the thumbnail block, which the counts line must precede

From `src/saneless/web/templates/partials/history.html` (15 lines):
- line 4 `<td>{{ job.created_at.strftime("%Y-%m-%d %H:%M") }}</td>` — the exact expression S7 replaces
- line 6 `<td>{{ job.title }}</td>` — the cell that gains the counts second line
- line 7 the `state -> class` chain (the one tolerated in-template vocabulary exception, comparing
  against real `JobState` members through the `JobState` global)
- line 13 `<tr><td colspan="4">No scan history yet.</td></tr>`

From `src/saneless/web/templates/index.html` lines 78-84: four `<th>`s — Time, Profile, Title, Status.

From `src/saneless/web/static/app.css`: `.status-error` reads `--pico-del-color`; the media query at
line 111+ already gives `#history-body td` `white-space: normal; word-break: break-word`, which is
what makes a two-line Title cell work on a phone with no new breakpoint.

Filters available (registered in plan 30-09): `error_message`, `error_next_step`, `page_counts`,
`local_time`.

Tests that **must be rewritten in this plan** (UI-SPEC § "Tests this phase must rewrite"):
- `tests/test_web_state_rendering.py:247` pins `<p role="alert" class="status-error">&#10007; Error: ...</p>`
- `tests/test_web_errors.py:206` asserts the error partial body **equals** the single `<p>`
- `tests/test_cross_origin.py:356` makes the same exact-body assertion on the 403 path
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the status-area ERROR branch and the technical-details disclosure</name>
  <files>src/saneless/web/templates/partials/status.html, src/saneless/web/static/app.css, tests/test_web_state_rendering.py</files>
  <read_first>
    - src/saneless/web/templates/partials/status.html (whole file)
    - tests/test_web_state_rendering.py lines 1-90 (module docstring and helpers) and lines 230-270 (the ERROR prose assertion at ~247)
    - src/saneless/web/static/app.css lines 16-70 (status colour rules and their comment style)
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § S2 "Status-area ERROR branch" in full, including the five contract bullets and the disclosure CSS
  </read_first>
  <behavior>
    - An ERROR job with a non-NULL `error_category` renders: exactly one `role="alert"` element in the status area; it is a `<div>`; it contains **both** `error_message(category)` and `error_next_step(category)`
    - The `<p class="status-error">` inside the alert keeps the U+2717 glyph and the `.status-error` class and no longer carries the literal prefix `Error: `
    - A `<details class="tech-details">` follows, **outside** the alert, without an `open` attribute, whose text contains `job.error`, the category value and the job id
    - An ERROR job with a NULL `error_category` renders today's shape verbatim -- `<p role="alert" class="status-error">&#10007; Error: {{ job.error }}</p>` -- with no next step and no disclosure
    - The rendered page contains no log-file path: assert that the configured `log_file` value does not appear anywhere in the markup, and assert no template file under `web/templates/` references `log_file` or `log_path`
    - The page still has exactly one `role="alert"` overall (`#status-message` plus this one would be two -- assert the status area's own count is one and that nothing nests an alert inside another)
    - Every other state branch renders exactly as before (parametrise over `list(JobState)` as the file already does)
  </behavior>
  <action>
    RED (commit `test(30-12): ...`): rewrite the ERROR assertion at tests/test_web_state_rendering.py:247
    to the new shape, and add tests for the NULL-category branch, the disclosure contents, the
    disclosure being collapsed and outside the alert, and the log-path absence (both the rendered-page
    assertion and the template-source grep assertion). Extend the existing `list(JobState)`
    parametrisation rather than hand-writing state names. This is a legitimate "could not have passed
    before" rewrite -- record it as such in the SUMMARY. Run: they fail.

    GREEN (commit `feat(30-12): ...`): rewrite `status.html`'s ERROR branch exactly as UI-SPEC S2
    specifies: an outer `{% if job.error_category %}` wrapping a `<div role="alert">` that holds the
    `<p class="status-error">` with the U+2717 entity and `{{ job.error_category | error_message }}`,
    followed by a `<p>` with `{{ job.error_category | error_next_step }}`; then the
    `<details class="tech-details">` with a `Technical details` summary and three paragraphs holding
    `{{ job.error }}`, `Category: {{ job.error_category }}` and `Job: {{ job.id }}`; the `{% else %}`
    branch keeps today's single `<p role="alert">` line byte-identical; then the existing
    `{% include "partials/terminal_reload.html" %}`. Add a comment above the branch recording the
    three decisions a reader will question: `role="alert"` moved from the `<p>` to a wrapping `<div>`
    so the alert covers the next step, which is the most actionable content on the page; the specific
    message is relocated into the disclosure, not removed, because `vocabulary.error_message`'s own
    docstring warns that swapping it for a category sentence would be a regression; and the log file
    path is deliberately absent (D-13) because it is a host filesystem path on a LAN-visible page.
    Append the `details.tech-details > summary` rule to app.css with the UI-SPEC comment (a rare
    control, but still one someone taps standing at the scanner, so give the summary a finger-sized
    row) and `min-height: 2.75rem`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_web_state_rendering.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'details class="tech-details"' src/saneless/web/templates/partials/status.html` is 1
    - `grep -c 'role="alert"' src/saneless/web/templates/partials/status.html` is 2 (the new wrapping div and the NULL-category fallback `<p>`), and no line contains both on the same element
    - `grep -c ' open' src/saneless/web/templates/partials/status.html` is 0
    - `grep -rniE "log_file|log_path|logfile" src/saneless/web/templates/ | wc -l` is 0
    - `grep -n "error_next_step" src/saneless/web/templates/partials/status.html` matches once
    - `grep -n "min-height: 2.75rem" src/saneless/web/static/app.css` matches at least once
    - `uv run pytest tests/test_web_state_rendering.py -k "error or details" -q` selects at least 8 tests and passes
    - Gate commands exit 0; `git log --oneline` shows `test(30-12):` before `feat(30-12):`
  </acceptance_criteria>
  <done>A household member reads a sentence and a next step; the detail is one tap away and holds no path. VALIDATION row: APPL-04 details contents and log-path absence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the request-error slot's short disclosure</name>
  <files>src/saneless/web/templates/partials/error.html, tests/test_web_errors.py, tests/test_cross_origin.py</files>
  <read_first>
    - src/saneless/web/templates/partials/error.html (whole file)
    - tests/test_web_errors.py lines 190-260 (the exact-body assertion at ~206 and the "no role=alert inside the partial" guard at ~243)
    - tests/test_cross_origin.py lines 340-370 (the same exact-body assertion on the 403 path at ~356)
    - src/saneless/web/errors.py (the renderer, to confirm which context keys are available: message, status_code, refresh_history, and whether a rejected job id is passed)
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § S2 "Request-error slot"
  </read_first>
  <behavior>
    - The slot renders the Phase 26 message unchanged in a `<p class="status-error">` with the U+2717 glyph
    - A `<details class="tech-details">` follows, collapsed, containing exactly two facts at most: `Status: {{ status_code }}` and, when a rejected job row was written, `Job: {{ job_id }}`
    - The partial contains **no** `role="alert"` -- `#status-message` is the alert region and `tests/test_web_errors.py:243` still passes
    - No exception text, request input or log path appears anywhere in the slot
    - The `refresh_history` hidden loader still renders when the flag is set, exactly as today
    - The twelve Phase 26 slot messages are unchanged; no copy table is edited
  </behavior>
  <action>
    RED (commit `test(30-12): ...`): rewrite the exact-body assertions at tests/test_web_errors.py:206
    and tests/test_cross_origin.py:356 to the new body, and add assertions for the disclosure being
    collapsed, holding the status code, holding the job id only when one was written, and holding
    nothing else. Keep the existing `role="alert"`-absence test untouched. Run: they fail.

    GREEN (commit `feat(30-12): ...`): rewrite `partials/error.html` exactly as UI-SPEC S2
    specifies. Add a comment stating why the disclosure is deliberately short: Phase 26 D-10 and
    ASVS V7 forbid exception text, request input and the log path from ever reaching this surface,
    so the HTTP status code and the rejected job's id are the only technical facts the slot is
    permitted to carry, and saying so is better than a uniform affordance that sometimes lies about
    having detail. Do not touch the twelve Phase 26 slot messages -- each is already "the problem,
    then what to do next" in one sentence pair, so APPL-04's requirement is already met by the
    string, and splitting a locked, doc-pinned copy table would be churn with no reader benefit.
    If `job_id` is not currently passed into the error context, add it in `web/errors.py` from the
    `RequestRejected.refresh_history` path only, and say in the comment that it is the id of the row
    the refused submit wrote (Phase 26 D-05), never an arbitrary request value.
  </action>
  <verify>
    <automated>uv run pytest tests/test_web_errors.py tests/test_cross_origin.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'details class="tech-details"' src/saneless/web/templates/partials/error.html` is 1
    - `grep -c 'role="alert"' src/saneless/web/templates/partials/error.html` is 0
    - `grep -c "status_code" src/saneless/web/templates/partials/error.html` is 1
    - `grep -n "refresh_history" src/saneless/web/templates/partials/error.html` still matches once
    - `uv run pytest tests/test_web_errors.py -q` passes with the rewritten exact-body assertion
    - `uv run pytest tests/test_cross_origin.py -q` passes
    - Gate commands exit 0
  </acceptance_criteria>
  <done>Both error surfaces now offer detail behind a disclosure, and neither leaks. VALIDATION row: APPL-04 slot rendering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: page counts and the local Time cell</name>
  <files>src/saneless/web/templates/partials/status.html, src/saneless/web/templates/partials/history.html, src/saneless/web/static/app.css, tests/test_web_state_rendering.py</files>
  <read_first>
    - src/saneless/web/templates/partials/status.html (the DONE and FALLBACK branches and the thumbnail block that must stay last)
    - src/saneless/web/templates/partials/history.html (whole file -- line 4's strftime and line 6's Title cell)
    - src/saneless/web/templates/index.html lines 76-88 (the four `<th>`s that stay four)
    - src/saneless/web/static/app.css lines 105-140 (the mobile media query giving `#history-body td` wrapping)
    - src/saneless/worker.py lines 1340-1360 (the except branch that leaves all three counts NULL) and src/saneless/job.py lines 775-790 (`create_rejected_job` writing them NULL)
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § S3 in full (the sentence, the status-area placement, the history-cell decision and its four reasons, the `.page-counts` rule) and § S7
  </read_first>
  <behavior>
    - A DONE job with 12/2/10 renders `<p class="page-counts">12 pages scanned, 2 blank removed, 10 uploaded</p>` as the last paragraph of the terminal branch, after the Done line and before the thumbnail
    - A FALLBACK job renders the same line after the outcome line and after the optional warning line
    - A job with 1/0/1 renders `1 page scanned, 0 blank removed, 1 uploaded` -- a measured zero renders as 0
    - Parametrised over the six terminal cases (DONE, FALLBACK, ERROR, CANCELLED, REJECTED, a pre-Phase-23 row with NULL counts): the `.page-counts` element is present for exactly the two that record counts and **absent entirely** for the other four -- no empty element, no blank line
    - No branch of `status.html` or `history.html` guards a count with truthiness; the guard is the `page_counts` filter returning `None` (assert by source grep that no template contains `if job.pages_`)
    - The history Title cell renders the same counts line as a `<span class="page-counts">` when counts exist and nothing when they do not
    - The history table still has four `<th>`s and the empty-state row still says `colspan="4"`
    - The history Time cell matches `^\d{4}-\d{2}-\d{2} \d{2}:\d{2} \S+$`
    - `app.css` gains exactly one `.page-counts` rule, `display: block` with `--pico-muted-color`, and no new colour literal
  </behavior>
  <action>
    RED (commit `test(30-12): ...`): extend tests/test_web_state_rendering.py with a
    `TestPageCounts` class parametrised over the six terminal cases, and a `TestHistoryTimeCell`
    class with the regex assertion driven under a fixed `TZ`. Add the source-grep assertions for
    truthiness guards, the four `<th>`s and `colspan="4"`. Add a CSS assertion that the hex-literal
    set in app.css is unchanged. Run: they fail.

    GREEN (commit `feat(30-12): ...`):
    - `status.html`: add `{% set counts = job | page_counts %}{% if counts %}<p class="page-counts">{{ counts }}</p>{% endif %}`
      to the DONE and FALLBACK branches only, positioned per UI-SPEC S3 (after the outcome line and
      FALLBACK's warning line, before the thumbnail). Add a comment stating D-32 and Pitfall 4: the
      guard is the filter returning `None`, never truthiness, because four of the six terminal cases
      have NULL counts (`worker.py:1348-1352` leaves them NULL on error and cancel, `job.py:782-786`
      on a refused submit, and every pre-Phase-23 row was never backfilled), while a measured 0 is a
      true measurement and renders. Add a second sentence recording that success criterion 3 means
      "every terminal job that recorded counts" -- ERROR and CANCELLED rows have none by
      construction and D-32 accepts that -- so a later reader does not chase a phantom.
    - `history.html`: replace line 4's `strftime` with `{{ job.created_at | local_time }}`, and put
      the counts `<span class="page-counts">` inside the existing Title `<td>`. Comment the choice
      against a fifth column with UI-SPEC S3's reasons: no `colspan` drift (Pitfall 11 is designed
      out rather than guarded against), the existing `@media (max-width: 576px)` rule already wraps
      `#history-body td` so a two-line cell needs no new breakpoint, and a fifth column would compete
      with the Time column that S7 simultaneously widens.
    - `app.css`: append the `.page-counts` rule with the UI-SPEC comment (the counts are a quiet
      second line under the outcome, in the status area and the Title cell alike; muted, never a
      status colour, because a count is a measurement and not an outcome). Do **not** wrap the line
      in `<small>` -- inside the table the mobile rule already reduces the cell and a nested
      `<small>` would compound to about 12 px and create a fifth type size.
  </action>
  <verify>
    <automated>uv run pytest tests/test_web_state_rendering.py tests/test_web.py tests/test_web_checks.py -x -q && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "page_counts" src/saneless/web/templates/partials/status.html` is 2 (DONE and FALLBACK)
    - `grep -c "page_counts" src/saneless/web/templates/partials/history.html` is 1
    - `grep -rn "if job.pages_" src/saneless/web/templates/ | wc -l` is 0
    - `grep -n 'colspan="4"' src/saneless/web/templates/partials/history.html` matches once
    - `grep -c "<th>" src/saneless/web/templates/index.html` is 4
    - `grep -n "strftime" src/saneless/web/templates/partials/history.html` matches zero times
    - `grep -c ".page-counts" src/saneless/web/static/app.css` is 1
    - `grep -cE "#[0-9a-fA-F]{6}" src/saneless/web/static/app.css` is unchanged from before this task
    - `uv run pytest tests/test_web_state_rendering.py -k "pages or time_cell" -q` selects at least 10 tests and passes
    - Full non-browser suite passes
    - Gate commands exit 0
  </acceptance_criteria>
  <done>Counts appear where they were recorded and nowhere else; timestamps name their zone. VALIDATION rows: APPL-03 counts per terminal case, APPL-12 history Time cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job row (`error`, `title`) -> rendered HTML | Exception-derived text and user-supplied titles cross into the page |
| server configuration (log path) -> rendered HTML | A host filesystem path must never cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-52 | Information disclosure | the technical-details disclosure | mitigate | D-13 limits it to `job.error`, the category and the job id. The log file path is explicitly excluded and a grep assertion over `web/templates/` proves no template references one |
| T-30-53 | Information disclosure | the request-error slot disclosure | mitigate | Limited to the HTTP status code and, when written, the rejected job id; Phase 26 D-10 and ASVS V7 forbid exception text and request input on this surface |
| T-30-54 | Tampering (XSS) | `job.error` and `job.title` inside the disclosure and the Title cell | mitigate | Jinja autoescape is on; no `\|safe` is introduced; the template source is asserted to contain no `\|safe` |
| T-30-55 | Spoofing (data) | rendering `0` for a NULL count | mitigate | The `page_counts` filter returns `None` unless all three are non-NULL; a six-case parametrised test asserts the element is absent, and a source grep forbids truthiness guards |
| T-30-56 | Spoofing (accessibility) | the next step falling outside the alert | mitigate | `role="alert"` wraps both the sentence and the next step; a test asserts the alert element contains both strings and that the `<details>` is outside it |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `uv run pytest tests/test_web_state_rendering.py tests/test_web_errors.py tests/test_cross_origin.py -q` green
- `uv run pytest -m "not browser and not sane_hardware" -q` green
- `grep -rniE "log_file|log_path|logfile" src/saneless/web/templates/` empty
- `grep -rn "|safe" src/saneless/web/templates/` empty
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests` all exit 0
</verification>

<success_criteria>
- One alert containing both the sentence and the next step; one collapsed disclosure outside it
- NULL `error_category` renders today's shape verbatim
- Counts render for exactly the two terminal cases that record them, and a measured 0 renders
- Four columns, `colspan="4"`, and a Time cell that names the zone
- No new colour literal in `app.css`
</success_criteria>

<output>
Create `.planning/phases/30-appliance-layer/30-12-SUMMARY.md` when done
</output>
