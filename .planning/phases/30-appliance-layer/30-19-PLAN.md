---
phase: 30-appliance-layer
plan: 19
type: execute
wave: 13
depends_on: [30-17]
files_modified:
  - tests/test_browser.py
autonomous: true
requirements: [APPL-07, APPL-09, APPL-10]
decisions: [D-15, D-23, D-24, D-26, D-27, D-28, D-30, D-31]

must_haves:
  truths:
    - "Two real browser contexts, both behind the extracted egress gate, show the owner the Continue and Abort controls and the non-owner 'Waiting for the stack to be flipped' with zero flip controls (P9, success criterion 5)"
    - "The owner context's cookie jar holds saneless_owner with httpOnly true, sameSite Lax and no expiry; the non-owner context has no such cookie -- the browser-level proof of research assumption A6"
    - "The owner's flip prompt renders exactly two controls and no third override, force-continue or take-over control exists in either context (D-26 absence guard)"
    - "Abort raises a native dialog whose message equals the locked copy; dismissing it issues no request and accepting it aborts (P10)"
    - "Every tag row is at least 44x44 CSS px (P11); filtering keeps both ticks, pins the excluded one, and submits both ids (P12)"
    - "POST /api/scan carries no q field, and Enter in the filter issues GET /api/tags and no POST /api/scan (P13)"
    - "show_tags=false removes the tag markup entirely and a scan still applies the profile's default_tags (P14)"
    - "With a placeholder token the Scan button stays disabled across several status polls -- the C-10 regression guard for the new flag -- and stripping the disabled attribute in devtools and clicking is still refused, with a Failed history row (P16, P17)"
  artifacts:
    - path: "tests/test_browser.py"
      provides: "the two-context owner test and browser assertions P9-P14, P16, P17"
      contains: "browser.new_context()"
  key_links:
    - from: "tests/test_browser.py two-context test"
      to: "_make_gate"
      via: "both hand-made contexts install the same gate and share one blocked list"
      pattern: "_make_gate\\("
---

<objective>
Prove success criterion 5 honestly: two browsers, two cookie jars, one owner. Then prove the form is
usable with a thumb, and that the disabled Scan button is only a courtesy.

Purpose: the owner gate, the filter's tick preservation and the form-owner behaviour all depend on
real cookie jars, real HTML form-owner semantics and a real `window.confirm`. A unit test can only
approximate them; per CLAUDE.md none of this may be deferred to a human.
Output: P9-P14, P16 and P17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-appliance-layer/30-UI-SPEC.md
@.planning/phases/30-appliance-layer/30-VALIDATION.md
@.planning/phases/30-appliance-layer/30-RESEARCH.md
@CLAUDE.md

<interfaces>
From `tests/test_browser.py` (after plan 30-17):
- `_make_gate(blocked, allowlist)` — the module-level gate factory. Every context created in this
  module must install it, and the shared `blocked` list must be asserted empty at teardown
- the session-scoped `browser_server` fixture and the `egress_allowlist` fixture
- `_BrowserTestScanner` — the stub backend gated on a `threading.Event`, which is how a job is held
  at `AWAITING_FLIP` while assertions run
- the existing flip test around line 622, which builds a job with
  `job_store.create_job(profile="duplex", title="Flip In Browser")`
- `app.state.paperless.get_tags` is patched by attribute assignment, the technique used at
  `test_browser.py:326-345` for `upload_document` / `poll_task`
- `time.sleep` appears once, at line ~226. Add none.

Exact copy these tests assert, from `.planning/phases/30-appliance-layer/30-UI-SPEC.md`:
- non-owner line `Waiting for the stack to be flipped`
- Abort dialog `Abort this scan? It will stop and cannot be resumed.`
- blocked reason `The paperless-ngx API token has not been set — see System status above.`
- rejected job-row error `Not started: the paperless-ngx API token has not been set`

Playwright facilities needed: `browser.new_context()` for a second cookie jar,
`context.cookies()` for the cookie-attribute assertions, `page.on("dialog", ...)` for the native
confirm, `page.on("request", ...)` or `page.route(...)` to record and inspect requests,
`bounding_box()` for touch-target measurement, and `page.evaluate` to remove an attribute.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: two browser contexts, the owner gate, and the Abort dialog (P9, P10)</name>
  <files>tests/test_browser.py</files>
  <read_first>
    - tests/test_browser.py `_make_gate` and the `context` fixture, as left by plan 30-17
    - tests/test_browser.py the existing flip test around line 622 -- the technique for driving a job to the flip prompt
    - src/saneless/web/templates/partials/status.html three-way AWAITING_FLIP branch and partials/flip.html (plan 30-13)
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § Verification Contract rows P9 and P10, and § S5 in full
    - .planning/phases/30-appliance-layer/30-RESEARCH.md § 9 "Two browser contexts" and § Pitfall 9
  </read_first>
  <action>
    Add two tests marked `@pytest.mark.browser`.

    P9, two contexts. Create `owner_ctx = browser.new_context()` and
    `viewer_ctx = browser.new_context()`. Install `_make_gate(blocked, egress_allowlist)` on **both**
    with `ctx.route("**/*", ...)`, sharing one `blocked` list, and assert that list is empty in a
    `finally` that also closes both contexts. A hand-made context carries no gate otherwise, which is
    exactly the trap Pitfall 9 names. From the owner context submit a manual-duplex scan and drive it
    to `AWAITING_FLIP` with the stub scanner's `Event` gate, then assert:
    - the owner page shows `Continue` and `Abort scan`
    - the viewer page shows `Waiting for the stack to be flipped` and zero flip controls: count
      elements whose `hx-post` attribute starts with `/api/flip/` and assert the count is 0
    - `owner_ctx.cookies()` contains `saneless_owner` with `httpOnly` true, `sameSite` equal to
      `Lax`, and an expiry indicating a session cookie. Playwright reports `-1` for a session
      cookie; assert whichever representation the installed version produces and comment the
      assertion so a future reader knows why the number is what it is
    - `viewer_ctx.cookies()` contains no `saneless_owner`
    - both pages show the same state line and the same job title, so only the flip controls differ
    - the D-26 absence guard: the owner's flip prompt renders exactly two controls, and neither page
      contains any element whose text matches a case-insensitive `override|take over|force`
    Release the scanner gate in the same `finally`.

    P10, Abort confirmation. From an owner context at `AWAITING_FLIP`, register a
    `page.on("dialog", ...)` handler that records the dialog message. Click `Abort scan` and assert
    the recorded message equals `Abort this scan? It will stop and cannot be resumed.`. Dismiss it
    and assert no `POST /api/flip/abort` request was issued, using a request-recording
    `page.on("request", ...)` list rather than a timeout. Then click again, accept, and assert the
    abort takes effect.

    Use Playwright `expect(...)` auto-waiting for every wait. Add no `time.sleep`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_browser.py -k "two_contexts or abort_confirm" -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "browser.new_context()" tests/test_browser.py` is 2, and each is followed within ten lines by a `_make_gate` installation
    - `grep -n "saneless_owner" tests/test_browser.py` matches at least twice (owner present, viewer absent)
    - `grep -c "Waiting for the stack to be flipped" tests/test_browser.py` is 1
    - `grep -c "Abort this scan? It will stop and cannot be resumed." tests/test_browser.py` is 1
    - `grep -c "time.sleep" tests/test_browser.py` is not greater than before this task
    - `uv run pytest tests/test_browser.py -k two_contexts -q` passes with the shared `blocked` assertion covering both contexts
    - Gate commands exit 0
  </acceptance_criteria>
  <done>Success criterion 5's owner and non-owner requirement is proven in two real browsers, offline. VALIDATION rows: APPL-09 two contexts, Abort confirm.</done>
</task>

<task type="auto">
  <name>Task 2: the tag picker in a real browser (P11, P12, P13, P14)</name>
  <files>tests/test_browser.py</files>
  <read_first>
    - src/saneless/web/templates/index.html tags fieldset and the `#tag-filter-form` sibling (plan 30-16)
    - src/saneless/web/templates/partials/tags.html (plan 30-16)
    - src/saneless/web/static/app.css `label.tag-option` and `.tag-list`
    - tests/test_browser.py lines 320-350 (how `app.state.paperless` methods are patched for the browser server) and the `browser_server` fixture's parametrisation, to decide how P14 gets a second configuration
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § Verification Contract rows P11-P14 and § S6 "How this solves A-5 and A-6"
  </read_first>
  <action>
    Configure the browser server's tag list to at least ten tags with distinguishable names, then add
    four tests marked `@pytest.mark.browser`.

    P11, touch targets. For every `label.tag-option`, assert `bounding_box()["height"]` is at least
    44 and `bounding_box()["width"]` is at least 44. Once is enough; the rule is
    scheme-independent, so do not parametrise over colour schemes.

    P12, filtering preserves ticks (Amendment A-5). Tick two tags whose names share no substring.
    Type into the filter so that one of them is excluded by the filter text. After the swap assert:
    both are still checked; the excluded one is rendered above the filtered list, compared by DOM
    order; and submitting the form sends both ids -- intercept the `POST /api/scan` request and read
    its post data, or assert on the created job's tags through the job store, whichever the module's
    existing tests make cheaper.

    P13, the form owner (Amendment A-6). Record requests with `page.on("request", ...)`. Assert the
    `POST /api/scan` body contains no `q` field. Separately, focus the filter input, type a value,
    press Enter, and assert that a `GET /api/tags` request was issued and that no `POST /api/scan`
    was -- this is the browser-level proof that implicit submission goes to `#tag-filter-form` and
    that Enter in the filter can never start a scan.

    P14, the simpler form. Serve a configuration with `show_tags = false` -- read the
    `browser_server` fixture first and either parametrise it or start a second server the same way
    it does, whichever fits the existing structure. Assert the page contains no `#tags-list`, no
    `#tag-filter` and no `#tag-filter-form`, and that a scan submitted from that page still applies
    the selected profile's `default_tags`, asserted on the created job row rather than on markup.
  </action>
  <verify>
    <automated>uv run pytest tests/test_browser.py -k "touch_target or tag_filter or form_owner or simple_form" -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_browser.py -k touch_target -q` passes and the assertion reads a real `bounding_box()`
    - `uv run pytest tests/test_browser.py -k tag_filter -q` passes and asserts DOM order for the pinned tag
    - `uv run pytest tests/test_browser.py -k form_owner -q` passes and asserts both halves: no `q` in the scan body, and Enter issuing `GET /api/tags` with no `POST /api/scan`
    - `uv run pytest tests/test_browser.py -k simple_form -q` passes and asserts the profile's `default_tags` landed on the job
    - `grep -c "time.sleep" tests/test_browser.py` is not greater than before this task
    - Gate commands exit 0
  </acceptance_criteria>
  <done>A thumb can use the tag picker, a tick can never be lost, and Enter never scans. VALIDATION rows: APPL-10 touch targets, filter preserves ticks, simpler form.</done>
</task>

<task type="auto">
  <name>Task 3: the blocked Scan button and the guard behind it (P16, P17)</name>
  <files>tests/test_browser.py</files>
  <read_first>
    - src/saneless/web/templates/partials/scan_button.html and the reason line in index.html (plan 30-14)
    - src/saneless/web/routes.py `start_scan`'s placeholder guard (plan 30-14)
    - tests/test_browser.py -- how the browser server's settings are built, so a placeholder token can be configured
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § Verification Contract rows P16 and P17, and § S8 in full
  </read_first>
  <action>
    Serve a configuration whose paperless-ngx token is `changeme`, and add two tests marked
    `@pytest.mark.browser`.

    P16, the courtesy. Assert `#scan-btn` is `disabled`, its trimmed text is exactly `Scan`, it
    carries `aria-describedby="scan-blocked-reason"`, and `#scan-blocked-reason` is visible with the
    exact copy `The paperless-ngx API token has not been set — see System status above.`. Then let
    the status poll run several ticks -- wait on an observable consequence such as a completed
    `GET /api/jobs/current/status` request rather than sleeping -- and assert the button is **still**
    disabled. This is the C-10 regression guard for the new flag: on htmx 2.0.8 an inherited
    `hx-disabled-elt` strips `disabled` from a server-disabled button when a child request finishes,
    and the flag living in the single button partial is what prevents it. Finally, with a real token
    configured, assert `#scan-btn` is enabled and `#scan-blocked-reason` does not exist.

    P17, the enforcement. With the placeholder token, use `page.evaluate` to remove the `disabled`
    attribute from `#scan-btn`, then click it. Assert `#status-message` shows the new rejection
    message, that no job started (the status area does not enter a busy state), and that a `Failed`
    row with `Not started: the paperless-ngx API token has not been set` appears in Job History.
    Comment the test with D-15's split: the button is the courtesy and this test is what proves the
    route guard is the enforcement.

    Finally, add or extend a module-level note recording, per CLAUDE.md, that every browser
    behaviour in this phase is automated and none is deferred to a human, and that the only
    out-of-suite item is real-scanner reachability against physical hardware, which
    `30-VALIDATION.md` records.
  </action>
  <verify>
    <automated>uv run pytest tests/test_browser.py -q && uv run pytest -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "The paperless-ngx API token has not been set — see System status above." tests/test_browser.py` is 1
    - `grep -c "Not started: the paperless-ngx API token has not been set" tests/test_browser.py` is 1
    - `grep -n "removeAttribute\|page.evaluate" tests/test_browser.py` matches at least once in the P17 test
    - `grep -riE "manual.only|needs human|human verification" tests/test_browser.py` matches zero times
    - `uv run pytest tests/test_browser.py -q` passes offline with the gate's `blocked` list empty on every context
    - `uv run pytest -q` (full suite) passes
    - `uv run prek run --all-files` and `uv run prek run --stage pre-push --all-files` both exit 0
    - Gate commands exit 0
  </acceptance_criteria>
  <done>The disabled button is proven to stay disabled, and proven not to be the thing that stops a scan. VALIDATION rows: APPL-07 disabled button, guard enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI browser -> the public internet | A hand-made Playwright context that escapes the egress gate can make real network requests |
| browser DOM -> the scan route | A test deliberately tampers with the page to prove the server does not rely on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-90 | Information disclosure | two hand-made contexts bypassing the egress gate | mitigate | Both install `_make_gate` (plan 30-17) and share one `blocked` list asserted empty at teardown; a grep gate asserts every `new_context()` is followed by a gate installation (Pitfall 9) |
| T-30-91 | Elevation of privilege | a non-owner answering the flip prompt | mitigate | P9 asserts the viewer context renders zero `/api/flip/` controls and holds no owner cookie, in a real browser with a real cookie jar -- the honest sample for success criterion 5 |
| T-30-92 | Elevation of privilege | a tampered DOM bypassing the scan refusal | mitigate | P17 removes the `disabled` attribute and clicks; the route still refuses and a REJECTED row appears. This is the test that proves the guard, not the button, is the enforcement (D-15) |
| T-30-93 | Information disclosure | the owner token reaching the page or a script | mitigate | P9 reads the cookie only through `context.cookies()`, which is how an `HttpOnly` cookie is observable from a test but not from page script; no assertion reads `document.cookie`, because nothing can |
| T-30-94 | Tampering (data loss) | a filter swap dropping a tick in a real browser | mitigate | P12 exercises the real `hx-include` path and asserts both ids reach the submit |
| T-30-95 | Denial of service (test flake) | sleep-based waits | mitigate | Every wait is Playwright auto-waiting, a recorded-request assertion or a `threading.Event` handshake; a grep gate asserts the `time.sleep` count did not rise |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing; Chromium 1228 is already cached |
</threat_model>

<verification>
- `uv run pytest tests/test_browser.py -q` green **offline**, with every context gated and `blocked` empty
- `uv run pytest -q` (full suite) green
- `uv run prek run --all-files` and `uv run prek run --stage pre-push --all-files` both green -- the phase gate
- `grep -c "time.sleep" tests/test_browser.py` not greater than before this plan
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests` all exit 0
</verification>

<success_criteria>
- P9, P10, P11, P12, P13, P14, P16 and P17 are implemented and pass
- Every hand-made context is gated and the shared `blocked` list is asserted empty
- No browser check is marked manual-only; no new `time.sleep`
- The full suite and both `prek` stages are green, which is the phase's exit gate
</success_criteria>

<output>
Create `.planning/phases/30-appliance-layer/30-19-SUMMARY.md` when done
</output>
