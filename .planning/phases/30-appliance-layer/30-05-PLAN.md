---
phase: 30-appliance-layer
plan: 05
type: tdd
wave: 2
depends_on: [30-02]
files_modified:
  - src/saneless/auto_profiles.py
  - tests/test_auto_profiles.py
  - docs/how-to/configure-scan-profiles.md
autonomous: true
requirements: [APPL-05]
decisions: [D-18, D-19]

must_haves:
  truths:
    - "Generated profiles carry a label and a description derived from what the code already knows -- classify_source and duplex -- with no new probe and no new config key (D-19)"
    - "The three label forms are 'Feeder, single-sided', 'Feeder, double-sided' and 'Glass (flatbed)', and each has its own plain-language description sentence"
    - "label and description are members of _OWNED_KEYS, so --force overwrites them in place exactly like source/mode/resolution, and a profile without auto_generated is never touched (D-18, Phase 27 D-02/D-03)"
    - "_generated_values emits label and description unconditionally, which makes Phase 27 D-03's delete branch unreachable for them"
    - "The how-to guide documents both keys, that --force destroys a hand-typed label on a flagged profile, that removing auto_generated is the escape hatch, and that --force is what backfills labels into a config written before this phase (Amendment A-3)"
  artifacts:
    - path: "src/saneless/auto_profiles.py"
      provides: "_profile_label, _profile_description, extended _OWNED_KEYS and _generated_values"
      contains: "def _profile_label("
    - path: "docs/how-to/configure-scan-profiles.md"
      provides: "label/description rows and the escape-hatch paragraph"
      contains: "auto-profiles --force"
  key_links:
    - from: "src/saneless/auto_profiles.py _generated_values"
      to: "_profile_label / _profile_description"
      via: "unconditional emission"
      pattern: "\"label\": _profile_label\\("
---

<objective>
Make `saneless auto-profiles` write the human name and the sentence the scan form will show, as
tool-owned keys that behave exactly like every other generated key.

Purpose: APPL-05's dropdown has nothing to render until generation produces it, and D-18 chose one
consistent ownership rule over a special case. The text comes from the source classification the
code already performs, so nothing new is probed.
Output: `_profile_label`, `_profile_description`, two new owned keys, and the how-to rows that
tell an operator how to take a profile over.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-appliance-layer/30-CONTEXT.md
@.planning/phases/30-appliance-layer/30-RESEARCH.md
@.planning/phases/30-appliance-layer/30-PATTERNS.md
@.planning/phases/30-appliance-layer/30-UI-SPEC.md
@CLAUDE.md

<interfaces>
From `src/saneless/scanner/base.py`:
- `class SourceKind(StrEnum)` lines 37-56 — `FLATBED`, `FEEDER`, `FEEDER_DUPLEX`, `AUTO`,
  `UNKNOWN`, plus the `uses_feeder` property implemented with `match`/`assert_never`
- `classify_source(source: str) -> SourceKind` line 62 — its docstring states it is the ONLY
  source-classification rule in the codebase and that no other site may re-derive the answer

From `src/saneless/auto_profiles.py`:
- `_auto_source_mode(source: str, *, has_flatbed: bool) -> Literal["flatbed", "adf"]` line 280 —
  the keyword-only-boolean convention and its `Args:` note explaining that a positional boolean is
  not allowed by this project's lint rules
- `_duplex(source: str) -> Literal["none", "hardware"]` line 305 — its docstring already names
  "Phase 30's APPL-05 -- generated `label` / `description` such as 'Feeder, double-sided'" as its
  eventual reader, and must be updated now that it has one
- `generate_profiles(...)` line 340 — constructs each `ProfileConfig`, already passing
  `duplex=_duplex(source)` and `auto_source_mode=_auto_source_mode(...)`
- `_OWNED_KEYS: Final` lines 491-498 — `("source", "resolution", "mode", "auto_source_mode",
  "duplex", "auto_generated")`; the tuple order is the file key order for a newly written table
- `_generated_values(profile: ProfileConfig)` lines 590-622 — emits `source`/`resolution`/`mode`
  unconditionally and `auto_source_mode`/`duplex` only when non-default, then `auto_generated = True`
- `_merge_profile(...)` lines 763-813 — iterates `_OWNED_KEYS` and sets or deletes; **no change is
  needed here**, adding two keys to the tuple is sufficient
- `write_profiles_to_config(...)` line 817 — no change

From `src/saneless/config.py` (plan 30-02): `ProfileConfig.label`, `ProfileConfig.description`,
`PROFILE_LABEL_MAX_LENGTH`, `PROFILE_DESCRIPTION_MAX_LENGTH`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _profile_label and _profile_description, derived not probed</name>
  <files>src/saneless/auto_profiles.py, tests/test_auto_profiles.py</files>
  <read_first>
    - src/saneless/auto_profiles.py lines 276-340 (`_auto_source_mode` and `_duplex` in full -- signature style, keyword-only boolean, docstring shape)
    - src/saneless/scanner/base.py lines 37-110 (`SourceKind`, `uses_feeder`, `classify_source` and its "ONLY classification rule" docstring)
    - src/saneless/auto_profiles.py lines 340-440 (`generate_profiles` -- where the constructed ProfileConfig gains the two new arguments)
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § S4 "Generated description text"
  </read_first>
  <behavior>
    - `_profile_label("ADF Front")` is `"Feeder, single-sided"`; `_profile_label("ADF Duplex")` is `"Feeder, double-sided"`; `_profile_label("Flatbed")` is `"Glass (flatbed)"`
    - `_profile_label("Auto")` and `_profile_label("Some Unknown Source")` each return a non-empty label and never an empty string
    - `_profile_description` returns a distinct, non-empty, period-terminated sentence for each `SourceKind`, and the feeder-duplex one says both sides are scanned using the document feeder
    - Every label is at most `PROFILE_LABEL_MAX_LENGTH` and every description at most `PROFILE_DESCRIPTION_MAX_LENGTH`, parametrised over `list(SourceKind)`
    - Neither function interpolates the raw SANE source string into its return value (the strings are developer constants; a vendor source name never reaches the page through this path)
    - `generate_profiles` produces profiles whose `label` and `description` match the two functions for each generated source
  </behavior>
  <action>
    RED (commit `test(30-05): ...`): add a `TestProfileLabels` class to tests/test_auto_profiles.py
    parametrised over `list(SourceKind)` for the completeness and bound assertions, with explicit
    exact-string assertions for the three D-19 forms, plus a test that no returned string contains
    the source name passed in. Extend the existing `generate_profiles` test to assert the generated
    `ProfileConfig`s carry the labels. Run: they fail.

    GREEN (commit `feat(30-05): ...`): add `_profile_label(source: str) -> str` and
    `_profile_description(source: str) -> str` as siblings of `_duplex`, both dispatching on
    `classify_source(source)` with a `match` over `SourceKind` and `case _: assert_never(kind)`,
    one local assigned in every arm, one trailing `return`, and a `Raises: AssertionError:`
    docstring section -- the shape `SourceKind.uses_feeder` itself uses. Do not re-derive the
    classification from the string (`classify_source`'s docstring forbids it). Labels:
    `FEEDER` -> "Feeder, single-sided"; `FEEDER_DUPLEX` -> "Feeder, double-sided";
    `FLATBED` -> "Glass (flatbed)"; `AUTO` -> "Automatic"; `UNKNOWN` -> "Scanner source".
    Descriptions, one plain sentence each in the project's copy style (sentence case, no emoji,
    terminal period): FEEDER "Scans one side of every page using the document feeder.";
    FEEDER_DUPLEX "Scans both sides of every page using the document feeder.";
    FLATBED "Scans one page at a time from the glass."; AUTO "Lets the scanner choose where the
    page comes from."; UNKNOWN "Uses the scanner source this profile names." Both docstrings must
    state D-19: the text is derived from facts the code already has, there is no new probe and no
    new config key, and every string is a developer constant that never interpolates the SANE
    source name. Update `_duplex`'s docstring paragraph that says APPL-05 is its "eventual reader"
    to say `_profile_label` and `_profile_description` are its readers now. Pass `label=` and
    `description=` on each `ProfileConfig` constructed in `generate_profiles`, beside the existing
    `duplex=` and `auto_source_mode=` arguments.
  </action>
  <verify>
    <automated>uv run pytest tests/test_auto_profiles.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def _profile_label(source: str) -> str:" src/saneless/auto_profiles.py` matches once
    - `grep -n "def _profile_description(source: str) -> str:" src/saneless/auto_profiles.py` matches once
    - `grep -c "assert_never" src/saneless/auto_profiles.py` increases by exactly 2
    - `grep -n "Feeder, double-sided" src/saneless/auto_profiles.py` matches once
    - `grep -n "eventual reader" src/saneless/auto_profiles.py` matches zero times
    - `uv run pytest tests/test_auto_profiles.py -k label -q` selects at least 12 tests and passes
    - Gate commands exit 0; `git log --oneline` shows `test(30-05):` before `feat(30-05):`
  </acceptance_criteria>
  <done>Generated profiles carry human text derived from the one classification rule. VALIDATION row: APPL-05 three label/description forms.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: label and description as owned keys, and the how-to that explains ownership</name>
  <files>src/saneless/auto_profiles.py, tests/test_auto_profiles.py, docs/how-to/configure-scan-profiles.md</files>
  <read_first>
    - src/saneless/auto_profiles.py lines 485-500 (`_OWNED_KEYS` and its full comment)
    - src/saneless/auto_profiles.py lines 585-630 (`_generated_values` in full, including the conditional emission for auto_source_mode and duplex)
    - src/saneless/auto_profiles.py lines 760-830 (`_merge_profile`'s set/delete loop and `write_profiles_to_config`)
    - tests/test_auto_profiles.py -- the existing `--force` round-trip tests that assert default_tags and comments survive
    - docs/how-to/configure-scan-profiles.md lines 50-70 (the profile-field table, including the `Scan <time>` fallback shape at line ~58) and line ~157 (the verbatim owned-key list)
    - .planning/phases/30-appliance-layer/30-RESEARCH.md § 7 and § Amendment A-3
  </read_first>
  <behavior>
    - `_OWNED_KEYS` begins with `("label", "description", "source", ...)` -- the human name is the first key a reader meets in a newly written table
    - `_generated_values(profile)` always contains `label` and `description`, even when they equal the model defaults (unconditional emission, unlike `auto_source_mode`/`duplex`)
    - A `--force` refresh over an existing `auto_generated = true` table overwrites `label` and `description` in place and leaves `default_tags`, a hand-written `title` and any TOML comments untouched
    - A hand-typed `label` on an `auto_generated = true` profile is overwritten by `--force` (the documented, consistent rule)
    - A profile **without** `auto_generated` is not touched at all: its absent `label` stays absent
    - Because emission is unconditional, the `_merge_profile` delete branch never fires for `label` or `description`; assert that a table that had both keys still has both after a refresh
    - `docs/how-to/configure-scan-profiles.md` contains both key names, the string `auto-profiles --force`, and the word `auto_generated` in the escape-hatch paragraph
  </behavior>
  <action>
    RED (commit `test(30-05): ...`): extend tests/test_auto_profiles.py with the six behaviours,
    reusing the existing `--force` round-trip fixture so the comment/`default_tags` preservation
    assertions are the ones already trusted in that file. Add a doc-truth test asserting the how-to
    mentions both keys and the force command -- read the file in the test rather than hard-coding
    a line number. Run: they fail.

    GREEN (commit `feat(30-05): ...`):
    - Insert `"label"` and `"description"` as the **first two** entries of `_OWNED_KEYS` and extend
      its comment to say why they are first (the tuple order is the file key order for a newly
      written table, so a human opening the file reads the human name before the SANE source
      string) and that D-18 makes them behave exactly like `source`/`mode`/`resolution`.
    - In `_generated_values`, emit `"label": _profile_label(profile.source)` and
      `"description": _profile_description(profile.source)` unconditionally, at the top of the
      `values` dict. Comment that the emission is deliberately unconditional -- unlike
      `auto_source_mode` and `duplex`, which are written only when non-default -- and that this is
      what makes Phase 27 D-03's "an owned key a fresh generation does not write is deleted" branch
      unreachable for these two, so a free-text field can never be silently pruned.
    - Leave `_merge_profile` and `write_profiles_to_config` unchanged; confirm by test rather than
      by editing.
    - Edit `docs/how-to/configure-scan-profiles.md`: add `label` and `description` rows to the
      profile-field table describing each in one line; extend the verbatim owned-key list at
      line ~157 with both names; and add a short paragraph next to the escape hatch saying in plain
      words that these are the first free-text keys the tool owns, so a hand-typed label on a
      profile that still carries `auto_generated = true` is overwritten by
      `saneless auto-profiles --force`, that removing `auto_generated` hands the profile back to
      the operator permanently, and that `saneless auto-profiles --force` is also what backfills
      names into a config written before this release -- startup generation only runs on a bare
      default config, so an existing config keeps empty labels until it is run (Amendment A-3).
      Do not touch the `Scan <time>` fallback shape at line ~58; plan 30-10 owns that correction.
  </action>
  <verify>
    <automated>uv run pytest tests/test_auto_profiles.py tests/test_deployment_config.py -x -q && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n -A 3 "_OWNED_KEYS: Final = (" src/saneless/auto_profiles.py` shows `"label",` then `"description",` as the first two entries
    - `grep -n '"label": _profile_label(' src/saneless/auto_profiles.py` matches once and is not inside an `if`
    - `grep -c "label" docs/how-to/configure-scan-profiles.md` is at least 3
    - `grep -n "auto-profiles --force" docs/how-to/configure-scan-profiles.md` matches at least once
    - `git diff --stat src/saneless/auto_profiles.py` shows no change inside `_merge_profile` or `write_profiles_to_config`
    - `uv run pytest tests/test_auto_profiles.py -q` passes with at least 6 new tests
    - Full non-browser suite passes
    - Gate commands exit 0
  </acceptance_criteria>
  <done>The two keys are tool-owned with one consistent rule and the operator's escape hatch is documented. VALIDATION row: APPL-05 `--force` rewrites in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SANE device -> generated profile text -> HTML | Vendor-controlled source names reach a file that is later rendered on a page |
| tool-owned keys -> operator's config file | Generation overwrites keys in a file the operator also edits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-17 | Tampering (XSS) | generated `label` / `description` | mitigate | Both are developer-constant strings selected by `classify_source`; the raw SANE source name is never interpolated, asserted by a test. The fields are additionally bounded by `max_length` (plan 30-02) and autoescaped at render time |
| T-30-18 | Tampering (data loss) | `--force` overwriting a hand-typed label | accept | D-18 chose one consistent ownership rule deliberately; the documented escape hatch is removing `auto_generated`. The how-to now says so in plain words next to the rule, which is the mitigation the decision asked for |
| T-30-19 | Denial of service | unbounded generated text | mitigate | Parametrised test asserts every label and description fits the config field caps, so generation can never produce a value its own schema rejects |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `uv run pytest tests/test_auto_profiles.py -q` green with the new label and ownership tests
- `uv run pytest -m "not browser and not sane_hardware" -q` green
- `saneless auto-profiles --force` against a fixture config rewrites both keys in place and leaves comments and `default_tags` intact (asserted by the round-trip test)
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests` all exit 0
</verification>

<success_criteria>
- Generation writes the three D-19 label forms and a distinct description for every `SourceKind`
- `label`/`description` are the first two `_OWNED_KEYS` and are emitted unconditionally
- `_merge_profile` is unchanged and the delete branch is proven unreachable for these keys
- The how-to documents both keys, the overwrite rule, the escape hatch and the `--force` backfill
</success_criteria>

<output>
Create `.planning/phases/30-appliance-layer/30-05-SUMMARY.md` when done
</output>
