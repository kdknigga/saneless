---
phase: 30-appliance-layer
plan: 18
type: execute
wave: 12
depends_on: [30-08, 30-16]
files_modified:
  - docker-compose.yml
  - docs/how-to/deploy-docker-compose.md
  - docs/reference/docker.md
  - docs/reference/configuration.md
  - docs/reference/environment-variables.md
  - docs/reference/web-api.md
  - docs/explanation/architecture.md
  - docs/getting-started/first-web-ui-scan.md
  - tests/test_deployment_config.py
  - .planning/UI-SPEC.md
  - .planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md
autonomous: true
requirements: [APPL-05, APPL-07, APPL-10, APPL-11, APPL-12]
decisions: [D-17, D-28]

must_haves:
  truths:
    - "docker-compose.yml ships its environment: block fully commented out, with a note that uncommenting it overrides the config file, so ./config/config.toml is the one place for the secret (D-17)"
    - "docker-compose.yml gains the consume-directory mount with its two-line explanation (APPL-11) and a TZ line with a one-line explanation, because a container reports UTC by default and APPL-12 would be technically satisfied and practically useless (Pitfall 10)"
    - "The kris-knigga image reference and every other kris-knigga string in docker-compose.yml is left untouched -- that is DLVR-01 in Phase 31 and has a CI grep guard behind it"
    - "The docs tell an existing operator to remove their own SANELESS_PAPERLESS__TOKEN environment line, or their real config.toml stays overridden and the strip stays red"
    - "docs/reference/docker.md no longer ships SANELESS_PAPERLESS__TOKEN=changeme, which this release now detects as a placeholder and shows red"
    - "docs/reference/configuration.md documents the [web] section and acknowledges that web_host/web_port stayed in [output] for backwards compatibility"
    - "docs/reference/web-api.md documents every new route and the new RequestRejection member"
    - "docs/explanation/architecture.md's claim that nothing writes or displays pages_scanned, pages_removed, pages_uploaded and owner_token is corrected"
    - ".planning/UI-SPEC.md stops describing superseded behaviour: all nine edits the phase UI-SPEC requires are made"
  artifacts:
    - path: "docker-compose.yml"
      provides: "the commented env block, the consume mount, the TZ line"
      contains: "TZ="
    - path: "docs/reference/configuration.md"
      provides: "the [web] section reference"
      contains: "show_tags"
  key_links:
    - from: "docker-compose.yml"
      to: "./config/config.toml"
      via: "the read-write config mount as the one place for the secret"
      pattern: "./config:/etc/saneless"
---

<objective>
Make the shipped deployment template and every reference document true again, and stop the master
UI-SPEC from describing behaviour this phase replaced.

Purpose: the review's U-01 finding was that the compose file silently overrides the config file's
token. Fixing the code without fixing the template leaves the trap in place. And an operator whose
container reports UTC gets APPL-12 in letter and not in spirit.
Output: the corrected compose template, seven corrected documents, and the master UI-SPEC edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-appliance-layer/30-CONTEXT.md
@.planning/phases/30-appliance-layer/30-RESEARCH.md
@.planning/phases/30-appliance-layer/30-UI-SPEC.md
@CLAUDE.md

<interfaces>
`docker-compose.yml` today (37 lines, read it whole before editing):
- line 13 `image: ghcr.io/kris-knigga/saneless:latest` — **do not touch**; DLVR-01 in Phase 31 owns it
- line 23 a `https://github.com/kris-knigga/saneless#configuration` link — **do not touch**, same reason
- lines 17-24 the `./config:/etc/saneless` mount with the EBUSY comment (Phase 27 D-09)
- lines 25-27 the `saneless-data:/var/lib/saneless` volume
- lines 28-33 the `environment:` block: `SANELESS_PAPERLESS__URL=http://paperless:8000` and
  `SANELESS_PAPERLESS__TOKEN=changeme` live, plus two already-commented scanner-host lines
- lines 3-9 the header comment block, whose third bullet already mentions an optional consume
  directory that the file never actually shows

Documents to correct (all verified to exist):
- `docs/how-to/deploy-docker-compose.md`, `docs/reference/docker.md` (line ~178 ships
  `SANELESS_PAPERLESS__TOKEN=changeme`), `docs/reference/configuration.md`,
  `docs/reference/environment-variables.md`, `docs/reference/web-api.md`,
  `docs/explanation/architecture.md` (line ~99 claims nothing writes or displays `outcome`,
  `pages_scanned`, `pages_removed`, `pages_uploaded`, `warning` and `owner_token`),
  `docs/getting-started/first-web-ui-scan.md` (walks the current form)

`tests/test_deployment_config.py` already parses `docker-compose.yml` and the docs; its
`_documented_codes` machinery and the compose assertions are the hooks to extend.

New routes to document in `web-api.md`: `GET /api/checks`, `POST /api/checks/refresh`,
`GET /api/profiles/description`, `GET /api/jobs/{job_id}/status`, and `GET /api/tags`'s new `q` and
`tags` query parameters. New rejection: `TOKEN_UNSET`, HTTP 503, with its message.

Master UI-SPEC edits required: `.planning/phases/30-appliance-layer/30-UI-SPEC.md`
§ "Master UI-SPEC Edits Required in This Phase" lists nine numbered items; item 2 also names
`.planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md` S4's "rule carried forward unchanged"
heading, and item 9 says the `No correspondent` / `-- None --` copy inconsistency stays open.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: the compose template and the deployment docs</name>
  <files>docker-compose.yml, docs/how-to/deploy-docker-compose.md, docs/reference/docker.md, tests/test_deployment_config.py</files>
  <read_first>
    - docker-compose.yml (whole file)
    - docs/how-to/deploy-docker-compose.md (whole file -- find every place the environment block or the token is described)
    - docs/reference/docker.md (whole file, especially the `SANELESS_PAPERLESS__TOKEN=changeme` example around line 178 and the volumes section)
    - docs/explanation/consume-directory-fallback.md (for the correct description of what the consume directory does, so the compose comment matches it)
    - tests/test_deployment_config.py -- the existing compose-parsing assertions
    - .planning/phases/30-appliance-layer/30-CONTEXT.md D-17 and the APPL-11 paragraph
    - .planning/phases/30-appliance-layer/30-RESEARCH.md § Runtime State Inventory (the "live service config" rows) and § Pitfall 10
  </read_first>
  <action>
    Write the test first (commit `test(30-18): ...`), then the change (`docs(30-18): ...`).

    Tests to add to tests/test_deployment_config.py:
    - Every line inside `docker-compose.yml`'s `environment:` block that sets
      `SANELESS_PAPERLESS__URL` or `SANELESS_PAPERLESS__TOKEN` is commented out
    - The compose file contains a consume-directory bind mount line and at least two comment lines
      immediately above it explaining it
    - The compose file sets `TZ` (commented or live -- assert whichever form the change ships, and
      assert an explanatory comment line sits above it)
    - No token value in the compose file or in `docs/reference/docker.md` satisfies
      `is_placeholder_token` while being presented as a working example: assert
      `SANELESS_PAPERLESS__TOKEN=changeme` appears nowhere uncommented in either file
    - `kris-knigga` still appears in `docker-compose.yml` exactly as many times as before this
      phase, so the DLVR-01 rename is provably untouched (pin the current count)

    Changes:
    - `docker-compose.yml`: comment out both live `environment:` lines and add a comment above the
      block stating D-17 in plain words -- `./config/config.toml` is the one place for the secret;
      uncommenting any line here **overrides** the file, which is the silent override the review
      found; and an existing deployment that copied this template must remove its own
      `SANELESS_PAPERLESS__TOKEN` line or its real `config.toml` stays overridden and the status
      strip stays red. Keep the `environment:` key itself present (with only comments under it) only
      if that is valid YAML for this file -- if an empty mapping would break `docker compose config`,
      comment out the `environment:` key too and say so in the comment. Add the consume-directory
      bind mount with exactly the two-line explanation APPL-11 asks for: what it is for (paperless-ngx
      file-based ingestion when the API upload cannot be made) and that the same path must be set as
      `paperless.consume_dir` in the config. Add a `TZ=America/Chicago`-style line with a one-line
      explanation that a container reports UTC by default, so without it every timestamp saneless
      shows is UTC. Touch nothing containing `kris-knigga`.
    - `docs/how-to/deploy-docker-compose.md`: mirror all three changes, and add an upgrade note for
      an existing operator telling them to remove their own environment token line. State the
      consequence explicitly rather than implying it.
    - `docs/reference/docker.md`: replace the `SANELESS_PAPERLESS__TOKEN=changeme` example with one
      that is not a detected placeholder and add a sentence explaining that placeholder values are
      detected and refuse scans. Document the consume-directory mount and the `TZ` variable in the
      volumes and environment sections.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -x -q && docker compose -f docker-compose.yml config >/dev/null && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^      - SANELESS_PAPERLESS__TOKEN=" docker-compose.yml` matches zero times (only commented forms remain)
    - `grep -c "changeme" docker-compose.yml docs/reference/docker.md` is 0 for both files, or every occurrence is inside a comment explaining that it is a detected placeholder
    - `grep -n "TZ=" docker-compose.yml` matches once
    - `grep -c "kris-knigga" docker-compose.yml` equals the count recorded before this task
    - `grep -n "consume" docker-compose.yml` matches at least twice (the mount and its explanation)
    - `docker compose -f docker-compose.yml config` exits 0
    - `uv run pytest tests/test_deployment_config.py -q` passes with at least 5 new assertions
    - Gate commands exit 0
  </acceptance_criteria>
  <done>The shipped template no longer overrides the config file's secret, shows the fallback mount, and sets the zone. VALIDATION rows: APPL-07 one place for the secret, APPL-11 compose mount, APPL-12 TZ.</done>
</task>

<task type="auto">
  <name>Task 2: the reference and explanation documents</name>
  <files>docs/reference/configuration.md, docs/reference/environment-variables.md, docs/reference/web-api.md, docs/explanation/architecture.md, docs/getting-started/first-web-ui-scan.md, tests/test_deployment_config.py</files>
  <read_first>
    - docs/reference/configuration.md (whole file -- the `[scanner]`, `[paperless]`, `[output]`, `[profiles.NAME]` section structure to mirror)
    - docs/reference/environment-variables.md (whole file -- the row format)
    - docs/reference/web-api.md (whole file -- the per-route documentation format and the rejection-message table)
    - docs/explanation/architecture.md around line 99 (the claim about unwritten, undisplayed columns)
    - docs/getting-started/first-web-ui-scan.md (whole file -- the walkthrough of the current form)
    - src/saneless/config.py `WebConfig` and src/saneless/web/routes.py (the authoritative route list after plans 30-11 through 30-16)
    - .planning/phases/30-appliance-layer/30-RESEARCH.md § 10 (the table of additional pages this phase falsifies) and § Open Question 2
  </read_first>
  <action>
    Write the doc-truth tests first (commit `test(30-18): ...`), then the documents
    (`docs(30-18): ...`).

    Tests to add to tests/test_deployment_config.py:
    - Every route decorator in `src/saneless/web/routes.py` has a matching heading or entry in
      `docs/reference/web-api.md` -- derive the route list from the source rather than hard-coding
      it, so a future route cannot be added without documenting it
    - Every field of `WebConfig` appears in `docs/reference/configuration.md` and has a
      `SANELESS_WEB__*` row in `docs/reference/environment-variables.md`, derived from the model's
      fields rather than hard-coded
    - Every member of `RequestRejection` appears in `docs/reference/web-api.md`
    - `docs/explanation/architecture.md` does not contain the phrase "nothing writes any of them yet"

    Changes:
    - `docs/reference/configuration.md`: add a `[web]` section documenting `show_tags` and
      `show_correspondent`, each with its default and one sentence of effect, plus the D-29 note
      that hiding a control does not change what a scan does because the profile's `default_tags`
      and `default_correspondent` still apply. Add one sentence acknowledging that `web_host` and
      `web_port` stayed under `[output]` for backwards compatibility, so a reader is not confused by
      a `[web]` section that does not hold the bind address.
    - `docs/reference/environment-variables.md`: add the `SANELESS_WEB__SHOW_TAGS` and
      `SANELESS_WEB__SHOW_CORRESPONDENT` rows, and a `TZ` row noting it is a standard container
      variable rather than a saneless one and that it is what makes local timestamps local.
    - `docs/reference/web-api.md`: document `GET /api/checks`, `POST /api/checks/refresh`,
      `GET /api/profiles/description`, `GET /api/jobs/{job_id}/status`, and `GET /api/tags`'s new
      `q` and `tags` query parameters, each in the file's existing per-route format. Add the
      `TOKEN_UNSET` rejection with its HTTP 503 and its exact message to the rejection table.
    - `docs/explanation/architecture.md`: correct the line around 99. State what is true now:
      `pages_scanned`, `pages_removed` and `pages_uploaded` are written by the worker's success path
      (Phase 23) and are displayed in the status area and the history Title cell; `owner_token` is
      written by the web layer at submit (Phase 30) and gates the flip prompt; and name whichever of
      `outcome` and `warning` genuinely remain undisplayed, rather than deleting the sentence
      wholesale.
    - `docs/getting-started/first-web-ui-scan.md`: update the walkthrough for the status strip, the
      profile labels with their description line, the checkbox tag picker with its filter, and the
      help text. Keep it a getting-started narrative, not a reference.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -x -q && uv run pytest -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "show_tags" docs/reference/configuration.md` matches at least once and `show_correspondent` matches at least once
    - `grep -n "SANELESS_WEB__SHOW_TAGS" docs/reference/environment-variables.md` matches once
    - `grep -c "/api/checks" docs/reference/web-api.md` is at least 2
    - `grep -n "TOKEN_UNSET\|The paperless-ngx API token has not been set, so the scan was not started." docs/reference/web-api.md` matches at least once
    - `grep -c "nothing writes any of them yet" docs/explanation/architecture.md` is 0
    - `grep -n "System status" docs/getting-started/first-web-ui-scan.md` matches at least once
    - `uv run pytest tests/test_deployment_config.py -q` passes with the four new derived doc-truth tests
    - Gate commands exit 0
  </acceptance_criteria>
  <done>Every reference document describes what the code now does, and derived tests keep it that way. VALIDATION rows: APPL-05/07/10/11/12 documentation.</done>
</task>

<task type="auto">
  <name>Task 3: the master UI-SPEC edits</name>
  <files>.planning/UI-SPEC.md, .planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md</files>
  <read_first>
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § "Master UI-SPEC Edits Required in This Phase" (all nine numbered items) and the sections they reference: § Design System new classes, § Color, § Spacing Scale, § Copywriting Contract, § Interaction Map
    - .planning/UI-SPEC.md -- § Deliberate design decisions, § Interaction Patterns (the Scan button state table and the htmx orchestration map), § Components, § Project-specific classes, § Copywriting Contract, § Color, § Spacing Scale
    - .planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md § S4 (the "State table (rule carried forward unchanged)" heading and the table beneath it)
  </read_first>
  <action>
    Make all nine edits listed in `30-UI-SPEC.md` § "Master UI-SPEC Edits Required in This Phase",
    in order, as a documentation commit (`docs(30-18): ...`):
    1. § Deliberate design decisions: replace the recorded decision that `Abort scan` has no
       confirmation dialog with D-27's decision and the exact `hx-confirm` copy
       `Abort this scan? It will stop and cannot be resumed.`
    2. § Interaction Patterns Scan-button state table, **and** the identical table in
       `26-UI-SPEC.md` S4: add the row for a placeholder token as a second independent source of
       `disabled` that leaves the label `Scan` and `aria-busy` untouched and adds
       `aria-describedby="scan-blocked-reason"`, carried by every out-of-band `#scan-btn` re-render;
       add the note that `#scan-blocked-reason` is never an OOB target; and change
       `26-UI-SPEC.md` S4's "State table (rule carried forward unchanged)" heading so it stops
       claiming the rule is unchanged.
    3. § Components: `Tags multi-select` becomes `Tags checkbox list`; add rows for the status strip,
       the tag filter, the profile description, the technical-details disclosure, the disabled-Scan
       reason line and `partials/terminal_reload.html`; update the component count.
    4. § Project-specific classes: add all fourteen rows / seventeen selectors from
       `30-UI-SPEC.md` § Design System, `#scan-blocked-reason` included.
    5. § Copywriting Contract: the status-area ERROR row becomes the category sentence plus the next
       step plus the disclosure; add the strip rows, the queue line, the non-owner line, the counts
       sentence, the five help lines, `Check again`, S8's blocked-button reason, and the new
       `RequestRejection` member's slot message and job-row text -- the latter two into the existing
       Phase 26 copy tables, not a new one.
    6. § Interaction Patterns: merge `30-UI-SPEC.md` § Interaction Map's delta table, including the
       extended "which responses carry what" table and the restated `hx-disinherit` promise.
    7. § Color: add `.check-ok`, `.check-warn`, `.check-fail`, `.check-checking` and `.page-counts`
       to the measured contrast table with the same values under the new class names, and add the
       checked state of a tag checkbox as entry 5 of the accent-reserved list.
    8. § Spacing Scale: add the 8 px `#scan-blocked-reason` usage and the 44 px / 112 px / 280 px
       rows **as justified exceptions**, with the justifications `30-UI-SPEC.md` records.
    9. Leave the `No correspondent` / `-- None --` copy inconsistency note in place: this phase does
       not touch the correspondent partial, so the note is still open.

    Change no values: every colour, ratio and measurement is quoted from the phase spec, which
    quotes the master's own measured table. Do not re-measure and do not invent a new token.
  </action>
  <verify>
    <automated>grep -c "hx-confirm" .planning/UI-SPEC.md && grep -c "scan-blocked-reason" .planning/UI-SPEC.md && grep -c "check-warn" .planning/UI-SPEC.md && grep -c "tag-option" .planning/UI-SPEC.md && uv run pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Abort this scan? It will stop and cannot be resumed." .planning/UI-SPEC.md` matches at least once
    - `grep -riE "Abort scan.{0,60}no confirmation" .planning/UI-SPEC.md` matches zero times
    - `grep -c "scan-blocked-reason" .planning/UI-SPEC.md` is at least 3 (state table, classes, copy)
    - `grep -n "check-ok" .planning/UI-SPEC.md` and `check-warn`, `check-fail`, `check-checking`, `page-counts` each match at least once
    - `grep -c "tag-option" .planning/UI-SPEC.md` is at least 2
    - `grep -riE "multi-select" .planning/UI-SPEC.md` matches zero times in the Tags context
    - `grep -riE "rule carried forward unchanged" .planning/phases/26-worker-and-web-robustness/26-UI-SPEC.md` matches zero times
    - `grep -n "No correspondent" .planning/UI-SPEC.md` still matches (item 9 leaves it open)
    - `uv run pytest -q` (full suite) passes
  </acceptance_criteria>
  <done>The master design contract describes the shipped UI, with no re-measured or invented values. VALIDATION row: the UI contract's own accuracy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped compose template -> operator deployments | What this file says becomes what people run |
| documentation -> operator behaviour | A false document causes a misconfiguration the code cannot prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-85 | Spoofing (configuration) | the compose `environment:` block silently overriding `config.toml` | mitigate | D-17: the block ships fully commented with a note that uncommenting it overrides the file, and a doc-truth test asserts no live token or URL line remains. An upgrade note tells existing operators to remove their own line |
| T-30-86 | Information disclosure | a shipped example token | mitigate | `changeme` is now a detected placeholder; the docs no longer present it as a working value, and a test asserts it does not appear uncommented in either file |
| T-30-87 | Spoofing (data) | documentation claiming columns are unwritten and undisplayed | mitigate | `architecture.md` is corrected and a doc-truth test asserts the stale phrase is gone |
| T-30-88 | Tampering (scope) | accidentally performing Phase 31's rename | mitigate | A test pins the `kris-knigga` occurrence count in `docker-compose.yml`, so this plan provably does not touch DLVR-01's lines |
| T-30-89 | Spoofing (documentation drift) | a future route or `[web]` key shipped undocumented | mitigate | The new doc-truth tests derive their expectations from the source -- the route decorators and the `WebConfig` fields -- rather than hard-coding a list |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `uv run pytest tests/test_deployment_config.py -q` green with the new derived doc-truth tests
- `uv run pytest -q` (full suite) green
- `docker compose -f docker-compose.yml config` exits 0
- `grep -c "kris-knigga" docker-compose.yml` equals the pre-plan count
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests` all exit 0
</verification>

<success_criteria>
- The compose template has one place for the secret, the consume mount and a `TZ` line, with `kris-knigga` untouched
- Seven documents describe what the code now does, with derived tests keeping them honest
- All nine master UI-SPEC edits are made, quoting the phase spec's values rather than new ones
</success_criteria>

<output>
Create `.planning/phases/30-appliance-layer/30-18-SUMMARY.md` when done
</output>
