---
phase: 30-appliance-layer
plan: 10
type: tdd
wave: 5
depends_on: [30-01, 30-02, 30-05, 30-08]
files_modified:
  - src/saneless/cli.py
  - src/saneless/config.py
  - tests/test_cli.py
  - tests/test_config.py
  - docs/how-to/configure-scan-profiles.md
  - docs/reference/cli-commands.md
  - docs/how-to/cli-scripting.md
autonomous: true
requirements: [APPL-04, APPL-07, APPL-12]
decisions: [D-12, D-16, D-34, D-35]

must_haves:
  truths:
    - "The CLI prints Phase 28's failure line byte-identically and then a second line prefixed exactly 'Try: ' carrying the category's next step, so scripts parsing line 1 are unaffected and Phase 28's doc-truth message-shape and exit-code tests keep passing (D-12)"
    - "saneless scan refuses with exit 2 when the paperless-ngx token is blank, whitespace or a literal-set placeholder, checked BEFORE the scanner is opened so no paper moves for an upload that cannot succeed (D-16)"
    - "The refusal is unconditional: a configured consume-directory fallback does not soften it, so doctor, the web UI and scan all agree on whether the appliance can work (D-16)"
    - "devices, auto-profiles and jobs are unaffected -- they never touch paperless-ngx"
    - "saneless jobs prints timestamps through the same local_time function the web filter uses, seconds dropped, so ts_w stays 22 and title_w stays 24 at 80 columns (D-34, D-35)"
    - "saneless jobs --json stays UTC ISO-8601: it is a machine contract pinned by docs/how-to/cli-scripting.md"
    - "resolve_job_title's 'Scan <time>' fallback renders in the server's local zone with the zone named, and the two documents quoting the old shape are corrected (RESEARCH Open Question 4)"
  artifacts:
    - path: "src/saneless/cli.py"
      provides: "the Try: line, the scan token guard, the local-time jobs table"
      contains: "Try: "
    - path: "src/saneless/config.py"
      provides: "resolve_job_title rendering local time"
      contains: "local_time("
  key_links:
    - from: "src/saneless/cli.py jobs table"
      to: "vocabulary.local_time"
      via: "direct import of the same function the Jinja filter is"
      pattern: "from saneless.vocabulary import .*local_time"
    - from: "src/saneless/cli.py scan"
      to: "config.is_placeholder_token"
      via: "the pre-scanner guard"
      pattern: "is_placeholder_token\\("
---

<objective>
Finish the CLI side of the appliance: advice after a failure, a refusal before paper moves, and
timestamps a human can read.

Purpose: D-12 was chosen precisely so Phase 28's locked line shape survives while a human still
gets a next step. D-16 refuses before the scanner is opened because a scan that cannot upload is
wasted paper. D-34/D-35 put the CLI and the web on one formatter so they cannot drift.
Output: the `Try:` line, `scan`'s token guard, and a local-time `jobs` table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-appliance-layer/30-CONTEXT.md
@.planning/phases/30-appliance-layer/30-RESEARCH.md
@.planning/phases/30-appliance-layer/30-PATTERNS.md
@.planning/phases/30-appliance-layer/30-UI-SPEC.md
@CLAUDE.md

<interfaces>
From `src/saneless/cli.py`:
- `_failure_line(exc: SanelessError, category: ErrorCategory) -> str` lines 267-297 — Phase 28
  D-08's shape `<what saneless was doing, with identifiers>: <original message>`; **must stay
  byte-identical**
- `_GuardedGroup.invoke` lines 386-424 — the `except SanelessError` arm at lines ~412-420:
  `category = classify_error(exc)`, `code = exit_code_for(category)`, and either
  `_report_unexpected(ctx, exc)` for `ExitCode.UNEXPECTED` or `_log_failure(ctx, exc)` plus
  `click.echo(_failure_line(exc, category), err=True)`, then `ctx.exit(code)`
- `scan` lines 503-596 — `require_sane()` first (with its comment), then `_load_cli_settings(ctx)`,
  then the scanner is opened; `status_callback` is passed at line ~548
- `jobs` lines 705-762 — `--json` output uses `j.created_at.isoformat()` at line ~731; the table at
  lines 742-761 uses `ts_w = 22`, `profile_w = 15`, `_STATUS_COL_WIDTH`, three separator spaces,
  `title_w = max(15, cols - (ts_w + profile_w + _STATUS_COL_WIDTH + 3))`, and renders
  `j.created_at.strftime('%Y-%m-%d %H:%M:%S')`
- `_STATUS_COL_WIDTH` line 81 — computes to 16; the derive-never-hard-code precedent

From `src/saneless/config.py`:
- `resolve_job_title(...)` line 309 — its fallback is
  `f"Scan {now.astimezone(UTC).strftime('%Y-%m-%d %H:%M')}"` at line ~333, and its docstring says
  "The timestamp is rendered in UTC whatever the zone of `now` (local time is APPL-12)"
- `is_placeholder_token` (plan 30-02)

From `src/saneless/vocabulary.py` (plan 30-01): `error_next_step`, `local_time`, `LOCAL_TIME_FORMAT`.

Measured (RESEARCH § 8): `%Y-%m-%d %H:%M %Z` is 19-22 characters, so `ts_w` stays 22 and
`title_w` stays 24 at 80 columns; the worst realistic zone token is 5 characters (`+0545`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the second CLI line, "Try: &lt;next step&gt;"</name>
  <files>src/saneless/cli.py, tests/test_cli.py</files>
  <read_first>
    - src/saneless/cli.py lines 250-300 (`_unexpected_line`, `_failure_line` in full)
    - src/saneless/cli.py lines 355-430 (`_GuardedGroup.invoke` in full, especially the `except SanelessError` arm)
    - tests/test_cli.py -- the existing tests that assert `_failure_line`'s exact output and the exit codes per category (Phase 28's doc-truth message-shape tests)
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § S2 "CLI (D-12)" -- the exact two-line specimen and the `Try: ` prefix
  </read_first>
  <behavior>
    - A `FeederEmptyError` raised inside a command produces on stderr, in order: Phase 28's failure line **unchanged**, then `Try: Load the pages squarely in the feeder, clear any jam, then start the scan again.`
    - The prefix is exactly `Try: ` -- one space, no colour, no glyph
    - The advice line is written to stderr, the same stream as the failure line, so a script redirecting stdout is unaffected
    - Every `ErrorCategory` that reaches the `SanelessError` arm produces an advice line equal to `error_next_step(category)`; parametrise over `list(ErrorCategory)`
    - The `ExitCode.UNEXPECTED` path (`_report_unexpected`) is **unchanged** -- no advice line, because that arm handles non-saneless exceptions whose category is a guess
    - A `KeyboardInterrupt`/`ScanCancelledError` still exits 130 with no advice line -- a cancel is not a failure
    - Exit codes for every category are unchanged from Phase 28
  </behavior>
  <action>
    RED (commit `test(30-10): ...`): extend tests/test_cli.py with a `TestFailureAdvice` class.
    Assert the two-line output with a `CliRunner` whose `mix_stderr` behaviour the existing tests
    already establish -- read them and copy it. Parametrise the advice-equals-`error_next_step`
    assertion over `list(ErrorCategory)`. Add an explicit regression assertion that the first line
    still equals `_failure_line(exc, category)` byte for byte. Run: they fail.

    GREEN (commit `feat(30-10): ...`): in `_GuardedGroup.invoke`'s `except SanelessError` arm, after
    the existing `click.echo(_failure_line(exc, category), err=True)`, add
    `click.echo(f"Try: {error_next_step(category)}", err=True)`. Do not touch `_failure_line`, the
    `_report_unexpected` branch, the cancel branch or any exit code. Add a comment citing D-12: the
    first line is Phase 28 D-08's locked shape and is printed unchanged so scripts parsing it and
    its doc-truth tests are unaffected; the second line is the same `error_next_step` string the web
    page renders, which is why the copy is surface-neutral and never says "press Scan" or "run the
    command". Import `error_next_step` from `saneless.vocabulary` beside the existing imports.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -x -q && uv run pytest tests/test_deployment_config.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'f"Try: {error_next_step(category)}"' src/saneless/cli.py` matches once
    - `git diff src/saneless/cli.py | grep -c "^-.*_failure_line"` is 0 (the function and its call site are untouched)
    - `grep -n "def _failure_line(exc: SanelessError, category: ErrorCategory) -> str:" src/saneless/cli.py` matches once
    - `uv run pytest tests/test_cli.py -k advice -q` selects at least 9 tests and passes
    - `uv run pytest tests/test_deployment_config.py -q` passes (Phase 28's doc-truth tests unaffected)
    - Gate commands exit 0; `git log --oneline` shows `test(30-10):` before `feat(30-10):`
  </acceptance_criteria>
  <done>A human gets the next step; a script sees the same first line it always did. VALIDATION row: APPL-04 CLI second line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: saneless scan refuses a placeholder token before the scanner is opened</name>
  <files>src/saneless/cli.py, tests/test_cli.py</files>
  <read_first>
    - src/saneless/cli.py lines 500-600 (`scan` in full -- `require_sane()`, `_load_cli_settings`, and the exact point the scanner is opened)
    - src/saneless/cli.py lines 386-424 (how a `ConfigError` becomes exit 2 through the guard -- the refusal must go through the guard, never a bare `sys.exit`)
    - src/saneless/config.py `is_placeholder_token` (plan 30-02)
    - tests/conftest.py (`StubScannerBackend`) and tests/test_cli.py's scan tests -- find how they count scanner opens, or add a counting subclass
    - .planning/phases/30-appliance-layer/30-CONTEXT.md D-16
  </read_first>
  <behavior>
    - With a blank token, `saneless scan` exits 2 and the stub scanner records **zero** opens and zero `get_devices` calls
    - With `changeme`, likewise
    - With a real-looking token, `scan` proceeds exactly as today
    - The refusal happens even when `paperless.consume_dir` is configured -- a fallback does not soften it (D-16)
    - The refusal message names the problem in the project's CLI failure-line shape and is followed by the `Try:` advice line from Task 1 (it is a `ConfigError`, so it classifies as `ErrorCategory.CONFIG`)
    - `saneless devices`, `saneless auto-profiles` and `saneless jobs` are unaffected by a placeholder token: each still exits 0 in its normal path
  </behavior>
  <action>
    RED (commit `test(30-10): ...`): extend tests/test_cli.py with a `TestScanTokenRefusal` class
    covering the six behaviours. Use a `StubScannerBackend` subclass that counts `open`/`get_devices`
    calls so "before the scanner is opened" is asserted rather than assumed. Add the three
    unaffected-command tests explicitly. Run: they fail.

    GREEN (commit `feat(30-10): ...`): in `scan`, immediately after
    `settings = _load_cli_settings(ctx)` and **before** any scanner access, raise a `ConfigError`
    when `is_placeholder_token(settings.paperless.token.get_secret_value())`. The message is a
    developer constant naming the problem without the value -- for example
    `the paperless-ngx API token has not been set`, phrased so Phase 28's
    `_failure_line` renders `Scanning '<title>' with profile '<profile>': the paperless-ngx API
    token has not been set` (read `_failure_line` and match its expectations exactly). Add a
    comment citing D-16: the check is before the scanner is opened so no paper moves for an upload
    that cannot succeed, the refusal is unconditional because a configured consume-directory
    fallback must not make `scan`, `doctor` and the web UI disagree about whether the appliance can
    work, and `devices`/`auto-profiles`/`jobs` are deliberately untouched because they never talk
    to paperless-ngx. Note in the comment that this is the second unwrap site for the token in
    `cli.py` -- the `PaperlessClient` construction is the other -- and that the value is passed to
    the predicate and never logged or echoed.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "is_placeholder_token(" src/saneless/cli.py` matches once, inside `scan`
    - Reading `src/saneless/cli.py`'s `scan` body confirms the guard precedes every scanner call
    - `grep -n "sys.exit" src/saneless/cli.py` count is unchanged (the refusal goes through the guard)
    - `SANELESS_PAPERLESS__TOKEN=changeme uv run saneless scan --profile default --title x; test $? -eq 2` succeeds
    - `uv run pytest tests/test_cli.py -k placeholder -q` selects at least 6 tests and passes
    - Gate commands exit 0
  </acceptance_criteria>
  <done>No paper moves for an upload that cannot succeed. VALIDATION row: APPL-07 `saneless scan` exits 2 before the scanner is opened.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: local timestamps in the jobs table and the generated title</name>
  <files>src/saneless/cli.py, src/saneless/config.py, tests/test_cli.py, tests/test_config.py, docs/how-to/configure-scan-profiles.md, docs/reference/cli-commands.md, docs/how-to/cli-scripting.md</files>
  <read_first>
    - src/saneless/cli.py lines 70-90 (`_STATUS_COL_WIDTH` and its derive-never-hard-code comment)
    - src/saneless/cli.py lines 700-765 (`jobs` in full: the `--json` `isoformat()` at ~731 and the table at 742-761 with every width constant)
    - src/saneless/config.py lines 300-340 (`resolve_job_title` in full, including the docstring sentence naming APPL-12)
    - docs/how-to/cli-scripting.md lines 40-70 (the documented `jobs --json` shape, including `"created_at"`)
    - docs/how-to/configure-scan-profiles.md around line 58 (the quoted `Scan <time>` shape)
    - docs/reference/cli-commands.md -- every place the `jobs` table output or the generated title shape is quoted
    - .planning/phases/30-appliance-layer/30-RESEARCH.md § 8 (the measured width table and assumption A3)
  </read_first>
  <behavior>
    - `saneless jobs` renders each timestamp with `local_time`, producing e.g. `2026-09-16 14:03 CDT`; seconds are gone
    - The timestamp column width is **derived** from a rendered sample rather than left at a literal 22, so a six-character zone token cannot truncate (RESEARCH assumption A3's cheap insurance); the derived value equals 22 for a three-letter zone
    - At an 80-column terminal, `title_w` is 24 -- unchanged from before this plan; assert it by invoking `jobs` with `COLUMNS=80` and measuring the rendered header
    - At a 40-column terminal the table still renders without raising and `title_w` is floored at 15
    - `saneless jobs --json` still emits `created_at` as a UTC ISO-8601 string with `+00:00`; assert it parses back to the same instant
    - `resolve_job_title` with a blank title produces `Scan 2026-09-16 14:03 CDT` under `TZ=America/Chicago` and the corresponding UTC-named string under `TZ=UTC`
    - The `local_time` the CLI imports is the identical object the Jinja filter registers (assert `saneless.cli.local_time is saneless.vocabulary.local_time`)
    - `docs/how-to/cli-scripting.md`'s documented `created_at` example carries `+00:00`
  </behavior>
  <action>
    RED (commit `test(30-10): ...`): extend tests/test_cli.py with a `TestJobsTableWidth` class
    covering the table behaviours (drive the width with `monkeypatch.setenv("COLUMNS", ...)` or by
    patching `shutil.get_terminal_size`, whichever the existing tests use) and a
    `TestJobsJsonContract` class pinning the UTC ISO output. Extend tests/test_config.py with the
    `resolve_job_title` local-zone assertions, using the `TZ` + `time.tzset()` technique from plan
    30-01's tests. Add the filter-identity assertion. Add a doc-truth test asserting
    `docs/how-to/cli-scripting.md`'s `created_at` example string ends with `+00:00`. Run: they fail.

    GREEN (commit `feat(30-10): ...`):
    - cli.py: import `local_time` from `saneless.vocabulary` and replace
      `j.created_at.strftime('%Y-%m-%d %H:%M:%S')` with `local_time(j.created_at)`. Replace the
      literal `ts_w = 22` with a derived `_TIME_COL_WIDTH` computed once at module level from a
      rendered sample of a known instant, following the `_STATUS_COL_WIDTH` precedent
      (`cli.py:77-81`) and carrying the same style of comment: derived rather than written down, so
      a zone abbreviation longer than three characters cannot silently truncate the column. Leave
      `profile_w`, `_STATUS_COL_WIDTH`, the three separator spaces and the `title_w` formula
      untouched. Do **not** touch the `--json` branch; add a comment at that line stating that the
      JSON stays UTC ISO because it is a machine contract documented in
      `docs/how-to/cli-scripting.md`, while the human table goes local (APPL-12 says "user-facing").
    - config.py: change `resolve_job_title`'s fallback to use `local_time(now)` prefixed by `Scan `,
      and rewrite the docstring sentence that currently says the timestamp is rendered in UTC
      whatever the zone of `now`. The new sentence must say it renders in the server's local zone
      with the zone named, that this is the same shared function the web history table and the
      `saneless jobs` table use so the three cannot disagree, and that the string is user-facing
      twice over -- it becomes the paperless-ngx document title and the text of the History Title
      cell. Import `local_time` from `saneless.vocabulary`.
    - Docs: correct the quoted `Scan <time>` shape in `docs/how-to/configure-scan-profiles.md` and
      every place `docs/reference/cli-commands.md` quotes the `jobs` table output or the generated
      title. Correct `docs/how-to/cli-scripting.md`'s `created_at` example to carry `+00:00` and add
      one sentence stating explicitly that the JSON output stays UTC while the table renders local
      time with the zone named, so a script author is not surprised.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py tests/test_config.py tests/test_deployment_config.py -x -q && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check --select DTZ . && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "local_time(j.created_at)" src/saneless/cli.py` matches once
    - `grep -n "ts_w = 22" src/saneless/cli.py` matches zero times and `_TIME_COL_WIDTH` matches at least twice
    - `grep -n "isoformat()" src/saneless/cli.py` still matches in the `--json` branch
    - `grep -n "astimezone(UTC)" src/saneless/config.py` matches zero times inside `resolve_job_title`
    - `uv run python -c "import saneless.cli as c, saneless.vocabulary as v; assert c.local_time is v.local_time"` exits 0
    - `COLUMNS=80 uv run saneless jobs 2>/dev/null | head -1 | wc -c` is at most 81
    - `uv run ruff check --select DTZ src/saneless/cli.py src/saneless/config.py` reports no diagnostic
    - Full non-browser suite passes
    - Gate commands exit 0
  </acceptance_criteria>
  <done>The CLI and the web read one formatter; the machine contract is untouched. VALIDATION rows: APPL-12 local_time shared, table width at 80 columns, `jobs --json` stays UTC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config token -> CLI control flow | A secret decides whether a command proceeds |
| exception text -> terminal | The advice line sits beside a line that already carries exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-41 | Information disclosure | the `Try:` advice line | mitigate | It renders `error_next_step(category)`, a developer constant; the exception's own text stays on line 1 where Phase 28 already decided it belongs. The parametrised test asserts the advice equals the constant, so nothing can be interpolated into it |
| T-30-42 | Information disclosure | `scan`'s refusal message | mitigate | A developer-constant `ConfigError` message naming the problem, never the token value or the paperless-ngx URL; the token is unwrapped only to call the predicate |
| T-30-43 | Spoofing (script contract) | changing `jobs --json` | mitigate | The `--json` branch is untouched and a doc-truth test pins the `+00:00` shape, so a later change cannot silently localise a machine contract |
| T-30-44 | Denial of service | a wasted scan for an impossible upload | mitigate | D-16's guard runs before the scanner is opened; a counting stub asserts zero opens |
| T-30-45 | Information disclosure | `resolve_job_title` leaking the server's zone into paperless-ngx | accept | The zone abbreviation is the intent (D-35); the document title is created by the operator's own appliance for their own paperless-ngx |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `uv run pytest tests/test_cli.py tests/test_config.py -q` green
- `uv run pytest tests/test_deployment_config.py -q` green (Phase 28's message-shape and exit-code doc-truth tests unaffected)
- `uv run pytest -m "not browser and not sane_hardware" -q` green
- `uv run ruff check --select DTZ .` clean
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests` all exit 0
</verification>

<success_criteria>
- Line 1 of a CLI failure is byte-identical to Phase 28's; line 2 is `Try: ` plus the category's next step
- `saneless scan` exits 2 on a placeholder token with zero scanner opens, fallback or no fallback
- `saneless jobs` renders local time with the zone named and still fits 80 columns; `--json` stays UTC
- `resolve_job_title` renders local time and the three documents quoting the old shapes are corrected
</success_criteria>

<output>
Create `.planning/phases/30-appliance-layer/30-10-SUMMARY.md` when done
</output>
