---
phase: 30-appliance-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/saneless/vocabulary.py
  - tests/test_vocabulary.py
autonomous: true
requirements: [APPL-03, APPL-04, APPL-07, APPL-08, APPL-12]
decisions: [D-10, D-11, D-15, D-32, D-33, D-34, D-35]

must_haves:
  truths:
    - "error_advice(category) returns a frozen ErrorAdvice(message, next_step) built by ONE match with assert_never; error_message and error_next_step are one-line accessors over it, so no second match over ErrorCategory exists (D-10, D-11, Pitfall 7)"
    - "Every one of the seven ErrorCategory members has a non-empty message AND a non-empty next_step, and the messages are byte-identical to today's error_message strings (D-10: APPL-04 adds the next step, it never removes the specific message)"
    - "local_time(aware_dt) renders '2026-09-16 14:03 CDT' -- the server's local zone with the zone abbreviation named on every timestamp (D-34, D-35)"
    - "page_counts returns None unless all three counts are non-None, and renders a measured 0 as 0 (D-32: NULL renders nothing; a true zero is not a NULL)"
    - "busy_line renders the queue line, the manual-duplex front count, or progress_label, in that precedence (D-33, APPL-08)"
    - "RequestRejection gains one new member for an unset paperless-ngx token with its own 503 message and its own job-row error constant; WORKER_DEGRADED is not reused (D-15)"
    - "ProfileStorage names the three outcomes of the worker's one startup persist attempt so the status strip can tell 'no config file' from 'could not write' (Amendment A-2)"
  artifacts:
    - path: "src/saneless/vocabulary.py"
      provides: "ErrorAdvice, error_advice, error_next_step, local_time, LOCAL_TIME_FORMAT, page_counts, PageCounted, busy_line, ProfileStorage, RequestRejection.TOKEN_UNSET, TOKEN_UNSET_JOB_ERROR"
      contains: "def error_advice(category: ErrorCategory) -> ErrorAdvice"
    - path: "tests/test_vocabulary.py"
      provides: "completeness tests retargeted at error_advice, local_time/page_counts/busy_line tests"
  key_links:
    - from: "src/saneless/vocabulary.py error_message"
      to: "src/saneless/vocabulary.py error_advice"
      via: "one-line accessor"
      pattern: "return error_advice\\(category\\)\\.message"
    - from: "src/saneless/vocabulary.py rejection_status_code"
      to: "RequestRejection.TOKEN_UNSET"
      via: "the existing 503 arm"
      pattern: "TOKEN_UNSET"
---

<objective>
Add every new developer-constant string and every new pure lookup this phase needs, in the one
leaf module where `match`/`assert_never` lookups live. Nothing here imports `job.py`,
`config.py`, `worker.py`, `cli.py` or anything under `web/`.

Purpose: eight later plans render these strings. Defining them first means no plan invents copy,
and the completeness tests make a missing next step a type/test failure rather than a blank line
on a household member's screen.
Output: `error_advice` and its two accessors, the shared `local_time` formatter, `page_counts`,
`busy_line`, `ProfileStorage`, and the new `RequestRejection` member with its two strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-appliance-layer/30-CONTEXT.md
@.planning/phases/30-appliance-layer/30-RESEARCH.md
@.planning/phases/30-appliance-layer/30-PATTERNS.md
@.planning/phases/30-appliance-layer/30-UI-SPEC.md
@CLAUDE.md

<interfaces>
From `src/saneless/vocabulary.py` (leaf module; imports only `saneless.exceptions`):
- `class ErrorCategory(StrEnum)` lines 70-93 — exactly seven members: FEEDER, CONFIG, SCANNER,
  UPLOAD, UNKNOWN, REJECTED, ASSEMBLY
- `class JobState(StrEnum)` — ten members, includes SCANNING_REVERSE and CANCELLED
- `error_message(category: ErrorCategory) -> str` line 476 — the seven existing sentences; its
  docstring currently says it is deliberately unwired and that "the error-message rework" is its
  arrival. This plan is that arrival: the docstring must be rewritten, not left.
- `connection_status_message(status: ConnectionStatus) -> str` line 525 — the `match` + one
  `message` local + `assert_never` + `Raises: AssertionError:` shape every new lookup copies
- `progress_label(state: JobState) -> str` — the in-progress prose `busy_line` falls back to
- `class RequestRejection(StrEnum)` lines 227-249 — QUEUE_FULL, WORKER_DOWN, WORKER_DEGRADED,
  UNKNOWN_PROFILE, TITLE_TOO_LONG, INVALID_REQUEST, CROSS_SITE, NOT_FOUND, METHOD_NOT_ALLOWED,
  INTERNAL, CLIENT_ERROR
- `rejection_message` line 588, `rejection_status_code` line 661 — the latter groups members that
  share a code: `case RequestRejection.WORKER_DOWN | RequestRejection.WORKER_DEGRADED: status_code = 503`
- Job-row error constants lines 257-263: `QUEUE_FULL_JOB_ERROR`, `WORKER_DOWN_JOB_ERROR`,
  `WORKER_DEGRADED_JOB_ERROR` — no trailing period, with the comment explaining why
- `__all__` lines 23-52 — explicit, alphabetised

From `tests/test_vocabulary.py`:
- `class TestErrorMessage` lines 284-300 — `@pytest.mark.parametrize("category", list(ErrorCategory))`
  completeness plus an exact-string test. This class is retargeted at `error_advice`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: error_advice, the seven next steps, and the new token rejection member</name>
  <files>src/saneless/vocabulary.py, tests/test_vocabulary.py</files>
  <read_first>
    - src/saneless/vocabulary.py lines 1-60 (module docstring, leaf rule, `__all__`)
    - src/saneless/vocabulary.py lines 220-270 (RequestRejection, TITLE_MAX_LENGTH, the three job-row error constants and their comment)
    - src/saneless/vocabulary.py lines 470-560 (error_message in full, connection_status_message in full)
    - src/saneless/vocabulary.py lines 580-700 (rejection_message, rejection_status_code)
    - tests/test_vocabulary.py lines 280-310 (TestErrorMessage)
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § S2 "The seven next steps" and § S8 (the new member's two strings)
  </read_first>
  <behavior>
    - `error_advice(ErrorCategory.FEEDER)` is a frozen, slotted `ErrorAdvice` whose `.message` equals the existing `"The document feeder is empty or jammed."` and whose `.next_step` equals `"Load the pages squarely in the feeder, clear any jam, then start the scan again."`
    - Parametrised over `list(ErrorCategory)`: `.message` and `.next_step` are both non-empty, neither equals `category.value`, and both end with a period
    - `error_message(c) == error_advice(c).message` and `error_next_step(c) == error_advice(c).next_step` for every member
    - `error_advice(cast("ErrorCategory", "UNRECOGNISED"))` raises `AssertionError`
    - `ErrorAdvice` is immutable: assigning to `.message` raises
    - `RequestRejection.TOKEN_UNSET` exists; `rejection_status_code(RequestRejection.TOKEN_UNSET) == 503`; `rejection_message(RequestRejection.TOKEN_UNSET)` equals the S8 sentence exactly
    - `TOKEN_UNSET_JOB_ERROR == "Not started: the paperless-ngx API token has not been set"` and does not end with a period
    - No string returned by `rejection_message` or `error_advice` contains `{`, `%s`, `http`, or the word `Traceback` (the developer-constant guard, Pattern B / ASVS V7)
  </behavior>
  <action>
    RED (commit `test(30-01): ...`): retarget `TestErrorMessage` in tests/test_vocabulary.py at
    `error_advice` — keep the existing parametrisation over `list(ErrorCategory)`, add the
    `.next_step` assertions, the accessor-equality assertions, the `cast(...)` unrecognised-value
    assertion (copy the shape already in that file) and the frozen-dataclass immutability
    assertion. Add a `TestTokenUnsetRejection` class with the member, status-code, message and
    job-row-constant assertions, and a developer-constant guard test parametrised over
    `list(RequestRejection)`. Run: they fail on import.

    GREEN (commit `feat(30-01): ...`):
    - Add `from dataclasses import dataclass` if absent, and `@dataclass(frozen=True, slots=True) class ErrorAdvice` with fields `message: str` and `next_step: str`, docstring "What a reader is told about an error category, and what to do next." Use the frozen-dataclass idiom from `web/routes.py:246-256` (`_ScanForm`).
    - Add `def error_advice(category: ErrorCategory) -> ErrorAdvice` using the `connection_status_message` shape at `vocabulary.py:525-557`: one `advice` local assigned in every arm, `case _: assert_never(category)`, one trailing `return`, Google docstring with `Args:`/`Returns:`/`Raises: AssertionError:`. The seven pairs are the UI-SPEC S2 table verbatim — messages unchanged from today's `error_message`, next steps new: FEEDER "Load the pages squarely in the feeder, clear any jam, then start the scan again."; CONFIG "Correct the saneless configuration file, then restart saneless."; SCANNER "Check the scanner is switched on and connected, then start the scan again."; UPLOAD "Check paperless-ngx is running and the API token is correct, then start the scan again."; ASSEMBLY "Start the scan again. If it keeps failing, check the server's free disk space."; REJECTED "Check the system status list for anything marked Failed, then start the scan again."; UNKNOWN "Start the scan again. If it keeps failing, check the saneless log." The docstring must state that the wording is surface-neutral because both the web page and the CLI render the same string, and that every string is a developer constant carrying no exception text, request input, URL, token or filesystem path (Pattern B, ASVS V7).
    - Rewrite `error_message` as a one-line accessor `return error_advice(category).message`, and add `error_next_step` as `return error_advice(category).next_step`. Delete the "deliberately NOT wired" paragraph from `error_message`'s docstring and replace it with one naming APPL-04 as the arrival and stating the D-10 rule: one `match` over `ErrorCategory` in this module, never two that can drift (Pitfall 7).
    - Add `TOKEN_UNSET = "TOKEN_UNSET"` to `RequestRejection` beside `WORKER_DEGRADED`. Give it its `rejection_message` arm with the exact sentence `The paperless-ngx API token has not been set, so the scan was not started. Put a real API token in the saneless config file, then restart saneless.` and add it to the existing `WORKER_DOWN | WORKER_DEGRADED` 503 arm in `rejection_status_code`. Comment the member with D-15's reason: `WORKER_DEGRADED` is deliberately not reused because "the scan service was unavailable" is untrue when the truth is that nobody set the token.
    - Add `TOKEN_UNSET_JOB_ERROR: Final = "Not started: the paperless-ngx API token has not been set"` beside the three existing job-row error constants, inside the same comment block's rule (no trailing period).
    - Extend `__all__` alphabetically with `ErrorAdvice`, `error_advice`, `error_next_step`, `TOKEN_UNSET_JOB_ERROR`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_vocabulary.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "match category:" src/saneless/vocabulary.py` counts exactly one `match` whose subject is an `ErrorCategory` (verify by reading the surrounding lines; `classify_error` is an isinstance chain, not a match)
    - `grep -n "def error_advice(category: ErrorCategory) -> ErrorAdvice:" src/saneless/vocabulary.py` matches once
    - `grep -n "return error_advice(category).message" src/saneless/vocabulary.py` matches once
    - `grep -n "return error_advice(category).next_step" src/saneless/vocabulary.py` matches once
    - `grep -n "TOKEN_UNSET" src/saneless/vocabulary.py` matches at least four times (enum member, rejection_message arm, rejection_status_code arm, job-error constant)
    - `grep -n "deliberately NOT wired" src/saneless/vocabulary.py` matches zero times
    - `uv run pytest tests/test_vocabulary.py -k advice -q` selects at least 14 tests and passes
    - Gate commands exit 0; `git log --oneline` shows `test(30-01):` before `feat(30-01):`
  </acceptance_criteria>
  <done>APPL-04's message-plus-next-step pair exists once, is complete over ErrorCategory, and APPL-07's rejection vocabulary exists. VALIDATION rows: APPL-04 completeness, APPL-07 rejection member.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: local_time, page_counts and busy_line</name>
  <files>src/saneless/vocabulary.py, tests/test_vocabulary.py</files>
  <read_first>
    - src/saneless/vocabulary.py lines 1-60 (the leaf rule -- this task must not import `job.py`)
    - src/saneless/vocabulary.py `progress_label` (the in-progress prose busy_line falls back to)
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § S3 (the counts sentence, the busy line table) and § S5 (the queue-position table) and § S7 (LOCAL_TIME_FORMAT)
    - .planning/phases/30-appliance-layer/30-RESEARCH.md § 8 (the verified `astimezone().strftime` output and the DTZ clearance)
  </read_first>
  <behavior>
    - `LOCAL_TIME_FORMAT == "%Y-%m-%d %H:%M %Z"`; with `TZ=America/Chicago`, `local_time(datetime(2026, 9, 16, 19, 3, tzinfo=UTC)) == "2026-09-16 14:03 CDT"`; with `TZ=UTC` the same input renders `"2026-09-16 19:03 UTC"`
    - `local_time` on a datetime carrying an explicit non-UTC tzinfo still renders the server's local zone (it calls `astimezone()` with no argument)
    - `page_counts` with 12/2/10 returns `"12 pages scanned, 2 blank removed, 10 uploaded"`; with 1/0/1 returns `"1 page scanned, 0 blank removed, 1 uploaded"`; with any of the three `None` returns `None`; with 0/0/0 returns `"0 pages scanned, 0 blank removed, 0 uploaded"` (a measured zero is not a NULL)
    - `busy_line(JobState.SCANNING)` equals `progress_label(JobState.SCANNING)`
    - `busy_line(JobState.PENDING, queue_title="Tax return", queue_ahead=1)` equals `"Waiting for 'Tax return' to finish (1 ahead of you)"`
    - `busy_line(JobState.PENDING, queue_title="Tax return", queue_ahead=0)` equals `"Waiting for 'Tax return' to finish (next in line)"` -- `(0 ahead of you)` is never produced
    - `busy_line(JobState.SCANNING_REVERSE, front_pages=12)` equals `"Front: 12 pages · " + progress_label(JobState.SCANNING_REVERSE)`; with `front_pages=1` the noun is `page`; with `front_pages=None` it equals `progress_label(JobState.SCANNING_REVERSE)` alone
    - `busy_line(JobState.SCANNING_REVERSE, front_pages=12, queue_title="X", queue_ahead=2)` renders the queue line -- the queue branch wins
  </behavior>
  <action>
    RED (commit `test(30-01): ...`): add `TestLocalTime`, `TestPageCounts` and `TestBusyLine` to
    tests/test_vocabulary.py. Drive the zone with `monkeypatch.setenv("TZ", ...)` followed by
    `time.tzset()` so the assertion is deterministic on any host, and restore by letting
    monkeypatch undo the env var plus an explicit `time.tzset()` in a fixture teardown. Assert the
    exact strings above. Run: they fail.

    GREEN (commit `feat(30-01): ...`):
    - `LOCAL_TIME_FORMAT: Final = "%Y-%m-%d %H:%M %Z"` with a comment citing D-34 and D-35: the
      zone abbreviation appears on every timestamp so a copy-pasted line is self-describing, and
      this is the one format both the web filter and the CLI table read so they cannot disagree.
    - `def local_time(value: datetime) -> str` returning `value.astimezone().strftime(LOCAL_TIME_FORMAT)`.
      Docstring: the argument must be timezone-aware (`_row_to_job` parses `created_at` from an
      isoformat string written with `datetime.now(tz=UTC)`), `astimezone()` with no argument uses
      the process's local zone so a container needs `TZ` set, and no `zoneinfo` import and no new
      config key is involved. Import `datetime` under `TYPE_CHECKING` only if ruff permits;
      otherwise a runtime `from datetime import datetime` is fine -- `datetime` is stdlib and does
      not violate the leaf rule.
    - `class PageCounted(Protocol)` with three read-only `@property` members `pages_scanned`,
      `pages_removed`, `pages_uploaded`, each `-> int | None`. Docstring: it exists so this leaf
      module can format a job's counts without importing `job.py`; `Job` satisfies it structurally.
    - `def page_counts(job: PageCounted) -> str | None`: return `None` unless all three are
      `is not None`; otherwise the sentence with only the first clause pluralised. Docstring must
      state D-32 in full: a NULL count renders nothing at all because four of the six terminal
      cases are NULL (ERROR, CANCELLED, REJECTED and every pre-Phase-23 row), while a measured 0
      is a true measurement and renders as 0 -- consumers must guard with `is not none`, never
      truthiness (Pitfall 4).
    - `def busy_line(state: JobState, *, queue_title: str | None = None, queue_ahead: int | None = None, front_pages: int | None = None) -> str`
      with the three-branch precedence above. The queue branch uses single quotes around the title
      exactly as APPL-08 writes it, and the title is the only user data any of these strings carry
      (autoescaped by Jinja at render time; never logged here). The separator is U+00B7 with a
      space either side. Docstring cites D-33 and the UI-SPEC S3 deviation note: the trailing
      phrase is `progress_label(SCANNING_REVERSE)`, which is master-pinned copy, not the
      history-table `state_label`.
    - Extend `__all__` alphabetically with `LOCAL_TIME_FORMAT`, `PageCounted`, `busy_line`,
      `local_time`, `page_counts`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_vocabulary.py -x -q && uv run ruff check --select DTZ,PL . && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'LOCAL_TIME_FORMAT: Final = "%Y-%m-%d %H:%M %Z"' src/saneless/vocabulary.py` matches once
    - `grep -n "def local_time(value: datetime) -> str:" src/saneless/vocabulary.py` matches once
    - `grep -n "def page_counts(job: PageCounted) -> str | None:" src/saneless/vocabulary.py` matches once
    - `grep -n "def busy_line(" src/saneless/vocabulary.py` matches once and the signature contains `queue_title`, `queue_ahead` and `front_pages` as keyword-only parameters
    - `grep -n "import zoneinfo\|from zoneinfo" src/saneless/vocabulary.py` matches zero times
    - `uv run ruff check --select DTZ src/saneless/vocabulary.py` reports no diagnostic
    - `uv run pytest tests/test_vocabulary.py -k "local_time or page_counts or busy_line" -q` selects at least 15 tests and passes
    - Gate commands exit 0
  </acceptance_criteria>
  <done>The one shared timestamp formatter, the counts sentence and the busy line exist and are tested. VALIDATION rows: APPL-12 local_time, APPL-03 counts, APPL-08 queue line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ProfileStorage, the three outcomes of the startup persist attempt</name>
  <files>src/saneless/vocabulary.py, tests/test_vocabulary.py</files>
  <read_first>
    - src/saneless/vocabulary.py lines 60-100 (the StrEnum + doc-truth-docstring shape)
    - src/saneless/worker.py lines 832-905 (`_persist_generated_profiles` -- the INFO "no config file" branch and the two WARNING "could not write" branches this enum names)
    - .planning/phases/30-appliance-layer/30-RESEARCH.md § Amendment A-2
    - .planning/phases/30-appliance-layer/30-UI-SPEC.md § S1 the three Profiles rows (OK / WARN unwritable / WARN no config file)
  </read_first>
  <behavior>
    - `{m.name for m in ProfileStorage} == {"PERSISTED", "IN_MEMORY_NO_CONFIG_FILE", "IN_MEMORY_UNWRITABLE"}` and each `.value` equals its `.name`
    - `ProfileStorage` is a `StrEnum` (so it compares equal to its own string value, matching every other enum in this module)
  </behavior>
  <action>
    RED (commit `test(30-01): ...`): add a `TestProfileStorage` class asserting the member set and
    the StrEnum value identity, copying the member-set assertion style already used for `JobState`
    and `ErrorCategory` in tests/test_vocabulary.py. Run: it fails.

    GREEN (commit `feat(30-01): ...`): add `class ProfileStorage(StrEnum)` with the three members.
    Its docstring is doc-truth and must say: the worker makes exactly one attempt to persist
    generated profiles at startup and records the outcome here, because
    `_persist_generated_profiles` returns `None` for two genuinely different situations -- no
    config file was loaded, and the file could not be written -- and the status strip's Profiles
    row must tell them apart (Amendment A-2, D-22). It must also say why a fresh `os.access()`
    probe at check time cannot substitute: Phase 27 D-09's motivating failure is EBUSY on a
    single-file bind mount, where the directory is writable and only the rename fails. Add
    `"ProfileStorage"` to `__all__` alphabetically. Do NOT set it from the worker in this plan --
    plan 30-04 owns `worker.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_vocabulary.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class ProfileStorage(StrEnum):" src/saneless/vocabulary.py` matches once
    - `grep -n "IN_MEMORY_UNWRITABLE" src/saneless/vocabulary.py` matches once
    - `grep -n "A-2" src/saneless/vocabulary.py` matches at least once (the docstring cites the amendment)
    - `uv run pytest tests/test_vocabulary.py -k ProfileStorage -q` selects at least 2 tests and passes
    - Full non-browser suite passes: `uv run pytest -m "not browser and not sane_hardware" -q`
    - Gate commands exit 0
  </acceptance_criteria>
  <done>The vocabulary for APPL-06's amber row exists. VALIDATION row: APPL-06 (the enum the check reads).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exception / config values -> user-facing strings | Every string added here is rendered on a LAN-visible page and printed to a terminal |
| job row title -> busy_line | `queue_title` is user-supplied document text flowing into a rendered sentence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information disclosure | `error_advice` next steps and `rejection_message(TOKEN_UNSET)` | mitigate | Every string is a developer-authored constant; the parametrised guard test asserts no `{`, `%s`, `http` or `Traceback` appears in any `RequestRejection` message or any `ErrorAdvice` field (Pattern B, ASVS V7) |
| T-30-02 | Information disclosure | `TOKEN_UNSET` copy | mitigate | The sentence names the problem and the file to edit; it never interpolates the token value or the paperless-ngx URL (which may carry `user:pass@`, `paperless.py:461`) |
| T-30-03 | Tampering (XSS) | `busy_line`'s `queue_title` | mitigate | The function returns plain text; Jinja autoescape (on for `.html`) escapes it at render time and no consumer may use `\|safe`. Asserted by the template tests in plan 30-13 |
| T-30-04 | Information disclosure | `local_time` | accept | Renders only the server's zone abbreviation, which is already implied by every other timestamp on the page |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | This plan installs nothing. RESEARCH § Package Legitimacy Audit records that the phase adds no dependency and `uv sync` is unchanged; a plan proposing `uv add` contradicts REQUIREMENTS' Out of Scope table |
</threat_model>

<verification>
- `uv run pytest tests/test_vocabulary.py -q` green
- `uv run pytest -m "not browser and not sane_hardware" -q` green (no existing consumer of `error_message` regressed)
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests` all exit 0
- `grep -rn "error_message" src/saneless/` shows the accessor plus any pre-existing import, and no second `match` over `ErrorCategory`
</verification>

<success_criteria>
- `error_advice` is complete over all seven `ErrorCategory` members with both fields non-empty
- `error_message` returns byte-identical strings to before this plan (no user-visible regression)
- `local_time`, `page_counts`, `busy_line`, `ProfileStorage` and `RequestRejection.TOKEN_UNSET` exist, are exported and are tested
- Zero suppressions added anywhere
</success_criteria>

<output>
Create `.planning/phases/30-appliance-layer/30-01-SUMMARY.md` when done
</output>
