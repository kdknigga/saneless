---
phase: 23.1
slug: dark-mode-and-the-commit-gate
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-09-11
---

# Phase 23.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `23.1-RESEARCH.md` § Validation Architecture. Task IDs are `{plan}-{task}` (for example, `23.1-01-02` is plan 23.1-01, Task 2).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (+ pytest-playwright 0.7.2, pytest-timeout) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` (strict markers, `filterwarnings = error`, timeout 60) |
| **Quick run command** | `uv run pytest tests/test_browser.py -m browser -q` |
| **Full suite command** | `uv run pytest -m "not browser" -q && uv run pytest -m browser -q` |
| **Static gate** | `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests` |
| **Estimated runtime** | ~60 seconds (non-browser suite ~30s, browser suite ~30s) |

---

## Sampling Rate

- **After every task commit:** static gate + `uv run pytest -m "not browser" -q`; DARK tasks also run `uv run pytest tests/test_browser.py -m browser -q`
- **After every plan wave:** full suite (browser included). Once plan 23.1-04 has installed the shims, the pre-merge-commit hook itself runs the full type check on every worktree merge.
- **Before `/gsd-verify-work`:** full suite green; D-07 demo transcript present in the 23.1-04 SUMMARY; the GATE-03 grep lists only the 23.1-03 allowlist; the ROADMAP D-15/D-16 greps hit
- **Max feedback latency:** 60 seconds
- **Tool hazard:** the rtk hook rewrites and truncates `grep` and `git` output. Use `/usr/bin/grep` and `/usr/bin/git` for every count below.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 23.1-01-01 | 01 | 1 | DARK-01, DARK-02 (RED) | — | N/A | browser + unit | Exactly 4 TestDarkModeEngagement cases fail (T1 dark, T2 dark fallback ×2, T3); TestContrastHelper passes. Command: `uv run pytest tests/test_browser.py -m browser -q -k TestDarkModeEngagement` (expect exit 1) and `uv run pytest tests/test_browser.py -m "not browser" -q` | ❌ W0 (created by this task) | ⬜ pending |
| 23.1-01-02 | 01 | 1 | DARK-01 | — | N/A | browser | T1: root `backgroundColor` `rgb(19, 23, 31)` / `colorScheme` `dark` under dark; light values under light — `uv run pytest tests/test_browser.py -m browser -q` | after 01-01 | ⬜ pending |
| 23.1-01-02 | 01 | 1 | DARK-01 (mutation M1) | — | N/A | mutation, recorded | restore `data-theme="auto"` → T1 dark fails; revert; `/usr/bin/git diff --exit-code -- src/` | executor-run | ⬜ pending |
| 23.1-01-02 | 01 | 1 | DARK-02 | — | N/A | browser | T2: 3 classes × 2 contexts × 2 schemes ≥ 4.5:1 + exact fallback strings | after 01-01 | ⬜ pending |
| 23.1-01-02 | 01 | 1 | DARK-02 (forced) | — | N/A | browser | T3: `[data-theme="dark"]` → fallback `rgb(202, 138, 4)` | after 01-01 | ⬜ pending |
| 23.1-01-02 | 01 | 1 | DARK-02 (mutations M2, M3) | — | N/A | mutation, recorded | delete `@media … :root:not([data-theme])` → T2 fails (3.65:1); delete `[data-theme="dark"]` → T3 fails | executor-run | ⬜ pending |
| 23.1-01-02 | 01 | 1 | DARK-03 | T-23.1-CDN, T-23.1-PALETTE | Pico pinned to 2.1.1; no palette file | grep | `! grep -n -- '--pico-color-' src/saneless/web/static/app.css && ! grep -rn 'pico.colors' src/saneless/web/templates && grep -c 'pico@2.1.1/css/pico.min.css' src/saneless/web/templates/base.html` | ✅ | ⬜ pending |
| 23.1-01-03 | 01 | 1 | DARK-02, DARK-03 (record) | — | N/A | grep | master UI-SPEC: `--saneless-status-fallback` ≥ 3, `#ca8a04` ≥ 2, `pico@2.1.1` ≥ 2, no "No dark-mode-specific overrides are written" | ✅ | ⬜ pending |
| 23.1-02-01 | 02 | 1 | GATE-01, GATE-02 (stages) | T-23.1-FIXER | only the two full hooks at merge/push; src-only at commit | config check | `uv run prek run --stage pre-merge-commit --all-files --dry-run` and `--stage pre-push` list exactly `ty type checker (full)` and `pyrefly type checker (full)`; `--stage pre-commit` lists both `(src)` hooks | ✅ | ⬜ pending |
| 23.1-02-01 | 02 | 1 | GATE-02 (CI), GATE-03 | T-23.1-VACUOUS | CI lint runs both checkers with pyrefly paths | grep | `grep -nE 'uv run (ty check\|pyrefly check src tests)$' .github/workflows/ci.yml` → 2 lines | ✅ | ⬜ pending |
| 23.1-02-02 | 02 | 1 | GATE-03 (reason) | T-23.1-VACUOUS | N/A | grep | `grep -n 'use-ignore-files' CLAUDE.md CONTRIBUTING.md .pre-commit-config.yaml` ≥ 1 each; zero D-11 residue in the four live files | ✅ | ⬜ pending |
| 23.1-03-01 | 03 | 1 | GATE-03 | T-23.1-REWRITE | rewrite is pure ` src tests` insertion | grep + word-diff | `grep -rn 'src testss' .planning` and `grep -rn 'src tests src tests' .planning` empty; word-diff shows only ` src tests` | ✅ | ⬜ pending |
| 23.1-03-02 | 03 | 1 | GATE-03 | — | N/A | grep | `grep -rnE "pyrefly[[:space:]]check" .planning \| grep -vE "pyrefly[[:space:]]check (src\|tests)"` rows == the `## Residue allowlist` rows in 23.1-03-SUMMARY | ✅ | ⬜ pending |
| 23.1-04-01 | 04 | 2 | GATE-02 (shims) | T-23.1-SHIM | three shims, main-venv prek | observable | for each of pre-commit, pre-merge-commit, pre-push: `--hook-type=<t>` and `PREK="/home/kris/git/saneless/.venv/bin/prek"` in `/home/kris/git/saneless/.git/hooks/<t>` | ✅ | ⬜ pending |
| 23.1-04-01 | 04 | 2 | GATE-02 (CI read-back) | T-23.1-BYPASS | ruleset requires lint | read-back (D-08) | `gh api repos/kdknigga/saneless/rulesets/22777879 --jq …` → active, required_status_checks (lint, test), deletion, non_fast_forward | ✅ | ⬜ pending |
| 23.1-04-02 | 04 | 2 | GATE-01 | T-23.1-BYPASS | RED commit passes with no suppression | local demo (D-07.1a) | commit exit 0 with both `(src)` Passed; pytest exit 1 AttributeError; `git show demo-231-red \| grep -cE 'type: ignore\|noqa\|NotImplementedError'` == 0 | demo | ⬜ pending |
| 23.1-04-02 | 04 | 2 | GATE-02 (commit) | T-23.1-BYPASS | src/ type error rejected at commit | local demo (D-07.1b) | commit exit 1, both `(src)` hooks Failed | demo | ⬜ pending |
| 23.1-04-02 | 04 | 2 | GATE-02 (merge) | T-23.1-BYPASS | `--no-ff` merge with tests/ error rejected | local demo (D-07.2) | merge exit 1, MERGE_HEAD present, HEAD == autodev | demo | ⬜ pending |
| 23.1-04-02 | 04 | 2 | GATE-02 (push) | T-23.1-PUSH, T-23.1-DEMO | push rejected (from the broken tree) | local demo (D-07.3) | push exit 1; bare repo `for-each-ref` empty; `git branch --list 'demo-231-*'` empty after cleanup | demo | ⬜ pending |
| 23.1-04-03 | 04 | 2 | GATE-03 (merged tree) | — | N/A | grep | merged-tree D-11 residue == the 23.1-03 allowlist; zero from live files and 23.1 PLAN/SUMMARY files | ✅ | ⬜ pending |
| 23.1-04-03 | 04 | 2 | DARK-01 (D-15), DARK-03 (D-16) | — | N/A | grep | `grep -c 'document.documentElement' .planning/ROADMAP.md` ≥ 1 and `grep -c 'Coupling contract for Phase 26 (ROBU-09)' .planning/ROADMAP.md` ≥ 1 (applied in the planning commit) | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/test_browser.py`: T1, T2 (plus the module-level WCAG contrast helper and its non-browser TestContrastHelper self-check) and T3. All are type-clean, so their RED commit (23.1-01-01) works under today's hooks.

*No framework install needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Hook-gate demonstrations (RED commit / src error / merge / push) | GATE-01, GATE-02 | D-09: by user decision there is no automated regression test for the hook config. The proof is a one-time scripted demo in a scratch location, with the transcript recorded in the SUMMARY | 23.1-04 Task 2 follows the D-07 recipe in `23.1-RESEARCH.md`. The executor runs it, not the human |
| CSS mutation checks | DARK-01, DARK-02 | One-time proof that each override is load-bearing; the tests themselves are automated | 23.1-01 Task 2: the executor applies each mutation, runs the named test, records the failure and reverts |

*Both are executor-run, not human-run.*

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (T1/T2/T3 are created in 23.1-01-01)
- [x] No watch-mode flags
- [x] Feedback latency < 60s (browser suite ~30s)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planner sign-off 2026-09-11; execution evidence pending
