---
phase: 23.1-dark-mode-and-the-commit-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/CONCERNS.md
  - .planning/research/STACK.md
  - .planning/reviews/2026-09-09-code-review.md
  - .planning/quick/260320-j26-fix-linter-and-type-checker-failures-in-/**
  - .planning/phases/20-ci-gate/**
  - .planning/phases/21-vocabulary-and-contracts/**
  - .planning/phases/22-job-store-hardening/**
  - .planning/phases/23-honest-outcomes-and-never-lose-a-scan/**
  - .planning/milestones/v1.0-phases/**
autonomous: true
requirements: [GATE-03]

must_haves:
  truths:
    - "No verification command anywhere under .planning/ runs pyrefly without naming paths"
    - "Every line the D-11 grep still returns is a quotation or description of the pathless form as the defect, and each one is listed by file:line with a one-line justification in the SUMMARY"
    - "The rewrite changed nothing except inserting ' src tests' (plus the one hand-edited 21-05-SUMMARY line)"
  artifacts:
    - path: ".planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-03-SUMMARY.md"
      provides: "the residue allowlist that plan 23.1-04 and the verifier check against"
      contains: "Residue allowlist"
  key_links:
    - from: "23.1-03-SUMMARY.md residue allowlist"
      to: "the D-11 acceptance grep"
      via: "file:line entries, each categorised and justified"
      pattern: "Residue allowlist"
---

<objective>
Rewrite every pathless pyrefly command and mention under `.planning/` to `uv run pyrefly check src tests`,
literally, including the historical records (D-11: the user chose "Everything, literally"). Then
enumerate and justify each line that remains.

Purpose: GATE-03 / ROADMAP criterion 5 for `.planning/`. Plan 23.1-02 handles the four live files
(CLAUDE.md, CONTRIBUTING.md, ci.yml, .pre-commit-config.yaml). This plan must not touch them, which
keeps the two wave-1 plans free of file overlap.

Output: about 85 rewritten `.planning/` files (about 220 lines), plus a residue allowlist in the SUMMARY.

**The exception rule (D-11, as resolved by the orchestrator).** A line that quotes the pathless form
*as the thing forbidden*, or describes it *as the defect*, is not a verification command, and it stays.
That covers:
- the 23.1 CONTEXT, DISCUSSION-LOG and RESEARCH;
- ROADMAP criterion 5 (never edit it);
- REQUIREMENTS GATE-03;
- about 20 historical lines whose meaning is "running pyrefly without paths is the failure" (RESEARCH G7).

Rewriting those produces nonsense such as "the path-naming command with no arguments is a no-op". Do NOT narrow
the rewrite beyond this exception: prose that merely names the tool ("… passes clean") is rewritten too.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-CONTEXT.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-RESEARCH.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-PATTERNS.md

<interfaces>
Measured baseline (planning session, 2026-09-11, raw /usr/bin/grep): the D-11 grep returns 256 lines.
- 17 are in `.planning/phases/23.1-*`: CONTEXT ×9, DISCUSSION-LOG ×1, RESEARCH ×7. All of them are quotations; keep them.
- 1 is `.planning/ROADMAP.md` criterion 5: a quotation; keep it.
- 4 are in CLAUDE.md, CONTRIBUTING.md and ci.yml. Plan 23.1-02 owns them; exclude them here.
- The remaining 234 are spread across: `phases/20-*` 35, `21-*` 55, `22-*` 27, `23-*` 41, `milestones/v1.0-phases` about 65, `codebase/CONCERNS.md` 1, `research/STACK.md` 1, `reviews/2026-09-09-code-review.md` 4, and `quick/260320-j26-*` 5.

REQUIREMENTS.md's GATE-03 line does not show up in the grep: it also contains the path-naming form, so the `-v` filter hides it.

Tool hazard: the rtk hook rewrites and truncates grep/git output (paths collapse to `.../`, and counts are wrong). For every count and listing in this plan, call `/usr/bin/grep`, `/usr/bin/git` and `/usr/bin/perl` by absolute path.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: mechanical rewrite of .planning/ with the tested guarded recipe, plus the two hand fix-ups</name>
  <files>.planning/codebase/CONCERNS.md, .planning/research/STACK.md, .planning/reviews/2026-09-09-code-review.md, .planning/quick/260320-j26-fix-linter-and-type-checker-failures-in-/*, .planning/phases/20-ci-gate/*, .planning/phases/21-vocabulary-and-contracts/*, .planning/phases/22-job-store-hardening/*, .planning/phases/23-honest-outcomes-and-never-lose-a-scan/*, .planning/milestones/v1.0-phases/**</files>
  <read_first>
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-RESEARCH.md (G7 "GATE-03 inventory and a tested rewrite recipe", around lines 336-407: the candidate-file command, the GUARD regex, the perl substitution, the two hand fix-ups and the verification commands)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-PATTERNS.md ("`.planning/**` GATE-03 bulk rewrite set")
    - .planning/phases/21-vocabulary-and-contracts/21-05-SUMMARY.md (around line 293, the explicit-path-list line that needs a hand edit)
    - .planning/phases/20-ci-gate/20-RESEARCH.md (around line 467, the --python-interpreter-path line)
  </read_first>
  <action>
Implements D-11. Run the RESEARCH G7 recipe with ONE change: the candidate-file listing searches `.planning` ONLY. Drop `CLAUDE.md CONTRIBUTING.md .github` from its path arguments; plan 23.1-02 owns those files. Keep the recipe's exclusions: `^\.planning/(REQUIREMENTS|ROADMAP)\.md$` and everything under `^\.planning/phases/23\.1-`.

- The candidate search is a `/usr/bin/grep -rlP` for the tool name, a single space and the subcommand, NOT followed by ` src` or ` tests` (the negative lookahead from G7).
- The substitution is the G7 `/usr/bin/perl -pi -e` one-liner. It inserts ` src tests` after the subcommand, except on lines matching the G7 GUARD alternation: bare, no arguments, no path, no flags, checks? nothing, checked nothing, No Python files, matched nothing, cannot (work|run), no-op, must be scoped, exits? 1 for reasons, covers both trees; case-insensitive.
- Keep the recipe's `pyrefly[ ]check` spelling in anything you type, so your own commands never add a grep hit.
- Copy the recipe from RESEARCH G7 rather than retyping it from this description.

After the substitution:
(a) Hand-fix `.planning/phases/21-vocabulary-and-contracts/21-05-SUMMARY.md` near line 293, which would read "src tests <7 explicit paths>". Reword it so it keeps a path-prefixed form and reads naturally, for example `uv run pyrefly check src/… tests/… (7 explicit paths)`.
(b) Leave `20-RESEARCH.md` near line 467 as rewritten (`… src tests --python-interpreter-path .venv/bin/python`).
(c) Run the collision checks `/usr/bin/grep -rn 'src testss' .planning` and `/usr/bin/grep -rn 'src tests src tests' .planning`. Both must be empty. If the first hits, the inflected word "checks" was split by the insertion. If that line describes the defect, restore the original wording; otherwise reword it so it reads `pyrefly check src tests` followed by the rest of the sentence.
(d) Review `/usr/bin/git diff --word-diff=porcelain -- .planning`. Every added token must be ` src tests`, except the hand edit in (a). Any other change is a defect; revert it.

Commit with `/usr/bin/git add -- .planning` followed by `/usr/bin/git commit`, message `docs(23.1-03): name pyrefly's paths in every historical verification command`. The staged set must contain only files under `.planning/` and no file under `.planning/phases/23.1-*`, and neither `.planning/ROADMAP.md` nor `.planning/STATE.md`: the orchestrator restores those two from snapshots.
  </action>
  <verify>
    <automated>test -z "$(/usr/bin/grep -rn 'src testss' .planning)" && test -z "$(/usr/bin/grep -rn 'src tests src tests' .planning)" && test -z "$(/usr/bin/git diff HEAD~1 --name-only | /usr/bin/grep -vE '^\.planning/' )" && test -z "$(/usr/bin/git diff HEAD~1 --name-only | /usr/bin/grep -E '^\.planning/(phases/23\.1-|ROADMAP\.md|STATE\.md|REQUIREMENTS\.md)')" && test -z "$(/usr/bin/grep -rnE 'pyrefly[[:space:]]check' CLAUDE.md CONTRIBUTING.md .github .pre-commit-config.yaml >/dev/null; /usr/bin/git diff HEAD~1 --name-only -- CLAUDE.md CONTRIBUTING.md .github .pre-commit-config.yaml)"</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/git diff HEAD~1 --stat` lists only `.planning/` files, about 85 of them, and none under `.planning/phases/23.1-*` and neither ROADMAP.md, STATE.md nor REQUIREMENTS.md
    - `/usr/bin/git diff HEAD~1 --word-diff=porcelain -- .planning | /usr/bin/grep '^+' | /usr/bin/grep -v '^+++' | /usr/bin/grep -vE '^\+ ?src tests ?$'` returns only the 21-05-SUMMARY hand-edit token(s). List them in the SUMMARY.
    - `/usr/bin/grep -rn 'src testss' .planning` and `/usr/bin/grep -rn 'src tests src tests' .planning` both return nothing
    - The count of D-11 residue lines drops from 256 (the measured baseline) to about 42: 38 justified lines plus the 4 live-file lines that plan 23.1-02 removes in its own worktree
    - The SUMMARY records the files changed, the lines rewritten, and the before/after counts, all taken from /usr/bin/grep output
  </acceptance_criteria>
  <done>Every pathless pyrefly command and mention under .planning/ names `src tests`, and the diff is pure path insertion apart from one documented hand edit.</done>
</task>

<task type="auto">
  <name>Task 2: enumerate and justify every residual line (the GATE-03 allowlist)</name>
  <files>.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-03-SUMMARY.md</files>
  <read_first>
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-RESEARCH.md (G7's list of 20 guard-kept historical lines and 5 hidden lines; Open Question 1)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-CONTEXT.md (D-11 exception wording)
    - every file:line that the residue grep below returns (read each line in context before categorising it)
  </read_first>
  <action>
Run the D-11 acceptance grep over `.planning` with raw tools. That is `/usr/bin/grep -rnE` for `pyrefly[[:space:]]check` over `.planning`, piped through `/usr/bin/grep -vE` for `pyrefly[[:space:]]check (src|tests)`. Read every hit in context and put it in exactly one category:
- Q-231: a 23.1 planning artefact that quotes the forbidden form as the rule. Expected: 23.1-CONTEXT ×9, 23.1-DISCUSSION-LOG ×1, 23.1-RESEARCH ×7.
- Q-ROADMAP: ROADMAP criterion 5. Expected: ×1.
- D-HIST: a historical line that describes the pathless form as the defect or failure. It is not a command. Expected: the 20 lines listed in RESEARCH G7:
  - 23-01-SUMMARY:117,194; 23-02-SUMMARY:151; 23-04-SUMMARY:144,147; 23-07-SUMMARY:239; 23-08-SUMMARY:293;
  - 22-01-SUMMARY:114; 22-05-SUMMARY:160; 22-06-SUMMARY:134,138; 22-VALIDATION:84,85; 22-VERIFICATION:96;
  - 21-01-SUMMARY:154,170; 21-02-SUMMARY:221; 21-04-SUMMARY:216; 21-05-SUMMARY:239;
  - 20-RESEARCH:476.

Rules:
- If a hit is actually a runnable command, or a verification instruction a future author could copy, it is NOT exempt. Rewrite it by hand to name `src tests`, then commit that fix with `/usr/bin/git commit` and the message `docs(23.1-03): name pyrefly's paths in <file>`.
- If the result differs from the expected 38 lines, reconcile every difference by file:line in the SUMMARY.
- Also list, for completeness, the 5 hidden lines from G7 that carry both forms and so never appear in the grep: 23-03-SUMMARY:113, 22-VALIDATION:83, 22-03-SUMMARY:163, 22-02-SUMMARY:137, 22-01-SUMMARY:143. For each, confirm by reading it that it describes the defect.

Write the SUMMARY with a section headed exactly `## Residue allowlist`. It is a table with columns file:line | category (Q-231 / Q-ROADMAP / D-HIST) | one-line justification in your own words. Do NOT paste the matched text into the SUMMARY: it would add new grep hits (RESEARCH Pitfall 6). Refer to the form only as "the pathless form", and spell any grep `pyrefly[[:space:]]check`.
  </action>
  <verify>
    <automated>test "$(/usr/bin/grep -c '^## Residue allowlist$' .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-03-SUMMARY.md)" = 1 && test -z "$(/usr/bin/grep -nE 'pyrefly[[:space:]]check' .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-0*-SUMMARY.md | /usr/bin/grep -vE 'pyrefly[[:space:]]check (src|tests)')"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY's `## Residue allowlist` table has one row per residual D-11 line under `.planning`. The row count equals `/usr/bin/grep -rnE 'pyrefly[[:space:]]check' .planning | /usr/bin/grep -vE 'pyrefly[[:space:]]check (src|tests)' | wc -l`.
    - Every row's category is Q-231, Q-ROADMAP or D-HIST. No row lies outside `.planning/phases/23.1-*`, `.planning/ROADMAP.md`, or a SUMMARY/VALIDATION/VERIFICATION/RESEARCH file of phases 20-23 that describes the defect.
    - No 23.1 SUMMARY file adds a residue line: the automated check above returns nothing for `23.1-0*-SUMMARY.md`
    - The 5 hidden lines are listed separately, each with a one-line confirmation
  </acceptance_criteria>
  <done>The GATE-03 acceptance grep's allowlist exists as a checked, justified, file:line-exact table that plan 23.1-04 and the verifier can compare against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local .planning/ → any remote | `.planning/` is stripped from every push (Phase 20 D-22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.1-REWRITE | Tampering | historical planning records (about 85 files) | mitigate | Use the guarded recipe tested in RESEARCH G7. The word-diff check proves only ` src tests` was inserted, and the `src testss` / doubled-insertion greps catch collisions. The guard keeps the defect-narrative lines from being turned into nonsense |
| T-23.1-OVERLAP | Tampering | shared files with parallel plans | mitigate | The candidate list is restricted to `.planning`. REQUIREMENTS/ROADMAP/STATE and `phases/23.1-*` are excluded, and the verify command asserts the staged set |
| T-23.1-LEAK | Information disclosure | `.planning/` content | accept | It stays local: `.planning/` is stripped from every push (Phase 20 D-22). No secrets are involved |
</threat_model>

<verification>
- The raw-tool D-11 grep over `.planning` returns exactly the rows of the SUMMARY's `## Residue allowlist`
- `/usr/bin/git diff HEAD~2 --stat` (both commits) touches only `.planning/` files outside `phases/23.1-*`, ROADMAP.md and STATE.md, plus this plan's SUMMARY
</verification>

<success_criteria>
- ROADMAP criterion 5 / GATE-03 for `.planning/`: no verification command runs pyrefly without paths. Every remaining grep line is a justified quotation or defect description, listed by file:line.
</success_criteria>

<output>
Create `.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-03-SUMMARY.md` containing the rewrite statistics, the hand edit, the `## Residue allowlist` table and the 5 hidden lines.

Never spell the pathless pyrefly invocation in it (RESEARCH Pitfall 6).
</output>
