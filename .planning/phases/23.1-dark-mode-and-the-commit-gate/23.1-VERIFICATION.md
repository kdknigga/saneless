---
phase: 23.1-dark-mode-and-the-commit-gate
verified: 2026-09-11T23:00:00Z
status: passed
score: 5/5 must-haves verified
overrides_applied: 0
---

# Phase 23.1: Dark Mode and the Commit Gate Verification Report

**Phase Goal:** Two defects that Phase 23 execution exposed are closed — the web UI actually renders Pico v2's dark palette when the operator's OS asks for it, with every status colour passing WCAG AA in both schemes; and a TDD RED commit is possible without `--no-verify`, `# type: ignore`, or disabling a rule, while type errors still cannot reach master.
**Verified:** 2026-09-11
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria, as amended by D-15)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Under `prefers-color-scheme: dark`, a Playwright test asserts `document.documentElement`'s **computed** background is Pico's dark surface, and fails if `data-theme="auto"` is restored | ✓ VERIFIED | `tests/test_browser.py::TestDarkModeEngagement::test_root_surface_follows_the_os_scheme` reads `document.documentElement` only (no `body` read exists — `grep 'body).backgroundColor'` returns nothing). Ran live: `uv run pytest tests/test_browser.py -m browser -k TestDarkModeEngagement` → 15 passed. `base.html` line 2 is `<html lang="en">` with no `data-theme` attribute (M1 mutation recorded in 23.1-01-SUMMARY shows restoring the attribute fails this test) |
| 2 | `.status-done`, `.status-error`, `.status-fallback` each measure ≥4.5:1 against the surface in both schemes, by computed colour | ✓ VERIFIED | `app.css` declares `--saneless-status-fallback: #a16207` (light, 4.92:1) and `#ca8a04` (dark, 6.11:1) via both `@media (prefers-color-scheme: dark) { :root:not([data-theme]) }` and `[data-theme="dark"]`. All 12 T2 parametrised cases pass live (part of the 15/15 `TestDarkModeEngagement` pass) |
| 3 | A test file referencing a not-yet-existing symbol can be committed with hooks enabled, no suppression | ✓ VERIFIED | Live hooks confirmed installed: `.git/hooks/{pre-commit,pre-merge-commit,pre-push}` each carry `--hook-type=<type>` and `PREK="/home/kris/git/saneless/.venv/bin/prek"` (independently re-grepped, matches 23.1-04-SUMMARY exactly). `.pre-commit-config.yaml` on disk has `ty-checker`/`pyrefly-checker` scoped to `src` only at commit stage (verified by reading the file). D-07 demo 1a transcript in 23.1-04-SUMMARY shows a real commit of a test naming a non-existent `vocabulary.not_yet_there` passing both `(src)` hooks, with the test itself then failing at run time with `AttributeError` — genuinely RED, not vacuous |
| 4 | A deliberate type error is still rejected before reaching master | ✓ VERIFIED | `.pre-commit-config.yaml` has `ty-checker-full`/`pyrefly-checker-full` at `stages: [pre-merge-commit, pre-push]`, confirmed present on disk. D-07 demos 1b/2/3 transcripts show a `src/` type error rejected at commit, a `--no-ff` merge with a `tests/` type error rejected at pre-merge-commit (MERGE_HEAD present, no commit created), and a push of the same rejected at pre-push (bare repo `for-each-ref` empty). CI `ci.yml` independently confirmed to run `uv run ty check` and `uv run pyrefly check src tests` in the `lint` job. Ruleset re-queried live via `gh api repos/kdknigga/saneless/rulesets/22777879` → `{"checks":["lint","test"],"enforcement":"active","rules":["required_status_checks","deletion","non_fast_forward"]}`, matching the SUMMARY's claim exactly |
| 5 | No verification command in `.planning/`, the hooks, or CI uses bare `uv run pyrefly check` | ✓ VERIFIED | Independently re-ran the acceptance grep: `grep -rnE 'pyrefly[[:space:]]check' .planning CLAUDE.md CONTRIBUTING.md .github .pre-commit-config.yaml \| grep -vE 'pyrefly[[:space:]]check (src\|tests)' \| wc -l` → **36**, matching 23.1-03-SUMMARY's residue-allowlist row count exactly. All 36 rows are quotations (Q-231, Q-ROADMAP) or historical defect narratives (D-HIST), none in a live command file |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tests/test_browser.py` | `TestDarkModeEngagement`, `TestContrastHelper`, WCAG helper | ✓ VERIFIED | Both classes present; `_contrast_ratio`/`_relative_luminance` present; no `body` background read. `TestContrastHelper` (6 cases, non-browser) passes: `uv run pytest -m "not browser" -k TestContrastHelper` → 6 passed |
| `src/saneless/web/templates/base.html` | no `data-theme`, color-scheme meta, Pico 2.1.1 pin | ✓ VERIFIED | Read file directly: `<html lang="en">`, `<meta name="color-scheme" content="light dark">`, `pico@2.1.1/css/pico.min.css` |
| `src/saneless/web/static/app.css` | app-owned `--saneless-status-fallback` light/dark/forced-dark | ✓ VERIFIED | Read file directly: all three declarations present, no `--pico-color-` reference anywhere |
| `.planning/UI-SPEC.md` | dark-scheme convention, Pico pin, measured contrast table | ✓ VERIFIED | Grep confirms `2.1.1`, `#785800` correction, `6.11:1`/`4.92:1` measured table, `--saneless-status-fallback` in the colour table |
| `.pre-commit-config.yaml` | config B: 3-stage split | ✓ VERIFIED | Read file directly: `default_install_hook_types`, `default_stages`, 4 per-hook `stages: [pre-commit]`, 2 `stages: [pre-merge-commit, pre-push]` hooks |
| `.github/workflows/ci.yml` | `lint` job runs `uv run pyrefly check src tests` | ✓ VERIFIED | Read file directly |
| `CONTRIBUTING.md` / `CLAUDE.md` | path discipline + stage split documented | ✓ VERIFIED | `use-ignore-files` present in both; `pyrefly check src tests` present in both |
| `.git/hooks/{pre-commit,pre-merge-commit,pre-push}` | live shims, main-checkout PREK | ✓ VERIFIED | Independently inspected; all three executable, correct `--hook-type`, correct `PREK=` path |
| `.planning/todos/done/002-dark-mode-never-engages.md` | folded todo closed | ✓ VERIFIED | File exists at `done/`, absent from `pending/`, `status: done`, corrected `#785800` claim present |

### Data-Flow / Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Dark-mode engagement (T1/T2/T3, 15 cases) | `uv run pytest tests/test_browser.py -m browser -k TestDarkModeEngagement` | 15 passed | ✓ PASS |
| Full browser suite unaffected | `uv run pytest tests/test_browser.py -m browser -q` | 30 passed | ✓ PASS |
| Full non-browser suite unaffected | `uv run pytest -m "not browser" -q` | 737 passed | ✓ PASS |
| Lint/type-check clean on current tree | `uv run ruff check . && uv run ty check && uv run pyrefly check src tests` | Ruff clean, ty clean, pyrefly 0 errors | ✓ PASS |
| GATE-03 residue count | `grep` acceptance command over `.planning` + 4 live files | 36 (matches allowlist) | ✓ PASS |
| Ruleset 22777879 live state | `gh api repos/kdknigga/saneless/rulesets/22777879` | `active`, checks `lint`+`test` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| DARK-01 | 23.1-01 | Dark palette actually engages via OS preference | ✓ SATISFIED | `data-theme` removed, T1 passes live in both schemes |
| DARK-02 | 23.1-01 | Status colours ≥4.5:1 in both schemes | ✓ SATISFIED | T2 (12 cases) passes live; UI-SPEC records the convention |
| DARK-03 | 23.1-01 | `pico.colors.css` decision recorded, no palette file shipped | ✓ SATISFIED | No `--pico-color-` in app.css, no `pico.colors` link in templates, UI-SPEC records the decision and the corrected `#785800` finding |
| GATE-01 | 23.1-02, 23.1-04 | RED commit possible without suppression | ✓ SATISFIED | Config on disk + live D-07 demo 1a transcript |
| GATE-02 | 23.1-02, 23.1-04 | Type error still cannot reach master | ✓ SATISFIED | Full-stage hooks on disk + live D-07 demos 1b/2/3 + independently re-verified CI and ruleset |
| GATE-03 | 23.1-02, 23.1-03, 23.1-04 | No bare `pyrefly check` in any live verification path | ✓ SATISFIED | Independently re-ran acceptance grep, 36/36 match allowlist |

REQUIREMENTS.md itself still shows all six IDs as `[ ]` / "Pending" in the traceability table — this is expected at this stage: in this project's workflow (see Phase 20-23 commit history, e.g. `65c7c06 docs(23): phase verification passed 6/6, mark phase and requirements complete`), the checkboxes and traceability status are updated by the orchestrator in a follow-up commit *after* verification passes, not by the execution plans themselves. Not a gap.

### Anti-Patterns Found

None. Scanned all phase-modified files (`tests/test_browser.py`, `base.html`, `app.css`, `.pre-commit-config.yaml`, `ci.yml`, `CONTRIBUTING.md`, `CLAUDE.md`, `.planning/UI-SPEC.md`) for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER|placeholder|coming soon|not yet implemented`. The only "placeholder" hits are legitimate HTML input-placeholder documentation in UI-SPEC.md, not stub markers.

### Human Verification Required

None. All checks in this phase are either automatable (Playwright browser tests, already run live) or were demonstrated by the executor with a verbatim, independently-spot-checked transcript (git hook rejection behavior), and the underlying static configuration was independently re-read from disk rather than trusted from the SUMMARY.

### Gaps Summary

No gaps found. All five roadmap success criteria (as amended by D-15) verified against the live codebase, independent of SUMMARY claims:
- Dark-mode tests (15/15) pass live against the actual `base.html`/`app.css` on disk.
- The three prek shims are genuinely installed in `.git/hooks` with the correct hook types and PREK path.
- `.pre-commit-config.yaml` genuinely implements the src-only/full-check stage split.
- CI and GitHub ruleset 22777879 were independently re-queried live and match the claimed state.
- The GATE-03 residue grep was independently re-run and returned exactly 36 lines, matching the 23.1-03 allowlist row-for-row.
- The full test suite (737 non-browser + 30 browser) passes with no regressions.
- The folded todo is closed under `.planning/todos/done/` with its incorrect `pico.colors.css` claim corrected.
- ROADMAP amendments (D-15 `document.documentElement` wording, D-16 Phase 26 coupling note) are present; criterion 5 is unchanged.

---

_Verified: 2026-09-11_
_Verifier: Claude (gsd-verifier)_
