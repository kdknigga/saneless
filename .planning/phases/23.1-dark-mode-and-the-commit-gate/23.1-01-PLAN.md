---
phase: 23.1-dark-mode-and-the-commit-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_browser.py
  - src/saneless/web/templates/base.html
  - src/saneless/web/static/app.css
  - .planning/UI-SPEC.md
autonomous: true
requirements: [DARK-01, DARK-02, DARK-03]

must_haves:
  truths:
    - "Under an emulated dark OS preference the root element computes background rgb(19, 23, 31) and colorScheme dark; under light it computes rgb(255, 255, 255) and light"
    - ".status-done, .status-error and .status-fallback each measure at least 4.5:1 against their effective background in both schemes, both in #status-area and in a #history-body cell"
    - "Forcing data-theme=dark on <html> under a light OS keeps .status-fallback at rgb(202, 138, 4)"
    - "Restoring data-theme=\"auto\", deleting the @media override, or deleting the [data-theme=\"dark\"] override each makes a named test fail (recorded in the SUMMARY)"
    - "app.css holds no --pico-color- reference, no template links pico.colors.css, and Pico is pinned to 2.1.1"
    - "The master UI-SPEC records the five-point dark-scheme convention and no longer describes the defect as current"
  artifacts:
    - path: "tests/test_browser.py"
      provides: "WCAG helper, TestContrastHelper (non-browser), TestDarkModeEngagement T1/T2/T3 (browser)"
      contains: "class TestDarkModeEngagement"
    - path: "src/saneless/web/templates/base.html"
      provides: "<html lang=\"en\"> with no data-theme, color-scheme meta, Pico 2.1.1 pin"
      contains: "pico@2.1.1/css/pico.min.css"
    - path: "src/saneless/web/static/app.css"
      provides: "--saneless-status-fallback light/dark/forced-dark declarations"
      contains: "--saneless-status-fallback"
    - path: ".planning/UI-SPEC.md"
      provides: "dark-scheme convention, Pico pin, measured contrast table"
      contains: "--saneless-status-fallback"
  key_links:
    - from: "tests/test_browser.py"
      to: "document.documentElement"
      via: "getComputedStyle on the root element, never body"
      pattern: "document\\.documentElement"
    - from: "src/saneless/web/static/app.css"
      to: "Pico v2 automatic-dark selector"
      via: "@media only screen and (prefers-color-scheme: dark) { :root:not([data-theme]) }"
      pattern: ":root:not\\(\\[data-theme\\]\\)"
    - from: ".status-fallback"
      to: "--saneless-status-fallback"
      via: "color: var(--saneless-status-fallback)"
      pattern: "var\\(--saneless-status-fallback\\)"
---

<objective>
Make the web UI actually render Pico v2.1.1's dark palette when the operator's OS asks for it, with every
status colour at WCAG AA in both schemes. Prove it with computed-colour Playwright tests that fail for
the right reason first (TDD RED), then pass (GREEN), and prove each override is load-bearing with
three recorded mutation checks. Record the convention in the master UI-SPEC.

Purpose: DARK-01, DARK-02 and DARK-03 (D-14 through D-17). `23.1-UI-SPEC.md` is the locked design
contract. Do not re-litigate any value in it (D-14).

Output: T1/T2/T3 plus a WCAG helper and its non-browser self-check in `tests/test_browser.py`;
a one-attribute template fix, a meta tag and a CDN pin in `base.html`; the app-owned
`--saneless-status-fallback` block in `app.css`; the five master UI-SPEC edits; and an ICM correction.

The T1/T2/T3 tests are type-clean. They fail only at run time, on assertions. So the RED commit
passes today's commit hooks and this plan does not wait for plan 23.1-02.

ROADMAP note: D-15 (criterion 1 reads `document.documentElement`) and D-16 (the Phase 26 coupling
note) were applied to `.planning/ROADMAP.md` in the planning commit. Do NOT edit ROADMAP.md in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-CONTEXT.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-UI-SPEC.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-PATTERNS.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-VALIDATION.md

<interfaces>
Existing assets in tests/test_browser.py that the new tests reuse. Do not modify them.
- Session fixture `browser_server(...) -> Iterator[_BrowserServer]` (lines 89-137). `_BrowserServer` is a NamedTuple with `.url: str` and `.app: FastAPI`.
- Session fixture `browser_server_url(browser_server) -> str` (line 140). Use this one for T1/T2/T3.
- The pytest-playwright `page` fixture, typed `Page` (imported under TYPE_CHECKING, line 36).
- `from typing import TYPE_CHECKING, Literal, NamedTuple` is already imported. `from __future__ import annotations` is on.
- `_PROBE_STATUS_COLOURS` (line 241) is a JS arrow function string returning `{"status-done": css, "status-error": css, "status-fallback": css}`. It appends each probe to `document.body`. Reuse it for T3.
- `TestFallbackStatusRendering._goto(page, url, scheme)` (line 311) calls `page.emulate_media(color_scheme=scheme)` BEFORE `page.goto(url)`. Copy that order.
- Page anchors: `#status-area` (partials/status.html:1) and `<tbody id="history-body">` (partials/history.html:1).

Lint facts (pyproject.toml): for `tests/**` only S101, ARG and S104-S106 are ignored, so D (docstrings) and ANN (annotations) apply. PLR2004 and E501 are ignored globally. Docstrings use the multi-line-summary-second-line style (D213) when multi-line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): computed-colour tests T1/T2/T3 plus the WCAG helper</name>
  <files>tests/test_browser.py</files>
  <read_first>
    - tests/test_browser.py (the file being modified; read the whole file, especially lines 11-43 for imports, 89-143 for the fixtures, 237-272 for the JS-constant pattern, and 274-403 for the class, _goto, parametrize and docstring patterns)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-UI-SPEC.md ("Reference surface values", "Status Colour Contract", "Verification Contract": T1, T2, T3 and the mutation table)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-PATTERNS.md (section on tests/test_browser.py)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-RESEARCH.md ("WCAG helper shape (T2)" and Pitfall 7, around lines 643-726)
    - src/saneless/web/templates/partials/status.html and src/saneless/web/templates/partials/history.html (probe anchors)
    - pyproject.toml ([tool.ruff] per-file-ignores and [tool.pytest.ini_options] markers)
  </read_first>
  <behavior>
    - Helper self-check (no browser marker, so it also runs in CI): black on white gives 21.0. The ratio is symmetric in its arguments. `rgb(161, 98, 7)` on `rgb(255, 255, 255)` is 4.92 ± 0.01. `rgb(202, 138, 4)` on `rgb(19, 23, 31)` is 6.11 ± 0.01. `rgb(161, 98, 7)` on `rgb(19, 23, 31)` is 3.65 ± 0.01, the pre-phase failure. `rgba(255, 255, 255, 1)` parses to luminance 1.0.
    - T1 [light]: root backgroundColor == "rgb(255, 255, 255)" and colorScheme == "light". This passes at RED.
    - T1 [dark]: root backgroundColor == "rgb(19, 23, 31)" and colorScheme == "dark". This FAILS at RED: the root reads rgb(255, 255, 255) / light because data-theme="auto" is still set.
    - T2 [scheme × cls × context] (12 cases): the ratio is >= 4.5. For cls == status-fallback it additionally checks the exact colour: "rgb(161, 98, 7)" in light and "rgb(202, 138, 4)" in dark. At RED only the two dark status-fallback cases fail, on the exact-colour assertion.
    - T3: under light emulation with `document.documentElement.dataset.theme = "dark"`, the status-fallback probe computes "rgb(202, 138, 4)". This FAILS at RED: it computes rgb(161, 98, 7).
  </behavior>
  <action>
Add everything to tests/test_browser.py. Do not add runtime imports; everything needed is already imported. This implements D-14's T1/T2/T3 and D-17: the new browser tests are `@pytest.mark.browser` and local-only.

(1) Two module-level JS string constants, placed right after `_SWAP_STATUS_AREA`, each with a leading `#` comment that explains why, in the style of `_PROBE_STATUS_COLOURS`:
- `_READ_ROOT_SURFACE`: an arrow function returning an object with `background` = `getComputedStyle(document.documentElement).backgroundColor` and `colorScheme` = the same element's `.colorScheme`. The comment must say the page surface lives on the root element because Pico paints `:root`, and that `body` is transparent in both schemes, so it is never read.
- `_PROBE_CONTEXT_CONTRAST`: an arrow function taking one destructured argument `({cls, context})`.
  - When context is "status-area", it appends a `<p class=cls>probe</p>` inside `#status-area`.
  - When context is "history-cell", it appends a new `<tr>` holding one `<td class=cls>probe</td>` to `#history-body`.
  - It reads the probe's computed `color`.
  - It resolves the effective background: walk from the probe through its ancestors to the first element whose computed `backgroundColor` is not fully transparent (neither `transparent` nor an `rgba(...)` value with alpha 0). If none is found, use `document.documentElement`'s backgroundColor.
  - It removes the probe (and the added row), then returns `{colour, background}`.
  - All of these steps happen inside that one evaluate (RESEARCH Pitfall 7).

(2) Two module-level pure helpers after the JS constants, each with a type-annotated signature and a one-line docstring:
- `_relative_luminance(css_colour: str) -> float` parses "rgb(r, g, b)" or "rgba(r, g, b, a)". It takes the first three channels divided by 255, linearises each sRGB channel (c / 12.92 when c <= 0.04045, otherwise ((c + 0.055) / 1.055) ** 2.4), and returns 0.2126 R + 0.7152 G + 0.0722 B.
- `_contrast_ratio(foreground: str, background: str) -> float` returns (max(L1, L2) + 0.05) / (min(L1, L2) + 0.05).

(3) A class `TestContrastHelper` with NO browser marker, so CI's `-m "not browser"` run exercises it. Its docstring says it pins the helper to the UI-SPEC's measured numbers, so a broken formula cannot let T2 pass vacuously. It holds the checks in the behavior block: `test_black_on_white_is_the_maximum_ratio`, `test_ratio_is_symmetric`, `test_reference_ratios_match_the_ui_spec` (parametrised over the three reference pairs, using `pytest.approx(expected, abs=0.01)`), and `test_rgba_strings_parse`.

(4) A class `TestDarkModeEngagement` decorated `@pytest.mark.browser`. Its multi-line docstring explains that what is uncertain is this app's cascade (the absent attribute, the override selectors, the real surfaces), not Pico's internals, which is why the proof has to be a computed value in a real browser. It contains:
- A `_goto(self, page: Page, url: str, scheme: Literal["light", "dark"]) -> None` helper. It calls `page.emulate_media(color_scheme=scheme)`, then `page.goto(url)`, then `page.wait_for_selector("#history-body")`. The idle page renders no `.status-fallback`, so it must not wait for one.
- `test_root_surface_follows_the_os_scheme` (T1), parametrised on scheme. It evaluates `_READ_ROOT_SURFACE` and asserts both strings against a dict keyed by scheme: light is ("rgb(255, 255, 255)", "light"), dark is ("rgb(19, 23, 31)", "dark"). Each assertion message carries the observed object.
- `test_status_colour_meets_aa_contrast` (T2), with three stacked parametrize decorators:
  - scheme: `Literal["light", "dark"]`;
  - cls: `Literal["status-done", "status-error", "status-fallback"]`;
  - context: `Literal["status-area", "history-cell"]`.

  It evaluates `_PROBE_CONTEXT_CONTRAST` with `{"cls": cls, "context": context}`. It asserts `_contrast_ratio(colour, background) >= 4.5`, with a message carrying colour, background and the ratio. When cls is "status-fallback", it also asserts the colour equals "rgb(161, 98, 7)" in light or "rgb(202, 138, 4)" in dark.
- `test_forced_dark_theme_keeps_the_dark_amber` (T3). It goes to the page under "light", runs `page.evaluate("() => { document.documentElement.dataset.theme = 'dark'; }")`, evaluates `_PROBE_STATUS_COLOURS`, and asserts the "status-fallback" entry equals "rgb(202, 138, 4)". Its docstring says it guards the forced-dark block, which T2 cannot reach.

No test reads `body`'s background. No test asserts the attribute's absence as its proof (UI-SPEC T1).

Run the RED check below and confirm the failures are assertion failures, not errors. Then commit only tests/test_browser.py with `/usr/bin/git commit` (Pitfall 8; hooks still run) and the message `test(23.1-01): add failing computed-colour tests for dark mode and status contrast`. Do not use `--no-verify`, `SKIP=`, `# type: ignore` or `# noqa` (D-06).
  </action>
  <verify>
    <automated>uv run ruff check tests/test_browser.py && uv run ruff format --check tests/test_browser.py && uv run ty check && uv run pyrefly check src tests && uv run pytest tests/test_browser.py -m "not browser" -q && (uv run pytest tests/test_browser.py -m browser -q -k "TestDarkModeEngagement" -p no:cacheprovider; test $? -eq 1)</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'class TestDarkModeEngagement' tests/test_browser.py` == 1 and `/usr/bin/grep -c 'class TestContrastHelper' tests/test_browser.py` == 1
    - `/usr/bin/grep -c 'def _contrast_ratio(foreground: str, background: str) -> float' tests/test_browser.py` == 1
    - `/usr/bin/grep -n 'document.body).backgroundColor\|body).backgroundColor' tests/test_browser.py` returns nothing
    - `uv run pytest tests/test_browser.py -m "not browser" -q` exits 0, and the TestContrastHelper cases pass
    - `uv run pytest tests/test_browser.py -m browser -q -k TestDarkModeEngagement` reports exactly 4 failed, all AssertionError: T1 dark, T2 dark status-fallback in both contexts, and T3. The other 11 pass. The failing node ids and their observed values are copied into the SUMMARY's RED section.
    - `/usr/bin/git log --format=%s -1` == `test(23.1-01): add failing computed-colour tests for dark mode and status contrast`, and that commit touches only tests/test_browser.py
    - `/usr/bin/git show HEAD | /usr/bin/grep -cE 'type: ignore|noqa'` == 0
  </acceptance_criteria>
  <done>RED is committed. The three dark-mode claims fail on their assertions under the current template and CSS, and the helper is pinned to the UI-SPEC's measured ratios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): engage the dark palette, own the fallback amber, run the three mutation checks</name>
  <files>src/saneless/web/templates/base.html, src/saneless/web/static/app.css</files>
  <read_first>
    - src/saneless/web/templates/base.html (the file being modified, 21 lines)
    - src/saneless/web/static/app.css (the file being modified; lines 11-27 and the @media style at 73-95)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-UI-SPEC.md ("Theme Engagement Contract" rules 1-5, "CSS contract: the first dark-scheme override in app.css", "Palette File Decision", "Required mutation checks")
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-PATTERNS.md (sections on base.html and app.css)
  </read_first>
  <behavior>
    - All 15 TestDarkModeEngagement cases pass, and every existing browser test (TestBrowserRendering, TestHTMXPolling, TestFlipPromptUI, TestFallbackStatusRendering in both schemes) stays green.
    - M1: restoring data-theme="auto" makes T1 [dark] fail with the root at rgb(255, 255, 255) / light.
    - M2: deleting the @media automatic-dark override makes T2 [dark, status-fallback] fail, with #a16207 on rgb(19, 23, 31) at 3.65:1.
    - M3: deleting the [data-theme="dark"] override makes T3 fail, with the fallback computing rgb(161, 98, 7).
  </behavior>
  <action>
Implements D-14 per 23.1-UI-SPEC rules 1-3 and the CSS contract.

base.html:
- Line 2 becomes exactly `<html lang="en">`. Delete the attribute. Do not set "light", "dark" or any JS-set value.
- Insert `    <meta name="color-scheme" content="light dark">` (4-space indent, no self-closing slash) as a new line directly after the viewport meta.
- In the Pico link, change `@picocss/pico@2/` to `@picocss/pico@2.1.1/`. Do not add any pico.colors link (DARK-03).
- Nothing else changes.

app.css:
- Replace lines 20-27 (the consume-directory comment plus the `.status-fallback` rule that reads `var(--pico-color-amber-600, #a16207)`) in place, keeping the block next to the other status rules. The replacement is 23.1-UI-SPEC § "CSS contract", verbatim in structure:
  - `:root { --saneless-status-fallback: #a16207; }`
  - `@media only screen and (prefers-color-scheme: dark) { :root:not([data-theme]) { --saneless-status-fallback: #ca8a04; } }`
  - `[data-theme="dark"] { --saneless-status-fallback: #ca8a04; }`
  - `.status-fallback { color: var(--saneless-status-fallback); }`
- Use 4-space indents, with nested blocks indented a further 4 inside @media. Put one blank line between rules.
- The comment explains the reason in plain words, using ASCII `--` for dashes, and cites no planning artefact (SWP-07): Pico has no amber semantic token, so the app owns this one; the dark selectors mirror Pico v2's own.
- `.status-done` and `.status-error` (lines 11-18) stay byte for byte. No `--pico-color-` reference may remain anywhere in app.css.

Run the full browser suite. Commit both files with `/usr/bin/git commit` and the message `feat(23.1-01): engage Pico's automatic dark palette and own the fallback amber`.

Then run the three mutation checks on the committed tree. Each is a temporary working-tree edit that is never committed:
- M1: put `data-theme="auto"` back on `<html>`.
- M2: delete the whole @media block in app.css.
- M3: delete the `[data-theme="dark"]` block.

After each one, run `uv run pytest tests/test_browser.py -m browser -q -k TestDarkModeEngagement`, capture the failing node ids and the observed value from the assertion message, then revert with `/usr/bin/git checkout -- src/saneless/web/templates/base.html src/saneless/web/static/app.css`. Record a three-row table in the SUMMARY: mutation, failing test id, observed value, and the expected value from the UI-SPEC mutation table.

If all tests pass, the formatter makes no change and the linter reports nothing, do not create a REFACTOR commit.
  </action>
  <verify>
    <automated>uv run pytest tests/test_browser.py -m browser -q && uv run pytest -m "not browser" -q && ! /usr/bin/grep -n -- '--pico-color-' src/saneless/web/static/app.css && ! /usr/bin/grep -rn 'pico.colors' src/saneless/web/templates && ! /usr/bin/grep -rn 'data-theme' src/saneless/web/templates && test "$(/usr/bin/grep -c 'pico@2.1.1/css/pico.min.css' src/saneless/web/templates/base.html)" = 1 && /usr/bin/git diff --exit-code -- src/</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c '^<html lang="en">$' src/saneless/web/templates/base.html` == 1
    - `/usr/bin/grep -c '<meta name="color-scheme" content="light dark">' src/saneless/web/templates/base.html` == 1
    - `/usr/bin/grep -c 'pico@2.1.1/css/pico.min.css' src/saneless/web/templates/base.html` == 1
    - `/usr/bin/grep -c -- '--saneless-status-fallback' src/saneless/web/static/app.css` == 4 (3 declarations plus 1 var())
    - `/usr/bin/grep -c ':root:not(\[data-theme\])' src/saneless/web/static/app.css` == 1 and `/usr/bin/grep -c '^\[data-theme="dark"\]' src/saneless/web/static/app.css` == 1
    - `/usr/bin/grep -c 'var(--pico-ins-color, green)' src/saneless/web/static/app.css` == 1 and `/usr/bin/grep -c 'var(--pico-del-color, red)' src/saneless/web/static/app.css` == 1
    - `uv run pytest tests/test_browser.py -m browser -q` exits 0
    - The SUMMARY has a mutation table with three rows. M1 names T1 [dark] failing at rgb(255, 255, 255). M2 names a T2 dark status-fallback case failing at a ratio of about 3.65. M3 names T3 failing at rgb(161, 98, 7).
    - `/usr/bin/git diff --exit-code -- src/` exits 0 after the mutations, so all three were reverted
    - `/usr/bin/git log --format=%s -1 -- src/saneless/web/static/app.css` == `feat(23.1-01): engage Pico's automatic dark palette and own the fallback amber`
  </acceptance_criteria>
  <done>GREEN is committed. The dark palette engages, all 15 new cases and every existing browser test pass, and each of the three overrides is shown to be load-bearing.</done>
</task>

<task type="auto">
  <name>Task 3: record the convention in the master UI-SPEC and correct the pico.colors claim in ICM</name>
  <files>.planning/UI-SPEC.md</files>
  <read_first>
    - .planning/UI-SPEC.md (the file being modified; lines 28-66, 125-160 and 310-355)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-UI-SPEC.md ("Master UI-SPEC Edits Required in This Phase", "Measured contrast, after this phase's change" table, "Convention this establishes" points 1-5, "Palette File Decision", "Color" table)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-PATTERNS.md (the ".planning/UI-SPEC.md" line map)
    - src/saneless/web/static/app.css (after Task 2, for its line count)
  </read_first>
  <action>
Make the five edits from 23.1-UI-SPEC § "Master UI-SPEC Edits Required in This Phase" (D-14; DARK-02 requires the convention to be recorded here). Keep every table's existing column shape.

1. Line 39, Theme mode row, becomes: automatic from the OS `prefers-color-scheme`; `<html>` carries no `data-theme` attribute (Pico v2 engages automatic dark only when it is absent); `<meta name="color-scheme" content="light dark">` is present.
2. Line 34, Component library row: `@picocss/pico@2.1.1`, plus "`pico.colors.css` not shipped (DARK-03)". Line 350, Registry CDN row: `@picocss/pico@2.1.1/css/pico.min.css` with Version `2.1.1 (exact)`, worded like the htmx row on line 351. Add a row stating `pico.colors.min.css` is explicitly not linked.
3. Line 60, `.status-fallback` row: keep the purpose clause (a degraded success, deliberately neither the ins green nor the del red). Replace the mechanism with `var(--saneless-status-fallback)`: `#a16207` light (4.92:1) and `#ca8a04` dark (6.11:1).
4. Color section:
   - Line 129: reword the "one literal hex" sentence, which is now false. The only literal hexes are the two `--saneless-status-fallback` values.
   - Lines 131-134: the heading becomes "Two findings recorded in Phase 23, resolved in Phase 23.1". Finding 1 says linking `pico.colors.css` would NOT have been a no-op: Pico v2.1.1's `--pico-color-amber-600` is `#785800`. Keep that token name and `#785800` on the same line, and say the palette file is not shipped. Finding 2 says `data-theme` was removed in 23.1 and the OS preference is now honoured.
   - Lines 136-143, colour table: add a Dark column using the 23.1-UI-SPEC § Color values. The Warning row becomes `--saneless-status-fallback` (app-owned) with `#a16207` / `#ca8a04`.
   - Line 157: replace "No dark-mode-specific overrides are written in `app.css`…" with the five-point convention, copied from 23.1-UI-SPEC "Convention this establishes" points 1-5.
5. Accessibility: the line 318 Theme row says the OS preference is honoured through the absence of `data-theme` plus the color-scheme meta. The line 325 Contrast row carries the six-row measured contrast table from 23.1-UI-SPEC (class, scheme, computed colour, ratio vs page/td, ratio vs card), or a pointer to it plus the three dark ratios.
6. Line 45: replace "113 lines CSS" with the real count from `wc -l < src/saneless/web/static/app.css`.

Then store the ICM correction that D-14 and the UI-SPEC require: `icm store -t errors-resolved -c "Linking PicoCSS pico.colors.css would NOT have resolved .status-fallback with no CSS change: Pico v2.1.1 --pico-color-amber-600 is #785800, not #a16207 (Tailwind yellow-700); it would have given 2.74:1 in dark. Phase 23.1 ships no palette file and owns --saneless-status-fallback (#a16207 light / #ca8a04 dark)." -i high -k "pico,dark-mode,pico.colors,amber"`. Record the returned id in the SUMMARY.

Commit .planning/UI-SPEC.md with `/usr/bin/git commit` and the message `docs(23.1-01): record the dark-scheme convention in the master UI-SPEC`.
  </action>
  <verify>
    <automated>test "$(/usr/bin/grep -c -- '--saneless-status-fallback' .planning/UI-SPEC.md)" -ge 3 && test "$(/usr/bin/grep -c '#ca8a04' .planning/UI-SPEC.md)" -ge 2 && test "$(/usr/bin/grep -c 'pico@2.1.1' .planning/UI-SPEC.md)" -ge 2 && ! /usr/bin/grep -n 'No dark-mode-specific overrides are written' .planning/UI-SPEC.md && ! /usr/bin/grep -n 'not\*\* honoured today' .planning/UI-SPEC.md && ! /usr/bin/grep -n -- '--pico-color-amber-600' .planning/UI-SPEC.md | /usr/bin/grep -v '#785800'</automated>
  </verify>
  <acceptance_criteria>
    - `/usr/bin/grep -c 'resolved in Phase 23.1' .planning/UI-SPEC.md` >= 1
    - `/usr/bin/grep -c 'color-scheme" content="light dark"' .planning/UI-SPEC.md` >= 1
    - `/usr/bin/grep -c '6.11:1' .planning/UI-SPEC.md` >= 1 and `/usr/bin/grep -c '4.92:1' .planning/UI-SPEC.md` >= 1
    - `/usr/bin/grep -n 'Linking `pico.colors.css` would make the token resolve' .planning/UI-SPEC.md` returns nothing
    - Every remaining line containing `--pico-color-amber-600` also contains `#785800`
    - `/usr/bin/grep -c '113 lines CSS' .planning/UI-SPEC.md` == 0
    - The SUMMARY records the `icm store` id for the pico.colors correction
  </acceptance_criteria>
  <done>The master UI-SPEC describes the shipped dark-scheme behaviour and the five-point convention, the palette-file decision is recorded, and ICM carries the correction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → cdn.jsdelivr.net | The page loads Pico's stylesheet from a third-party CDN at run time |
| test process → local uvicorn | The Playwright tests drive a local, session-scoped server; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.1-CDN | Tampering | base.html Pico link | mitigate | Pin from floating `@2` to exact `@picocss/pico@2.1.1`. T1/T2/T3 assert exact computed colours, so a changed palette fails locally. SRI and vendoring are ROBU-09 (Phase 26), recorded in the ROADMAP D-16 note |
| T-23.1-PALETTE | Tampering | app.css / pico.colors.css | mitigate | Do not ship the palette file (DARK-03). The `! grep -- '--pico-color-'` gate on app.css and `! grep 'pico.colors'` on templates stop it being reintroduced silently |
| T-23.1-PROBE | Tampering | test JS probes | accept | Probes run only inside the local test browser, add and remove their own nodes in one evaluate, and never reach production markup |
</threat_model>

<verification>
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
- `uv run pytest -m "not browser" -q` (includes TestContrastHelper)
- `uv run pytest tests/test_browser.py -m browser -q` (local only, needs CDN egress, D-17)
- `/usr/bin/git log --format=%s --grep='^test(23.1-01)'` and `--grep='^feat(23.1-01)'` each return one commit, with RED before GREEN
- The mutation table (M1-M3) is in the SUMMARY
</verification>

<success_criteria>
- ROADMAP criterion 1 (as amended by D-15): T1 dark asserts the root element's computed background is rgb(19, 23, 31) and fails when data-theme="auto" is restored (M1)
- ROADMAP criterion 2: all 12 T2 cases are >= 4.5:1 in both schemes, asserted from computed colours
- DARK-03: no palette file, no `--pico-color-` in app.css, Pico pinned to 2.1.1, decision recorded in the master UI-SPEC
</success_criteria>

<output>
Create `.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-01-SUMMARY.md`. It includes the RED failing node ids with their observed values, the GREEN pass counts, the M1-M3 mutation table, and the ICM id.

Never spell the pathless pyrefly invocation in the SUMMARY. Commands always carry `src tests`, and any grep is spelled `pyrefly[[:space:]]check` (RESEARCH Pitfall 6).
</output>
