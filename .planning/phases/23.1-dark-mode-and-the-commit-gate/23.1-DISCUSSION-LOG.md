# Phase 23.1: Dark Mode and the Commit Gate - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-11
**Phase:** 23.1-dark-mode-and-the-commit-gate
**Areas discussed:** Where type checks run, Proving the gate, pyrefly rewrite scope, Dark-mode loose ends

**Todos:** 002-dark-mode-never-engages was matched (score 0.6) and **folded** into the phase.

---

## Where type checks run

| Option | Description | Selected |
|--------|-------------|----------|
| Split by path + merge gate (Recommended) | src-only ty/pyrefly at commit; full src+tests on pre-merge-commit and pre-push | ✓ |
| Full check at pre-push only | Type checks leave pre-commit entirely; run only on push, CI stays boundary | |
| Full check at merge + push | No type check at commit; full check on pre-merge-commit and pre-push | |
| CI only | Remove type checks from hooks; CI lint job is the only gate | |

**User's choice:** Split by path + merge gate.
**Notes:** The option rests on a finding made during discussion: GSD merges worktrees with
`git merge --no-ff` (execute-phase.md:787), which fires `pre-merge-commit`. Also verified: prek
0.3.6 supports that hook type and `pre-push`, and git is 2.47.3. The remaining details went to
Claude's discretion ("Next area").

---

## Proving the gate

| Option | Description | Selected |
|--------|-------------|----------|
| Local demos, no CI rerun (Recommended) | RED commit passes; --no-ff merge with tests/ type error rejected; push to local bare remote rejected; CI rests on Phase 20 proof + ci.yml read-back | ✓ |
| Local demos + one CI break | Same plus a seeded tests/ type error PR through CI | |
| Automated regression test | pytest building a temp repo and driving prek on every run | |

**User's choice:** Local demos, no CI rerun.

| Option | Description | Selected |
|--------|-------------|----------|
| Accept, pre-push + CI catch it (Recommended) | Conflicted merges get only the src-only commit check; document in CONTRIBUTING.md | ✓ |
| Close it in the hook | Commit-stage hook detects MERGE_HEAD and runs the full check | |

**User's choice:** Accept, and document.

---

## pyrefly rewrite scope

Measured before asking: bare forms sit in the live guidance (CLAUDE.md:17, CONTRIBUTING.md ×2,
ci.yml:32, codebase/CONCERNS.md, research/STACK.md) plus about 160 historical lines. Those are in
the v1.0 archive, phases 20–23 (35 + 55 + 27 + 41), the quick task and the code review.
REQUIREMENTS.md and ROADMAP.md only quote the form as the thing forbidden.

| Option | Description | Selected |
|--------|-------------|----------|
| Live guidance only (Recommended) | Fix CLAUDE.md, CONTRIBUTING, ci.yml, codebase/research; leave history | |
| Everything, literally | Also rewrite ~160 historical lines | ✓ |
| Live + active milestone | Live guidance plus phases 20–23, not the v1.0 archive | |

**User's choice:** Everything, literally. This diverges from the recommendation.

| Option | Description | Selected |
|--------|-------------|----------|
| Grep guard in the hooks (Recommended) | pygrep prek hook failing on bare `pyrefly check` | |
| No guard | Rewrite once, rely on the written reason | ✓ |
| CI grep step | Grep in CI lint job (cannot see stripped `.planning/`) | |

**User's choice:** No guard. This diverges from the recommendation.

---

## Dark-mode loose ends

| Option | Description | Selected |
|--------|-------------|----------|
| Amend ROADMAP now (Recommended) | Criterion 1 reads the root element, with a one-line note | ✓ |
| Leave it, rely on UI-SPEC | Keep roadmap text; planner/verifier read UI-SPEC's correction | |

**User's choice:** Amend ROADMAP now.

| Option | Description | Selected |
|--------|-------------|----------|
| Add to Phase 26 roadmap (Recommended) | Coupling note in ROADMAP Phase 26 pointing at 23.1 UI-SPEC | ✓ |
| Also amend ROBU-09 text | Also add constraints to the ROBU-09 requirement | |
| Leave it where it is | Trust 23.1's "check this when planning 26" line | |

**User's choice:** Add to the Phase 26 roadmap section.

---

## Claude's Discretion

- The hook ids and names, and per-hook `stages:` versus `default_stages`. The researcher must first
  confirm that file-modifying hooks will not start running at merge and push time once the extra
  shims are installed.
- Whether the full ty check stays bare `ty check` or becomes `ty check src tests`.
- The prose in CONTRIBUTING.md and CLAUDE.md.
- How the three demonstrations are scripted and cleaned up.
- The mechanism for the ROADMAP edits.
- Mechanical handling of prose mentions in the D-11 rewrite.

## Deferred Ideas

- The CI browser-test job for the DARK tests: Phase 26.
- SRI hashes and local vendoring of Pico and htmx: Phase 26 (ROBU-09).
- Declined: a grep guard, a MERGE_HEAD-aware hook, an automated prek regression test, a seeded-break
  CI PR, and amending ROBU-09's text.
