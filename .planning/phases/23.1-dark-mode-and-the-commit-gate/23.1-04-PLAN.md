---
phase: 23.1-dark-mode-and-the-commit-gate
plan: 04
type: execute
wave: 2
depends_on: ["23.1-01", "23.1-02", "23.1-03"]
files_modified:
  - .planning/todos/pending/002-dark-mode-never-engages.md
  - .planning/todos/done/002-dark-mode-never-engages.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-03]

must_haves:
  truths:
    - "The shared hooks directory holds prek shims for pre-commit, pre-merge-commit and pre-push, each pointing at the main checkout's .venv prek"
    - "A real RED commit (tests/ file calling a not-yet-existing module attribute) succeeds with hooks enabled and nothing suppressed, and its test fails at run time with AttributeError"
    - "A deliberate src/ type error is rejected at commit by both (src) hooks"
    - "A --no-ff merge of a branch carrying a tests/ type error is rejected by pre-merge-commit and creates no commit"
    - "A push of a checked-out branch carrying a tests/ type error to a local bare repo is rejected by pre-push and creates no ref"
    - "CI's lint job runs both type checkers with explicit pyrefly paths and ruleset 22777879 still requires lint"
    - "The D-11 grep over the merged tree returns only the lines in plan 23.1-03's residue allowlist"
  artifacts:
    - path: ".git/hooks/pre-merge-commit"
      provides: "prek shim, --hook-type=pre-merge-commit (untracked runtime state)"
      contains: "--hook-type=pre-merge-commit"
    - path: ".git/hooks/pre-push"
      provides: "prek shim, --hook-type=pre-push (untracked runtime state)"
      contains: "--hook-type=pre-push"
    - path: ".planning/todos/done/002-dark-mode-never-engages.md"
      provides: "folded todo closed, with the pico.colors correction note"
      contains: "status: done"
  key_links:
    - from: ".git/hooks/pre-merge-commit"
      to: ".pre-commit-config.yaml ty-checker-full / pyrefly-checker-full"
      via: "prek hook-impl reads the checked-out config at hook time"
      pattern: "hook-impl"
    - from: ".git/hooks/pre-*"
      to: "/home/kris/git/saneless/.venv/bin/prek"
      via: "PREK= absolute path written at install time"
      pattern: "PREK=\"/home/kris/git/saneless/.venv/bin/prek\""
---

<objective>
Turn the config from plan 23.1-02 into a live gate and prove it. Install the three prek shims, run
the D-07 demonstrations (RED commit passes; a src error is rejected at commit; a `--no-ff` merge is
rejected; a push is rejected) and record them verbatim. Read back CI and the ruleset (D-08). Confirm
the GATE-03 residue on the merged tree. Close the folded todo.

Purpose: the proof for GATE-01 and GATE-02 (ROADMAP criteria 3 and 4) and the final GATE-03 check
(criterion 5). This plan is alone in wave 2 on purpose.
- The shims live in the shared `.git/hooks`. Installing them while other worktrees were in flight
  would change which hooks those worktrees' merges run.
- This plan starts only after every wave-1 worktree has merged. From then on the orchestrator's own
  `--no-ff` merge of this plan is the first live pre-merge-commit run, and it runs the full type check
  on the main checkout. That is expected and passes on a type-clean tree; Task 1 re-checks this first.

ROADMAP mechanism (orchestrator resolution 2, Pitfall 4). GSD's per-plan worktree gate
(`execute-phase/steps/per-plan-worktree-gate.md`) disables worktree isolation only for plans that
touch git submodule paths. No other per-plan mechanism exists, and worktree executors must not write
ROADMAP.md, which the merge loop restores from a pre-merge snapshot anyway. So the D-15 amendment to
criterion 1 and the D-16 Phase 26 coupling note were applied to `.planning/ROADMAP.md` in the
planning commit, which ran on the main checkout before any worktree forked. This plan only greps for
them, read-only. Do NOT edit ROADMAP.md here, and never touch criterion 5.

Never push to `origin`, never merge anything into `autodev` from the demos, never create a tag, and
never merge a PR (Phase 20 D-21).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@CONTRIBUTING.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-CONTEXT.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-RESEARCH.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-02-SUMMARY.md
@.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-03-SUMMARY.md

<interfaces>
Runtime facts (RESEARCH G3-G5, verified on prek 0.3.6 and git 2.47.3):
- `core.hooksPath` is `/home/kris/git/saneless/.git/hooks`, scope local. Every worktree shares it. Keep it set.
- The existing `.git/hooks/pre-commit` shim contains `PREK="/home/kris/git/saneless/.venv/bin/prek"` and falls back to `prek` on PATH, which is 0.2.30 with unverified stage handling. The installer writes the absolute path of the prek binary that ran it. Therefore install from the MAIN checkout, whose `uv run` resolves `/home/kris/git/saneless/.venv/bin/prek`, never from a worktree `.venv`.
- A plain `uv run prek install` exits 2 in this clone. Use `uv run prek install --git-dir "$(git rev-parse --path-format=absolute --git-common-dir)"`. It installs every type in `default_install_hook_types` and idempotently overwrites existing prek shims. Do NOT upgrade prek.
- pre-push type-checks the checked-out working tree, not the pushed refs. Push `HEAD` from the tree that carries the error (G5, Pitfall 5).
- A rejected pre-merge-commit leaves MERGE_HEAD, MERGE_MODE and MERGE_MSG behind. Clean up with `git merge --abort` (Pitfall 3).
- rtk rewrites git and grep output and is refused inside worktrees. Use `/usr/bin/git` and `/usr/bin/grep` by absolute path everywhere (Pitfall 8). Hooks still fire.
- `src/saneless/vocabulary.py` exists. The RED demo references a function it does not define, as a module attribute (G8), so pytest fails at run time with AttributeError rather than at collection.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: install the three shims from the main checkout, re-check the stage split, read back CI and the ruleset</name>
  <files>(no tracked files; writes the untracked shims /home/kris/git/saneless/.git/hooks/pre-commit, pre-merge-commit and pre-push)</files>
  <read_first>
    - /home/kris/git/saneless/.pre-commit-config.yaml (must already carry default_install_hook_types from plan 23.1-02; read it from the main checkout path)
    - /home/kris/git/saneless/.git/hooks/pre-commit (the existing shim, to compare the PREK= line)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-RESEARCH.md (G3, Pitfall 1, and the Validation Architecture rows GATE-02 shims / stage config / CI)
    - .github/workflows/ci.yml (lint job, for the D-08 read-back)
  </read_first>
  <action>
Implements D-03 (the install half), plus the D-04/D-08 read-backs.

(1) Preconditions:
- In this worktree, the tree is type-clean: `uv run ty check && uv run pyrefly check src tests` both exit 0. That guarantees this plan's own merge passes the new pre-merge-commit gate.
- In the main checkout, `/usr/bin/grep -c '^default_install_hook_types: \[pre-commit, pre-merge-commit, pre-push\]$' /home/kris/git/saneless/.pre-commit-config.yaml` == 1, which proves plan 23.1-02 is merged into `autodev`.

If either precondition fails, STOP and report. Do not install.

(2) Install from the main checkout, in a subshell so this agent's cwd does not drift: `( cd /home/kris/git/saneless && uv run prek install --git-dir "$(/usr/bin/git rev-parse --path-format=absolute --git-common-dir)" )`. It must exit 0. This is the orchestrator's chosen form (resolution 3). Plain `prek install` fails here, and prek is not upgraded.

(3) Verify the shims. `ls /home/kris/git/saneless/.git/hooks` shows `pre-commit`, `pre-merge-commit` and `pre-push`. Each of the three files holds its own `--hook-type=<type>` and the line `PREK="/home/kris/git/saneless/.venv/bin/prek"`.

(4) From the main checkout, which is read-only apart from the hooks directory, re-run the dry runs: `( cd /home/kris/git/saneless && uv run prek run --stage pre-merge-commit --all-files --dry-run )` and the same with `--stage pre-push`. Each must list exactly `ty type checker (full)` and `pyrefly type checker (full)`.

(5) D-08 read-backs, both read-only:
- `/usr/bin/grep -nE 'uv run (ty check|pyrefly check src tests)$' .github/workflows/ci.yml` returns 2 lines.
- `gh api repos/kdknigga/saneless/rulesets/22777879 --jq '{enforcement: .enforcement, rules: [.rules[].type], checks: [.rules[] | select(.type=="required_status_checks") | .parameters.required_status_checks[].context]}'` must show enforcement `active`, rules including `required_status_checks`, `deletion` and `non_fast_forward`, and checks `lint` and `test`. If the jq path differs (RESEARCH A2), adjust the jq only.

If gh is not authenticated, that is an auth gate: report it and continue with the other steps. Do not push, re-run CI or open a PR (D-08).

Paste the install output, the three PREK/hook-type lines, both dry-run listings, the ci.yml grep and the ruleset JSON into the SUMMARY. There is nothing to commit in this task.
  </action>
  <verify>
    <automated>H=/home/kris/git/saneless/.git/hooks; for t in pre-commit pre-merge-commit pre-push; do test -x "$H/$t" && /usr/bin/grep -q -- "--hook-type=$t" "$H/$t" && /usr/bin/grep -q 'PREK="/home/kris/git/saneless/.venv/bin/prek"' "$H/$t" || exit 1; done; test "$(/usr/bin/grep -cE 'uv run (ty check|pyrefly check src tests)$' .github/workflows/ci.yml)" = 2</automated>
  </verify>
  <acceptance_criteria>
    - The install subshell exits 0, and its output is in the SUMMARY
    - For each t in pre-commit, pre-merge-commit and pre-push: `/home/kris/git/saneless/.git/hooks/$t` is executable, contains `--hook-type=$t`, and contains `PREK="/home/kris/git/saneless/.venv/bin/prek"`
    - `/usr/bin/git config --show-scope core.hooksPath` still prints `local	/home/kris/git/saneless/.git/hooks`
    - The main-checkout dry runs for pre-merge-commit and pre-push each list exactly the two `(full)` hooks
    - The ci.yml grep returns 2 lines. The ruleset read-back shows `active`, `required_status_checks`, `deletion`, `non_fast_forward`, `lint` and `test`, or an auth gate is recorded.
  </acceptance_criteria>
  <done>The gate is live in the shared hooks directory and points at the main venv's prek 0.3.6. CI and the ruleset are confirmed unchanged in kind.</done>
</task>

<task type="auto">
  <name>Task 2: the D-07 demonstrations: RED commit passes, src error rejected, --no-ff merge rejected, push rejected</name>
  <files>(scratch only: a throwaway worktree, throwaway branches demo-231-red and demo-231-base, and a bare repo, all removed afterwards; no tracked file changes)</files>
  <read_first>
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-RESEARCH.md ("D-07 demonstration recipe", around lines 653-712; G4, G5, G8; Pitfalls 1, 3, 5, 8)
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-CONTEXT.md (D-06, D-07)
    - src/saneless/vocabulary.py (confirm that no attribute named not_yet_there exists)
  </read_first>
  <action>
Implements D-07 (items 1-3) and D-06. Follow the RESEARCH "D-07 demonstration recipe" step for step, with these fixed parameters:
- G=/usr/bin/git and REPO=/home/kris/git/saneless.
- SCR is a fresh directory from `mktemp -d` under this agent's scratchpad or `$TMPDIR`. It is never inside the repo, and never under `.claude/worktrees/`.
- WT="$SCR/wt-231-demo" and BARE="$SCR/bare-231.git".

Create the throwaway worktree with `$G worktree add -b demo-231-red "$WT" autodev`. Worktree commands work from any worktree because refs are shared. Then `cd "$WT"` and run `uv sync --locked` once before recording anything, so the venv build output stays out of the transcript.

Capture every demo step's full stdout and stderr, and its exit code, into "$SCR/transcript.txt" (for example with `2>&1 | tee -a`, echoing `exit=$?` via PIPESTATUS).

- 1a RED commit. Write `tests/test_red_demo.py` with exactly the G8 form: a module docstring; `from saneless import vocabulary`; and `def test_not_yet_there() -> None:` with a one-line docstring and `assert vocabulary.not_yet_there() == "ready"`. The file must be ruff- and format-clean. `$G add` it and `$G commit -m "test(demo-231): add failing test for not_yet_there"`. Expect exit 0, with hook lines showing `ty type checker (src)` Passed and `pyrefly type checker (src)` Passed. Then run `uv run pytest tests/test_red_demo.py -q`: expect exit 1 with AttributeError.
- 1b src error. Append the recipe's `_demo_type_error() -> int` returning a str to `src/saneless/vocabulary.py`, `$G add` it and commit. Expect exit 1, with both `(src)` hooks Failed and no commit created (HEAD unchanged). Restore with `$G reset -q HEAD src/saneless/vocabulary.py && $G checkout -- src/saneless/vocabulary.py`.
- 2 merge. Record `BEFORE=$($G rev-parse demo-231-red)`. Then `$G switch -c demo-231-base autodev` and `$G merge demo-231-red --no-ff --no-edit -m "chore: merge executor worktree (demo-231-red)"`. Expect exit 1 with both `(full)` hooks Failed. Check that `$($G rev-parse --git-dir)/MERGE_HEAD` exists, and that `$G rev-parse HEAD` still equals `$G rev-parse autodev`, so no merge commit was created. Then `$G merge --abort` (exit 0).
- 3 push. `$G switch demo-231-red`, whose tree carries the tests/ error. `$G init -q --bare "$BARE"`. Then `$G push "$BARE" HEAD:refs/heads/demo-231-red`. Expect exit 1 with both `(full)` hooks Failed. Then `$G --git-dir="$BARE" for-each-ref` must print nothing.
- Suppression audit, taken before cleanup: `$G show demo-231-red | /usr/bin/grep -cE 'type: ignore|noqa|NotImplementedError'` == 0, and `/usr/bin/grep -cE -- '--no-verify|SKIP=' "$SCR/transcript.txt"` == 0.

Cleanup:
- `cd "$REPO"`
- `$G worktree remove --force "$WT"`
- `$G branch -D demo-231-red demo-231-base`
- `rm -rf "$BARE"`

Then confirm that `$G branch --list 'demo-231-*'` prints nothing and `$G worktree list` has no wt-231-demo entry.

Put the transcript in the SUMMARY verbatim, under `## D-07 demonstration transcript`. Precede it with a result table: demo, expected, observed exit code, and hook verdicts. Never merge into `autodev`, and never push anywhere except "$BARE". There is nothing to commit in this task.
  </action>
  <verify>
    <automated>test -z "$(/usr/bin/git branch --list 'demo-231-*')" && ! /usr/bin/git worktree list | /usr/bin/grep -q 'wt-231-demo' && test "$(/usr/bin/git rev-parse --abbrev-ref HEAD)" != "demo-231-red"</automated>
  </verify>
  <acceptance_criteria>
    - Demo 1a: the commit exits 0, and the transcript shows `ty type checker (src)` and `pyrefly type checker (src)` each followed by Passed. `uv run pytest tests/test_red_demo.py -q` exits 1, and the transcript contains `AttributeError`.
    - Demo 1b: the commit exits 1, and the transcript shows both `(src)` hooks Failed (ty `invalid-return-type`, pyrefly `bad-return`)
    - Demo 2: the merge exits 1, and the transcript shows both `(full)` hooks Failed, MERGE_HEAD present, and HEAD == autodev after the failed merge. `git merge --abort` exits 0.
    - Demo 3: the push exits 1, the transcript shows both `(full)` hooks Failed, and `for-each-ref` on the bare repo prints nothing
    - Suppression audit: both counts are 0 (no `type: ignore`, `noqa`, `NotImplementedError`, `--no-verify` or `SKIP=`)
    - Cleanup: `/usr/bin/git branch --list 'demo-231-*'` is empty, there is no wt-231-demo worktree, and the bare repo directory is gone
    - `/usr/bin/git log origin/master..autodev --format=%s | /usr/bin/grep -c 'demo-231'` == 0 (no demo commit reached autodev)
  </acceptance_criteria>
  <done>Criteria 3 and 4 are demonstrated on this repo's real hook configuration, with observed output recorded. Every throwaway artefact is gone.</done>
</task>

<task type="auto">
  <name>Task 3: close-out: GATE-03 residue on the merged tree, ROADMAP read-back, folded todo, ICM correction</name>
  <files>.planning/todos/pending/002-dark-mode-never-engages.md, .planning/todos/done/002-dark-mode-never-engages.md</files>
  <read_first>
    - .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-03-SUMMARY.md (the `## Residue allowlist` table)
    - .planning/todos/pending/002-dark-mode-never-engages.md (the file being moved; its Context paragraph holds the wrong pico.colors claim)
    - .planning/todos/done/001-default-dpi-document-scanning.md (the analog for a moved todo)
    - .planning/ROADMAP.md (Phase 23.1 criterion 1 and the Phase 26 section; read only)
  </read_first>
  <action>
(1) GATE-03 on the merged tree (D-11). Run the full acceptance grep with raw tools: `/usr/bin/grep -rnE` for `pyrefly[[:space:]]check` over `.planning CLAUDE.md CONTRIBUTING.md .github .pre-commit-config.yaml`, piped through `/usr/bin/grep -vE` for `pyrefly[[:space:]]check (src|tests)`. Compare the file:line set with plan 23.1-03's `## Residue allowlist`.
- They must be identical: nothing from the four live files, nothing from any 23.1 PLAN or SUMMARY.
- If an unexpected line appears in a non-23.1 file, it is a leftover command. Fix it by naming `src tests` and commit with the message `docs(23.1-04): name pyrefly's paths in <file>`.
- If a new line appears in a 23.1 artefact, reword it to "the pathless form".

Record the final count and the comparison result in the SUMMARY.

(2) ROADMAP read-back (D-15, D-16, applied at planning time). Both `/usr/bin/grep -c 'document.documentElement' .planning/ROADMAP.md` and `/usr/bin/grep -c 'Coupling contract for Phase 26 (ROBU-09)' .planning/ROADMAP.md` must be >= 1. If either is 0, do NOT edit ROADMAP.md from this worktree, because the orchestrator's merge loop would revert it. Instead, put a `## ROADMAP re-apply needed` section in the SUMMARY with the exact missing text from 23.1-RESEARCH § G9, for the orchestrator to apply on the main checkout.

(3) The folded todo. Run `/usr/bin/git mv .planning/todos/pending/002-dark-mode-never-engages.md .planning/todos/done/002-dark-mode-never-engages.md`, then make two small edits so git still sees a rename:
- Set frontmatter `status: done`, and add `resolved: 2026-09-11 (Phase 23.1)`.
- Append one short `## Resolution` paragraph. It says data-theme was removed, the dark palette is proven by computed-colour tests, and the fallback amber is app-owned. It also says the Context claim that linking `pico.colors.css` would resolve the token with no CSS change is wrong, because Pico v2.1.1's `--pico-color-amber-600` is `#785800`; see 23.1-UI-SPEC § Palette File Decision.

Commit with `/usr/bin/git commit` and the message `docs(23.1-04): close the dark-mode todo folded into phase 23.1`.

(4) ICM. The two Phase 23 workaround notes are 01M26TBPEY4PA14Z79J2RT90NC (which recommends `SKIP=pyrefly-checker`) and 01M28H0NCS7PC7TCA2B9YS2T3K (the `raise NotImplementedError` stub). For each, read the current content with `icm recall` (query by its topic words), then `icm update <id> -c "<original text> SUPERSEDED by Phase 23.1: commit-stage ty/pyrefly now check src only, so a TDD RED commit lands with every hook enabled; neither SKIP= nor a NotImplementedError stub is needed or allowed. Full checks run at pre-merge-commit and pre-push."`. Also run `icm store -t decisions-saneless -c "Phase 23.1 gate: pre-commit runs ty check src + pyrefly check src; pre-merge-commit and pre-push run ty check + pyrefly check src tests; shims installed from the main checkout with uv run prek install --git-dir \"\$(git rev-parse --path-format=absolute --git-common-dir)\" (prek 0.3.6 refuses plain install because core.hooksPath is set)." -i high`. Record the ids in the SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/todos/done/002-dark-mode-never-engages.md && test ! -e .planning/todos/pending/002-dark-mode-never-engages.md && /usr/bin/grep -q '^status: done' .planning/todos/done/002-dark-mode-never-engages.md && /usr/bin/grep -q '#785800' .planning/todos/done/002-dark-mode-never-engages.md && test "$(/usr/bin/grep -c 'document.documentElement' .planning/ROADMAP.md)" -ge 1 && test "$(/usr/bin/grep -c 'Coupling contract for Phase 26 (ROBU-09)' .planning/ROADMAP.md)" -ge 1 && test -z "$(/usr/bin/grep -nE 'pyrefly[[:space:]]check' CLAUDE.md CONTRIBUTING.md .pre-commit-config.yaml .github/workflows/ci.yml .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-0*-PLAN.md .planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-0*-SUMMARY.md | /usr/bin/grep -vE 'pyrefly[[:space:]]check (src|tests)')"</automated>
  </verify>
  <acceptance_criteria>
    - The merged-tree D-11 residue set equals plan 23.1-03's `## Residue allowlist` (same file:line entries), and the SUMMARY states the final count
    - No residue line comes from CLAUDE.md, CONTRIBUTING.md, .github, .pre-commit-config.yaml, or any `23.1-0*-PLAN.md` / `23.1-0*-SUMMARY.md`
    - `.planning/ROADMAP.md` contains `document.documentElement` (criterion 1, D-15) and `Coupling contract for Phase 26 (ROBU-09)` (Phase 26 note, D-16). Criterion 5 is unchanged.
    - `/usr/bin/git show --stat HEAD` shows the todo as a rename (pending → done), and the moved file has `status: done` and mentions `#785800`
    - The SUMMARY records the ICM ids updated and stored
  </acceptance_criteria>
  <done>GATE-03 is confirmed on the integrated tree, the ROADMAP amendments are present, the folded todo is closed with its wrong claim corrected, and ICM no longer recommends the obsolete workarounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo scratch area → real repository | Throwaway worktree, branches and bare repo share the real object store and hooks |
| local hooks → origin/master | Local gates are convenience; CI + ruleset 22777879 enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23.1-DEMO | Tampering | demos touching `origin` or `autodev` | mitigate | Pushes go only to a `mktemp -d` bare repo. Merges run only between throwaway branches (demo-231-base ← demo-231-red), never into autodev. Cleanup is verified by `git branch --list 'demo-231-*'` returning nothing and no wt-231-demo worktree, and there is an explicit check that no demo commit reached autodev |
| T-23.1-SHIM | Tampering / Elevation | shim hard-coding a vanished worktree venv, which falls back to PATH prek 0.2.30 | mitigate | Install from the main checkout, then assert `PREK="/home/kris/git/saneless/.venv/bin/prek"` in all three shims |
| T-23.1-PUSH | Tampering / Repudiation | pre-push checks the working tree, not the pushed refs (G5) | accept | Documented in CONTRIBUTING (plan 23.1-02). The demo pushes HEAD from the broken tree. CI is the boundary |
| T-23.1-BYPASS | Tampering | `--no-verify` / `SKIP=` / suppression in the RED proof | mitigate | Suppression audit: `type: ignore`/`noqa`/`NotImplementedError` absent from the RED commit; `--no-verify`/`SKIP=` absent from the transcript |
| T-23.1-RULESET | Information disclosure | `gh api` read-back | accept | Read-only GET of a ruleset the maintainer owns; no token is printed |
</threat_model>

<verification>
- `/home/kris/git/saneless/.git/hooks/{pre-commit,pre-merge-commit,pre-push}` exist with the right `--hook-type` and PREK path
- The D-07 transcript and result table are in the SUMMARY. All four expectations were observed, and the suppression audit is 0/0.
- The merged-tree D-11 residue equals the 23.1-03 allowlist, and both ROADMAP greps hit
- After the orchestrator merges this plan, the merge output shows `ty type checker (full)` and `pyrefly type checker (full)` Passed. This is the first live pre-merge-commit run; the verifier notes it.
</verification>

<success_criteria>
- ROADMAP criterion 3 / GATE-01: the RED commit landed with hooks enabled and no suppression (demo 1a)
- ROADMAP criterion 4 / GATE-02: the type error was rejected at commit (src), at `--no-ff` merge and at push. CI `lint` and ruleset 22777879 were read back unchanged in kind.
- ROADMAP criterion 5 / GATE-03: only justified quoting lines remain, as enumerated in 23.1-03
</success_criteria>

<output>
Create `.planning/phases/23.1-dark-mode-and-the-commit-gate/23.1-04-SUMMARY.md`. It includes the install output, the shim lines, the dry runs, the CI/ruleset read-backs, the D-07 result table with the verbatim transcript, the residue comparison, the ROADMAP read-back and the ICM ids.

Never spell the pathless pyrefly invocation in it (RESEARCH Pitfall 6). If the transcript contains hook output with the full hook's command, that output always carries `src tests`.
</output>
