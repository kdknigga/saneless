---
phase: 15-create-user-facing-documentation-using-the-di-taxis-approach
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mkdocs.yml
  - docs/index.md
  - docs/tutorials/scan-your-first-document.md
  - .github/workflows/docs.yml
  - pyproject.toml
  - uv.lock
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-10, D-11, D-14, D-15, D-16]

must_haves:
  truths:
    - "mkdocs build --strict succeeds with zero warnings"
    - "docs site has a landing page linking to all four Diataxis quadrants"
    - "Tutorial page walks through install, config, first scan, and paperless verification"
    - "GitHub Actions workflow deploys docs on push to main"
    - "mkdocs-material is listed as a dev dependency"
  artifacts:
    - path: "mkdocs.yml"
      provides: "MkDocs configuration with Material theme and full nav structure"
      contains: "name: material"
    - path: "docs/index.md"
      provides: "Documentation landing page"
      contains: "Tutorials"
    - path: "docs/tutorials/scan-your-first-document.md"
      provides: "End-to-end tutorial for new users"
      contains: "Prerequisites"
    - path: ".github/workflows/docs.yml"
      provides: "GitHub Actions workflow for docs deployment"
      contains: "mkdocs gh-deploy"
    - path: "pyproject.toml"
      provides: "mkdocs-material dev dependency"
      contains: "mkdocs-material"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/"
      via: "nav configuration"
      pattern: "tutorials/scan-your-first-document.md"
    - from: ".github/workflows/docs.yml"
      to: "mkdocs.yml"
      via: "mkdocs gh-deploy command"
      pattern: "mkdocs gh-deploy"
---

<objective>
Set up MkDocs Material documentation scaffolding and write the flagship tutorial.

Purpose: Establish the documentation infrastructure (theme, config, CI/CD) and create the single most important page -- the "Scan Your First Document" tutorial that new users will land on.

Output: Working MkDocs site that builds clean with `mkdocs build --strict`, a GitHub Actions deployment workflow, and one complete tutorial page.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-create-user-facing-documentation-using-the-di-taxis-approach/15-CONTEXT.md
@.planning/phases/15-create-user-facing-documentation-using-the-di-taxis-approach/15-RESEARCH.md

@README.md
@docker-compose.yml
@src/saneless/config.py
@src/saneless/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: MkDocs scaffolding -- dependency, config, landing page, GitHub Actions workflow</name>
  <files>mkdocs.yml, docs/index.md, .github/workflows/docs.yml, pyproject.toml, uv.lock</files>
  <read_first>
    - pyproject.toml (current dev dependencies)
    - .github/workflows/release.yml (existing workflow patterns)
    - README.md (project description for site metadata)
  </read_first>
  <action>
1. Add mkdocs-material dev dependency (per D-01, D-04):
   ```bash
   uv add --dev mkdocs-material
   ```

2. Create `mkdocs.yml` at repo root (per D-03) with this exact content:
   ```yaml
   site_name: saneless
   site_description: SANE scanner to paperless-ngx bridge
   site_url: https://kris-knigga.github.io/saneless/
   repo_url: https://github.com/kris-knigga/saneless
   repo_name: kris-knigga/saneless

   theme:
     name: material
     palette:
       - scheme: default
         primary: indigo
         accent: indigo
         toggle:
           icon: material/brightness-7
           name: Switch to dark mode
       - scheme: slate
         primary: indigo
         accent: indigo
         toggle:
           icon: material/brightness-4
           name: Switch to light mode
     features:
       - navigation.sections
       - navigation.expand
       - search.highlight
       - content.code.copy

   markdown_extensions:
     - admonition
     - pymdownx.details
     - pymdownx.superfences
     - pymdownx.tabbed:
         alternate_style: true
     - attr_list
     - def_list
     - tables

   nav:
     - Home: index.md
     - Tutorials:
       - Scan Your First Document: tutorials/scan-your-first-document.md
     - How-To Guides:
       - Install on Bare Metal: how-to/install-bare-metal.md
       - Deploy with Docker Compose: how-to/deploy-docker-compose.md
       - Configure Scan Profiles: how-to/configure-scan-profiles.md
       - Set Up ADF Duplex Scanning: how-to/set-up-adf-duplex.md
       - Scanner Host Discovery (Containers): how-to/scanner-host-discovery.md
       - Use the CLI for Scripting: how-to/cli-scripting.md
     - Reference:
       - CLI Commands: reference/cli-commands.md
       - Configuration (TOML): reference/configuration.md
       - Environment Variables: reference/environment-variables.md
       - Web API: reference/web-api.md
       - Docker: reference/docker.md
     - Explanation:
       - Architecture Overview: explanation/architecture.md
       - Empty Page Detection: explanation/empty-page-detection.md
       - Consume Directory Fallback: explanation/consume-directory-fallback.md
   ```

3. Create `docs/index.md` -- landing page that orients users to the four Diataxis quadrants (per D-05). Include:
   - One-paragraph project description (from README line 1: "SANE scanner to paperless-ngx bridge...")
   - Four sections with brief descriptions and links:
     - **Tutorials** -- "New to saneless? Start here." Link to scan-your-first-document
     - **How-To Guides** -- "Step-by-step instructions for specific tasks." Links to all 6 guides
     - **Reference** -- "Technical details: CLI, configuration, API, Docker." Links to all 5 reference pages
     - **Explanation** -- "Background and design decisions." Links to all 3 explanation pages
   - Do NOT duplicate README content (per D-18)

4. Create `.github/workflows/docs.yml` (per D-02):
   ```yaml
   name: Docs

   on:
     push:
       branches: [main]

   permissions:
     contents: write

   jobs:
     deploy:
       runs-on: ubuntu-latest
       steps:
         - uses: actions/checkout@v5
         - uses: actions/setup-python@v5
           with:
             python-version: "3.x"
         - run: pip install mkdocs-material
         - run: mkdocs gh-deploy --force
   ```

5. Create placeholder `.md` files for ALL nav entries (one line each: `# Page Title`) so that `mkdocs build --strict` passes. These will be filled in by subsequent plans. Create files at:
   - docs/how-to/install-bare-metal.md
   - docs/how-to/deploy-docker-compose.md
   - docs/how-to/configure-scan-profiles.md
   - docs/how-to/set-up-adf-duplex.md
   - docs/how-to/scanner-host-discovery.md
   - docs/how-to/cli-scripting.md
   - docs/reference/cli-commands.md
   - docs/reference/configuration.md
   - docs/reference/environment-variables.md
   - docs/reference/web-api.md
   - docs/reference/docker.md
   - docs/explanation/architecture.md
   - docs/explanation/empty-page-detection.md
   - docs/explanation/consume-directory-fallback.md
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mkdocs.yml exists at repo root and contains `name: material`
    - mkdocs.yml contains `nav:` with all 15 page entries
    - docs/index.md exists and contains `# saneless`
    - docs/index.md contains the word `Tutorials` and `How-To Guides` and `Reference` and `Explanation`
    - .github/workflows/docs.yml exists and contains `mkdocs gh-deploy --force`
    - .github/workflows/docs.yml contains `branches: [main]`
    - pyproject.toml contains `mkdocs-material` in dev dependencies
    - `uv run mkdocs build --strict` exits 0 with no warnings
    - All 14 placeholder .md files exist under docs/how-to/, docs/reference/, docs/explanation/
  </acceptance_criteria>
  <done>MkDocs site builds clean with Material theme, all nav entries resolve, GitHub Actions workflow ready, mkdocs-material installed as dev dependency</done>
</task>

<task type="auto">
  <name>Task 2: Write "Scan Your First Document" tutorial</name>
  <files>docs/tutorials/scan-your-first-document.md</files>
  <read_first>
    - README.md (current quick-start content to avoid duplication per D-18)
    - src/saneless/config.py (config model fields, defaults, search paths)
    - src/saneless/cli.py (scan command flags, exit codes)
    - docker-compose.yml (Docker deployment example)
  </read_first>
  <action>
Write the flagship tutorial (per D-06, D-11). This is the most important documentation page -- it takes a new user from zero to a scanned document in paperless-ngx.

Structure the tutorial with these exact sections:

**Title:** `# Scan Your First Document`

**Introduction** (2-3 sentences): What you'll accomplish -- install saneless, configure it to talk to your scanner and paperless-ngx, scan a document, and see it appear in paperless.

**Prerequisites section** (per D-16):
- A SANE-compatible scanner connected via USB or network
- A running paperless-ngx instance with an API token
- Python 3.12+ (for bare metal) or Docker (for container install)

**Step 1: Install saneless** -- Two tabs using pymdownx.tabbed (per D-14 copy-paste commands):
- Bare metal tab: `sudo apt-get install libsane-dev && pip install saneless`
- Docker tab: Reference the Docker Compose how-to guide for full setup; show `docker run -p 8080:8080 ghcr.io/kris-knigga/saneless` for quick test

**Step 2: Verify your scanner is detected:**
```bash
saneless devices
```
Show example output. Explain what to do if no scanner appears (check USB, install SANE drivers). Note: for Docker/container users, link to the scanner host discovery how-to guide.

**Step 3: Create a configuration file:**
Show creating `saneless.toml` in the current directory with realistic values (per D-15):
```toml
[paperless]
url = "http://paperless.local:8000"
token = "abc123def456"
```
Explain this is the minimum config -- scanner auto-detection handles the rest. Mention the 4 config search paths: `--config`, `./saneless.toml`, `~/.config/saneless/config.toml`, `/etc/saneless/config.toml`. Link to Configuration reference for all options.

**Step 4: Scan a document:**
```bash
saneless scan --title "My First Scan"
```
Explain what happens: saneless detects the scanner, scans from the flatbed at 300 DPI in color, assembles a single-page PDF, and uploads it to paperless-ngx with the title "My First Scan".

**Step 5: Verify in paperless-ngx:**
Describe: Open your paperless-ngx web UI, search for "My First Scan", confirm the document appears with the correct title.

**Next steps:** Brief list linking to:
- Configure Scan Profiles (for ADF, duplex, resolution)
- Deploy with Docker Compose (for permanent setup alongside paperless)
- CLI Commands reference (for all options)

Writing style: Step-by-step, copy-paste friendly (D-14), realistic values (D-15), no screenshots (D-13), written for least technical user (D-11).
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - docs/tutorials/scan-your-first-document.md contains `# Scan Your First Document`
    - File contains `## Prerequisites` or `## What you'll need`
    - File contains `saneless scan --title` (copy-paste command per D-14)
    - File contains `saneless.toml` (config file creation step)
    - File contains `192.168` or `paperless.local` (realistic values per D-15)
    - File contains `saneless devices` (scanner verification step)
    - File does NOT contain any image/screenshot references (per D-13)
    - File length is at least 80 lines (substantive tutorial, not stub)
    - `uv run mkdocs build --strict` exits 0
  </acceptance_criteria>
  <done>Tutorial walks a new user through install, config, first scan, and paperless verification with copy-paste commands and realistic config values</done>
</task>

</tasks>

<verification>
- `uv run mkdocs build --strict` exits 0 with zero warnings
- `ls docs/tutorials/scan-your-first-document.md` exists with substantive content
- `ls .github/workflows/docs.yml` exists
- `grep -q "mkdocs-material" pyproject.toml` succeeds
</verification>

<success_criteria>
MkDocs site builds cleanly with Material theme. Landing page orients users to all four Diataxis quadrants. Tutorial page provides a complete walkthrough from install to verified scan in paperless-ngx. GitHub Actions workflow is ready for deployment on merge to main.
</success_criteria>

<output>
After completion, create `.planning/phases/15-create-user-facing-documentation-using-the-di-taxis-approach/15-01-SUMMARY.md`
</output>
