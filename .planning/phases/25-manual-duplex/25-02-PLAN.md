---
phase: 25-manual-duplex
plan: 02
type: tdd
wave: 2
depends_on: [25-01]
files_modified:
  - src/saneless/vocabulary.py
  - src/saneless/pipeline.py
  - src/saneless/cli.py
  - src/saneless/worker.py
  - tests/test_vocabulary.py
  - tests/test_pipeline.py
autonomous: true
requirements: [DPLX-06]
decisions: [D-12, D-13]

must_haves:
  truths:
    - "JobState has nine members and SCANNING_REVERSE is the ninth"
    - "SCANNING_REVERSE is an active state, therefore a busy state, so the Scan button stays disabled and aria-busy stays on during pass B"
    - "PipelineEvent.SCANNING_REVERSE projects to JobState.SCANNING_REVERSE instead of None"
    - "The CLI prints the same 'Scanning reverse sides...' line it printed before, now through the general progress branch"
    - "During SCANNING_REVERSE the status partial shows no flip prompt"
  artifacts:
    - path: "src/saneless/vocabulary.py"
      provides: "the ninth JobState member, its ACTIVE_STATES membership, and both label arms"
      contains: "SCANNING_REVERSE"
    - path: "src/saneless/pipeline.py"
      provides: "PipelineEvent.job_state narrowed to JobState"
  key_links:
    - from: "src/saneless/pipeline.py::PipelineEvent.job_state"
      to: "vocabulary.JobState.SCANNING_REVERSE"
      via: "match arm"
      pattern: "case PipelineEvent.SCANNING_REVERSE"
    - from: "src/saneless/vocabulary.py::ACTIVE_STATES"
      to: "BUSY_STATES"
      via: "derived frozenset difference"
      pattern: "ACTIVE_STATES"
---

<objective>
Make pass B visible: add `JobState.SCANNING_REVERSE`, give it both labels, put it in
`ACTIVE_STATES`, and fill the reserved `PipelineEvent.job_state` seam so the property stops
returning `None`.

Purpose: DPLX-06's vocabulary half. Today `PipelineEvent.SCANNING_REVERSE.job_state` returns `None`,
so the job stays `AWAITING_FLIP` for the whole of pass B — the flip prompt stays on screen while
pages feed. This plan is what makes the prompt disappear.

Output: a ninth lifecycle state that every existing derived consumer picks up automatically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-manual-duplex/25-CONTEXT.md
@.planning/phases/25-manual-duplex/25-PATTERNS.md
@.planning/phases/25-manual-duplex/25-RESEARCH.md
@CLAUDE.md

**Hard atomicity constraint — read this before splitting anything.** The commit-stage hooks run
`ty check src` and `pyrefly check src`. Adding an enum member **without** its `match` arms is caught
at commit stage by both checkers. `JobState.SCANNING_REVERSE`, the `state_label` arm, the
`progress_label` arm, the `ACTIVE_STATES` membership, the `PipelineEvent.job_state` arm and both
`state is None` branch deletions therefore **must land in ONE commit** (the GREEN task). Do not try
to stage them.

**No migration.** `src/saneless/job.py:289` declares `state TEXT NOT NULL` with **no `CHECK`
constraint**, and the `PRAGMA user_version` ladder at `:350-364` governs *columns*, not values. A
ninth value persists and reads back untouched. **Do not invent a migration task.**
</context>

<interfaces>
Created here, consumed by plans 25-03, 25-05 and 25-09:

From `src/saneless/vocabulary.py`:
- `JobState.SCANNING_REVERSE = "SCANNING_REVERSE"` — the ninth member
- `state_label(JobState.SCANNING_REVERSE) -> "Scanning backs"` (D-13)
- `progress_label(JobState.SCANNING_REVERSE) -> "Scanning reverse sides..."` (D-13, **byte-identical**
  to what `cli.py:131` already prints, which is what makes the CLI collapse invisible)
- `SCANNING_REVERSE in ACTIVE_STATES` — and therefore in the derived `BUSY_STATES`

From `src/saneless/pipeline.py`:
- `PipelineEvent.job_state -> JobState` — the return annotation **narrows** from `JobState | None`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): update the four hand-written rosters and the two projection tests</name>
  <files>tests/test_vocabulary.py, tests/test_pipeline.py</files>
  <read_first>
    - `tests/test_vocabulary.py` — the whole file. The four sites that need a hand edit are
      `:46-55` (`test_job_state_has_exactly_eight_members`), `:123-136`
      (`test_active_states_membership`, an explicit five-member frozenset), `:162-177`
      (`TestStateLabel::test_state_label_strings`, an eight-row parametrize) and `:190-202`
      (`TestProgressLabel::test_progress_label_strings`, a five-row parametrize)
    - `tests/test_pipeline.py` — `test_scanning_reverse_has_no_job_state`,
      `test_job_state_projection_mapping`, `test_job_state_projection_is_total`, and the
      `len(PipelineEvent) == 6` pin at `:987`
    - `.planning/phases/25-manual-duplex/25-PATTERNS.md` §4 — the six production sites traced
      against the `JobState.FALLBACK` precedent, with each roster excerpt
    - `.planning/phases/25-manual-duplex/25-RESEARCH.md` Findings 7 and 8, and Pitfall 5
  </read_first>
  <behavior>
    - `len(list(JobState)) == 9`
    - `ACTIVE_STATES` is the **six** in-flight states: PENDING, SCANNING, AWAITING_FLIP,
      SCANNING_REVERSE, ASSEMBLING, UPLOADING
    - `state_label(JobState.SCANNING_REVERSE) == "Scanning backs"`
    - `progress_label(JobState.SCANNING_REVERSE) == "Scanning reverse sides..."`
    - `PipelineEvent.SCANNING_REVERSE.job_state is JobState.SCANNING_REVERSE` — replacing the test
      that asserts it is `None`
    - Every `PipelineEvent` member projects to a `JobState` (the totality test tightens from
      "maps or is None" to `isinstance(event.job_state, JobState)`)
  </behavior>
  <action>
    Edit exactly the four hand-written rosters in `tests/test_vocabulary.py`. Rename
    `test_job_state_has_exactly_eight_members` to `..._exactly_nine_members` and update its
    assertion and its docstring's count. Add `JobState.SCANNING_REVERSE` to the explicit frozenset in
    `test_active_states_membership` and reword its docstring to "the six in-flight lifecycle states".
    Add the `("Scanning backs")` row to the `state_label` parametrize and the
    `("Scanning reverse sides...")` row to the `progress_label` parametrize — three ASCII dots, not
    an ellipsis character (UI-SPEC copy style).

    Do **not** touch the `list(JobState)` completeness tests at `:57`, `:110`, `:179` and `:204` —
    they are Phase 21 D-09 working as designed and pick the new member up for free.

    **`tests/test_web_state_rendering.py` needs ZERO edits.** All six of its `list(JobState)` cases
    build their expectations from `state_label` / `progress_label` / `ACTIVE_STATES` / `BUSY_STATES`.
    In particular `test_status_area_prose` (`:206-241`) already asserts
    `("flip-prompt" in text) is (state is JobState.AWAITING_FLIP)` parametrised over every state, so
    it **proves D-16's "Continue and Abort vanish during pass B" with zero new test code** the moment
    `SCANNING_REVERSE` joins `ACTIVE_STATES`. Do not add a case to it and do not read its untouched
    state as an omission.

    In `tests/test_pipeline.py`: rewrite `test_scanning_reverse_has_no_job_state` — it asserts the
    very defect DPLX-06 removes. Rename it to reflect the new guarantee and assert the projection.
    Add the `SCANNING_REVERSE` row to `test_job_state_projection_mapping`. Tighten
    `test_job_state_projection_is_total` to `assert isinstance(event.job_state, JobState)`, which is
    what totality means once the type is non-Optional.

    Leave `:987`'s `len(PipelineEvent) == 6` alone — `PipelineEvent` gains **no** member; only its
    projection changes. Confirmed by RESEARCH Finding 7.
  </action>
  <verify>
    <automated>uv run pytest tests/test_vocabulary.py tests/test_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_vocabulary.py -q` FAILS with `AttributeError: SCANNING_REVERSE` or a
      count assertion, NOT a collection error
    - `git diff --name-only` shows `tests/test_web_state_rendering.py` is **not** modified
    - `grep -c "len(list(JobState)) == 9" tests/test_vocabulary.py` returns 1
    - `grep -c "Scanning backs" tests/test_vocabulary.py` returns 1
    - `uv run ruff check .` and `uv run ruff format --check .` pass
    - Commit message starts with `test(25-02):`
  </acceptance_criteria>
  <done>The four rosters and the two projection tests are updated and committed, failing for the right reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): add the ninth state, both labels, and fill the projection seam — one commit</name>
  <files>src/saneless/vocabulary.py, src/saneless/pipeline.py, src/saneless/cli.py, src/saneless/worker.py</files>
  <read_first>
    - `src/saneless/vocabulary.py` — `JobState` (`:42-49`), `ACTIVE_STATES` (`:114-122`),
      `BUSY_STATES` (`:138`, derived), `state_label` (`:182-183` for the `FALLBACK` arm),
      `progress_label` (`:228-229`), `job_state_for` (`:235-268`) for the canonical
      `match` + `assert_never` shape and its `Raises: AssertionError` docstring line
    - `src/saneless/pipeline.py:54-93` — `PipelineEvent.job_state`, especially the reserving
      comment at `:82-84` and the docstring paragraph at `:56-64`
    - `src/saneless/cli.py:121-133` — the `state is None` branch whose own comment predicts its
      deletion
    - `src/saneless/worker.py:215-238` — the twin `state is None` branch and the busy-state write
      that follows it
    - `.planning/phases/25-manual-duplex/25-PATTERNS.md` §4
  </read_first>
  <action>
    In `vocabulary.py`: add `SCANNING_REVERSE = "SCANNING_REVERSE"` to `JobState` positioned
    between `AWAITING_FLIP` and `ASSEMBLING` so the enum reads in lifecycle order. Add the member to
    the `ACTIVE_STATES` frozenset literal. `BUSY_STATES` is **derived** (`ACTIVE_STATES -
    {AWAITING_FLIP}`) so busy-ness follows automatically — do not edit it. Add
    `case JobState.SCANNING_REVERSE: label = "Scanning backs"` to `state_label` and
    `case JobState.SCANNING_REVERSE: label = "Scanning reverse sides..."` to `progress_label`,
    following the module's uniform assign-to-a-local-then-return-once shape. Both completeness tests
    assert `label != state.value`, so neither label may echo the enum name.

    In `pipeline.py`: replace the `case PipelineEvent.SCANNING_REVERSE: state = None` arm and its
    reserving comment with `state = JobState.SCANNING_REVERSE`. Narrow the property's return
    annotation from `JobState | None` to `JobState`. Replace the docstring paragraph at `:56-64`
    that explains what `None` means with the answer — do not leave prose describing an optional
    projection beside a total one.

    In `cli.py`: delete the `elif state is None:` branch at `:125-131` entirely. Its
    `click.echo("Scanning reverse sides...")` is byte-identical to what `progress_label` now returns,
    so the general `else` branch produces exactly the same output and **users see no change**. Delete
    the comment that says SCANNING_REVERSE is the only event that persists no state — it is now
    false.

    In `worker.py`: delete the `if state is None:` early-return branch at `:215-220`. Note what this
    does and why it is safe: SCANNING_REVERSE now falls through to the general busy-state path, which
    **both** persists the state and still calls `self._transition_event.set()` at `:231`. The pass
    boundary therefore remains observable to any current waiter — the branch deletion strictly adds
    behaviour rather than removing it. `_transition_event` itself is deleted in plan 25-05 (D-14);
    leave every other touch of it alone here.

    Commit all four files together. Splitting them fails `ty check src` at commit stage.
  </action>
  <verify>
    <automated>uv run pytest -q && uv run ruff check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_vocabulary.py tests/test_pipeline.py -q` PASSES
    - `uv run pytest tests/test_web_state_rendering.py -q` PASSES **with no edits to that file** —
      this is DPLX-06's "aria-busy and no flip-prompt during SCANNING_REVERSE" observable, and it
      passing untouched is the proof D-16 holds
    - `uv run pytest -q` PASSES with no regressions against the 966-test baseline
    - `grep -rn "state is None" src/saneless/cli.py src/saneless/worker.py` returns nothing
    - `grep -rn "SCANNING_REVERSE is the only event" src/` returns nothing
    - `grep -c "SCANNING_REVERSE" src/saneless/vocabulary.py` is at least 4 (member, ACTIVE_STATES,
      two label arms)
    - `git diff --name-only HEAD~1` shows `src/saneless/web/templates/partials/status.html` is
      **not** modified — adding a branch there would give pass B its own presentation and break D-16
    - All four static gates pass
    - Commit message starts with `feat(25-02):`
  </acceptance_criteria>
  <done>Nine JobState members, both labels, ACTIVE_STATES membership and a non-Optional projection all land in one green commit; the rendering tests pass untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker thread → SQLite `jobs.state` | A new enum value is persisted to a TEXT column |
| `jobs.state` → Jinja templates | A persisted state selects a rendering branch shown on the LAN UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06 | Tampering (data integrity) | `jobs.state` column | accept | `state TEXT NOT NULL` with no `CHECK` constraint (re-verified). A ninth value round-trips; existing rows are untouched. No migration, so no migration failure mode is introduced |
| T-25-07 | Information Disclosure | `state_label` / `progress_label` | mitigate | Both are closed total lookups over an enum returning fixed literals. No caller-supplied text reaches the template through this path, so no injection surface is created |
| T-25-08 | Spoofing (UI misrepresentation) | flip prompt shown during pass B | mitigate | This plan **removes** the defect: once SCANNING_REVERSE is its own busy state, `status.html`'s `AWAITING_FLIP` branch no longer matches during pass B, so Continue/Abort controls that do nothing disappear (D-16). Enforced by the untouched `test_status_area_prose` |
| T-25-SC | Tampering | package installs | N/A | This phase installs nothing |
</threat_model>

<verification>
- `uv run pytest -q` — full suite green
- `uv run pytest tests/test_web_state_rendering.py -q` green with a clean `git diff` on that file
- All four static gates: `uv run ruff check .`, `uv run ruff format --check .`, `uv run ty check`,
  `uv run pyrefly check src tests`
</verification>

<success_criteria>
- Nine `JobState` members, each with a label that is not its raw value
- `PipelineEvent.job_state` is total and non-Optional; both `state is None` branches are gone
- The flip prompt is structurally impossible to render during pass B
- CLI output is byte-identical to before (D-13)
</success_criteria>

<output>
Create `.planning/phases/25-manual-duplex/25-02-SUMMARY.md` when done, recording the RED/GREEN commit
SHAs and confirming that `tests/test_web_state_rendering.py` and `partials/status.html` were not
modified.
</output>
