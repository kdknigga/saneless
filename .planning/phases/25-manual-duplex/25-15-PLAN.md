---
phase: 25-manual-duplex
plan: 15
type: execute
wave: 4
depends_on: [25-11, 25-12, 25-13, 25-14]
gap_closure: true
files_modified:
  - docs/reference/web-api.md
  - docs/reference/cli-commands.md
  - docs/reference/configuration.md
  - docs/how-to/set-up-adf-duplex.md
  - docs/getting-started/first-web-ui-scan.md
  - docs/explanation/architecture.md
  - .planning/UI-SPEC.md
  - .planning/phases/25-manual-duplex/deferred-items.md
autonomous: true
requirements: [DPLX-04, DPLX-05, DPLX-06]
decisions: [D-16, D-18]
review_items: [WR-07, WR-06, CR-01]

must_haves:
  truths:
    - "docs/reference/web-api.md no longer claims the web UI cannot send an early flip answer or that the buttons are gone before a second click; it documents the required job_id field, the drop rules, and the acknowledgment response"
    - "The scan exit-code table in docs/reference/cli-commands.md renders as one table with rows 0, 1, 2, 3, and the manual-duplex paragraph follows the table"
    - "deferred-items.md no longer says the early-answer defect is reachable by direct API callers only; it records the corrected reach and that 25-10/25-11 resolved it"
    - "The ADF duplex how-to describes single-sided feeder preference, the both-sides-only refusal and the no-source-option behaviour exactly as the code now behaves, and still never mentions source = \"Manual Duplex\" (D-18)"
    - "configuration.md states flip_timeout_seconds must be between 1 and 86400"
    - "UI-SPEC records the flip acknowledgment copy and the job_id carried by the flip buttons"
  artifacts:
    - path: "docs/reference/web-api.md"
      provides: "corrected flip endpoint reference"
      contains: "job_id"
    - path: "docs/reference/cli-commands.md"
      provides: "repaired exit-code table"
    - path: ".planning/phases/25-manual-duplex/deferred-items.md"
      provides: "corrected reach classification, marked resolved"
  key_links:
    - from: "docs/reference/web-api.md"
      to: "routes.continue_flip / routes.abort_flip"
      via: "documented required form field"
      pattern: "job_id"
    - from: ".planning/UI-SPEC.md"
      to: "vocabulary.flip_answer_label"
      via: "acknowledgment copy rows"
      pattern: "Aborting scan\\.\\.\\."
---

<objective>
Make the documentation and the phase's paper trail true after the gap fixes: WR-07 (web-api.md's
false safety claims), WR-06 (broken exit-code table), the incorrect "direct API callers only" reach
classification in deferred-items.md, and the doc sentences the other gap plans changed (flip timeout
bounds, feeder preference/refusal, no-source-option devices, the prompt-failure abort, the flip
acknowledgment).

Purpose: the phase rule is "correct what you changed", and the verifier flagged deferred-items.md's
reach classification as having understated CR-01's severity. D-18 still binds: no page may mention
the legacy `source = "Manual Duplex"` form, including when describing WR-05/IN-04's warnings -- do not
document those warnings.

This plan is docs-only and runs last so every quoted message is the shipped text. Before writing any
quoted error or UI string, copy it from the source file named in read_first, not from this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-manual-duplex/25-CONTEXT.md
@.planning/phases/25-manual-duplex/25-REVIEW.md
@.planning/phases/25-manual-duplex/25-VERIFICATION.md
@.planning/phases/25-manual-duplex/25-10-SUMMARY.md
@.planning/phases/25-manual-duplex/25-11-SUMMARY.md
@.planning/phases/25-manual-duplex/25-12-SUMMARY.md
@.planning/phases/25-manual-duplex/25-13-SUMMARY.md
@.planning/phases/25-manual-duplex/25-14-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the flip endpoint reference, the UI contract and the deferred-items entry (WR-07, CR-01 paper trail)</name>
  <files>docs/reference/web-api.md, .planning/UI-SPEC.md, .planning/phases/25-manual-duplex/deferred-items.md, docs/explanation/architecture.md, docs/getting-started/first-web-ui-scan.md</files>
  <read_first>
    - docs/reference/web-api.md (whole file; flip endpoints at ~130-155)
    - src/saneless/web/routes.py -- `continue_flip`, `abort_flip`, `_status_context` (shipped behaviour and docstrings)
    - src/saneless/worker.py -- `ScanWorker.continue_flip`, `abort_flip`, `flip_answer`, `WorkerFlipCoordinator.arm`
    - src/saneless/vocabulary.py -- `flip_answer_label` exact strings
    - src/saneless/web/templates/partials/status.html and partials/flip.html
    - .planning/UI-SPEC.md lines 190-260 and 305-330 -- component inventory row "Status states", status table, flip prompt copy table, "Flip routes (Phase 25)" note
    - .planning/phases/25-manual-duplex/deferred-items.md
    - docs/explanation/architecture.md lines 50-58
    - docs/getting-started/first-web-ui-scan.md lines 40-48
  </read_first>
  <action>
    docs/reference/web-api.md:
    - For both `POST /api/flip/continue` and `POST /api/flip/abort`, add a **Request** line: form field
      `job_id` (string, required) -- the id of the job the answer is for; the web UI's buttons send it
      automatically. Missing field -> 422.
    - Response: status partial for the current job, else most recent (D-17). When the answer was
      accepted, or the job's flip wait was already answered, and the job is still recorded
      `AWAITING_FLIP`, the partial shows the acknowledgment (quote the two `flip_answer_label` strings
      for CONTINUED and ABORTED from vocabulary.py) instead of the Continue and Abort buttons.
    - Rewrite "How the two flip endpoints interact": keep "The first answer is final" (D-16). Replace
      the "Call them only while the job is AWAITING_FLIP" bullet and the "controls disappear on their
      own ... before a second click can land on them" bullet with accurate bullets: (a) an answer is
      accepted only for the job named by `job_id`, and only once that job has reached `AWAITING_FLIP`;
      an answer sent during pass A, for a different job, or after the job ended is dropped, changes
      nothing, and the endpoint still returns the current status; (b) a repeated click therefore cannot
      reach the next queued manual-duplex job; (c) after an accepted answer the partial shows the
      acknowledgment until the job leaves `AWAITING_FLIP`, and once pass B starts the status shows
      `Scanning reverse sides...`. Remove the words "The web UI cannot do this" and "gone before a
      second click".

    .planning/UI-SPEC.md:
    - Status area table: split the AWAITING_FLIP row into two rows, "AWAITING_FLIP (unanswered)" (the
      existing flip prompt description) and "AWAITING_FLIP (answered)" with copy
      `Flip confirmed. Scanning reverse sides next...` / `Aborting scan...` (copy from
      `flip_answer_label`) and decoration `<p aria-busy="true">`, no buttons. Update the component
      inventory "Status states" row count from "Ten" to "Eleven" and its slash list (add
      AwaitingFlipAnswered after AwaitingFlip). Do not add a SCANNING_REVERSE branch; keep that
      paragraph, adjusting its "a late Continue or Abort click has nothing to land on" sentence to also
      say a click for any other job is dropped by `job_id`.
    - "Flip routes (Phase 25)" note: add that both buttons send `job_id` via `hx-vals`, the routes drop
      answers for other jobs or sent before AWAITING_FLIP (CR-01), and an accepted answer renders the
      acknowledgment rather than re-rendering the prompt.

    .planning/phases/25-manual-duplex/deferred-items.md: keep the entry as a record but correct it.
    Change its heading to mark it RESOLVED by 25-10 and 25-11. Replace the "Reach" bullet with: the
    original classification ("direct API callers only") was wrong -- the flip routes re-rendered the
    stale AWAITING_FLIP prompt, buttons included, so an ordinary double-click or retry reached it, and
    the verifier reproduced a second Abort ending the next queued job (CR-01, BLOCKER; see
    25-REVIEW.md and 25-VERIFICATION.md). Replace "Why deferred" with "Resolution": coordinator armed
    only when AWAITING_FLIP is announced, signals scoped by `job_id`, routes render an acknowledgment;
    regression tests in tests/test_worker.py and tests/test_web.py. Remove the "Possible fix" bullet.

    docs/explanation/architecture.md (the FlipCoordinator paragraph): after "Whichever answer arrives
    first -- including the timeout -- is final, and later answers are dropped." add one sentence: in
    the web server the worker's coordinator is bound to its job and accepts an answer only once that
    job is waiting at the flip prompt, and the routes name the job they answer, so a stale or repeated
    click is dropped instead of reaching another job.

    docs/getting-started/first-web-ui-scan.md: after "click **Continue** to scan the back sides.", add
    that the prompt is replaced by a short confirmation as soon as the click is received. No other
    change.

    Never mention `source = "Manual Duplex"` (D-18).
  </action>
  <verify>
    <automated>! grep -n "The web UI cannot do this\|gone before a second click\|direct API callers only" docs/reference/web-api.md .planning/phases/25-manual-duplex/deferred-items.md && grep -n "job_id" docs/reference/web-api.md && grep -n "Aborting scan\.\.\." .planning/UI-SPEC.md && grep -n "RESOLVED" .planning/phases/25-manual-duplex/deferred-items.md && ! grep -rn "Manual Duplex\"" docs --include=*.md --exclude=PRD.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "The web UI cannot do this" docs/reference/web-api.md` returns 0
    - `grep -c "before a second click" docs/reference/web-api.md` returns 0
    - `grep -c "job_id" docs/reference/web-api.md` returns at least 3
    - `grep -n "422" docs/reference/web-api.md` matches in the flip section
    - `grep -c "was wrong" .planning/phases/25-manual-duplex/deferred-items.md` returns at least 1, and `grep -c "Reach:\*\* direct API callers only" .planning/phases/25-manual-duplex/deferred-items.md` returns 0
    - `grep -n "RESOLVED" .planning/phases/25-manual-duplex/deferred-items.md` matches once
    - `grep -n "Eleven presentations" .planning/UI-SPEC.md` matches once and `grep -c "Ten presentations" .planning/UI-SPEC.md` returns 0
    - `grep -n "Flip confirmed. Scanning reverse sides next..." .planning/UI-SPEC.md` and `grep -n "Aborting scan..." .planning/UI-SPEC.md` each match
    - `grep -rn 'source = "Manual Duplex"' docs --include=*.md | grep -v PRD.md` returns nothing
  </acceptance_criteria>
  <done>The flip API reference, the UI contract, the architecture explanation and the phase's deferred-items record all describe the job-scoped, armed flip answer the code now implements.</done>
</task>

<task type="auto">
  <name>Task 2: Repair the exit-code table and bring the CLI, configuration and ADF how-to in line with WR-01, WR-02, WR-03 and WR-08 (WR-06)</name>
  <files>docs/reference/cli-commands.md, docs/reference/configuration.md, docs/how-to/set-up-adf-duplex.md</files>
  <read_first>
    - docs/reference/cli-commands.md lines 15-45 -- the `scan` exit-code table with the paragraph inserted between rows 2 and 3
    - docs/reference/configuration.md lines 40-70 -- `[output]` and `[profiles.NAME]` tables
    - docs/how-to/set-up-adf-duplex.md (whole file) -- Manual Duplex section, the "Manual duplex needs a document feeder" note, "Scanners with no document feeder"
    - src/saneless/config.py -- `OutputConfig.flip_timeout_seconds` bounds
    - src/saneless/scanner/sane_backend.py -- `_resolve_feeder_source` and `_resolve_source` exact messages (WR-02 warning and refusal, WR-03 refusal, unchanged no-feeder refusal)
    - src/saneless/cli.py -- `ClickFlipCoordinator._prompt` (WR-08 behaviour and log message)
  </read_first>
  <action>
    docs/reference/cli-commands.md: move the `| 3 | Paperless-ngx upload error (unreachable, auth failure, etc.) |`
    row up to directly after row 2 so the table is contiguous (rows 0, 1, 2, 3), then place the
    manual-duplex paragraph after the table, separated by a blank line. In that paragraph, after
    "no, Ctrl-C or end of input aborts the scan", add that an error reading the terminal at the prompt
    also aborts the scan at once and is logged (WR-08).

    docs/reference/configuration.md: in the `flip_timeout_seconds` row, add that the value must be a
    whole number of seconds from 1 to 86400 (one day); 0 and negative values are rejected at load --
    there is no "wait forever" setting. In the `duplex` row, if it says `manual` "needs a feeder
    source", extend it to "a single-sided feeder source" and link the how-to as it already does.

    docs/how-to/set-up-adf-duplex.md:
    - Rewrite the "Manual duplex needs a document feeder" note body: both passes use a single-sided
      feeder source. saneless uses the `source` you configured when your scanner reports it and it is
      a single-sided feeder; otherwise it uses the first single-sided feeder the scanner reports. A
      source that already scans both sides (such as `ADF Duplex`) is never used for manual duplex --
      each pass would return every page twice -- so if you name one, saneless logs a warning and uses
      the single-sided feeder instead. Keep the "not a flatbed workflow" and automation sentences.
    - Under "Scanners with no document feeder" keep the existing refusal and add two short
      subsections or paragraphs, each quoting the shipped message copied from sane_backend.py:
      (1) scanners whose only feeder scans both sides: manual duplex is refused before any page is
      fed; use `duplex = "hardware"` with that source instead (link the ADF Hardware Duplex section);
      (2) scanners that report no source list at all: manual duplex runs when `source` names the
      feeder (for example `ADF`), because saneless does not have to select a source on such a device;
      with a non-feeder source such as `Flatbed` it is refused with the quoted message.
    - In the CLI bullet of "How you confirm the flip", after the Ctrl-C/closing-input sentence, add
      that an error reading the terminal also ends the scan the same way.
    - Do NOT mention `source = "Manual Duplex"` or the deprecation warning anywhere (D-18).
  </action>
  <verify>
    <automated>uv run python -c "import re,sys;t=open('docs/reference/cli-commands.md').read();s=t.index('**Exit codes:**');blk=t[s:s+1200];rows=re.findall(r'^\| (\d) \|',blk,re.M);sys.exit(0 if rows[:4]==['0','1','2','3'] and '\n\n| 3 |' not in blk else 1)" && grep -n "86400\|86,400" docs/reference/configuration.md && grep -n "single-sided" docs/how-to/set-up-adf-duplex.md && ! grep -rn 'source = "Manual Duplex"' docs/how-to docs/reference docs/getting-started docs/explanation</automated>
  </verify>
  <acceptance_criteria>
    - In docs/reference/cli-commands.md the first four table rows after `**Exit codes:**` are `| 0 |`, `| 1 |`, `| 2 |`, `| 3 |` with no blank line between them (the verify one-liner exits 0)
    - The manual-duplex paragraph in cli-commands.md appears after the `| 3 |` row
    - `grep -n "86400\|86,400" docs/reference/configuration.md` matches in the `flip_timeout_seconds` row
    - `grep -c "single-sided" docs/how-to/set-up-adf-duplex.md` returns at least 2
    - `grep -n 'duplex = "hardware"' docs/how-to/set-up-adf-duplex.md` matches in the new both-sides-only refusal text
    - Every error message quoted in set-up-adf-duplex.md for WR-02/WR-03 is a byte-for-byte substring of src/saneless/scanner/sane_backend.py (executor checks each with `grep -F`)
    - `grep -rn 'source = "Manual Duplex"' docs/how-to docs/reference docs/getting-started docs/explanation` returns nothing
    - `uv run mkdocs build` (mkdocs-material is a dev dependency) run before this plan's edits and after them reports no additional WARNING lines (compare `uv run mkdocs build 2>&1 | grep -c WARNING` before vs after; build into the scratchpad with `-d` so no `site/` directory is committed)
  </acceptance_criteria>
  <done>The exit-code table renders correctly, and the CLI reference, configuration reference and ADF how-to match the shipped timeout bounds, feeder selection, no-source-option behaviour and prompt-failure abort.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> operator and API integrator | docs describe safety properties operators rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-63 | Repudiation / misinformation | web-api.md promising a double-click safety property the code lacked (WR-07) | mitigate | Bullets rewritten to the job-scoped, armed behaviour; grep gates for the removed false sentences |
| T-25-64 | Repudiation | deferred-items.md understating CR-01's reach | mitigate | Entry corrected and marked resolved with references to the review and verification |
| T-25-65 | Information Disclosure | docs reintroducing the legacy form (D-18) | mitigate | grep gate over docs for `source = "Manual Duplex"` |
| T-25-SC | Tampering | package installs | N/A | This plan installs nothing |
</threat_model>

<verification>
- All grep gates in both tasks pass
- `uv run prek run --all-files` passes (markdown hooks, if any)
</verification>

<success_criteria>
- WR-06 and WR-07 closed; deferred-items.md corrected; every doc sentence touched by 25-10..25-14 is true
</success_criteria>

<output>
Create `.planning/phases/25-manual-duplex/25-15-SUMMARY.md` when done
</output>
