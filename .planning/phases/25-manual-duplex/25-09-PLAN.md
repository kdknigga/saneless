---
phase: 25-manual-duplex
plan: 09
type: execute
wave: 7
depends_on: [25-05, 25-07, 25-08]
files_modified:
  - docs/how-to/set-up-adf-duplex.md
  - docs/how-to/configure-scan-profiles.md
  - docs/how-to/cli-scripting.md
  - docs/reference/configuration.md
  - docs/reference/web-api.md
  - docs/reference/cli-commands.md
  - docs/getting-started/first-web-ui-scan.md
  - docs/explanation/architecture.md
  - .planning/UI-SPEC.md
autonomous: true
requirements: [DPLX-01, DPLX-02, DPLX-03, DPLX-04, DPLX-05, DPLX-06, DPLX-07]
decisions: [D-08, D-11, D-18]

must_haves:
  truths:
    - "No documentation page teaches or mentions source = \"Manual Duplex\""
    - "The ADF how-to teaches a real device source plus duplex = \"manual\""
    - "The documented CLI flip interaction matches click.confirm, not a bare Enter keypress"
    - "The documented JobState list has nine members"
    - "The scan exit-code table includes the non-TTY refusal"
    - "UI-SPEC's status table carries SCANNING_REVERSE and its summary count matches its own table"
  artifacts:
    - path: "docs/how-to/set-up-adf-duplex.md"
      provides: "the rewritten manual-duplex section, the only page teaching the feature"
    - path: ".planning/UI-SPEC.md"
      provides: "the project-level UI contract extended with the ninth state"
  key_links:
    - from: "docs/how-to/set-up-adf-duplex.md"
      to: "ProfileConfig.duplex"
      via: "the taught two-key profile form"
      pattern: "duplex = \"manual\""
    - from: ".planning/UI-SPEC.md"
      to: "vocabulary.state_label"
      via: "the humanized-label list"
      pattern: "Scanning backs"
---

<objective>
Make the documentation true. Remove the legacy `source = "Manual Duplex"` form entirely, teach the
two-key form, and correct every sentence this phase falsified.

Purpose: the phase rule is "correct what you changed", and D-18 makes this load-bearing rather than
cosmetic — the ADF duplex how-to is named in the phase goal itself. Four rows of the code review's
documentation audit (rows 1, 2, 22, 24) are claims this phase makes true.

Output: nine files corrected, and a `SCANNING_REVERSE` row in the project-level UI contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-manual-duplex/25-CONTEXT.md
@.planning/phases/25-manual-duplex/25-PATTERNS.md
@.planning/phases/25-manual-duplex/25-RESEARCH.md
@.planning/UI-SPEC.md
@CLAUDE.md

**D-18 diverges from the recommendation, and the divergence has a binding consequence.** The
recommendation was to keep one short deprecation note; D-18 removes the legacy form from the
documentation **entirely**. The accepted cost: `ProfileConfig` still translates the legacy key and
still warns, while no page mentions that key any more — so the **warning text is the only migration
instruction an operator will ever receive.** That text shipped in plan 25-01; read its SUMMARY and do
not contradict it here. A warning pointing at documentation that no longer exists is exactly the dead
end this milestone's appliance goal exists to remove.

**`.planning/UI-SPEC.md` is the project-level design contract (Phase 23's D-20) and is extended
in-phase. Do NOT generate a phase-level UI-SPEC.**

**The UI-SPEC re-check CONTEXT asked for has already been done, and the summary is wrong.** Its
`:197` line claims "Status states | **Nine** presentations" while the table at `:238-247` lists
**eight** rows and omits `AWAITING_FLIP` entirely. Fix against the table, not the summary: the table
gains **two** rows (the missing `AWAITING_FLIP` and the new `SCANNING_REVERSE`), making **ten**
presentations.

**Planner discretion recorded — `docs/PRD.md` is out of scope.** It lists `ADF Manual Duplex` as a
scanner source (`:110`) and describes the two-pass flow (`:116`), but the PRD records what was asked
for; it is a historical record rather than a claim about current behaviour. Recorded here explicitly
so the verifier does not read its absence from the diff as an omission. If the developer considers
the PRD live documentation, it is two more sentences and cheap to add.

**Three files that must NOT be touched, recorded so an incomplete-looking diff is not flagged:**
`tests/test_web_state_rendering.py`, `src/saneless/web/templates/partials/status.html` (adding a
`SCANNING_REVERSE` branch would give pass B its own presentation and **break** D-16) and
`partials/flip.html` (its binding to the `AWAITING_FLIP` branch alone is the whole mechanism of D-16).
Note the doc claim that flip.html's buttons are "Continue and **Cancel**" is wrong — `:42-43` say
`Continue` / `Abort scan` — but that is a **docs** fix, made here, not a template fix.
</context>

<tasks>

<task type="auto">
  <name>Task 1: rewrite the ADF duplex how-to and the two config-facing pages</name>
  <files>docs/how-to/set-up-adf-duplex.md, docs/how-to/configure-scan-profiles.md, docs/reference/configuration.md</files>
  <read_first>
    - `docs/how-to/set-up-adf-duplex.md` — the whole file. Its own § "ADF Simplex" and § "ADF Hardware
      Duplex" (`:14-42`) are the house form the rewritten Manual Duplex section should match: a
      one-paragraph explanation, a fenced toml profile block, a fenced bash invocation
    - `docs/how-to/configure-scan-profiles.md:56` (the `source` row) and `:71-77` (§ "Source values")
    - `docs/reference/configuration.md:63` (the `source` row listing `ADF Manual Duplex`) and the
      `[output]` table
    - `src/saneless/web/templates/partials/flip.html:42-43` — the real button labels
    - The plan 25-01 SUMMARY (the shipped deprecation-warning text) and the plan 25-06 SUMMARY (the
      no-feeder refusal message)
    - `.planning/phases/25-manual-duplex/25-RESEARCH.md` § "Documentation Scope"
  </read_first>
  <action>
    Rewrite § Manual Duplex in `set-up-adf-duplex.md` (`:44-82`). Teach **only** the two-key form: a
    real device source plus `duplex = "manual"`. Remove the `source = "Manual Duplex"` block at
    `:56-67` and the sentence at `:64` telling operators to set the source to any string containing
    both "manual" and "duplex" — that substring rule no longer exists on the strategy path.

    Fix the admonition at `:48-52`. Its claim that "a source name containing 'duplex' is treated as a
    feeder source" is a `classify_source` statement, not a strategy statement, and as written it reads
    as the old rule. Either scope it explicitly to source classification or drop it.

    Replace "press Enter (CLI)" at `:60` with what the CLI actually does: a confirmation prompt that
    blocks until the operator answers. Add the non-interactive case — manual duplex needs an
    interactive terminal and exits 2 without one — because `docs/how-to/cli-scripting.md` actively
    invites people onto the automation path where this matters.

    Fix `:77`'s "Continue and **Cancel** buttons" to `Continue` and `Abort scan`.

    Extend § Page count mismatch (`:100-105`) with D-08's note: blank-page removal is **deliberately**
    skipped on the mismatch path, because a blank back side is *evidence* about why the two passes
    disagreed, and the partial PDFs exist to provide exactly that diagnostic. The profile's blank-page
    toggle visibly does not apply on this one path, so without the note an operator reads it as a bug.
    Per Phase 24 D-04, also state that the page cap is per `scan_pages` call and therefore **per
    pass** — this is a duplex page where that matters.

    Document what happens on a device with no feeder source: the scan fails before pass A with a
    message listing the sources the device does report. Quote the shape from plan 25-06's SUMMARY.

    In `configure-scan-profiles.md`, add a `duplex` row beneath the `source` row at `:56` and update
    § "Source values" (`:71-77`) so its manual-duplex pointer names the two-key form.

    In `configuration.md`, remove `ADF Manual Duplex` from the `source` row's value list at `:63`, add
    a `duplex` row (values `none` / `hardware` / `manual`, default `none`), and add
    `flip_timeout_seconds` (int, default 600) to the `[output]` table.

    Note when documenting `duplex`: `hardware` is **declarative** — nothing reads it today. It records
    what the scanner does so a profile is self-describing. Say this plainly rather than implying it
    changes behaviour.

    **Do not** reintroduce the legacy form anywhere, including as a "previously you could write…"
    aside. D-18 removes it outright.
  </action>
  <verify>
    <automated>grep -rn "Manual Duplex" docs/ ; grep -rn "ADF Manual Duplex" docs/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'source = "Manual Duplex"' docs/` returns nothing
    - `grep -rn "ADF Manual Duplex" docs/` returns nothing
    - `grep -c 'duplex = "manual"' docs/how-to/set-up-adf-duplex.md` is at least 1
    - `grep -rn "Cancel" docs/how-to/set-up-adf-duplex.md` returns nothing referring to the flip
      buttons
    - `grep -c "flip_timeout_seconds" docs/reference/configuration.md` returns 1
    - `grep -c "duplex" docs/reference/configuration.md` shows a `duplex` row exists
    - `grep -rn "press Enter" docs/how-to/set-up-adf-duplex.md` returns nothing
    - Every toml block in the rewritten section is a profile a reader could paste and load
    - Commit message starts with `docs(25-09):`
  </acceptance_criteria>
  <done>The how-to teaches only the two-key form, the config reference documents both new keys, and the legacy form appears nowhere in `docs/`.</done>
</task>

<task type="auto">
  <name>Task 2: correct the CLI, web-API, getting-started and architecture pages</name>
  <files>docs/how-to/cli-scripting.md, docs/reference/cli-commands.md, docs/reference/web-api.md, docs/getting-started/first-web-ui-scan.md, docs/explanation/architecture.md</files>
  <read_first>
    - `docs/how-to/cli-scripting.md:56-57` — the eight-`JobState` list described as "safe to compare
      against in a script"; and `:67-77` — the exit-code table
    - `docs/reference/cli-commands.md:29-36` — the `scan` exit-code table
    - `docs/reference/web-api.md:124-136` — "Flip endpoints return HTML partial with updated job
      status"
    - `docs/getting-started/first-web-ui-scan.md:35-38` (the stage list) and `:42` ("face-up", and
      "Cancel")
    - `docs/explanation/architecture.md:28-33` (the pipeline-stage list) and `:51` ("the worker blocks
      between pass A and pass B using a `threading.Event`")
    - `src/saneless/vocabulary.py` — the shipped `JobState` members and labels
    - The plan 25-05 and 25-07 SUMMARYs (route behaviour, the exit-2 message, the prompt text)
  </read_first>
  <action>
    In `cli-scripting.md`, add `SCANNING_REVERSE` to the state list at `:56-57` — a ninth state
    falsifies a list presented as exhaustive and safe to compare against. Add the non-TTY refusal to
    the exit-code table at `:67-77`: code 2, manual duplex started without an interactive terminal.
    This page is the reason that refusal exists, so make the connection explicit for a reader
    automating `saneless scan`.

    In `cli-commands.md`, add the same case to the `scan` exit-code table at `:29-36`.

    In `web-api.md:124-136`, correct the flip-endpoint description. Both endpoints return the status
    partial for the job they acted on, resolved as "current job, else most recent" — so a call
    arriving after the job has ended reports **that job** rather than the idle state. State D-16's
    contract plainly: the coordinator answers once and its answer is final, so an Abort arriving after
    a Continue is dropped and the endpoint returns current status rather than an error. Note that the
    Continue and Abort controls are rendered only while the job is `AWAITING_FLIP`, so they disappear
    on their own once pass B starts.

    In `first-web-ui-scan.md`, fix `:42`: the button is `Abort scan`, not "Cancel", and the instruction
    is to flip the stack over rather than to place pages "face-up". Extend the stage list at `:35-38`
    — it names Scanning / Assembling / Uploading / Done and omits the flip and pass B entirely, which
    is precisely the journey a first-time duplex operator is on.

    In `architecture.md`, update the pipeline-stage list at `:28-33` to include the flip wait and pass
    B as distinct stages, and rewrite `:51`: the worker no longer "blocks between pass A and pass B
    using a `threading.Event`". It waits through a `FlipCoordinator` with a bounded timeout, and the
    transition-event protocol has been deleted. Mention that the device is opened and closed per
    `scan_pages` call, so the scanner handle is already released between passes — what the timeout
    releases is the worker thread.
  </action>
  <verify>
    <automated>grep -rn "wait_transition\|threading.Event" docs/ ; grep -rn "SCANNING_REVERSE" docs/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SCANNING_REVERSE" docs/how-to/cli-scripting.md` is at least 1
    - The state list in `cli-scripting.md` has nine entries matching `list(JobState)` exactly
    - `grep -rn "wait_transition" docs/` returns nothing
    - `grep -n "threading.Event" docs/explanation/architecture.md` returns nothing describing the flip
      wait
    - `grep -rn "Cancel" docs/getting-started/first-web-ui-scan.md` returns nothing referring to the
      flip buttons
    - Both exit-code tables include the non-TTY manual-duplex case with code 2
    - Commit message starts with `docs(25-09):`
  </acceptance_criteria>
  <done>Every falsified sentence in the CLI, web-API, getting-started and architecture pages is corrected against shipped behaviour.</done>
</task>

<task type="auto">
  <name>Task 3: extend the project-level UI-SPEC with the ninth state</name>
  <files>.planning/UI-SPEC.md</files>
  <read_first>
    - `.planning/UI-SPEC.md:197` — the "Status states | Nine presentations" summary row
    - `.planning/UI-SPEC.md:238-247` — the status-area table, which lists eight rows and **omits**
      `AWAITING_FLIP`
    - `.planning/UI-SPEC.md:269` — the humanized status-label list
    - `.planning/UI-SPEC.md:300-313` — the htmx orchestration map
    - `.planning/UI-SPEC.md` § "Copy style guide" — sentence case, three ASCII dots for in-progress
      states, not an ellipsis character
    - `src/saneless/vocabulary.py` — the shipped `state_label` and `progress_label` values
  </read_first>
  <action>
    Add two rows to the status-area table at `:238-247`: the missing `AWAITING_FLIP` row and the new
    `SCANNING_REVERSE` row. `SCANNING_REVERSE` renders `Scanning reverse sides...` decorated
    `<p aria-busy="true">`, exactly like the other busy states — it has **no distinct presentation**,
    and that is deliberate, not an omission. Record why in a note: giving pass B its own branch would
    break D-16, because the busy branch is precisely what makes the Continue and Abort controls
    disappear once pass B starts.

    Correct the summary at `:197`. It already disagreed with its own table before this phase — it
    claimed nine while the table listed eight. After adding both rows there are **ten** presentations.
    Fix the count against the table.

    Add `Scanning backs` to the humanized status-label list at `:269`, matching `state_label`'s shipped
    value.

    Update the htmx map at `:300-313` for the flip routes: both still target `#status-area` with an
    `outerHTML` swap, but the job they report is now resolved as "current job, else most recent", and
    the flip controls are present only while the job is `AWAITING_FLIP`.

    Follow the file's own copy style guide: sentence case, and three ASCII dots for in-progress
    statuses rather than an ellipsis character.
  </action>
  <verify>
    <automated>grep -n "SCANNING_REVERSE\|Scanning backs\|presentations" .planning/UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SCANNING_REVERSE" .planning/UI-SPEC.md` is at least 1
    - `grep -c "AWAITING_FLIP" .planning/UI-SPEC.md` shows the previously-missing table row now exists
    - `grep -c "Scanning backs" .planning/UI-SPEC.md` returns 1
    - The status-area table row count and the summary's presentation count agree, and both say ten
    - `grep -n "…" .planning/UI-SPEC.md` shows no ellipsis character introduced in a new
      in-progress label
    - No phase-level UI-SPEC file was created anywhere under
      `.planning/phases/25-manual-duplex/`
    - Commit message starts with `docs(25-09):`
  </acceptance_criteria>
  <done>The project-level UI contract carries the ninth state, its own internal count inconsistency is fixed, and no phase-level UI-SPEC was created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator action | What the docs teach determines how operators configure a system that moves their paper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-38 | Repudiation (misleading guidance) | docs teaching a config form that cannot reach real hardware | mitigate | The legacy form is removed from every page and the two-key form replaces it, asserted by a grep gate over `docs/` |
| T-25-39 | Tampering (data loss via documented workflow) | `cli-scripting.md` inviting automation onto a path that needs a human | mitigate | The non-TTY refusal is documented in both exit-code tables, so an operator automating `saneless scan` learns the constraint before losing a stack of paper to it |
| T-25-40 | Repudiation (dead-end migration path) | the deprecation warning pointing at documentation that no longer exists | mitigate | D-18's accepted cost is handled at the source: the warning text itself names the replacement key, its value and the command that lists real sources. This plan must not contradict that text |
| T-25-41 | Information Disclosure | example configs leaking a credential | accept | The pages touched here carry profile and output settings only; the Paperless token is documented elsewhere and is unchanged by this phase |
| T-25-SC | Tampering | package installs | N/A | This phase installs nothing |
</threat_model>

<verification>
- `grep -rn 'source = "Manual Duplex"\|ADF Manual Duplex' docs/` returns nothing
- `grep -rn "wait_transition" docs/` returns nothing
- The documented `JobState` list matches `list(JobState)` exactly (nine members)
- `uv run pytest -q` still green and all four static gates pass (no source file is touched by this
  plan, so this is a regression check only)
</verification>

<success_criteria>
- The ADF duplex how-to is rewritten and no page documents `source = "Manual Duplex"` as current
  (the phase goal names this explicitly)
- Code-review documentation rows 1, 2, 22 and 24 are all made true
- The project-level UI-SPEC carries `SCANNING_REVERSE` and its own counts are self-consistent
- `docs/PRD.md` is deliberately untouched, and the reason is recorded
</success_criteria>

<output>
Create `.planning/phases/25-manual-duplex/25-09-SUMMARY.md` when done, listing every file changed,
which code-review doc rows each closes, and restating that `docs/PRD.md` was excluded by planner
discretion (assumption A5) so the verifier does not read it as an omission.
</output>
