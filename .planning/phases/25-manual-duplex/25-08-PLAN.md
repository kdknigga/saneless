---
phase: 25-manual-duplex
plan: 08
type: tdd
wave: 2
depends_on: [25-01]
files_modified:
  - src/saneless/auto_profiles.py
  - tests/test_auto_profiles.py
autonomous: true
requirements: [DPLX-07, DPLX-01]
decisions: [D-05, D-06]

must_haves:
  truths:
    - "A generated config round-trips through load_settings with a default profile for a flatbed-only device"
    - "The same holds for a feeder-only device and for a mixed device"
    - "auto-profiles writes duplex = \"hardware\" for a FEEDER_DUPLEX source"
    - "auto-profiles omits the duplex key entirely for every other source kind"
  artifacts:
    - path: "src/saneless/auto_profiles.py"
      provides: "duplex on generated profiles, written only when non-default"
      contains: "duplex"
    - path: "tests/test_auto_profiles.py"
      provides: "the three-way write-then-load round trip proving DPLX-07"
  key_links:
    - from: "src/saneless/auto_profiles.py::generate_profiles"
      to: "scanner.base.classify_source"
      via: "SourceKind.FEEDER_DUPLEX decides duplex='hardware'"
      pattern: "FEEDER_DUPLEX"
    - from: "src/saneless/auto_profiles.py::write_profiles_to_config"
      to: "config.toml"
      via: "write-only-when-non-default"
      pattern: "profile.duplex"
---

<objective>
Emit `duplex = "hardware"` on generated profiles for hardware-duplex sources, and prove with a
write-then-load round trip that auto-profiles always produces a loadable `default` profile.

Purpose: DPLX-06's config-generation sibling (D-06) and DPLX-07.

Output: one conditional in the generator, one conditional in the writer, and a three-way parametrised
round-trip test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-manual-duplex/25-CONTEXT.md
@.planning/phases/25-manual-duplex/25-PATTERNS.md
@.planning/phases/25-manual-duplex/25-RESEARCH.md
@CLAUDE.md

**DPLX-07 is already satisfied — verify it, do not rebuild it.** Both CONTEXT and RESEARCH agree,
and RESEARCH confirmed it by reading the code: Phase 24 rewrote `generate_profiles`
(`auto_profiles.py:293-380`) so a `default` profile is emitted whenever the device reports any
source, backed by a flatbed when one exists and otherwise by the device's first reported source, with
the `"default"` slug reserved up front to stop a colliding source name from stealing it. Its docstring
says as much and names `Settings.validate_default_profile` as the reason.

`tests/test_auto_profiles.py` already has `test_real_flatbed_still_backs_the_default_profile` (`:521`),
`test_feeder_only_device_still_gets_a_default_profile` (`:531`), and — crucially — exactly one genuine
**write-then-load round trip**, `test_a_feeder_only_config_round_trips_through_load_settings` (`:548`).
Missing: the flatbed-only and mixed round trips. **DPLX-07 therefore reduces to generalising that one
existing test three ways.**

**If you find the emission logic genuinely missing or broken, stop and raise it loudly** — that would
contradict two independent readings of the code and changes this plan's shape entirely.

**This plan runs in parallel with plan 25-02.** It depends only on `ProfileConfig.duplex` from plan
25-01 and touches no file any wave-2 sibling touches.

**Scope fence:** Phase 24's D-16 prune and Phase 27's CFG-07 `--force` merge semantics must treat
`duplex` as a generated key — but **neither is implemented here.** Do not pull CFG-07's merge
semantics forward.
</context>

<interfaces>
Consumed from plan 25-01: `ProfileConfig.duplex: Literal["none", "hardware", "manual"] = "none"`.
Consumed, unchanged: `classify_source()` / `SourceKind` from `scanner/base.py` — this becomes its
**fourth** consumer, which is correct per Phase 24 D-01. It must not be a fresh substring test.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (RED): the three-way round trip and the duplex emission rules</name>
  <files>tests/test_auto_profiles.py</files>
  <read_first>
    - `tests/test_auto_profiles.py:521-566` — the two existing default-profile tests and, at `:548`,
      the one genuine write-then-load round trip whose docstring explains why loading the file back
      is the guarantee that matters and "default is present" is only a proxy
    - `tests/test_auto_profiles.py:613-627` — the `caplog` assertion shape (for reference; not needed
      here)
    - `src/saneless/auto_profiles.py:293-382` — `generate_profiles`, especially `:303-309` where the
      `"default"` slug is reserved, and the two `ProfileConfig` construction sites at `:361-367` and
      `:370-380`
    - `src/saneless/auto_profiles.py:519-527` — `write_profiles_to_config`'s
      write-only-when-non-default block for `auto_source_mode`
    - `.planning/phases/25-manual-duplex/25-RESEARCH.md` Code Examples 6 and 7, Finding 10
    - `.planning/phases/25-manual-duplex/25-PATTERNS.md` §10
  </read_first>
  <behavior>
    - Flatbed-only device (`["Flatbed"]`): the written config loads and
      `settings.profiles["default"].source == "Flatbed"`
    - Feeder-only device (`["Automatic Document Feeder", "ADF Duplex"]`): loads with
      `default.source == "Automatic Document Feeder"`
    - Mixed device (`["Flatbed", "Automatic Document Feeder", "ADF Duplex"]`): loads with
      `default.source == "Flatbed"`
    - A profile generated for a `FEEDER_DUPLEX` source has `duplex == "hardware"`, and the written
      TOML contains a `duplex` key for it
    - Profiles generated for flatbed, plain feeder and auto sources have `duplex == "none"`, and the
      written TOML contains **no** `duplex` key for them
    - `auto-profiles` never emits `duplex = "manual"` for any device
  </behavior>
  <action>
    Generalise `test_a_feeder_only_config_round_trips_through_load_settings` into a parametrised
    `test_generated_config_round_trips_with_a_default_profile` covering the three device shapes. Keep
    the existing test's docstring reasoning — that loading the file back through `load_settings` is the
    user-visible promise, and asserting "default is present" on the in-memory dict is only a proxy for
    it. That distinction is what makes this test DPLX-07's proof rather than a restatement.

    Add the emission tests: one asserting `duplex == "hardware"` on the generated `ProfileConfig` for a
    hardware-duplex source, and one asserting the written TOML text contains no `duplex` key for a
    flatbed-only device. Assert on both the model and the file — D-06 is a statement about what is
    written to disk, and an in-memory assertion alone would not catch a writer that drops the key.

    Add the never-manual assertion. `auto-profiles` can never emit `"manual"` because manual duplex is
    not a device source at all — those profiles are always hand-written. Pinning it stops a later
    change from inventing a heuristic.
  </action>
  <verify>
    <automated>uv run pytest tests/test_auto_profiles.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_auto_profiles.py -q` FAILS only on the `duplex` emission tests; the
      three round-trip cases **PASS immediately** because the generation logic already satisfies
      DPLX-07 — if a round-trip case fails, stop and raise it, because that contradicts the recorded
      analysis
    - `grep -c "round_trips" tests/test_auto_profiles.py` shows the parametrised replacement exists
    - `uv run ruff check .` and `uv run ruff format --check .` pass
    - Commit message starts with `test(25-08):`
  </acceptance_criteria>
  <done>DPLX-07 is proven three ways and the duplex emission rules are pinned; the round-trip cases passing on arrival is itself the verification that DPLX-07 was already satisfied.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): emit duplex from the classifier, write it only when non-default</name>
  <files>src/saneless/auto_profiles.py</files>
  <read_first>
    - `src/saneless/auto_profiles.py:289-291` — `_auto_source_mode`, the one-expression helper pattern
    - `src/saneless/auto_profiles.py:294-382` — `generate_profiles`, including the docstring at
      `:311-317` stating that every question it asks about a source name is answered by
      `classify_source`, and the two construction sites at `:361-367` and `:370-380`
    - `src/saneless/auto_profiles.py:519-527` — `write_profiles_to_config`
    - `src/saneless/scanner/base.py:55-113` — `classify_source` and `SourceKind`
    - `.planning/phases/25-manual-duplex/25-RESEARCH.md` Code Example 7
  </read_first>
  <action>
    In `generate_profiles`, pass `duplex=` beside the existing `auto_source_mode=` argument in **both**
    construction sites — the per-source loop and the default profile. The value is `"hardware"` when
    `classify_source(source)` is `SourceKind.FEEDER_DUPLEX`, and `"none"` otherwise. Use
    `classify_source`; this is its fourth consumer and Phase 24 D-01 makes it the only classification
    rule. Do **not** write a substring test for "duplex" in the name. If an expression helper reads
    better than an inline conditional, follow `_auto_source_mode`'s one-expression form.

    In `write_profiles_to_config`, add `if profile.duplex != "none": profile_table.add("duplex",
    profile.duplex)` in the same place the `auto_source_mode` conditional sits, following Phase 16's
    recorded "only write `auto_source_mode` to TOML when non-default (keep config clean)" precedent.

    Note in a comment that nothing reads `"hardware"` — consistent with D-05, it records operator
    intent and makes a profile self-describing, and Phase 30's APPL-05 (generated `label` /
    `description`, "Feeder, double-sided") is its eventual reader. The same fact is stated in
    `config.py`; stating it here too is deliberate, because this is where the value is produced and a
    reader here will ask what consumes it.

    Do not add cross-field validation that `"hardware"` only appears on a `FEEDER_DUPLEX` source. It
    would give the value a reader, but this codebase has deliberately never cross-validated profile
    fields — `auto_source_mode` is meaningful only when `source` is `Auto` and is never checked
    against it (D-05, rejected alternative).
  </action>
  <verify>
    <automated>uv run pytest tests/test_auto_profiles.py tests/test_config.py -q && uv run ruff check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_auto_profiles.py -q` PASSES — every behavior from Task 1
    - `uv run pytest -q` PASSES with no regressions against the 966-test baseline
    - `grep -c "FEEDER_DUPLEX" src/saneless/auto_profiles.py` is at least 1 and there is no new
      substring test: `grep -n '"duplex" in' src/saneless/auto_profiles.py` returns nothing
    - `grep -v '^#' src/saneless/auto_profiles.py | grep -c 'profile.duplex != "none"'` returns 1
    - `grep -rn "manual" src/saneless/auto_profiles.py` shows no path that can emit `"manual"`
    - All four static gates pass
    - Commit message starts with `feat(25-08):`
  </acceptance_criteria>
  <done>Generated profiles carry `duplex` from the classifier, the key is written only when non-default, and DPLX-07's round trip is proven for all three device shapes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SANE device → generated TOML | Device-reported source names become profile names, slugs and values in a file saneless later loads |
| generated TOML → `load_settings` | The generator's output must survive its own loader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-34 | Tampering (unloadable config) | a generated file that saneless cannot load | mitigate | The write-then-load round trip is the test, run three ways. `"default"` is reserved up front so a device source named "default" cannot steal the slug and break `validate_default_profile` |
| T-25-35 | Tampering (device-supplied name) | a hostile or odd source name reaching a TOML key | mitigate | Names are written through `tomlkit`, which quotes and escapes; they are never concatenated into TOML text by hand. Slug collisions already warn by name (existing behaviour, unchanged here) |
| T-25-36 | Tampering (value drift) | a second, disagreeing duplex-detection rule | mitigate | `duplex` is derived from `classify_source`, the single classification rule (Phase 24 D-01). A fresh substring test here is asserted absent by a grep gate |
| T-25-37 | Tampering (config clobbering) | regeneration overwriting operator-added keys | accept (deferred) | `--force` merge semantics for `duplex` are **Phase 27's CFG-07**, which must treat `duplex` as a generated key. Explicitly not implemented here; recorded so its absence is not read as an omission |
| T-25-SC | Tampering | package installs | N/A | This phase installs nothing |
</threat_model>

<verification>
- `uv run pytest -q` — full suite green
- All four static gates: `uv run ruff check .`, `uv run ruff format --check .`, `uv run ty check`,
  `uv run pyrefly check src tests`
</verification>

<success_criteria>
- A write-then-load round trip proves a `default` profile for flatbed-only, feeder-only and mixed
  devices (roadmap criterion 5)
- `duplex = "hardware"` is emitted only for `FEEDER_DUPLEX` sources and the key is omitted otherwise
- No new source-classification rule was created
</success_criteria>

<output>
Create `.planning/phases/25-manual-duplex/25-08-SUMMARY.md` when done, recording the commit SHAs and
**explicitly confirming** whether the three round-trip cases passed on arrival — that result is the
verification of the recorded claim that DPLX-07's emission logic was already satisfied by Phase 24.
</output>
