---
phase: 29-geometry-memory-and-timeouts
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - src/saneless/pages.py
  - src/saneless/pdf.py
  - src/saneless/pipeline.py
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "D-01: the pipeline owns the spool — it constructs a `SpooledPageSink` inside the per-job workspace and hands it to `scan_pages`"
    - "D-02: page order comes from the record list; nothing sorts or globs the spool directory"
    - "D-03: `assemble_pdf` takes records and embeds the spooled PNG directly — the second PNG encode at pdf.py:208-214 is gone"
    - "D-04: `_interleave_duplex` reverses and zips record objects; no file is renamed"
    - "D-05: the first page's thumbnail fires during pass A, from the spool, not after the batch returns"
    - "D-06: `is_empty_page` reads the stored mean and stddev instead of converting the page to greyscale a second time"
  artifacts:
    - path: "src/saneless/pages.py"
      provides: "stats-based empty-page detection over records"
      contains: "def filter_empty_pages"
    - path: "src/saneless/pdf.py"
      provides: "assemble_pdf over page records, embedding the spooled PNG"
    - path: "src/saneless/pipeline.py"
      provides: "spool lifetime inside the workspace, records through interleave, filtering and assembly"
  key_links:
    - from: "src/saneless/pipeline.py"
      to: "src/saneless/spool.py"
      via: "SpooledPageSink constructed per scanning pass inside the workspace"
      pattern: "SpooledPageSink\\("
    - from: "src/saneless/pipeline.py"
      to: "ScannerBackend.scan_pages"
      via: "sink passed as the third argument"
      pattern: "scan_pages\\([^)]*sink"
    - from: "src/saneless/pdf.py"
      to: "PageRecord.path"
      via: "img2pdf embeds the spooled file itself"
      pattern: "record\\.path|\\.path\\)"
---

<objective>
Finish the contract switch on the source side: `pages.py` judges from stored statistics, `pdf.py`
assembles from spooled paths, and `pipeline.py` owns the spool's lifetime and carries records
through interleaving, filtering and assembly.

Purpose: at the end of this plan all of `src/` is internally consistent again — `uv run ty check src`
and `uv run pyrefly check src` are green — which is the first real gate since plan 01.
Output: records everywhere in the source tree, and the second PNG encode deleted.

**RED INTERVAL.** `tests/` is still written against the old contract, so `uv run pytest`,
`uv run ty check` (full) and `uv run pyrefly check src tests` remain red until plan 06. Plans 04,
05 and 06 close that. Do not add shims. Never use `--no-verify` or `SKIP=`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-geometry-memory-and-timeouts/29-CONTEXT.md
@.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md
@.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md
@.planning/phases/29-geometry-memory-and-timeouts/29-02-SUMMARY.md

<interfaces>
Signatures this plan establishes. Plans 05, 08 and 09 are written against exactly these.

`src/saneless/pages.py`:

- `is_empty_page(mean: float, stddev: float, mean_threshold: float = 250.0,
  stddev_threshold: float = 5.0) -> bool` — takes the measurements, not the image.
- `filter_empty_pages(pages: Sequence[PageRecord], mean_threshold: float = 250.0,
  stddev_threshold: float = 5.0) -> list[PageRecord]`.
- `generate_thumbnail(image, max_edge=300, quality=85) -> str` — unchanged (plan 01 already changed
  its internals).

`src/saneless/pdf.py`:

- `assemble_pdf(records: Sequence[PageRecord], output_dir: Path, filename: str, dpi: int) -> Path`
  — the parameter names, order and keyword/positional shape of the existing function are kept; only
  the first parameter's type changes from images to records.

`src/saneless/pipeline.py`:

- `_interleave_duplex(fronts: Sequence[PageRecord], backs: Sequence[PageRecord]) -> list[PageRecord]`
- `_drop_empty_pages(pages: Sequence[PageRecord], profile) -> list[PageRecord]`
- `_DuplexMismatch` holds `PageRecord` sequences for fronts and backs.
- `_scan_simplex` / `_scan_manual_duplex` construct `SpooledPageSink` and pass it to `scan_pages`.

From earlier plans: `PageRecord(sequence, path, size, mode, mean, stddev)`,
`SpooledPageSink(directory, pass_label, min_free_space_mb, thumbnail_callback=None)` with a
`records` property, `ScanBatch.pages: tuple[PageRecord, ...]`,
`scan_pages(device_id, settings, sink)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make empty-page detection read the stored statistics</name>
  <files>src/saneless/pages.py</files>
  <read_first>
    - `src/saneless/pages.py` (whole file — `is_empty_page` at :28-63 holds the `convert("L")` plus
      `Stat` pair that plan 01 already duplicated at spool time; `filter_empty_pages` at :66-90 is
      the list walker)
    - `src/saneless/spool.py` (where `mean` and `stddev` are now measured, once, per page)
    - `src/saneless/scanner/base.py` (`PageRecord` field names)
    - `src/saneless/pipeline.py` lines 563-598 (`_drop_empty_pages`, the only caller)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md` section
      "src/saneless/pages.py" and the `_drop_empty_pages` excerpt
  </read_first>
  <behavior>
    - `is_empty_page(mean=255.0, stddev=0.0)` is True; `is_empty_page(mean=200.0, stddev=40.0)` is
      False; the dual-threshold rule (mean strictly above `mean_threshold` AND stddev strictly
      below `stddev_threshold`) is unchanged, including at the boundaries.
    - `filter_empty_pages` over a sequence of records returns the non-empty records, in the same
      relative order, and returns records — not images.
    - `pages.py` contains no `convert("L")` call and no `ImageStat` import after this task: the one
      greyscale conversion in the process now happens at spool time (D-06).
    - The per-page DISCARD/KEEP log lines are still emitted at the same levels, naming the record's
      `sequence` rather than a positional index.
  </behavior>
  <action>
    Re-sign `is_empty_page` to `(mean: float, stddev: float, mean_threshold: float = 250.0,
    stddev_threshold: float = 5.0) -> bool` and delete its `convert("L")` plus `Stat` body,
    keeping the dual-threshold comparison and the existing `logger.debug` page-stats line exactly
    as they read today. Rewrite the docstring: the thresholds and the AND rule are unchanged, and
    the reason the image no longer arrives here is D-06 — the greyscale conversion was being paid
    twice per page, and the spool already measured it once at write time (measured: convert 6 ms,
    Stat 3 ms per page, plus a full-size intermediate).

    Re-sign `filter_empty_pages` to take and return `PageRecord` sequences. Keep the per-page
    `logger.info` DISCARD/KEEP lines, using `record.sequence` in place of `i + 1`. Pass
    `record.mean` and `record.stddev` to `is_empty_page`.

    Remove the now-unused `ImageStat`/`Stat` import and any other import left dangling, but keep
    `PIL.Image.MAX_IMAGE_PIXELS = 200_000_000` and the `Image`/`Resampling`/`ImageOps` imports that
    `generate_thumbnail` still needs. Update `__all__` if anything changed name (it should not).
    Update the module docstring's first paragraph so it no longer claims the filter operates on
    images.

    Import `PageRecord` from `saneless.scanner.base` under the file's existing `TYPE_CHECKING`
    convention if the annotation allows it, otherwise at module level.
  </action>
  <verify>
    <automated>uv run ruff check src/saneless/pages.py &amp;&amp; uv run python -c "from saneless.pages import is_empty_page; assert is_empty_page(255.0,0.0) is True; assert is_empty_page(200.0,40.0) is False; assert is_empty_page(250.0,0.0) is False; print('stats ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `stats ok`.
    - `grep -c 'convert("L")' src/saneless/pages.py` returns 0.
    - `grep -c 'ImageStat' src/saneless/pages.py` returns 0.
    - `grep -c 'MAX_IMAGE_PIXELS' src/saneless/pages.py` returns 1.
    - `uv run python -c "import inspect; from saneless.pages import is_empty_page; print(list(inspect.signature(is_empty_page).parameters))"`
      prints `['mean', 'stddev', 'mean_threshold', 'stddev_threshold']`.
  </acceptance_criteria>
  <done>Blank-page policy reads the record's stored statistics; the second greyscale conversion is
  gone from the process.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: assemble_pdf embeds the spooled PNG instead of re-encoding it</name>
  <files>src/saneless/pdf.py</files>
  <read_first>
    - `src/saneless/pdf.py` (whole file — `assemble_pdf` at :128-239; the re-save block at
      :208-214 that D-03 deletes; the `layout_fun` comment at :216-227 that stays because it is
      still true; the empty guard at :197-202; the error boundary at :229-237 and the docstring
      paragraph at :166-176 that justifies its breadth; `build_pdf_filename` at :86-125)
    - `src/saneless/scanner/base.py` (`PageRecord.path`)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md` section
      "src/saneless/pdf.py" (the verbatim blocks that go, stay and change)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md` Finding 3 (why
      `outputstream=` alone does not bound memory — plan 08 owns the bounded merge, this task must
      not claim the bound)
  </read_first>
  <behavior>
    - `assemble_pdf(records, output_dir, filename, dpi)` writes a PDF with one page per record, in
      record order, and returns its path.
    - The PDF's embedded image stream is byte-identical to the spooled PNG's IDAT payload: no
      second PNG encode happens, and no `TemporaryDirectory` full of `page_NNNN.png` files is
      created.
    - An empty record sequence still raises `PdfError` with the existing message
      `Could not assemble a PDF: no pages were given`.
    - A failure inside img2pdf still surfaces as `PdfError` whose message names the page count and
      the target path.
    - The page geometry is unchanged: an A4 page at 300 DPI still produces a `/MediaBox` of
      `[0 0 595.2 841.92]`, because the fixed-DPI layout function is still passed the
      `(dpi, dpi)` 2-tuple.
  </behavior>
  <action>
    Change `assemble_pdf`'s first parameter from a sequence of images to a sequence of `PageRecord`,
    keeping the parameter name, the remaining parameters and the return type.

    Delete the block at :208-214 that creates a `TemporaryDirectory` of `page_NNNN.png` files and
    re-saves every image — that second PNG encode is exactly what D-03 removes, because the spooled
    PNG already *is* what img2pdf embeds. Build `image_paths` directly as the string form of each
    record's `path`, in record order. Do not sort, glob or otherwise re-derive the order (D-02).

    Keep, verbatim: the `layout_fun` comment explaining that the argument is an `(x_dpi, y_dpi)`
    2-tuple and that an int silently yields wrong geometry; the empty-input guard and its message;
    the `dpi`-is-supplied-by-the-caller docstring paragraph; the `except PdfError: raise` /
    `except Exception as exc:` boundary and its "narrow in span, broad in type" justification.
    Change the count in the boundary's message from the old images length to the record count.

    Keep the current single `img2pdf.convert(...)` call and its `pdf_bytes is None` guard for now.
    Plan 08 replaces this call with the per-page convert plus `pikepdf.Job` merge that actually
    bounds memory, and it is plan 08 that deletes the None guard (`convert` returns `None` by design
    when `outputstream=` is passed). Do not pre-empt that here and do not write any docstring
    sentence claiming assembly memory is bounded — RESEARCH.md Finding 3 measured that it is not,
    yet.

    Update the docstring's `Args:` entry for the first parameter to describe page records and to
    say that the spooled PNG is embedded losslessly, with no re-encode.
  </action>
  <verify>
    <automated>uv run ruff check src/saneless/pdf.py &amp;&amp; uv run ty check src/saneless/pdf.py &amp;&amp; uv run python -c "import inspect; from saneless.pdf import assemble_pdf; print(list(inspect.signature(assemble_pdf).parameters))"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'page_%d\|page_{i:04d}\|format="PNG"' src/saneless/pdf.py` returns 0 — no second PNG
      encode remains.
    - `grep -c 'get_fixed_dpi_layout_fun' src/saneless/pdf.py` returns 1.
    - `grep -c 'Could not assemble a PDF: no pages were given' src/saneless/pdf.py` returns 1.
    - `grep -c 'pdf_bytes is None' src/saneless/pdf.py` returns 1 — plan 08 removes it, not this
      task.
    - `grep -vi '^\s*#' src/saneless/pdf.py | grep -ci 'bounded\|one page in memory'` returns 0.
    - `uv run ty check src/saneless/pdf.py` exits 0.
  </acceptance_criteria>
  <done>`assemble_pdf` takes records and embeds the spooled PNG directly; the duplicate encode is
  deleted and no memory claim has been made yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Give the pipeline the spool's lifetime and carry records end to end</name>
  <files>src/saneless/pipeline.py</files>
  <read_first>
    - `src/saneless/pipeline.py` — read the whole file; this task touches ten separate regions:
      `_check_disk_space` (:342-366), `_open_workspace` (:369), `_preserving` (:473-560),
      `_drop_empty_pages` (:563-598), `_DuplexMismatch` (:663-685), `_handle_duplex_mismatch`
      (:773-882), `_interleave_duplex` (:959-988), `_scan_manual_duplex` (:991-1088),
      `_scan_simplex` (:1144-1180), `run_pipeline` (:1286-1404)
    - `src/saneless/spool.py` and `src/saneless/scanner/base.py` (the sink and record contracts)
    - `src/saneless/pages.py` (task 1's new signatures)
    - `src/saneless/pdf.py` (task 2's new signature)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md` sections
      "src/saneless/pipeline.py — records everywhere" and "— partial-scan preservation" (the
      verbatim `_interleave_duplex`, `_drop_empty_pages`, `_scan_simplex` thumbnail and workspace
      lifetime excerpts, and the total `match` plus `assert_never` dispatch rule)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md` Finding 8 (the workspace
      lifetime trap: anything preserved must move out before the `with workspace` block exits)
  </read_first>
  <behavior>
    - A simplex scan constructs one `SpooledPageSink` under the per-job workspace and passes it to
      `scanner.scan_pages` as the third argument; the returned batch's records point at files that
      exist inside that workspace.
    - A manual duplex job constructs two sinks, labelled `a` for pass A and `b` for pass B, so the
      two passes spool into distinguishable names (D-02); order still comes from the record list.
    - `_interleave_duplex` over records returns fronts and reversed backs zipped, as record objects;
      no file is renamed and no path is rebuilt.
    - `_drop_empty_pages` filters records using their stored statistics and still raises
      `ScanError("All pages were blank")` when nothing survives.
    - The thumbnail callback fires exactly once per job, during pass A, from inside the spool — the
      pipeline no longer calls `generate_thumbnail` itself.
    - `pages_scanned` in the result is the record count.
    - The spool directory lives inside the workspace `TemporaryDirectory` and is gone after
      `run_pipeline` returns.
  </behavior>
  <action>
    Carry records through the pipeline and give the spool a lifetime.

    - In `run_pipeline`, inside the existing `with workspace as tmp_dir:` block, create a spool
      directory under `tmp_path` (for example `tmp_path / "spool"`) with `mkdir`. It must be inside
      the workspace so it is deleted with it. Keep the up-front `_check_disk_space` call exactly as
      it is — D-07's per-page check is an addition to it, not a replacement.
    - `_scan_simplex` builds `SpooledPageSink(directory=<spool dir>, pass_label="a",
      min_free_space_mb=settings.output.min_free_space_mb,
      thumbnail_callback=request.thumbnail_callback)` and passes it as `scan_pages`' third argument.
      Delete the three lines that call `generate_thumbnail(batch.pages[0])` and invoke
      `request.thumbnail_callback` — the spool fires it during acquisition now (D-05), which is also
      a small visible improvement because the strip appears during pass A instead of after it.
    - `_scan_manual_duplex` does the same twice, with `pass_label="a"` for pass A and `pass_label="b"`
      for pass B, and passes `thumbnail_callback` only to the pass A sink so the callback still fires
      exactly once. Delete its copy of the thumbnail block too.
    - Re-sign `_interleave_duplex`, `_drop_empty_pages` and `_DuplexMismatch` to carry `PageRecord`
      instead of `Image.Image`. Their bodies are a type-only change: keep the mismatch message
      `Page count mismatch: N fronts, M backs`, keep `reversed(backs)` and the `zip(..., strict=True)`
      pairing, keep the `enable_empty_page_detection` early return and its log line, keep the
      `Empty page filter: N -> M pages` log, and keep `ScanError("All pages were blank")`.
    - `_handle_duplex_mismatch` passes record sequences to `assemble_pdf` for the `(fronts)` and
      `(backs)` PDFs. Its naming convention, its single shared `_preserving` guard over both halves
      and its comment explaining why one guard covers both are unchanged.
    - Delete the `Step 1.5: Strip EXIF from all images (Pitfall #5)` loop in `run_pipeline`
      (:1329-1331). It is dead once the spool owns the write: Pillow's PNG encoder writes `exif`
      only when it is supplied through `encoderinfo`, and `generate_thumbnail` still strips it for
      the JPEG. Record that reason in a one-line comment where the loop used to be, or in
      `run_pipeline`'s docstring, so a future reader does not restore it.
    - Wherever a count of scanned pages is computed, take it from the record sequence length.
    - Do NOT implement D-09 or D-10 preservation behaviour here. `_preserving`,
      `_warn_if_failed_dir_growing` and the pass-B gap comment at :1080 belong to plan 09; leaving
      them untouched keeps this plan's diff type-shaped and keeps plan 09's diff behaviour-shaped.
    - Where this task introduces or touches a variant dispatch, keep the established total `match`
      plus `assert_never` shape and its comment explaining why a dict or isinstance chain draws no
      diagnostic from either checker.
    - If a function grows past ruff's `PLR0915` statement cap, extract a helper the way
      `_finish_duplex_mismatch` already exists for that reason — do not add a suppression.
  </action>
  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check src &amp;&amp; uv run pyrefly check src</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty check src` exits 0 and `uv run pyrefly check src` exits 0 — the whole source tree
      is internally consistent again.
    - `grep -c 'SpooledPageSink(' src/saneless/pipeline.py` returns 2 or more.
    - `grep -c 'pass_label="b"' src/saneless/pipeline.py` returns 1.
    - `grep -c 'generate_thumbnail' src/saneless/pipeline.py` returns 0.
    - `grep -c 'Strip EXIF from all images' src/saneless/pipeline.py` returns 0.
    - `grep -c 'Page count mismatch' src/saneless/pipeline.py` returns 1 and
      `grep -c 'All pages were blank' src/saneless/pipeline.py` returns 1.
    - `grep -c 'glob(' src/saneless/pipeline.py` does not increase relative to the pre-task value
      (the spool is never globbed).
    - `grep -c 'The fronts are still lost here' src/saneless/pipeline.py` still returns 1 — plan 09
      deletes that comment, not this task.
    - `uv run ruff check . && uv run ruff format --check .` exits 0.
  </acceptance_criteria>
  <done>The pipeline owns the spool's lifetime, records flow through interleaving, filtering and
  assembly, the thumbnail fires from the spool, and all of `src/` type-checks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-supplied job title → filesystem | Titles reach PDF names through `build_pdf_filename` and `sanitise_title_for_filename` |
| process → per-job `TemporaryDirectory` | The spool directory is created and destroyed with the workspace |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Tampering | spool directory location | mitigate | The spool is created under the per-job `TemporaryDirectory` that `_open_workspace` already owns; no operator-controlled string contributes to its path (ASVS V12) |
| T-29-10 | Tampering | `assemble_pdf` input paths | mitigate | Paths come only from `PageRecord.path` values the spool itself produced; the order is the record list's, never a glob or a sort, so a planted file in the spool directory cannot enter the PDF (D-02) |
| T-29-11 | Denial of service | workspace disk growth | mitigate | The up-front `_check_disk_space` call is retained, and plan 01's per-page check plus the `min_free_space_mb` assembly reserve bound the spool |
| T-29-12 | Information disclosure | PDF filenames | accept | Unchanged: `sanitise_title_for_filename`'s existing allow-list and 60-character cap still govern every name this plan writes |
</threat_model>

<verification>
Gates for this plan:

- `uv run ruff check . && uv run ruff format --check .`
- `uv run ty check src` — **green**
- `uv run pyrefly check src` — **green**
- `uv run pytest tests/test_spool.py -x -q`

Still expected red until plan 06: `uv run pytest` (whole suite), `uv run ty check` (full, includes
tests), `uv run pyrefly check src tests`, `uv run prek run --all-files`.
</verification>

<success_criteria>
- `is_empty_page` takes measurements, not an image; `pages.py` has no `convert("L")` left.
- `assemble_pdf` takes records and no longer re-encodes a PNG per page.
- The pipeline constructs the sink, passes it to `scan_pages`, and deletes its own thumbnail calls.
- Duplex passes spool under `a-` and `b-` labels; interleave operates on records.
- The EXIF strip loop is gone with its reason recorded.
- `uv run ty check src` and `uv run pyrefly check src` both exit 0.
</success_criteria>

<output>
Create `.planning/phases/29-geometry-memory-and-timeouts/29-03-SUMMARY.md` when done. Record the
exact spool directory path convention chosen, since plans 05 and 09 assert against it.
</output>
