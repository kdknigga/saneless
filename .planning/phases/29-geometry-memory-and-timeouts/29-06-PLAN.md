---
phase: 29-geometry-memory-and-timeouts
plan: 06
type: execute
wave: 5
depends_on: ["29-04", "29-05"]
files_modified:
  - tests/test_app_lifespan.py
  - tests/test_web.py
  - tests/test_web_errors.py
  - tests/test_cross_origin.py
  - tests/test_web_state_rendering.py
  - tests/test_worker.py
  - tests/test_cli.py
  - tests/test_browser.py
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "D-01: every `ScannerBackend` subclass in the suite implements the sink signature, so the type checkers catch any future contract drift"
    - "The whole tree is green again: `uv run pytest`, `uv run ty check` and `uv run pyrefly check src tests` all pass"
    - "The red interval opened by plan 02 is closed, and `uv run prek run --all-files` passes"
  artifacts:
    - path: "tests/test_app_lifespan.py"
      provides: "one-page stub on the shared conftest base"
    - path: "tests/test_worker.py"
      provides: "gated scanner stubs on the sink signature"
    - path: "tests/test_cli.py"
      provides: "CLI scanner stubs on the sink signature"
  key_links:
    - from: "tests/test_web.py"
      to: "tests/conftest.py"
      via: "StubScannerBackend base collapses get_devices and get_capabilities"
      pattern: "StubScannerBackend"
---

<objective>
Migrate the remaining eight test files' `ScannerBackend` stub classes to the sink signature and
close the red interval: at the end of this plan `uv run pytest`, `uv run ty check`,
`uv run pyrefly check src tests` and `uv run prek run --all-files` are all green again.

Purpose: this is the gate that proves the atomic contract migration (plans 02-06) actually landed.
Output: eight migrated test files and a fully green tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-geometry-memory-and-timeouts/29-CONTEXT.md
@.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md
@.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md
@.planning/phases/29-geometry-memory-and-timeouts/29-04-SUMMARY.md
@.planning/phases/29-geometry-memory-and-timeouts/29-05-SUMMARY.md

<interfaces>
- `ScannerBackend.scan_pages(self, device_id: str, settings: ScanSettings, sink: PageSink) -> ScanBatch`
- `ScannerBackend.close(self) -> None` — concrete on the ABC, so a stub only overrides it when it
  has something to release
- `tests.conftest.StubScannerBackend` — concrete base implementing `get_devices`,
  `get_capabilities` and a one-page `scan_pages`; import it as
  `from tests.conftest import StubScannerBackend`
- `tests.conftest.spooling`, `spooling_in_turn`, `images_of`, `scan_batch`
- `SpooledPageSink(directory, pass_label, min_free_space_mb, thumbnail_callback=None)` with a
  `records` property

Per RESEARCH.md Finding 7, the stub classes to migrate are:
`tests/test_worker.py` (`_PassBGatedScanner`, `_GatedScanner`), `tests/test_cli.py`
(`MockSaneBackend`, `FailScanner`, `CountingScanner`, `RaisingScanner`), `tests/test_browser.py`
(`_BrowserTestScanner`), and one one-page stub each in `tests/test_web.py`,
`tests/test_web_errors.py`, `tests/test_cross_origin.py`, `tests/test_web_state_rendering.py` and
`tests/test_app_lifespan.py`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse the five one-page web stubs onto the shared conftest base</name>
  <files>tests/test_app_lifespan.py, tests/test_web.py, tests/test_web_errors.py, tests/test_cross_origin.py, tests/test_web_state_rendering.py</files>
  <read_first>
    - `tests/test_app_lifespan.py` lines 40-90 (`_StubScanner` — the canonical shape the other four
      are near-copies of)
    - `tests/test_web.py`, `tests/test_web_errors.py`, `tests/test_cross_origin.py`,
      `tests/test_web_state_rendering.py` — locate each file's stub class (one `scan_pages`
      occurrence per file) and read it plus its instantiation sites
    - `tests/conftest.py` (`StubScannerBackend` and the import form)
    - `src/saneless/scanner/base.py` (the contract)
  </read_first>
  <behavior>
    - Each of the five files' stub class subclasses `tests.conftest.StubScannerBackend` and defines
      only what differs from the base — in most cases nothing, so the local class disappears
      entirely and the base is used directly.
    - Each file's tests still pass with the same assertions they make today; none of these suites
      runs a real scan, so the one-page default is sufficient.
    - `uv run pytest` over these five files collects and passes.
  </behavior>
  <action>
    In each of the five files, replace the locally defined one-page `ScannerBackend` subclass with
    `tests.conftest.StubScannerBackend`, imported as `from tests.conftest import StubScannerBackend`
    — the bare `conftest` form raises `ModuleNotFoundError` under pytest 9's importlib mode.

    Where the local class had a distinguishing behaviour (a different capability list, a counter, a
    raise), keep a local subclass of `StubScannerBackend` that overrides only that one member, and
    delete the duplicated `get_devices` and `get_capabilities`. Where it had none, delete the class
    and use `StubScannerBackend()` at the instantiation sites.

    If a stub's `scan_pages` is overridden, its signature must be
    `(self, device_id, settings, sink)` and it must produce its pages through `sink.add`, returning
    `ScanBatch(pages=sink.records, actual_resolution=settings.resolution, pages_rejected=0)` — never
    a hand-built tuple of records, because that would bypass the real write path and hide a spool
    regression.

    Keep every docstring truthful to what the stub now does.
  </action>
  <verify>
    <automated>uv run pytest tests/test_app_lifespan.py tests/test_web.py tests/test_web_errors.py tests/test_cross_origin.py tests/test_web_state_rendering.py -q &amp;&amp; uv run ruff check tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_app_lifespan.py tests/test_web.py tests/test_web_errors.py tests/test_cross_origin.py tests/test_web_state_rendering.py -q`
      exits 0 with zero failures and zero collection errors.
    - `grep -l 'StubScannerBackend' tests/test_app_lifespan.py tests/test_web.py tests/test_web_errors.py tests/test_cross_origin.py tests/test_web_state_rendering.py`
      lists all five files.
    - `grep -c 'def get_capabilities' tests/test_web.py tests/test_web_errors.py tests/test_cross_origin.py tests/test_web_state_rendering.py`
      returns 0 for each file.
    - `uv run ruff check tests/` exits 0.
  </acceptance_criteria>
  <done>The five one-page web stubs share one conftest base and their suites pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the gated worker, CLI and browser stubs</name>
  <files>tests/test_worker.py, tests/test_cli.py, tests/test_browser.py</files>
  <read_first>
    - `tests/test_worker.py` lines 1100-1180 (`_PassBGatedScanner` — already `threading.Event`
      based, already argues against `MagicMock`) and the second gated class `_GatedScanner`; there
      are 11 `scan_pages` occurrences in this file
    - `tests/test_cli.py` — locate `MockSaneBackend`, `FailScanner`, `CountingScanner` and
      `RaisingScanner` (8 `scan_pages` occurrences) and read each class plus the tests that drive it
    - `tests/test_browser.py` — locate `_BrowserTestScanner` (3 `scan_pages` occurrences)
    - `tests/conftest.py` (`StubScannerBackend`, `spooling`)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md` section
      "Test stub classes (7 files)"
  </read_first>
  <behavior>
    - Each gated stub keeps its gate semantics exactly: `_PassBGatedScanner` still holds pass B
      until its `Event` is set and still counts its calls, `CountingScanner` still counts,
      `RaisingScanner` still raises the same exception type, `FailScanner` still fails the same way.
    - Each produces its pages through the supplied `sink` and returns
      `ScanBatch(pages=sink.records, ...)`.
    - `tests/test_worker.py`, `tests/test_cli.py` pass; `tests/test_browser.py` collects without
      error (its tests run under the `browser` marker).
  </behavior>
  <action>
    Re-sign each gated stub's `scan_pages` to `(self, device_id, settings, sink)` and route its
    pages through `sink.add`, returning `ScanBatch(pages=sink.records, ...)`. Where a stub currently
    returns `scan_batch([_inked_page()])`, the equivalent is to add the inked page to the sink and
    return the sink's records.

    Keep the call-count dispatch these classes already use — it is the in-repo answer to
    RESEARCH.md's Pitfall 5 and the model the conftest `spooling_in_turn` factory copied. Do not
    replace a gated class with a `MagicMock`; their docstrings already argue against it and the type
    checkers only catch contract drift on real subclasses.

    Where a stub can inherit `get_devices` and `get_capabilities` from
    `tests.conftest.StubScannerBackend` without changing behaviour, do so and delete the duplicates.
    Where its capabilities differ, keep the local override.

    `tests/test_browser.py` is under the `browser` marker and needs Playwright to run; migrating it
    is still mandatory because both type checkers read it at pre-push. Verify it at minimum by
    collection, and run the browser suite if chromium is available in the environment.
  </action>
  <verify>
    <automated>uv run pytest tests/test_worker.py tests/test_cli.py -q &amp;&amp; uv run pytest tests/test_browser.py --collect-only -q &amp;&amp; uv run ruff check tests/</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_worker.py tests/test_cli.py -q` exits 0 with zero failures.
    - `uv run pytest tests/test_browser.py --collect-only -q` exits 0 with zero collection errors.
    - `grep -c 'def scan_pages(self, device_id: str, settings: ScanSettings)' tests/test_worker.py tests/test_cli.py tests/test_browser.py`
      returns 0 for each file — no two-argument override remains.
    - `grep -c 'release_pass_b' tests/test_worker.py` is unchanged from its pre-task value (the gate
      semantics survived).
    - `uv run ruff check tests/` exits 0.
  </acceptance_criteria>
  <done>The gated worker, CLI and browser stubs implement the sink signature with their gate
  behaviour intact.</done>
</task>

<task type="auto">
  <name>Task 3: Close the red interval — full tree green gate</name>
  <files>tests/</files>
  <read_first>
    - `.planning/phases/29-geometry-memory-and-timeouts/29-02-SUMMARY.md`,
      `29-03-SUMMARY.md`, `29-04-SUMMARY.md`, `29-05-SUMMARY.md` (what each plan deliberately left
      for a later one, so residue is not mistaken for a regression)
    - `.pre-commit-config.yaml` (which hooks run at which stage: `ty check src` and
      `pyrefly check src` at commit, `ty check` and `pyrefly check src tests` at pre-push)
    - `CLAUDE.md` (the pyrefly worktree gotcha: always name paths, never rely on discovery)
  </read_first>
  <action>
    Run the four whole-tree gates and fix whatever residue the file-scoped gates in plans 02 through
    06 could not see. Expect residue in exactly three shapes: a stub scanner or `scan_pages` call
    site that no plan's file list covered, an import left dangling by a deleted helper, and a
    docstring that still describes the old contract.

    Run, in this order, and fix until each is clean:

    1. `uv run ruff check . && uv run ruff format --check .`
    2. `uv run ty check` (the full-tree form, not the `src`-only commit-stage form)
    3. `uv run pyrefly check src tests` — always name both paths; inside a gitignored worktree
       pyrefly's `use-ignore-files` default filters out every source file unless files are named
    4. `uv run pytest -m "not browser and not sane_hardware"`
    5. `uv run prek run --all-files` and `uv run prek run --stage pre-push --all-files`

    Do not fix a failure by adding `# type: ignore`, `# noqa`, or by disabling a rule; and do not
    reach for `--no-verify` or `SKIP=`. If a genuine contract problem surfaces, fix the contract.

    If a failure belongs to work a later plan owns — the timeout and cancel rewrite (plan 07),
    bounded assembly (plan 08), preservation (plan 09), the init guard and entry-point closes
    (plan 10), or documentation (plan 11) — record it in the summary as a known follow-up rather
    than implementing it here. Anything else is residue of this migration and must be fixed now.
  </action>
  <verify>
    <automated>uv run ruff check . &amp;&amp; uv run ruff format --check . &amp;&amp; uv run ty check &amp;&amp; uv run pyrefly check src tests &amp;&amp; uv run pytest -m "not browser and not sane_hardware" &amp;&amp; uv run prek run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -m "not browser and not sane_hardware"` exits 0 with zero failures and zero
      errors.
    - `uv run ty check` exits 0.
    - `uv run pyrefly check src tests` exits 0.
    - `uv run ruff check . && uv run ruff format --check .` exits 0.
    - `uv run prek run --all-files` exits 0 and
      `uv run prek run --stage pre-push --all-files` exits 0.
    - `git grep -c 'type: ignore' -- src tests` and `git grep -c 'noqa' -- src tests` are both no
      greater than their values at the start of phase 29 (record both numbers in the summary).
  </acceptance_criteria>
  <done>The red interval opened by plan 02 is closed. The whole tree lints, type-checks and passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test stub → app under test | Stub scanners are what the web and CLI suites exercise the app through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-19 | Spoofing | stub scanners diverging from the real contract | mitigate | Every stub subclasses the `ScannerBackend` ABC rather than duck-typing it, so both checkers catch a future signature drift — the measured reason the repo already prefers subclasses to `MagicMock` |
| T-29-20 | Tampering | suppressing a type error to close the gate | mitigate | Task 3's acceptance criteria count `type: ignore` and `noqa` occurrences against the pre-phase baseline, so closing the gate by suppression fails the gate |
| T-29-21 | Denial of service | full-suite runtime after the migration | accept | The suite's own `pytest-timeout` of 60 s per test with `timeout_method="signal"` is unchanged |
</threat_model>

<verification>
- `uv run ruff check . && uv run ruff format --check .`
- `uv run ty check`
- `uv run pyrefly check src tests`
- `uv run pytest -m "not browser and not sane_hardware"`
- `uv run prek run --all-files` and `uv run prek run --stage pre-push --all-files`
- `uv run pytest -m browser` where chromium is available
</verification>

<success_criteria>
- All eight remaining test files implement the sink signature.
- The five one-page web stubs share `tests.conftest.StubScannerBackend`.
- The gated worker, CLI and browser stubs keep their gate semantics.
- The whole tree is green under ruff, both type checkers, the full non-browser suite and prek.
- No new `# type: ignore` or `# noqa` was added anywhere in the phase so far.
</success_criteria>

<output>
Create `.planning/phases/29-geometry-memory-and-timeouts/29-06-SUMMARY.md` when done. Record the
`type: ignore` / `noqa` baseline counts and any known follow-ups deferred to plans 07 through 11.
</output>
