---
phase: 29-geometry-memory-and-timeouts
plan: 05
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - tests/test_pages.py
  - tests/test_pdf.py
  - tests/test_pipeline.py
  - tests/test_outcomes_e2e.py
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "HARD-01: a 12-page scan with distinct per-page content comes out in order 1..12, proven by reading the assembled PDF back with pikepdf"
    - "D-04: duplex interleave reorders the page records while the spool file names sort differently from the document order"
    - "D-02: nothing recovers page order from the filesystem — the duplex test is built to punish a `glob` or a `sorted`"
    - "D-06: empty-page detection is asserted against stored mean and stddev, not against a re-converted image"
    - "D-03: `assemble_pdf` is asserted to embed the spooled PNG without a second encode"
  artifacts:
    - path: "tests/test_pipeline.py"
      provides: "pipeline suite on the sink/record contract, plus the twelve-page order proof and the interleave-records proof"
    - path: "tests/test_pdf.py"
      provides: "PDF suite over records"
    - path: "tests/test_pages.py"
      provides: "stats-based empty-page tests"
    - path: "tests/test_outcomes_e2e.py"
      provides: "end-to-end outcome tests on the new signature"
  key_links:
    - from: "tests/test_pipeline.py"
      to: "tests/conftest.py"
      via: "spooling / spooling_in_turn as MagicMock side_effect"
      pattern: "side_effect = spooling|side_effect=spooling"
    - from: "tests/test_pipeline.py"
      to: "pikepdf"
      via: "reading the assembled PDF back to prove per-page content order"
      pattern: "pikepdf"
---

<objective>
Migrate the pipeline, PDF, pages and end-to-end outcome suites to the sink/record contract, and add
HARD-01's two ordering proofs: the twelve-page read-back and the duplex interleave whose filesystem
order differs from its document order.

Purpose: these are the tests success criterion 1 is graded on.
Output: green `tests/test_pages.py`, `tests/test_pdf.py`, `tests/test_pipeline.py` and
`tests/test_outcomes_e2e.py`.

**RED INTERVAL.** `tests/test_scanner.py` (plan 04, running beside this one) and the stub-scanner
files (plan 06, after it) are migrated elsewhere, so the full suite is still red. This plan's gate
is its own four files. Never use `--no-verify` or `SKIP=`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-geometry-memory-and-timeouts/29-CONTEXT.md
@.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md
@.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md
@.planning/phases/29-geometry-memory-and-timeouts/29-VALIDATION.md
@.planning/phases/29-geometry-memory-and-timeouts/29-03-SUMMARY.md

<interfaces>
Contracts under test (established by plans 01-03):

- `PageRecord(sequence, path, size, mode, mean, stddev)`; `ScanBatch.pages: tuple[PageRecord, ...]`
- `SpooledPageSink(directory, pass_label, min_free_space_mb, thumbnail_callback=None)`, `records`
- `ScannerBackend.scan_pages(device_id, settings, sink) -> ScanBatch`
- `saneless.pages.is_empty_page(mean, stddev, mean_threshold=250.0, stddev_threshold=5.0) -> bool`
- `saneless.pages.filter_empty_pages(records, mean_threshold=250.0, stddev_threshold=5.0) -> list[PageRecord]`
- `saneless.pdf.assemble_pdf(records, output_dir, filename, dpi) -> Path`
- `saneless.pipeline._interleave_duplex(fronts, backs) -> list[PageRecord]`
- `tests.conftest.spooling(pages, *, resolution=300, rejected=0)` — one callable for
  `MagicMock.side_effect`
- `tests.conftest.spooling_in_turn(*page_lists, resolution=300)` — one dispatching callable for
  multi-pass sites
- `tests.conftest.images_of(batch)`, `tests.conftest.scan_batch(records, ...)`,
  `tests.conftest.StubScannerBackend`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the pages and PDF suites</name>
  <files>tests/test_pages.py, tests/test_pdf.py</files>
  <read_first>
    - `tests/test_pages.py` (whole file, 140 lines, 22 `pages.py` call sites)
    - `tests/test_pdf.py` (whole file, 502 lines, 18 `assemble_pdf(` sites; it already imports
      `pikepdf` for PDF read-backs, which is the pattern the ordering proofs reuse)
    - `src/saneless/pages.py` and `src/saneless/pdf.py` (the signatures from plan 03)
    - `src/saneless/spool.py` (how a record is produced, so tests can build real ones)
    - `tests/conftest.py` (`spooling`, `images_of`, `scan_batch`)
  </read_first>
  <behavior>
    - `is_empty_page` tests pass mean and stddev directly; the dual-threshold boundary cases
      (mean exactly at the threshold, stddev exactly at the threshold) still assert the same
      results they do today.
    - `filter_empty_pages` tests build real `PageRecord` objects via a `SpooledPageSink` over
      `tmp_path` and assert the surviving records, in order.
    - `assemble_pdf` tests pass records whose `path` points at real spooled PNGs and assert the
      resulting PDF's page count and geometry.
    - At least one PDF test asserts that no second PNG encode happened — the spool directory
      contains exactly the pages that were spooled and no `page_NNNN.png` scratch files exist
      anywhere under `output_dir` after assembly.
    - The existing `PdfError` tests (empty input, unwritable output, img2pdf failure) still assert
      the same types and the same message substrings.
  </behavior>
  <action>
    Migrate both files mechanically.

    In `tests/test_pages.py`, replace image arguments with the measured values. Where a test built
    an all-white or an inked image purely to produce a mean and stddev, pass the numbers instead and
    delete the image construction — that indirection no longer models anything. Keep at least one
    test that goes end to end through a real `SpooledPageSink`, so the numbers a record carries are
    still proven to come from a real page rather than from a literal. Update every docstring that
    describes converting an image.

    In `tests/test_pdf.py`, add a fixture that spools N synthetic pages through a real
    `SpooledPageSink` into a `tmp_path` subdirectory and returns the records, then feed those
    records to `assemble_pdf` at each call site. Keep the existing `pikepdf` read-back assertions
    and the existing `PdfError` tests unchanged in intent.

    Add one test asserting the D-03 no-re-encode property: after `assemble_pdf` returns, no PNG file
    exists under `output_dir` other than the spooled pages themselves. Name it so it is findable,
    for example containing `no_second_encode`.

    Do not add a memory-bound test for assembly here — plan 08 owns the per-page convert plus qpdf
    merge and its `-k merge` test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pages.py tests/test_pdf.py -q &amp;&amp; uv run ruff check tests/test_pages.py tests/test_pdf.py &amp;&amp; uv run pyrefly check src tests/conftest.py tests/test_pages.py tests/test_pdf.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pages.py tests/test_pdf.py -q` exits 0 with zero failures and zero
      collection errors.
    - `uv run pytest tests/test_pdf.py -k no_second_encode -x -q` passes and reports at least 1
      test.
    - `grep -c 'is_empty_page(Image\|is_empty_page(img\|is_empty_page(page)' tests/test_pages.py`
      returns 0.
    - `grep -c 'assemble_pdf(\[Image\|assemble_pdf(images' tests/test_pdf.py` returns 0.
    - `uv run pyrefly check src tests/conftest.py tests/test_pages.py tests/test_pdf.py` exits 0.
  </acceptance_criteria>
  <done>The pages and PDF suites are green against the stats and record signatures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate tests/test_pipeline.py through the conftest seam</name>
  <files>tests/test_pipeline.py</files>
  <read_first>
    - `tests/test_pipeline.py` — read the whole file; 50 `scan_pages` sites across 36
      `MagicMock(spec=ScannerBackend)` blocks (the first are at :94, :360, :391), plus the duplex
      internals tests
    - `tests/conftest.py` (`spooling`, `spooling_in_turn`, `scan_batch`, `images_of` and the import
      form `from tests.conftest import ...`)
    - `src/saneless/pipeline.py` (the record-carrying internals from plan 03; the spool directory
      convention recorded in 29-03-SUMMARY.md)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md` Finding 7 point 2 and
      Pitfall 5 (a `side_effect` list of callables is returned, not called — one dispatching
      callable is required)
  </read_first>
  <behavior>
    - `uv run pytest tests/test_pipeline.py -q` collects and passes.
    - Single-pass sites use `scanner.scan_pages.side_effect = spooling([...])`; multi-pass
      (manual duplex) sites use `spooling_in_turn(fronts, backs)`. No site assigns a list of
      callables to `side_effect`.
    - Tests that asserted on returned images now assert on records or go through `images_of`.
    - The duplex tests still cover: a matched pair, a count mismatch producing two partial PDFs,
      an empty pass B, a flip timeout and a flip abort — with the same exception types and message
      substrings as today.
  </behavior>
  <action>
    Migrate every `MagicMock(spec=ScannerBackend)` site to the conftest seam. The 36 sites that
    already route through `scan_batch(...)` change by roughly one word: a `return_value` assignment
    of a prebuilt batch becomes a `side_effect` assignment of `spooling([...])`. Multi-pass sites
    take `spooling_in_turn(fronts, backs)` — never a `side_effect` list of callables, which
    `unittest.mock` returns rather than calls.

    Where a test needs a specific failure on a specific pass, keep using a single dispatching
    callable that tracks its own call count, following `_PassBGatedScanner`'s in-repo shape rather
    than a list.

    Convert image assertions to record assertions or `images_of(batch)` calls, choosing the cheapest
    form that still proves the original intent. Update docstrings so they describe what the migrated
    test now asserts.

    Do not add preservation tests here. D-09 and D-10 (partial-scan preservation, pass-B fronts, a
    cancel preserving nothing, the `failed/` page directory) are plan 09's, and it adds its tests to
    this same file in a later wave.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline.py -q &amp;&amp; uv run ruff check tests/test_pipeline.py &amp;&amp; uv run pyrefly check src tests/conftest.py tests/test_pipeline.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pipeline.py -q` exits 0 with zero failures and zero collection
      errors.
    - `grep -c 'scan_pages.return_value' tests/test_pipeline.py` returns 0.
    - `grep -c 'side_effect = \[spooling\|side_effect=\[spooling' tests/test_pipeline.py` returns 0
      — no list of callables.
    - `grep -c 'spooling' tests/test_pipeline.py` returns at least 30.
    - `uv run pyrefly check src tests/conftest.py tests/test_pipeline.py` exits 0.
  </acceptance_criteria>
  <done>The pipeline suite drives the new contract through the conftest seam, with no
  `side_effect` list of callables anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove page ordering end to end, and migrate the outcomes suite</name>
  <files>tests/test_pipeline.py, tests/test_outcomes_e2e.py</files>
  <read_first>
    - `tests/test_pipeline.py` (as migrated in task 2, plus the existing `_interleave_duplex` tests
      to extend)
    - `tests/test_outcomes_e2e.py` (whole file, 768 lines, 5 `scan_pages` sites across 2
      `MagicMock(spec=...)` blocks)
    - `tests/test_pdf.py` (the existing `pikepdf` read-back idiom to copy)
    - `src/saneless/pipeline.py` (`_interleave_duplex` and the spool naming convention from
      29-03-SUMMARY.md: pass A spools `a-NNNN.png`, pass B spools `b-NNNN.png`)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-VALIDATION.md` (the `twelve_page_order`
      and `interleave_records` rows)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-CONTEXT.md` D-02, D-04 and the
      "Specific Ideas" section ("Order comes from the record list, never from the filesystem" is the
      invariant the HARD-01 test attacks)
  </read_first>
  <behavior>
    Write these tests first and watch them fail.

    - `test_twelve_page_scan_keeps_document_order` (name must contain `twelve_page_order`): run a
      12-page simplex job whose pages carry distinct, machine-checkable content, assert the records
      carry `sequence` `list(range(1, 13))`, then open the assembled PDF with `pikepdf` and assert
      it has 12 pages whose per-page content is distinct and in the same order — compare each
      embedded image stream's bytes against the corresponding spooled PNG's IDAT, or against a
      per-page fingerprint derived from the source page.
    - `test_interleave_reorders_records_not_files` (name must contain `interleave_records`): build a
      duplex job whose **filesystem order differs from its document order** — pass A spools
      `a-0001.png` through `a-0003.png` and pass B spools `b-0001.png` through `b-0003.png`, so
      sorting the spool directory by name yields all fronts then all backs, while the document order
      is front1, back3, front2, back2, front3, back1. Assert the interleaved record list is the
      document order and that `sorted(spool_dir.iterdir())` is a **different** order — the test must
      fail if anyone ever recovers order from the filesystem.
    - No file is renamed or moved by the interleave: assert each record's `path` is unchanged before
      and after.
    - `tests/test_outcomes_e2e.py` still asserts the same job outcomes, categories and exit codes it
      does today.
  </behavior>
  <action>
    Add the two ordering tests to `tests/test_pipeline.py` in their own test class, with a class
    docstring naming D-02 and D-04 and stating the invariant the tests attack: order comes from the
    record list, never from the filesystem.

    For the twelve-page test, make per-page content distinct in a way a PDF read-back can check.
    The shared fake's `_page_image(index)` already varies an ellipse by index, which is enough if
    the assertion compares the PDF's embedded stream against the spooled PNG's bytes; state in the
    test docstring which equivalence is being asserted so the test cannot silently weaken into
    "there are twelve pages".

    For the interleave test, assert the negative explicitly: build the sorted filesystem listing,
    build the record order, and assert they differ before asserting the record order is correct.
    Without that first assertion the test would still pass against a `glob`-based implementation.

    Migrate `tests/test_outcomes_e2e.py`'s two `MagicMock(spec=...)` blocks to
    `spooling` / `spooling_in_turn` exactly as task 2 did, and convert its `.pages` assertions.
    Do not add the partial-scan preservation test here — `partial_scan_preserved` is plan 09's, and
    it lands in this same file in a later wave.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline.py tests/test_outcomes_e2e.py -q &amp;&amp; uv run pytest tests/test_pipeline.py -k "twelve_page_order or interleave_records" -x -q &amp;&amp; uv run ruff check tests/ &amp;&amp; uv run pyrefly check src tests/conftest.py tests/test_pipeline.py tests/test_outcomes_e2e.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pipeline.py -k twelve_page_order -x -q` passes and reports at least
      1 test.
    - `uv run pytest tests/test_pipeline.py -k interleave_records -x -q` passes and reports at
      least 1 test.
    - `grep -c 'pikepdf' tests/test_pipeline.py` returns at least 1.
    - `grep -c 'list(range(1, 13))' tests/test_pipeline.py` returns at least 1.
    - The interleave test source contains an assertion that the sorted filesystem listing differs
      from the record order (verifiable by
      `grep -c 'sorted(' tests/test_pipeline.py` returning at least 1 inside that test and by the
      test failing when the implementation is replaced with a sorted glob — state in the summary
      that this was checked).
    - `uv run pytest tests/test_outcomes_e2e.py -q` exits 0.
    - `uv run pyrefly check src tests/conftest.py tests/test_pipeline.py tests/test_outcomes_e2e.py`
      exits 0.
  </acceptance_criteria>
  <done>HARD-01's ordering half is proven end to end, including a duplex case that fails if page
  order is ever recovered from the filesystem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → `tmp_path` spool and `failed/` | Real writes happen during these tests |
| assembled PDF → pikepdf read-back | The only place the test suite parses a PDF it produced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-16 | Repudiation | a weakened ordering test | mitigate | The interleave test asserts the filesystem order differs from the document order *before* asserting the document order, so a glob-based implementation fails it |
| T-29-17 | Denial of service | 12-page PDF assembly in a unit test | accept | Synthetic small pages under `tmp_path`; `pytest-timeout` at 60 s bounds a hang |
| T-29-18 | Tampering | pikepdf parsing a PDF in tests | accept | The PDF is one the test just produced from its own spooled pages; no untrusted document is parsed |
</threat_model>

<verification>
- `uv run pytest tests/test_pages.py tests/test_pdf.py tests/test_pipeline.py tests/test_outcomes_e2e.py -q` — green
- `uv run pytest tests/test_pipeline.py -k "twelve_page_order or interleave_records" -x -q` — green
- `uv run pytest tests/test_pdf.py -k no_second_encode -x -q` — green
- `uv run ruff check . && uv run ruff format --check .`
- `uv run pyrefly check src tests/conftest.py tests/test_pages.py tests/test_pdf.py tests/test_pipeline.py tests/test_outcomes_e2e.py`

Still expected red until plan 06: the whole-suite `uv run pytest`, `uv run ty check` (full) and
`uv run pyrefly check src tests`.
</verification>

<success_criteria>
- All four migrated files pass on their own.
- A 12-page scan is proven in order 1..12 by a pikepdf read-back of the assembled PDF.
- Duplex interleave is proven to reorder records while the spool's filename order differs.
- No `side_effect` list of callables exists anywhere in the migrated suites.
- Empty-page tests assert against stored statistics.
</success_criteria>

<output>
Create `.planning/phases/29-geometry-memory-and-timeouts/29-05-SUMMARY.md` when done. Record how the
twelve-page content equivalence is asserted, since the verifier grades success criterion 1 on it.
</output>
