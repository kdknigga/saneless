---
phase: 29-geometry-memory-and-timeouts
plan: 04
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - tests/fake_sane.py
  - tests/test_scanner.py
  - tests/test_sane_hardware.py
autonomous: true
requirements: [HARD-01]
must_haves:
  truths:
    - "D-08: a 12-page scan's high-water mark of live page images is at most 2, and is the same number for 3 pages as for 12 — the independence from N is the proof of the memory bound"
    - "D-01: the backend calls `sink.add` exactly once per accepted page, with a single image, and returns records"
    - "D-08: the live-image counter uses `list[weakref.ref]`, never a `WeakSet`, because Pillow's `Image` defines `__eq__` and is therefore unhashable"
    - "The scanner suite exercises the new contract without reconstructing page order from the filesystem"
  artifacts:
    - path: "tests/fake_sane.py"
      provides: "weakref issue log over every page image the fake hands out"
      contains: "weakref"
    - path: "tests/test_scanner.py"
      provides: "the scanner suite migrated to sinks and records, plus the D-08 memory-bound proof"
    - path: "tests/test_sane_hardware.py"
      provides: "opt-in real-backend calls migrated to the sink signature"
  key_links:
    - from: "tests/test_scanner.py"
      to: "tests/conftest.py"
      via: "images_of / SpooledPageSink for image assertions"
      pattern: "images_of\\(|SpooledPageSink\\("
    - from: "tests/fake_sane.py"
      to: "tests/test_scanner.py"
      via: "the weakref issue log the high-water assertion reads"
      pattern: "live_page_images|issued_pages"
---

<objective>
Migrate `tests/test_scanner.py` (105 `scan_pages` sites, 48 `.pages` assertions, 37 `SaneBackend(`
constructions) and `tests/test_sane_hardware.py` to the sink/record contract, and add HARD-01's
memory-bound proof.

Purpose: this is the long pole of the contract migration, and it is also where the only honest
proof of "peak memory bounded by roughly one page" lives.
Output: a green `tests/test_scanner.py`, a migrated hardware suite, and a weakref-based live-image
counter in the shared fake.

**RED INTERVAL.** `tests/test_pipeline.py` and the stub-scanner files are migrated by plans 05 and
06, running beside and after this one, so the full suite is still red. This plan's gate is
`tests/test_scanner.py` and `tests/test_sane_hardware.py` collecting and passing on their own.
Never use `--no-verify` or `SKIP=`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-geometry-memory-and-timeouts/29-CONTEXT.md
@.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md
@.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md
@.planning/phases/29-geometry-memory-and-timeouts/29-VALIDATION.md
@.planning/phases/29-geometry-memory-and-timeouts/29-03-SUMMARY.md

<interfaces>
Contracts under test (established by plans 01-03):

- `PageRecord(sequence, path, size, mode, mean, stddev)` in `saneless.scanner.base`
- `PageSink.add(image) -> PageRecord`
- `SpooledPageSink(directory, pass_label, min_free_space_mb, thumbnail_callback=None)` with a
  `records` property, in `saneless.spool`
- `ScanBatch.pages: tuple[PageRecord, ...]`
- `SaneBackend.scan_pages(device_id, settings, sink) -> ScanBatch`
- `tests.conftest.images_of(batch) -> list[Image.Image]` — opens the spooled PNGs in record order
- `tests.conftest.spooling(pages, ...)` and `spooling_in_turn(*page_lists, ...)`
- `tests.conftest.StubScannerBackend`

New in this plan, consumed by plan 07:

- `FakeSaneDev.issued_pages` — a `list[weakref.ref[Image.Image]]` of every page image the fake has
  handed out, appended to inside `snap()`.
- `FakeSaneDev.live_page_images() -> int` — the count of entries in `issued_pages` whose referent
  is still alive.
- `FakeSaneDev.high_water_live_pages` — the maximum `live_page_images()` observed, sampled inside
  `snap()` at the moment the page is about to be returned (so the page being returned is counted).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Give FakeSaneDev a weakref issue log for the live-image proof</name>
  <files>tests/fake_sane.py</files>
  <read_first>
    - `tests/fake_sane.py` lines 520-545 (`_page_image` — one RGB page per index, already
      distinguishable by index), 620-660 (the class-body bare annotations and
      `FakeSaneDev.__init__`, which is at ruff `PLR0913`'s five-argument ceiling), 690-735
      (`__setattr__`, overridden to model SANE option validation, which is why new state is written
      through `self.__dict__`), 770-800 (`set_page_delay` — the model for a knob-as-method and its
      docstring argument), 1010-1080 (`start` and `snap`)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md` section
      "tests/fake_sane.py" (the `PLR0913` ceiling rule and the verbatim `start`/`snap` bodies)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md` Finding 4 and Pitfall 6
      (a 26 MB Pillow image adds 460 bytes to `tracemalloc`, so RSS and `tracemalloc` are both
      useless here; `Image` defines `__eq__` without `__hash__`, so `weakref.WeakSet` raises
      `TypeError: unhashable type: 'Image'`)
  </read_first>
  <behavior>
    - After a fake device has issued three pages and the test dropped its references to two of
      them, `live_page_images()` returns the number still referenced.
    - `high_water_live_pages` never decreases, and is sampled inside `snap()` counting the page
      about to be returned.
    - `issued_pages` is a plain `list` of `weakref.ref`; constructing a `weakref.WeakSet` of
      Pillow images raises `TypeError`, which is why it is not used — record that in the docstring.
    - The existing behaviour of `start()` and `snap()` is otherwise unchanged: the feeder-empty
      probe still raises `FakeSaneError` with `_FEEDER_EMPTY_MESSAGE` and charges no page delay,
      `load_feeder()` still supplies exact per-page images, and `_page_image(index)` still produces
      visibly distinct content per index.
    - Every page the fake produces stays in mode `RGB` or `L`, so the backend's
      width-times-height-times-bands byte count stays exact (Pitfall 7).
  </behavior>
  <action>
    Add the three members from the interfaces block to `FakeSaneDev`.

    Declare `issued_pages` and `high_water_live_pages` as bare annotations in the class body with no
    assigned value, following the file's existing convention so `__getattr__` still runs, and
    initialise them by writing through `self.__dict__` — `__setattr__` is overridden to model SANE
    option validation, so a plain assignment would go through that path. Do not add constructor
    parameters: `__init__` is already at ruff `PLR0913`'s five-argument ceiling, and `set_page_delay`
    is the in-file precedent for a knob that is a method instead.

    Inside `snap()`, immediately before returning the page, append `weakref.ref(page)` to
    `issued_pages`, then recompute `live_page_images()` and raise `high_water_live_pages` to it if
    it is larger. Sampling at that moment is deliberate: it counts the page about to be returned,
    which is what makes 2 the honest high-water mark when the backend's loop variable still holds
    page k-1.

    `live_page_images()` counts entries whose referent is still alive by calling each `ref` and
    testing for `None`. Its docstring must state, citing RESEARCH.md Finding 4 and Pitfall 6, that a
    `weakref.WeakSet` cannot be used because Pillow's `Image` defines `__eq__` without `__hash__`,
    and that `tracemalloc` cannot be used because a 26 MB Pillow image adds only 460 bytes to the
    traced total since its pixels are malloc'd in C. Add a sentence noting what the counter does not
    see: `snap()` itself transiently holds three copies of a page, and `crop` and `convert("L")` each
    add one, so the real decoded ceiling is roughly two to three pages — constant in N, which is the
    claim being defended.

    Import `weakref` at module level. Do not change `start()`, `cancel()`, `close()` or
    `FakeSaneModule` in this task — plan 07 owns the Event-gated blocking mode and the
    close/exit-while-blocked flags, and touching them here would collide.
  </action>
  <verify>
    <automated>uv run ruff check tests/fake_sane.py &amp;&amp; uv run python -c "import ast; t=ast.parse(open('tests/fake_sane.py').read()); names={n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)}; assert 'live_page_images' in names, sorted(names)[:20]; src=open('tests/fake_sane.py').read(); assert 'WeakSet' not in src; assert 'weakref.ref(' in src; print('issue log ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `issue log ok`.
    - `grep -c 'WeakSet' tests/fake_sane.py` returns 0.
    - `grep -c 'weakref.ref(' tests/fake_sane.py` returns at least 1.
    - `grep -c 'high_water_live_pages' tests/fake_sane.py` returns at least 3 (annotation,
      initialisation, update).
    - `grep -c 'def set_page_delay' tests/fake_sane.py` still returns 1 — the existing sleep-based
      knob is not removed or extended here (TEST-02).
    - `uv run ruff check tests/fake_sane.py` exits 0.
  </acceptance_criteria>
  <done>The shared fake records every page image it hands out with a weakref and tracks the
  high-water mark of live ones.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate tests/test_scanner.py to sinks and records</name>
  <files>tests/test_scanner.py</files>
  <read_first>
    - `tests/test_scanner.py` — read the whole file; there are 105 `scan_pages` sites, 48 `.pages`
      assertions and 37 `SaneBackend(` constructions to migrate. Pay particular attention to the
      fixtures at :270-305 (the module-level `sane` monkeypatch seam every test depends on) and
      `TestSaneBackendPerPageTimeout` at :1426-1454 (whose `match="timed out"` is why the timeout
      wording must not change)
    - `tests/conftest.py` (the `spooling`, `spooling_in_turn`, `images_of` and `StubScannerBackend`
      seam from plan 02, and the `wait_for_state` fixture pattern for exposing a conftest function)
    - `src/saneless/spool.py` and `src/saneless/scanner/base.py`
    - `.planning/phases/29-geometry-memory-and-timeouts/29-PATTERNS.md` section
      "Scanner tests — timeout, fixtures, and the subprocess proof"
    - `.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md` Finding 7 point 4
  </read_first>
  <behavior>
    - `uv run pytest tests/test_scanner.py -q` collects and passes with no errors and no skips that
      were not skipped before.
    - Every call to `backend.scan_pages(...)` supplies a real `SpooledPageSink` writing into a
      `tmp_path` subdirectory — not a `MagicMock` sink — so the tests exercise the real write path.
    - Assertions that previously inspected `batch.pages[i]` as an image now either inspect the
      record's fields directly (`sequence`, `size`, `mode`, `mean`, `stddev`, `path`) or go through
      `images_of(batch)` when the assertion is genuinely about pixels.
    - No test reconstructs page order by sorting or globbing the spool directory; order always comes
      from `batch.pages` or `record.sequence` (D-02).
    - `test_page_timeout_raises_scan_error` still asserts `ScanError` with `match="timed out"` and
      still uses the existing `set_page_delay(2.0)` / `timeout_per_page=0.1` pair — TEST-02 forbids
      extending that sleep, and this is the one existing sleep the phase leaves alone until plan 07
      replaces it with an Event gate.
  </behavior>
  <action>
    Mechanically migrate the file. Add one module-level pytest fixture that yields a real
    `SpooledPageSink` rooted in `tmp_path` — give it a name like `page_sink` and construct it with
    `pass_label="a"` and a `min_free_space_mb` low enough that the check never fires in a normal
    test (0 is acceptable; state why in the fixture docstring). Add a second fixture or helper for
    tests that need two passes with distinct labels.

    For each `scan_pages` call site, pass the sink as the third argument. For each `.pages`
    assertion, decide between three forms and use the cheapest that still proves the same thing:
    a record-field assertion, a `len(batch.pages)` assertion, or `images_of(batch)` when the
    assertion is about pixel content. Import `images_of` as `from tests.conftest import images_of` —
    the bare `conftest` form raises `ModuleNotFoundError` under pytest 9's importlib mode.

    Where a test asserted that the backend returned specific `Image` objects by identity, that
    assertion is no longer meaningful — the backend no longer returns images. Replace it with an
    assertion on the record's `size`/`mode` plus, where the test's intent was content, an
    `images_of` pixel comparison against the page the fake was loaded with via `load_feeder()`.

    Do not change any timeout, cancel, wedge, init-guard or `sane.exit` behaviour or the tests
    covering them; plans 07 and 10 own those and will add their own tests to this file. Do not
    rename tests whose names over-claim — that is TEST-04 / Phase 32, explicitly out of scope,
    except for the one init-once test plan 10 replaces.

    Keep every existing docstring accurate to what the migrated test now does. A docstring that says
    "returns two images" must become one that says what it now asserts.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scanner.py -q &amp;&amp; uv run ruff check tests/test_scanner.py &amp;&amp; uv run pyrefly check src tests/conftest.py tests/fake_sane.py tests/test_scanner.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_scanner.py -q` exits 0 with zero failures and zero collection
      errors.
    - `grep -c 'scan_pages(' tests/test_scanner.py` returns a count, and
      `grep -c 'scan_pages([^)]*settings)' tests/test_scanner.py` returns 0 — no two-argument call
      remains.
    - `grep -c 'MagicMock' tests/test_scanner.py` does not increase relative to the pre-task value.
    - `grep -c 'sorted(.*glob\|\.glob(' tests/test_scanner.py` returns 0.
    - `grep -c 'match="timed out"' tests/test_scanner.py` returns at least 1.
    - `uv run pyrefly check src tests/conftest.py tests/fake_sane.py tests/test_scanner.py` exits 0.
  </acceptance_criteria>
  <done>`tests/test_scanner.py` is green against the sink/record contract and no test recovers page
  order from the filesystem.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove the memory bound, the one-page-per-sink-call rule, and migrate the hardware suite</name>
  <files>tests/test_scanner.py, tests/test_sane_hardware.py</files>
  <read_first>
    - `tests/test_scanner.py` (the file migrated in task 2, and the existing test-class structure
      to add to)
    - `tests/fake_sane.py` (the issue log added in task 1)
    - `tests/test_sane_hardware.py` (whole file — 3 `scan_pages` sites, 2 `.pages` assertions,
      5 `SaneBackend(` constructions, all behind the `sane_hardware` marker)
    - `.planning/phases/29-geometry-memory-and-timeouts/29-RESEARCH.md` Finding 4 (the recommended
      bound and why a hard `== 1` would be dishonest) and the Validation Architecture success-criteria
      table rows for `live_page_images` and `sink`
    - `.planning/phases/29-geometry-memory-and-timeouts/29-VALIDATION.md` (the per-task verification
      map rows this task fills)
  </read_first>
  <behavior>
    Write these tests first and watch them fail before they pass.

    - `test_live_page_images_stays_bounded` (name must contain `live_page_images`): run a 12-page
      ADF scan through `SaneBackend.scan_pages` with a real `SpooledPageSink`, then assert
      `dev.high_water_live_pages <= 2`.
    - The same test runs a 3-page scan on a fresh device and asserts the 3-page high-water mark
      equals the 12-page one. The independence from N is the actual proof; a hard equality to 1
      would only be reachable by adding a `del` that exists to satisfy a test, and is explicitly
      rejected.
    - `test_sink_receives_one_image_per_page` (name must contain `sink`): a recording sink wrapping
      a real `SpooledPageSink` asserts `add` was called exactly once per page, each time with a
      single `PIL.Image.Image` argument, and that the returned batch's `pages` is the tuple of
      records `add` returned, in order.
    - A 12-page scan's records carry `sequence` 1 through 12 with no gaps and no repeats.
    - The hardware tests keep their `sane_hardware` marker and still pass when run with
      `uv run pytest -m sane_hardware`, or skip cleanly when the backend is unavailable, exactly as
      they do today.
  </behavior>
  <action>
    Add a test class to `tests/test_scanner.py` for HARD-01's memory bound, holding the three tests
    described above. Give the class a docstring that records the measured facts behind the numbers:
    Pillow allocates pixel memory outside `tracemalloc`'s view (a 26 MB image adds 460 bytes to the
    traced total), RSS is too noisy for CI, and the honest high-water mark is 2 because the
    backend's loop variable still references page k-1 while page k is acquired.

    Drive the page count from the fake's `load_feeder()` so each page carries distinct content, and
    build the sink with a `tmp_path` subdirectory. Assert `sequence` values as a list comparison
    against `list(range(1, 13))` rather than checking each in a loop, so a failure prints the whole
    sequence.

    For the recording sink, subclass `PageSink` (not `MagicMock`) so both type checkers see the
    contract, hold the real `SpooledPageSink` as a delegate, and record each `add` argument's type
    and the returned record. Assert on the recorded calls afterwards.

    Migrate `tests/test_sane_hardware.py`: pass a real `SpooledPageSink` rooted in `tmp_path` at each
    `scan_pages` call and convert the two `.pages` assertions to record assertions or `images_of`.
    Do not add the cancel-during-read hardware test here — that is plan 07's, and it needs the
    Event-gated fake and the real `read-delay` option wiring.
  </action>
  <verify>
    <automated>uv run pytest tests/test_scanner.py -q &amp;&amp; uv run pytest tests/test_scanner.py -k "live_page_images or sink" -x -q &amp;&amp; uv run pytest tests/test_sane_hardware.py --collect-only -q &amp;&amp; uv run ruff check tests/ &amp;&amp; uv run pyrefly check src tests/conftest.py tests/fake_sane.py tests/test_scanner.py tests/test_sane_hardware.py</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_scanner.py -k live_page_images -x -q` passes and reports at least
      1 test.
    - `uv run pytest tests/test_scanner.py -k sink -x -q` passes and reports at least 1 test.
    - `grep -c 'high_water_live_pages' tests/test_scanner.py` returns at least 2.
    - `grep -c 'tracemalloc' tests/test_scanner.py` returns 0 in executable code (it may appear only
      inside the class docstring explaining why it is not used).
    - `grep -c 'list(range(1, 13))' tests/test_scanner.py` returns at least 1.
    - `uv run pytest tests/test_sane_hardware.py --collect-only -q` exits 0 with no collection
      errors.
    - `uv run pyrefly check src tests/conftest.py tests/fake_sane.py tests/test_scanner.py tests/test_sane_hardware.py`
      exits 0.
  </acceptance_criteria>
  <done>HARD-01's memory bound is proven by a weakref counter that is independent of page count, the
  one-image-per-sink-call rule is proven, and the hardware suite collects against the new
  signature.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fake → backend under test | The fake is the only device the in-process suite talks to |
| test process → `tmp_path` | Real spool writes happen during these tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-13 | Denial of service | the 12-page memory-bound test | mitigate | Pages are the fake's small synthetic images, and the spool writes into `tmp_path`, which pytest removes; `pytest-timeout` at 60 s with `timeout_method="signal"` bounds a hang |
| T-29-14 | Tampering | weakref issue log | accept | Test-only instrumentation on the fake; it cannot be reached from `src/` |
| T-29-15 | Repudiation | a test asserting a weaker bound than it claims | mitigate | The bound is asserted two ways — at most 2, and equal for N=3 and N=12 — so a regression that grows with page count fails even if the constant is later raised |
</threat_model>

<verification>
- `uv run pytest tests/test_scanner.py -q` — green
- `uv run pytest tests/test_scanner.py -k "live_page_images or sink" -x -q` — green
- `uv run pytest tests/test_sane_hardware.py --collect-only -q` — no collection errors
- `uv run pytest -m sane_hardware -q` — green where libsane's `test` backend is available
- `uv run ruff check . && uv run ruff format --check .`
- `uv run pyrefly check src tests/conftest.py tests/fake_sane.py tests/test_scanner.py tests/test_sane_hardware.py`

Still expected red until plan 06: the whole-suite `uv run pytest`, `uv run ty check` (full) and
`uv run pyrefly check src tests`.
</verification>

<success_criteria>
- `tests/test_scanner.py` passes in full against the sink/record contract.
- `FakeSaneDev` records issued page images with `weakref.ref` in a list, never a `WeakSet`.
- The live-image high-water mark is asserted at most 2 and equal for 3 and 12 pages.
- A test proves `sink.add` is called once per page with a single image.
- 12-page records carry `sequence` 1 through 12.
- `tests/test_sane_hardware.py` collects and passes against the new signature.
</success_criteria>

<output>
Create `.planning/phases/29-geometry-memory-and-timeouts/29-04-SUMMARY.md` when done. Record the
observed high-water value so plan 07 and the verifier can compare against it.
</output>
