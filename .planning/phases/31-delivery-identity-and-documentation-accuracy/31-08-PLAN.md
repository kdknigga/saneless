---
phase: 31-delivery-identity-and-documentation-accuracy
plan: 08
type: execute
wave: 6
depends_on: ["31-07"]
files_modified:
  - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-AUDIT.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    - "Every one of review section 8's 34 rows has a named disposition and traceable evidence (criterion 5)"
    - "Every row is marked either permanently defended by a test or verified point-in-time by a read (criterion 5)"
    - "Section 8's ~20 'checked and correct' claims are re-checked against the tree twelve phases later (criterion 5)"
    - "The original review file is not edited in place"
  artifacts:
    - path: ".planning/phases/31-delivery-identity-and-documentation-accuracy/31-AUDIT.md"
      provides: "the 34-row disposition table, the D-44 re-check table, and a verification-evidence section"
      min_lines: 80
      contains: "permanently defended"
  key_links:
    - from: "31-AUDIT.md"
      to: ".planning/reviews/2026-09-09-code-review.md § 8"
      via: "row numbers lining up one-to-one with the source table"
      pattern: "row"
    - from: "31-AUDIT.md"
      to: "tests/test_deployment_config.py"
      via: "a named test per permanently-defended row"
      pattern: "test_"
---

<objective>
Produce the row-by-row verification record for the milestone's final documentation audit, and
re-check the claims the original review said were already correct.

Purpose: D-42 and D-44. DOCS-01 asks for every one of review section 8's 34 rows to be corrected
or the behaviour implemented; this artifact is the evidence that it happened, with a named
disposition and traceable evidence per row. D-44 puts the review's "claims that were checked and
are correct" list back in scope, because twelve phases have changed behaviour since 2026-09-09 and
a claim that was true in September and is false now is exactly what this audit exists to catch.

This plan runs LAST among the documentation strands, because the earlier plans change sentences it
audits.

Output: `.planning/phases/31-delivery-identity-and-documentation-accuracy/31-AUDIT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-02-SUMMARY.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-05-SUMMARY.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-06-SUMMARY.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-07-SUMMARY.md

<interfaces>
Source tables to mirror, so row numbers line up one-to-one:
  .planning/reviews/2026-09-09-code-review.md:982-1027  the 34-row table
  .planning/reviews/2026-09-09-code-review.md:~1029      "Claims that were checked and are correct"

RESEARCH § 9 supplies both tables' content, every row re-checked against the shipped tree on
2026-09-18. Its classification:
  23 rows corrected by Phases 21-30 ..... 1-14, 20, 22-28, 33
   7 rows still false, sentence edits ... 16, 17, 18, 21, 30, 31, 32
   4 rows this phase's own work ......... 15, 19, 29, 34
   0 rows needing new behaviour ......... D-43's escalation clause did not fire

Where each live row was closed in this phase:
  15  DLVR-10, Plan 01 task 2 — `--version` added; the doc sentence at
      docs/how-to/install-bare-metal.md:47 needed NO edit, it became true
  16  Plan 02 task 2 — README scan example carries `--title`
  17  Plan 02 task 2 — README `source = "Flatbed"`
  18  Plan 02 task 2 — README tutorial deep link repointed at getting-started/first-cli-scan
  19  DLVR-01, Plan 02 task 1 — 24 lines in nine files renamed, guarded
  21  DLVR-05, Plan 05 task 3 — saneless.toml.example web_port commented; docker.md caveat
  29  DOCS-04, Plan 06 task 2 — four USB statements reconciled; passthrough section deleted
  30  Plan 07 task 1 — job JSON id is a UUID4
  31  Plan 07 task 1 — every `docker run -v` host path is `$(pwd)`
  32  DOCS-06, Plan 07 task 2 — partial Paperless service replaced by a pointer
  34  DOCS-03, Plan 07 task 3 — docs/PRD.md moved to .planning/milestones/v1.0-PRD.md

D-44's three at-risk claims, already re-checked in RESEARCH:
  "`scan` exit codes 1, 2 and 3"          -> claim SUPERSEDED; the docs are correct and are pinned
                                             by Phase 28's ExitCode-derived tests
  "all eleven routes and methods"         -> claim SUPERSEDED; source has 15 route decorators, the
                                             reference documents 15, and Phase 30 added a
                                             decorator-derived test
  "three upload attempts with 2**attempt" -> STILL TRUE (paperless.py:455, :707)

Format analog: `.planning/v1.0-MILESTONE-AUDIT.md` — YAML frontmatter carrying machine-readable
status, then a per-row cross-reference table with a bolded final disposition.

Hard rules:
  - The original review file is NOT edited in place (D-42). Annotating it would muddy what the
    reviewers actually found.
  - `.planning/` is excluded from the naming guard and from the Docker build context by
    construction, so this artifact is exempt without needing an exemption.
  - `docs/reference/web-api.md`'s OpenAPI claims belong to Phase 33. If a row touches them, record
    it and leave it to that phase.
  - D-43: if any row turns out to need NEW BEHAVIOUR rather than a corrected sentence, STOP and
    escalate to the user. Do not implement. RESEARCH found zero such rows, so this is a tripwire,
    not an expectation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 34-row disposition table</name>
  <files>.planning/phases/31-delivery-identity-and-documentation-accuracy/31-AUDIT.md</files>
  <read_first>
    - .planning/reviews/2026-09-09-code-review.md lines 980-1027 — the 34-row source table, in full
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md § 9 — the
      re-checked table with `file:line` evidence for every row
    - .planning/v1.0-MILESTONE-AUDIT.md lines 1-55 — the frontmatter shape and the row-table shape
      to mirror
    - The SUMMARY files of plans 01, 02, 05, 06 and 07 — for the actual test names and commits that
      closed each live row
    - tests/test_deployment_config.py — to read the REAL test names, not the names the plans
      predicted. Every "permanently defended" row must cite a test that exists
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md D-41, D-42,
      D-43
  </read_first>
  <action>
    Create `.planning/phases/31-delivery-identity-and-documentation-accuracy/31-AUDIT.md` with YAML
    frontmatter in the shape `.planning/v1.0-MILESTONE-AUDIT.md` uses — `phase: 31`,
    `audited: <ISO timestamp>`, `status:`, a `scores:` block counting rows disposed, and a `gaps:`
    list that must be empty for the phase to pass.

    Write the 34-row table with row numbers lining up one-to-one with review § 8, one row per item,
    with these columns:
      # | Where (file:line) | Claim | Disposition | Evidence | Defence
    - **Disposition** is one of: `corrected in Phase <N>`, `corrected in Phase 31 (<plan>-<task>)`,
      `became true without a doc edit`, or `superseded`. Use the last only where the ORIGINAL claim
      is stale but the documentation is now correct.
    - **Evidence** is either a test name that exists in `tests/test_deployment_config.py` (or
      another test module) or a `file:line` reference. Never both blank, never a prose hand-wave.
    - **Defence** is `permanently defended` when a test pins the row so a regression fails the
      suite, or `point-in-time read` when the row was verified by a human reading a paragraph. D-41
      requires this distinction to be explicit, and the artifact must be honest about it: forcing a
      prose-judgement row into a test produces a brittle keyword ban that gets weakened later.

    Verify each row against the tree as it stands NOW, not against RESEARCH's 2026-09-18 snapshot —
    plans 01 through 07 have changed many of these files since. For every row you mark
    `permanently defended`, confirm the named test actually exists and passes. For every row you
    mark `point-in-time read`, record the exact `file:line` you read and the date.

    If any row turns out to need NEW BEHAVIOUR rather than a corrected sentence, STOP and escalate
    to the user rather than implementing it (D-43). New behaviour at the end of a hardening
    milestone is how a phase blows its boundary, and DOCS-01's "or the behaviour is implemented"
    clause is not a licence to build. RESEARCH found zero such rows, so treat this as a tripwire.

    Do NOT edit `.planning/reviews/2026-09-09-code-review.md`. This artifact is a verification
    record; the review stays as the reviewers left it.
  </action>
  <verify>
    <automated>test "$(command grep -cE '^\| *[0-9]+ \|' .planning/phases/31-delivery-identity-and-documentation-accuracy/31-AUDIT.md)" = "34" && uv run pytest tests/test_deployment_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `31-AUDIT.md` exists with YAML frontmatter carrying `phase: 31`, an `audited:` timestamp and a
      `gaps:` list
    - The row table has exactly 34 numbered rows, numbered 1 through 34, matching review § 8
    - Every row has a non-empty Disposition, a non-empty Evidence cell, and a Defence value of
      either `permanently defended` or `point-in-time read`
    - Every test name cited in an Evidence cell exists: extracting them and running
      `uv run pytest -k "<name>"` finds and passes each one
    - Every `file:line` cited in an Evidence cell points at a line that exists in the current tree
    - `git diff --stat .planning/reviews/2026-09-09-code-review.md` shows no change
    - The `gaps:` frontmatter list is empty, or the plan stops and escalates rather than shipping a
      non-empty one
  </acceptance_criteria>
  <done>All 34 rows are disposed with traceable evidence, and the artifact says plainly which rows a test will defend forever and which were verified once by reading.</done>
</task>

<task type="auto">
  <name>Task 2: The D-44 re-check of the "checked and correct" claims</name>
  <files>.planning/phases/31-delivery-identity-and-documentation-accuracy/31-AUDIT.md</files>
  <read_first>
    - .planning/reviews/2026-09-09-code-review.md — the "Claims that were checked and are correct"
      paragraph immediately after the 34-row table; enumerate every claim in it
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md § 9
      "D-44: re-checking the 'correct' list" — the ten-row re-check table with evidence
    - src/saneless/paperless.py around lines 455 and 707 — `max_retries: int = 3` and
      `time.sleep(2**attempt)`
    - src/saneless/vocabulary.py `ExitCode` and docs/how-to/cli-scripting.md's exit-code table
    - src/saneless/web/routes.py route decorators and docs/reference/web-api.md's route table
    - tests/test_deployment_config.py — the existing decorator-derived and model-field-derived tests
      that already defend several of these claims
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md D-44
  </read_first>
  <action>
    Add a second table to `31-AUDIT.md` covering every claim in review § 8's "Claims that were
    checked and are correct" paragraph — roughly twenty of them, verified 2026-09-09. Use the same
    columns as the 34-row table so the two read alike.

    Re-check each claim against the tree as it stands now. Twelve phases have changed behaviour
    since, and three claims are squarely in the blast radius:
    - "`scan` exit codes 1, 2 and 3 for the cases that are caught" — Phase 28 replaced the table
      with 0/1/2/3/4/5/130.
    - "all eleven routes and methods in `web-api.md`" — Phases 26 and 30 added routes; source now
      has fifteen route decorators.
    - "three upload attempts with `2**attempt` backoff" — Phase 28 reworked retries.

    For the first two, RESEARCH found the same thing: the 2026-09-09 CLAIM is stale but the
    DOCUMENTATION is now correct and is pinned by a derived test. Record that as
    `claim superseded; the documentation is correct and is now pinned by <test name>` — a materially
    different disposition from "still correct", and exactly what D-41 means by being honest about
    what is permanently defended. Do not collapse the two into one label.

    Confirm each cited test really exists before naming it; cite the real name from
    `tests/test_deployment_config.py`, not the one a plan predicted.

    Close the artifact with a short evidence section recording: the commands run for the phase gate
    (`uv run pytest -m "not browser and not sane_hardware"`, `uv run zizmor .`,
    `uv run mkdocs build --strict`, `uv run ruff check .`, `uv run ruff format --check .`,
    `uv run ty check`, `uv run pyrefly check src tests`) with their outcomes, and a placeholder
    section headed for the release-rehearsal evidence that Plan 10 will fill in (the RC tag, the
    workflow run URL, and the `pip install` / `docker pull` output required by D-21 and D-22).

    Also record, in one short paragraph, the fact that makes this rename unusual and that nobody
    should go looking for migration work over: nothing was ever published under the old name. There
    is no `ghcr.io/kris-knigga/saneless` package, GitHub Pages never deployed under the old owner,
    no stored data, OS registration, secret or environment variable carries the slug, and the only
    stale artefact is a gitignored `site/` build that is regenerated.
  </action>
  <verify>
    <automated>uv run pytest -m "not browser and not sane_hardware" -q && uv run zizmor . && uv run mkdocs build --strict && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `31-AUDIT.md` contains a second table with one row per claim in review § 8's "checked and
      correct" paragraph, and no claim from that paragraph is missing
    - The exit-code claim and the route-count claim are each dispositioned `claim superseded` with a
      named, existing test as evidence — not as `still correct`
    - The retry claim is dispositioned `still true` with `src/saneless/paperless.py` line evidence
    - Every test name cited in the second table exists and passes
    - The artifact contains an evidence section listing the seven phase-gate commands with their
      outcomes
    - The artifact contains a clearly-marked placeholder for Plan 10's release-rehearsal evidence
    - `git diff --stat .planning/reviews/2026-09-09-code-review.md` shows no change
    - All seven phase-gate commands pass
  </acceptance_criteria>
  <done>The claims the review said were already true are re-checked twelve phases later, the two stale ones are labelled as superseded rather than silently reaffirmed, and the phase-gate evidence is on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification record -> future phases' trust in it | An audit that overstates its own coverage is worse than no audit; downstream phases will skip re-checking what it claims is defended |
| `.planning/` -> the naming guard and the Docker build context | This artifact quotes the old owner slug freely; it must live where both exclusions already apply |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-31 | Repudiation | the audit artifact's own claims | mitigate | Every row cites either a test name that must exist and pass or a `file:line` that must resolve, and the acceptance criteria re-run the cited tests rather than trusting the table. The `Defence` column forces the permanently-defended-versus-point-in-time distinction to be stated per row (D-41) rather than implied |
| T-31-32 | Tampering | the original review file | mitigate | D-42: the review is not edited in place, and `git diff --stat` on it is an acceptance criterion. Annotating the reviewers' findings would destroy the record of what they actually found |
| T-31-33 | Elevation of privilege (scope) | D-43's "or the behaviour is implemented" clause | mitigate | If a row needs new behaviour the task STOPS and escalates. New behaviour at the end of a hardening milestone is how a phase blows its boundary. RESEARCH found zero such rows, so this is a tripwire, not an expectation |
| T-31-34 | Information disclosure | the old owner slug quoted throughout the artifact | accept | The artifact lives under `.planning/`, which the naming guard excludes by construction (D-10) and which the `.dockerignore` allow-list excludes from the build context. The slug is public information about a GitHub account that never hosted anything |
</threat_model>

<verification>
- `uv run pytest -m "not browser and not sane_hardware" -q`
- `uv run zizmor .` — exit 0
- `uv run mkdocs build --strict` — exit 0
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
- `uv run prek run --all-files` and `uv run prek run --stage pre-push --all-files`
- Extract every test name cited in `31-AUDIT.md` and run them; every one must exist and pass
- `git diff --stat .planning/reviews/2026-09-09-code-review.md` — empty
</verification>

<success_criteria>
- All 34 rows of review section 8 are disposed with traceable evidence and an explicit
  permanently-defended-versus-point-in-time marker (DOCS-01, criterion 5)
- Section 8's "checked and correct" claims are re-checked, with the two stale ones labelled
  superseded rather than reaffirmed (D-44, criterion 5)
- Every cited test exists and passes; every cited `file:line` resolves
- The original review file is unchanged
- No row required new behaviour; if one had, the plan escalated rather than building it
</success_criteria>

<output>
Create `.planning/phases/31-delivery-identity-and-documentation-accuracy/31-08-SUMMARY.md` when done
</output>
