---
phase: 31-delivery-identity-and-documentation-accuracy
plan: 05
type: tdd
wave: 3
depends_on: ["31-02", "31-04"]
files_modified:
  - .dockerignore
  - .gitignore
  - Dockerfile
  - docker-compose.yml
  - saneless.toml.example
  - docs/reference/docker.md
  - docs/how-to/deploy-docker-compose.md
  - tests/test_deployment_config.py
autonomous: true
requirements: [DLVR-05, DLVR-06, DLVR-07, DLVR-09]

must_haves:
  truths:
    - "The container runs as UID/GID 1000, not root (criterion 3)"
    - "All three base image references are digest-pinned and Dependabot can see all three (criterion 3, 4)"
    - "A `WORKDIR` is set so a stray relative write lands in the durable data dir, not `/` (criterion 3)"
    - "A real `saneless.toml`, anything under `.planning/`, and `tests/` cannot reach the Docker build context (criterion 3)"
    - "The example config, `EXPOSE`, `HEALTHCHECK` and every doc page agree on port 8080 (criterion 3)"
    - "`.gitignore` no longer blanket-ignores `*.png` (criterion 4)"
  artifacts:
    - path: ".dockerignore"
      provides: "an allow-list whose first non-comment line is `*`"
      contains: "!src/"
    - path: "Dockerfile"
      provides: "FROM ... AS uv, digest-pinned bases, explicit COPY lines, UID/GID 1000, WORKDIR, USER"
      contains: "USER saneless"
    - path: ".gitignore"
      provides: "no `*.png` glob; `/test-results/` instead"
    - path: "saneless.toml.example"
      provides: "web_port commented out with bare-metal-only guidance"
    - path: "tests/test_deployment_config.py"
      provides: "the .dockerignore, Dockerfile, .gitignore and port-agreement contracts"
  key_links:
    - from: "Dockerfile"
      to: "src/saneless/config.py OutputConfig.web_port"
      via: "EXPOSE and HEALTHCHECK, both asserted against the model default"
      pattern: "EXPOSE 8080"
    - from: "Dockerfile builder stage"
      to: "ghcr.io/astral-sh/uv"
      via: "FROM ... AS uv then COPY --from=uv, so Dependabot's FROM-only parser sees the pin"
      pattern: "AS uv"
---

<objective>
Harden the container image and its build context: non-root, digest-pinned, `WORKDIR`-anchored,
with an allow-list build context and one agreed port.

Purpose: DLVR-05, DLVR-06, DLVR-07 and DLVR-09. Today the image runs as root from mutable tags,
pulls an executable into the build from `ghcr.io/astral-sh/uv:latest`, copies the entire working
tree — including a real `saneless.toml` with a live Paperless token — into a build layer, has no
`WORKDIR`, and ships an example config whose `web_port = 8081` disagrees with `EXPOSE` and
`HEALTHCHECK`.

Output: a `.dockerignore` allow-list, a restructured `Dockerfile`, a `.gitignore` without the
blanket `*.png`, a corrected example config, and four static contracts in the doc-truth harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-02-SUMMARY.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-04-SUMMARY.md

<interfaces>
Measured allow-list (a probe image was built with exactly this and the received context
enumerated — `LICENSE`, `README.md`, `pyproject.toml`, `src/saneless/cli.py`,
`src/saneless/web/templates/index.html`, `uv.lock`; `config/config.toml`, `saneless.toml`,
`tests/` and `.planning/` all excluded):

  *
  !src/
  !pyproject.toml
  !uv.lock
  !README.md
  !LICENSE

`!src/` re-includes the directory AND everything beneath it — `!src/**` is not needed (verified).
Two traps, both confirmed live: `pyproject.toml`'s `readme = "README.md"` and the new PEP 639
`license-files = ["LICENSE"]` each make `uv build --wheel` FAIL if that file is excluded.
`uv.lock` is not required by `uv build --wheel` but costs nothing and keeps a future
`uv sync --locked` in the builder stage possible.

Image digests resolved 2026-09-18 — re-resolve at implementation time (assumption A7):
  python:3.14-slim        sha256:cad9a2c871761c413caa6fdd6441c783451e740a48aaeba60ae62a8b53525ef6
  ghcr.io/astral-sh/uv    0.10.3 sha256:7a88d4c4e6f44200575000638453a5a381db0ae31ad5c3a51b14f8687c9d93a3

Dependabot's Docker parser matches `FROM` lines ONLY — it explicitly skips `COPY --from=`
(verified in dependabot-core/docker/lib/dependabot/docker/file_parser.rb). The tag must stay in
the reference (`image:tag@sha256:...`), not only in the comment, for Dependabot to track it.

Volume ownership, measured against a non-root image:
  fresh NAMED volume     -> owner 1000:1000 inside, writable   OK
  ANONYMOUS volume       -> owner 1000:1000 inside, writable   OK
  BIND mount             -> host's own ownership, unchanged    depends on the host directory

`.gitignore:307` is the blanket `*.png`. `git check-ignore -v` on every candidate PNG path:
  site/assets/images/favicon.png   ignored by .gitignore:207 `/site`           survives
  .planning/ui-reviews/**/*.png    ignored by .planning/ui-reviews/.gitignore  survives
  .playwright-mcp/*.png            ignored by .gitignore:310 `.playwright-mcp/` survives
  test-results/*.png               ignored by .gitignore:307 ONLY              does NOT survive
`test-results` is pytest-playwright's default artifact directory and also holds `.webm` and
`.zip`, so the replacement is the directory, not a narrower glob. No tracked `.png` exists.

Source constant for the port expectation: `src/saneless/config.py:477` `web_port: int = 8080` on
`OutputConfig`. `saneless.toml.example:11` currently reads `web_port = 8081  # change if 8080 is
taken`; lines 12-13 already show the commented-with-guidance form (`# tmp_dir = ...`,
`# log_level = "INFO"  # DEBUG, INFO, ...`).

Current Dockerfile shape: `FROM python:3.14-slim AS builder`,
`COPY --from=ghcr.io/astral-sh/uv:latest /uv /usr/local/bin/uv`, `WORKDIR /app`, `COPY . .`,
`RUN uv build --wheel --out-dir /dist`; then the runtime stage with apt, a pip install of the
wheel, `ENV SANELESS_OUTPUT__DATA_DIR=/var/lib/saneless`, `VOLUME ["/var/lib/saneless"]`,
`EXPOSE 8080`, a `HEALTHCHECK` curling `http://localhost:8080/health`, `ENTRYPOINT ["saneless"]`,
`CMD ["serve"]`. The comment above the `ENV` line says "The container runs as root" — that
sentence becomes FALSE in this plan and must be rewritten, not appended to.

Harness helpers to reuse: `REPO_ROOT`, `_read`, `_numbered`, `_is_comment`, `_doc_pages()`,
`_deployment_files()`, and the offender-list failure-message convention.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The `.dockerignore` allow-list and the `.gitignore` PNG replacement</name>
  <files>.dockerignore, .gitignore, tests/test_deployment_config.py</files>
  <read_first>
    - .gitignore lines 30-40 (the `dist/` entry) and 200-215 and 300-315 (the `/site` entry, the
      blanket `*.png`, and the bare-directory tail style)
    - tests/test_deployment_config.py — the module constants block, `_read`, `_numbered`,
      `_is_comment`, and the Phase 31 section banner added in Plan 02
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md § 6.4 and
      § 6.5 in full
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
      § "`.gitignore` (config, declarative)", § "No Analog Found" (this repo has never had a
      `.dockerignore`), and § "Shared Patterns" 1
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md D-29, D-30,
      D-33, D-51, D-52, D-53
  </read_first>
  <behavior>
    New tests in `tests/test_deployment_config.py`, Phase 31 section:

    - `test_dockerignore_starts_with_a_deny_everything_line`: the first non-comment, non-blank line
      of `.dockerignore` is exactly `*`. This is the allow-list's whole premise; if it stops being
      first, every subsequent `!` line becomes decoration.
    - `test_dockerignore_never_reincludes_a_secret_or_test_path`: no `!`-prefixed line re-includes
      any of `saneless.toml`, `config`, `config.toml`, `.planning`, `tests`, `.env`, or `.git` —
      offender list with `file:line`. Nothing else in CI would notice an allow-list being weakened
      (D-30), and the exclusion of `.planning/` is what makes the D-42 audit artifact exempt by
      construction.
    - `test_dockerignore_reincludes_everything_the_wheel_build_needs`: `!src/`, `!pyproject.toml`,
      `!README.md` and `!LICENSE` are all present. The docstring must name the two live traps:
      `readme = "README.md"` and `license-files = ["LICENSE"]` each make `uv build --wheel` fail if
      excluded.
    - `test_gitignore_has_no_blanket_png_glob`: no line in `.gitignore`, after stripping comments,
      equals `*.png` (DLVR-09).
    - `test_gitignore_ignores_the_playwright_artifact_directory`: `/test-results/` is present —
      the only path that actually depended on the blanket glob.

    Use `_is_comment`-style filtering rather than a bare `grep -c` equivalent, so a comment
    mentioning `*.png` cannot make the test self-invalidating.
  </behavior>
  <action>
    Create `.dockerignore` with the measured five-entry allow-list from `<interfaces>`: `*` on the
    first non-comment line, then `!src/`, `!pyproject.toml`, `!uv.lock`, `!README.md`, `!LICENSE`.
    Do not add `!src/**` — `!src/` already re-includes the directory and everything beneath it.
    Do not re-include `tests/`, `config/`, `saneless.toml`, `.planning/`, `.env` or `.git`.

    This repo has never had a `.dockerignore`, so there is no in-file comment style to copy — use
    the house rationale-comment form from `docker-compose.yml` and `Dockerfile`: a header comment
    saying this is an allow-list, that `*` must stay first, that it is one of TWO independent gates
    (the other being the explicit `COPY` lines in Task 2) so a mistake in either alone cannot leak a
    real `config.toml` with a live Paperless token into a layer, and that `README.md` and `LICENSE`
    are not optional because the wheel build reads them. Cite D-29, D-30 and DLVR-06.

    Edit `.gitignore`: delete the blanket `*.png` line and add `/test-results/` in the
    bare-directory style the tail of the file already uses, near `.playwright-mcp/`. Per
    `git check-ignore -v` on every candidate, `test-results/` is the only path that depended on the
    glob, and it holds `.webm` and `.zip` as well as `.png`, so the directory is the correct
    replacement rather than a narrower glob. There are no tracked `.png` files anywhere in the
    repo, so nothing becomes newly tracked.

    Add the five tests from `<behavior>` with a `DOCKERIGNORE = REPO_ROOT / ".dockerignore"` and
    `GITIGNORE = REPO_ROOT / ".gitignore"` module constant.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -q -k "dockerignore or gitignore" && git status --porcelain && uv run pytest -m "not browser and not sane_hardware" -q</automated>
  </verify>
  <acceptance_criteria>
    - The first non-comment, non-blank line of `.dockerignore` is exactly `*`
    - `command grep -c '^!' .dockerignore` returns 5 and none of those lines mentions `tests`,
      `config`, `saneless.toml` or `.planning`
    - `command grep -v '^#' .gitignore | command grep -cx '\*\.png'` returns 0
    - `command grep -cx '/test-results/' .gitignore` returns 1
    - `git check-ignore -v test-results/foo.png` names the `/test-results/` line
    - `git status --porcelain` shows no newly-untracked PNG appearing as a result of the change
    - `uv run pytest tests/test_deployment_config.py -q -k "dockerignore or gitignore"` passes with
      5 tests
    - ruff, ruff format --check, ty and pyrefly all clean with no suppressions
  </acceptance_criteria>
  <done>The Docker build context is an allow-list that cannot carry a real config, a secret, `.planning/` or tests, and `.gitignore` names paths instead of blanket-ignoring an extension — both pinned by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restructure the Dockerfile — digest pins, explicit COPY, UID 1000, WORKDIR</name>
  <files>Dockerfile, tests/test_deployment_config.py</files>
  <read_first>
    - Dockerfile in full, including the rationale comments at lines 19-29 (especially the one
      beginning "The container runs as root", which this task makes false)
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md § 6.1, § 6.2,
      § 6.3 in full, and Pitfalls 4 and 5
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md D-25, D-26,
      D-27 (and D-27 AMENDED), D-28, D-29, and the carried-forward Phase 27 D-09 note that the
      `./config:/etc/saneless` mount is read-write and must not break
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
      § "`Dockerfile` (config/build)"
    - .github/dependabot.yml as rewritten in Plan 04 (the `docker` ecosystem entry that will
      maintain these pins)
  </read_first>
  <behavior>
    New tests in `tests/test_deployment_config.py`, Phase 31 section, all static text assertions
    over `Dockerfile` with `DOCKERFILE = REPO_ROOT / "Dockerfile"`:

    - `test_dockerfile_pins_every_base_image_by_digest`: every `FROM` line's image reference
      matches `<name>:<tag>@sha256:<64-hex>` — tag present as well as digest, because Dependabot
      needs the tag to know what to bump to.
    - `test_dockerfile_gets_uv_from_a_named_from_stage`: there is a `FROM ghcr.io/astral-sh/uv...
      AS uv` line and no `COPY --from=ghcr.io/` line anywhere. The docstring must say why:
      Dependabot's Docker parser matches `FROM` lines only and deliberately skips `COPY --from=`,
      so a digest written on a `COPY --from=` line would never be updated.
    - `test_dockerfile_copies_only_the_wheel_build_inputs`: there is no `COPY . .` line; the
      builder stage copies `pyproject.toml`, `uv.lock`, `README.md`, `LICENSE` and `src`
      explicitly. This is the second of the two independent build-context gates.
    - `test_dockerfile_runs_as_a_non_root_user`: a `USER` instruction is present and its argument
      is not `root` or `0`; `groupadd` / `useradd` with `1000` appear.
    - `test_dockerfile_chowns_the_data_dir_before_declaring_the_volume`: the line index of the
      `chown` of `/var/lib/saneless` is strictly less than the line index of the `VOLUME`
      instruction. The docstring must record that Docker discards build steps that change data
      within a declared volume path AFTER the `VOLUME` instruction, that buildah tolerates the
      wrong order, and that a local podman build is therefore NOT evidence the ordering is safe.
    - `test_dockerfile_sets_a_workdir_in_the_runtime_stage`: `WORKDIR /var/lib/saneless` appears
      after the runtime stage's `FROM`, so a stray relative write lands in the durable data
      directory rather than `/` (N-26).
  </behavior>
  <action>
    Re-resolve both image digests before writing them — those in `<interfaces>` are dated
    2026-09-18 and drift. Resolve `python:3.14-slim` via the Docker Hub v2 API and
    `ghcr.io/astral-sh/uv` for a pinned version (not `latest`), and keep the tag in each reference
    with a matching version comment in `ci.yml`'s convention.

    Restructure `Dockerfile`:
    - Add a first stage `FROM ghcr.io/astral-sh/uv:<version>@sha256:<digest> AS uv` and change the
      builder's copy to `COPY --from=uv /uv /usr/local/bin/uv`. This is not cosmetic: today's
      `COPY --from=ghcr.io/astral-sh/uv:latest` pulls an executable into the build from a MUTABLE
      tag, and Dependabot cannot see a `COPY --from=` line at all. Comment both reasons.
    - Digest-pin both `python:3.14-slim` references (builder and runtime) with the tag retained and
      a version comment.
    - Replace `COPY . .` in the builder stage with `COPY pyproject.toml uv.lock README.md LICENSE
      ./` and `COPY src ./src`. Comment that this is the second of two independent gates alongside
      `.dockerignore`, so a mistake in either alone cannot leak a real `config.toml`.
    - In the runtime stage, AFTER the apt step and the pip install of the wheel (both of which must
      stay as root, before any `USER`), add a single `RUN` that does `groupadd --gid 1000 saneless`,
      `useradd --uid 1000 --gid 1000 --no-create-home --shell /usr/sbin/nologin saneless`,
      `mkdir -p /var/lib/saneless` and `chown 1000:1000 /var/lib/saneless`. Fixed UID/GID 1000 is
      D-25: on the overwhelmingly common single-user Linux host the operator's own `./config`
      directory is already `1000:1000`, so Phase 27 D-09's read-write bind mount just works with no
      `chown`. Comment why 10001 and a PUID/PGID entrypoint were both rejected (a high UID can never
      match, so every deployment would need a fix-up on the happy path; a PUID entrypoint still
      starts as root, which is the thing DLVR-07 exists to stop).
    - Keep that `RUN` strictly BEFORE the existing `VOLUME ["/var/lib/saneless"]`. Comment that
      named and anonymous volumes inherit the image directory's ownership on first use (measured)
      while bind mounts do not — which is why D-26 handles non-1000 operators with documentation
      instead of a runtime fix-up — and that a local podman/buildah build tolerating the wrong order
      is not evidence Docker BuildKit will.
    - Add `WORKDIR /var/lib/saneless` and `USER saneless` after `VOLUME`, before `EXPOSE`.
    - Rewrite the comment above `ENV SANELESS_OUTPUT__DATA_DIR=/var/lib/saneless`. Its current first
      sentence, "The container runs as root, so an unset data_dir would resolve to
      /root/.local/state/saneless", is now false. Rewrite it to say the app runs as UID 1000 and the
      baked path plus the `WORKDIR` keep both an unset `data_dir` and any stray relative write inside
      the declared volume. Do not merely append to it.
    - Leave `EXPOSE 8080`, the `HEALTHCHECK` curling `http://localhost:8080/health`,
      `ENTRYPOINT ["saneless"]` and `CMD ["serve"]` alone. `curl` to `localhost:8080` needs no
      privilege and 8080 is above 1024, so the non-root process binds it fine. The `HEALTHCHECK`
      keeps calling `/health` and never `doctor` — doctor does network I/O and would flap.
      `CMD ["serve"]` already selects Plan 03's streaming logging mode, which is why DLVR-04 needs
      no Dockerfile change at all.

    Then build and run the image locally and show the output: build it, run
    `--entrypoint id` on it and confirm `uid=1000`, and run it with `--version` to confirm the
    entrypoint still works. On this host `docker` is podman/buildah with SELinux enforcing, so any
    bind-mount check needs `--userns=keep-id:uid=1000,gid=1000` and a `:Z` suffix — that is a
    property of the verification host, not of the image.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -q -k dockerfile && docker build -t saneless-local-verify . && docker run --rm --entrypoint id saneless-local-verify | command grep -q 'uid=1000'</automated>
  </verify>
  <acceptance_criteria>
    - `docker run --rm --entrypoint id saneless-local-verify` prints a line containing `uid=1000`
      and `gid=1000`
    - `docker run --rm saneless-local-verify --version` prints `saneless, version 0.2.0`
    - `command grep -c 'COPY \. \.' Dockerfile` returns 0
    - `command grep -c 'COPY --from=ghcr.io/' Dockerfile` returns 0 and
      `command grep -c 'AS uv' Dockerfile` returns 1
    - `command grep -c ':latest' Dockerfile` returns 0
    - Every `FROM` line's image reference matches `:.*@sha256:[0-9a-f]{64}`
    - `command grep -n 'chown' Dockerfile` reports a lower line number than
      `command grep -n 'VOLUME' Dockerfile`
    - `command grep -c 'WORKDIR /var/lib/saneless' Dockerfile` returns 1 and
      `command grep -c 'USER saneless' Dockerfile` returns 1
    - `command grep -c 'The container runs as root' Dockerfile` returns 0
    - `command grep -c 'EXPOSE 8080' Dockerfile` returns 1 and the `HEALTHCHECK` line still
      contains `localhost:8080/health`
    - `uv run pytest tests/test_deployment_config.py -q -k dockerfile` passes with 6 tests
  </acceptance_criteria>
  <done>The image builds from digest-pinned bases via a Dependabot-visible `FROM ... AS uv` stage, copies only the wheel build's inputs, runs as UID 1000 from a `WORKDIR` inside the durable data dir, and `docker run --entrypoint id` proves it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: One port everywhere, and the non-1000 operator's escape hatch</name>
  <files>saneless.toml.example, docker-compose.yml, docs/reference/docker.md, docs/how-to/deploy-docker-compose.md, tests/test_deployment_config.py</files>
  <read_first>
    - saneless.toml.example lines 1-25 (the `[output]` block, including the commented-with-guidance
      form at lines 12-13 and `source = "Flatbed"` at line 16)
    - src/saneless/config.py line 477 — `web_port: int = 8080` on `OutputConfig`
    - docker-compose.yml in full — its rationale-comment personality, the `volumes:` block with the
      `./config:/etc/saneless` explanation, and the commented-out paperless env block
    - docs/reference/docker.md lines 100-120 — the env-var reference table and the
      `SANELESS_OUTPUT__WEB_PORT` row (row 21's second half), plus the `DATA_DIR` and `TZ` rows for
      the house caveat style
    - docs/how-to/deploy-docker-compose.md — where the UID guidance belongs
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md D-26, D-31,
      D-32, and the carried-forward Phase 27 D-09 mount note
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
      § "`saneless.toml.example`", § "`docker-compose.yml`", § "Reference-table row convention"
  </read_first>
  <behavior>
    New tests in `tests/test_deployment_config.py`, Phase 31 section:

    - `test_the_example_config_does_not_ship_a_live_web_port`: the `web_port` line in
      `saneless.toml.example` is commented out. That live `web_port = 8081` is row 21's trap — a
      shipped example setting a port that disagrees with `EXPOSE` and `HEALTHCHECK`.
    - `test_every_documented_container_port_matches_the_model_default`: derive the expected port
      from `OutputConfig.model_fields["web_port"].default` by importing the model, then assert
      `Dockerfile`'s `EXPOSE` line and its `HEALTHCHECK` URL both carry it, and that no deployment
      file or doc page mentions a different container-side port in an `EXPOSE`, a `HEALTHCHECK`, or
      a `-p <host>:<container>` mapping's container half. Offender list with `file:line`. Derived
      from the model rather than hard-coded so a future default change cannot leave the image and
      the docs disagreeing.
    - `test_compose_carries_a_commented_user_override`: `docker-compose.yml` contains a commented
      `user:` line mentioning `1000`, and it is commented — a live `user: "${UID:-1000}:..."` line
      was rejected because compose does not populate `${UID}` unless it is exported, so it would
      silently fall back.
    - `test_the_uid_is_documented_where_operators_will_look`: both `docs/reference/docker.md` and
      `docs/how-to/deploy-docker-compose.md` state the UID `1000` and give the
      `chown -R 1000:1000 ./config` command.
  </behavior>
  <action>
    In `saneless.toml.example`, replace the live line `web_port = 8081          # change if 8080 is
    taken` with the commented form `# web_port = 8080  # bare metal only; in Docker remap with -p`,
    so it joins the commented pair on the two lines below it (D-32).

    In `docs/reference/docker.md`, rewrite the `SANELESS_OUTPUT__WEB_PORT` row of the env-var table
    in the house caveat style — bold the surprising part, then one sentence of consequence: the
    container's port is **fixed at 8080**; `web_port` is a bare-metal setting, and in Docker you
    remap on the host with `-p 8888:8080`. D-31 is explicit that no shell-form `HEALTHCHECK` env
    expansion is used: it would work for the environment variable but silently not for a `web_port`
    set in `config.toml`, replacing one false claim with a half-true one.

    In `docker-compose.yml`, add a commented `# user: "1000:1000"` line to the saneless service with
    a multi-sentence rationale comment above it in the file's established style: the image already
    runs as UID/GID 1000, so on a single-user Linux host whose `./config` is `1000:1000` this needs
    no action; uncomment and edit it only if your own UID differs, and remember that the
    `./config:/etc/saneless` mount is deliberately read-write because saneless rewrites profiles by
    renaming a temp file over `config.toml`. Do NOT write a live `user: "${UID}:${GID}"` line —
    compose does not populate `${UID}` unless it is exported and it would silently fall back.

    Add the D-26 operator guidance to both `docs/reference/docker.md` and
    `docs/how-to/deploy-docker-compose.md`: state that the container runs as UID/GID 1000, and give
    `chown -R 1000:1000 ./config` for operators whose UID differs. Keep it to a short paragraph on
    each page; this is documentation plus a commented compose line, not a feature.

    Add the four tests from `<behavior>`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -q -k "web_port or port or user_override or uid" && uv run mkdocs build --strict && uv run pytest -m "not browser and not sane_hardware" -q</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c '^web_port' saneless.toml.example` returns 0 and
      `command grep -c '# web_port = 8080' saneless.toml.example` returns 1
    - `command grep -c '8081' saneless.toml.example` returns 0
    - `command grep -c '# user:' docker-compose.yml` returns 1 and
      `command grep -c '^\s*user:' docker-compose.yml` returns 0
    - `command grep -c 'chown -R 1000:1000' docs/reference/docker.md` returns at least 1 and the
      same for `docs/how-to/deploy-docker-compose.md`
    - `docs/reference/docker.md`'s `SANELESS_OUTPUT__WEB_PORT` row contains both `8080` and
      `-p 8888:8080`
    - `uv run pytest tests/test_deployment_config.py -q -k "web_port or uid"` passes with 4 tests
    - `uv run mkdocs build --strict` exits 0
    - The Plan 02 naming guard still passes (no page re-introduces the old slug)
    - The full non-browser suite passes
  </acceptance_criteria>
  <done>The example config, `EXPOSE`, `HEALTHCHECK` and every doc page agree on 8080, and an operator whose UID is not 1000 finds the exact command and a commented compose line where they will look.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree -> Docker build context -> image layer | Everything the daemon receives is recoverable from the built image, including from a discarded builder stage in a local cache |
| container process -> host kernel / mounted host directories | A root process in the container has far more reach into a bind mount and into the kernel than UID 1000 |
| upstream registry (Docker Hub, GHCR) -> the build | A mutable tag can be repointed between builds; `ghcr.io/astral-sh/uv:latest` pulls an executable straight into the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Information disclosure | `COPY . .` sweeping a real `saneless.toml` with a live Paperless token into a build layer | mitigate | Two independent gates: an allow-list `.dockerignore` whose first non-comment line is `*` and which re-includes only `src/`, `pyproject.toml`, `uv.lock`, `README.md` and `LICENSE`; and explicit `COPY` lines replacing `COPY . .`. A mistake in either alone cannot leak the file. Pinned by `test_dockerignore_never_reincludes_a_secret_or_test_path` — nothing else in CI would notice the allow-list being weakened (D-30). No token-pattern hook is added: 40 hex characters is also a git SHA and it would fire on the very SHA pins and image digests this phase adds (D-52) |
| T-31-02 | Tampering | `ghcr.io/astral-sh/uv:latest` and both `python:3.14-slim` references | mitigate | All three digest-pinned with the tag retained, and uv promoted to a named `FROM ... AS uv` stage so Dependabot's `FROM`-only parser can see and bump it. Pinned by `test_dockerfile_pins_every_base_image_by_digest` and `test_dockerfile_gets_uv_from_a_named_from_stage` |
| T-31-08 | Elevation of privilege | the container process running as root | mitigate | Fixed UID/GID 1000 created in the runtime stage after the privileged apt and pip steps, with `USER saneless` before the entrypoint. No `--privileged`, no `/dev/bus/usb` passthrough. Proven by `docker run --rm --entrypoint id <img>` printing `uid=1000`, not by reading the Dockerfile |
| T-31-23 | Tampering | a stray relative write landing in `/` | mitigate | `WORKDIR /var/lib/saneless` anchors relative writes inside the declared volume, which is owned by 1000 and is the path `SANELESS_OUTPUT__DATA_DIR` already points at (N-26) |
| T-31-24 | Denial of service | volume ownership breaking the read-write config mount | mitigate | `mkdir` + `chown` strictly before `VOLUME`, so named and anonymous volumes inherit `1000:1000` (measured). Bind mounts do not inherit, so D-26 handles non-1000 operators with a documented `chown -R 1000:1000 ./config` and a commented compose `user:` line rather than a runtime fix-up that would have to start as root |
| T-31-53 | Information disclosure | the Paperless token already present in the working tree | accept | D-53, the user's call: the file is gitignored and was never committed; the exposure was the build context and the discarded builder stage only; the runtime stage copies only `/dist/*.whl`; and `release.yml` has never successfully run, so no image built from this tree was ever published. D-51 moves the file to `~/.config/saneless/config.toml`, which removes the vector structurally — that is a USER action, not phase work |
</threat_model>

<verification>
- `uv run pytest tests/test_deployment_config.py -q` — all four new contracts green
- `docker build -t saneless-local-verify .` then
  `docker run --rm --entrypoint id saneless-local-verify` (expect `uid=1000`) and
  `docker run --rm saneless-local-verify --version`
- `uv run mkdocs build --strict`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
- `uv run zizmor .` — still exit 0

Note for whoever runs the container checks: `docker` on this host is podman/buildah with SELinux
enforcing. Rootless podman maps host UID 1000 to container UID 0, so a bind-mount write check needs
`--userns=keep-id:uid=1000,gid=1000` and a `:Z` mount suffix. That is a property of the verification
host, not of the image; D-25 is correct for real Docker on a non-SELinux host.
</verification>

<success_criteria>
- `docker run --rm --entrypoint id <image>` prints `uid=1000` (DLVR-07, criterion 3)
- Every `FROM` reference is `tag@sha256:...` and uv arrives through a named `FROM` stage Dependabot
  can maintain (DLVR-07, criterion 3/4)
- `WORKDIR /var/lib/saneless` is set in the runtime stage (DLVR-07, criterion 3)
- `.dockerignore` is an allow-list starting with `*` that re-includes nothing secret, nothing under
  `.planning/` and no tests, and `COPY . .` is gone (DLVR-06, criterion 3)
- The example config, `EXPOSE`, `HEALTHCHECK` and every doc page agree on 8080 (DLVR-05, criterion 3)
- `.gitignore` has no `*.png` glob and ignores `/test-results/` instead (DLVR-09, criterion 4)
</success_criteria>

<output>
Create `.planning/phases/31-delivery-identity-and-documentation-accuracy/31-05-SUMMARY.md` when done
</output>
