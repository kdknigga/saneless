---
phase: 31-delivery-identity-and-documentation-accuracy
plan: 04
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - .github/workflows/docs.yml
  - .github/dependabot.yml
  - .pre-commit-config.yaml
autonomous: true
requirements: [CI-02, DLVR-02, DLVR-03]

must_haves:
  truths:
    - "`uv run zizmor .` exits 0 — no unpinned action, no persisted credential, no unscoped job, no cache-poisoning vector, no missing cooldown (criterion 4)"
    - "Every `uses:` in every workflow is a full 40-hex SHA with a version comment (criterion 4)"
    - "Every job in every workflow has a `permissions:` block (criterion 4)"
    - "Dependabot maintains both the GitHub Actions pins and the Docker digest pins (criterion 4)"
    - "The naming guard runs in CI, so CI fails if the old slug reappears (criterion 1)"
    - "A pre-release tag can never leave GHCR's `latest` pointing at it (criterion 2)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "workflow_call trigger, per-job permissions, persist-credentials:false, the zizmor step"
      contains: "workflow_call"
    - path: ".github/workflows/release.yml"
      provides: "reusable CI gate, environment-routed OIDC publish with attestations, GHCR publish with provenance"
      contains: "attestations: true"
    - path: ".github/workflows/docs.yml"
      provides: "master trigger, uv-based strict mkdocs build, Pages-artifact deploy"
      contains: "upload-pages-artifact"
    - path: ".github/dependabot.yml"
      provides: "github-actions and docker ecosystems, each with a cooldown block"
      contains: "package-ecosystem: \"docker\""
    - path: ".pre-commit-config.yaml"
      provides: "the zizmor hook and the doc-truth harness push hook"
      contains: "zizmor"
  key_links:
    - from: ".github/workflows/release.yml"
      to: ".github/workflows/ci.yml"
      via: "a reusable-workflow call, so the release gate cannot drift from the CI gate"
      pattern: "/.github/workflows/ci.yml"
    - from: ".github/workflows/ci.yml lint job"
      to: "zizmor"
      via: "uv run zizmor ."
      pattern: "uv run zizmor"
---

<objective>
Bring all three workflows to the supply-chain posture DLVR-03 requires and make the release
pipeline actually capable of running: SHA pins everywhere, a `permissions:` block on every job,
`persist-credentials: false` on every checkout, zizmor as a blocking lint step, Dependabot
covering both ecosystems, a reusable CI gate on releases, environment-routed OIDC publishing with
attestations, and a Pages-artifact docs deploy that triggers on the branch this repo actually has.

Purpose: `zizmor --no-online-audits .` reports 23 findings and exits 14 today. `release.yml`'s
test job is broken three independent ways. `docs.yml` triggers on `main` while this repo's branch
is `master`, which is why the docs site has never deployed and why every README deep link 404s.

Output: `uv run zizmor .` exits 0, and `release.yml` / `docs.yml` are workflows that could
actually run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-01-SUMMARY.md

<interfaces>
The 23 zizmor findings to clear (baseline measured 2026-09-18, zizmor 1.30.1, `regular` persona):

  unpinned-uses        11  docs.yml:14,15; release.yml:11,12,25,26,28,37,38,43,50
  artipacked            7  ci.yml:22,39,61; docs.yml:14; release.yml:11,25,37
  excessive-permissions 2  release.yml workflow level and its `test` job
  cache-poisoning       2  release.yml:12,26 (setup-uv on a tag-triggered workflow)
  dependabot-cooldown   1  dependabot.yml:4

Action SHAs resolved 2026-09-18 — a STARTING POINT, not gospel. RESEARCH assumption A7 requires
re-resolving every one at implementation time:

  actions/checkout               v7.0.1  3d3c42e5aac5ba805825da76410c181273ba90b1
  astral-sh/setup-uv            v10.1.0  bec219d24cd3e171d82865faccec33120bb574f4
  pypa/gh-action-pypi-publish   v1.14.2  dc37677b2e1c63e2034f94d8a5b11f265b73ba33
  docker/login-action            v4.6.0  dbcb813823bdd20940b903addbd779551569679f
  docker/metadata-action         v6.2.0  dc802804100637a589fabce1cb79ff13a1411302
  docker/build-push-action       v7.4.0  c3c9e263c25d99ce0380d002d59b67737d91b0dc
  actions/upload-pages-artifact  v5.0.0  fc324d3547104276b827a68afc52ff2a11cc49c9
  actions/deploy-pages           v5.0.1  368f82528645a54fb793d4d04e342629a3f51346

`pypa/gh-action-pypi-publish` uses ANNOTATED tags: `GET /git/ref/tags/<tag>` returns
`type: "tag"`, and the tag-object SHA does NOT resolve as an action ref. Dereference with
`GET /git/tags/<sha>` to the commit SHA. `@v1.12` does not exist at all (404).

`ci.yml`'s existing style canon — match it exactly:
  header comment `# SHAs resolved <date> via gh api; comments must carry the full version.`
  `uses: owner/repo@<40-hex> # vX.Y.Z`
  jobs `lint` / `test` / `browser`, each `runs-on: ubuntu-latest` with `timeout-minutes:`
  steps: checkout -> apt libsane-dev -> setup-uv -> `uv sync --locked` -> the checks
  lint's check list today: `uv run ruff check .`, `uv run ruff format --check .`,
                           `uv run ty check`, `uv run pyrefly check src tests`
  top-level `on: push: branches: [master]` + `pull_request:`, `permissions: contents: read`,
  `concurrency: group: ${{ github.workflow }}-${{ github.ref }}`,
  `cancel-in-progress: ${{ github.event_name == 'pull_request' }}`

`.pre-commit-config.yaml`: `default_install_hook_types: [pre-commit, pre-merge-commit, pre-push]`
(line 4), `default_stages: [pre-commit]` (line 23), and a `repo: local` block at line 93 whose six
hooks all use `language: system`, `pass_filenames: false`, `always_run: true`; the push-stage
variants carry `stages: [pre-merge-commit, pre-push]`.

`.github/dependabot.yml` in full today: `version: 2` and one `updates:` entry for
`github-actions` at `directory: "/"` with a block-style `schedule: interval: "weekly"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-resolve the pins, make `ci.yml` callable and clean, and wire zizmor into CI and prek</name>
  <files>.github/workflows/ci.yml, .pre-commit-config.yaml</files>
  <read_first>
    - .github/workflows/ci.yml in full (header comment, `on:`, `permissions:`, `concurrency:`,
      and all three jobs)
    - .pre-commit-config.yaml in full, especially the `repo: local` block at line 93 with its
      block-level convention comment at 94-102 and the push-stage hook shape at 143-150
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md § 3 in full
      (baseline, the 23 findings, exit-code semantics, collection scope, prek hook shape) and § 4
      "`ci.yml` — becomes callable" including the five reusable-workflow gotchas
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
      § "`.github/workflows/ci.yml` — THE workflow style canon" and § "`.pre-commit-config.yaml`"
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md D-08
      CORRECTED, D-14, D-15
  </read_first>
  <action>
    First, re-resolve all eight action SHAs listed in `<interfaces>` against `api.github.com`
    before writing a single pin. RESEARCH's SHAs are dated 2026-09-18 and are a starting point
    (assumption A7). For `pypa/gh-action-pypi-publish` follow the annotated-tag dereference:
    `GET /git/ref/tags/<tag>` returning `type: "tag"` must be followed by `GET /git/tags/<sha>` to
    reach the commit SHA; pinning the tag-object SHA does not resolve. Record the resolved table in
    the plan summary and update each workflow's header comment to
    `# SHAs resolved <today> via gh api; comments must carry the full version.`

    Edit `.github/workflows/ci.yml`:
    - Add `workflow_call:` to the `on:` block, beside the existing `push: branches: [master]` and
      `pull_request:`. Add a comment saying `release.yml` calls this workflow so the release gate
      and the CI gate are one definition of green (D-16).
    - Add `permissions: contents: read` to EVERY job (`lint`, `test`, `browser`), not only at
      workflow level. A called workflow's `GITHUB_TOKEN` permissions can only be the same or more
      restrictive than the calling job's, so the callee must declare `contents: read` explicitly.
    - Add `with: persist-credentials: false` to all three `actions/checkout` steps (lines 22, 39,
      61). This clears the three `artipacked` findings. Note in a comment that checkout still
      produces a full `.git`, so the Plan 02 naming guard's `git ls-files` still works in CI.
    - Add `- run: uv run zizmor .` as the last check in the `lint` job's step list, in the same
      bare shape as its four neighbours. No `continue-on-error`, no `--min-severity`, no
      `--min-confidence`, no `|| true`, no `--persona` flag: the default persona is `regular`,
      findings exit 14 and a clean run exits 0, so a bare `run:` already fails the build on any
      finding (D-15). `# zizmor: ignore` is forbidden — findings are fixed, not suppressed.
    - Record in a comment that `ci.yml`'s `concurrency.group` resolves to the CALLER's workflow
      name under `workflow_call` (so a release run groups as `Release-refs/tags/vX.Y.Z`) and that
      `cancel-in-progress` evaluates false there. Also record that the `browser` job becomes part
      of the release gate, costing a Chromium download per release — that is D-16's intent, not a
      surprise.
    - `ci.yml` uses no secrets, so no `secrets: inherit` plumbing is needed anywhere.

    Edit `.pre-commit-config.yaml`, adding two hooks to the existing `repo: local` block, matching
    its `language: system` / `pass_filenames: false` / `always_run: true` shape:
    - `id: zizmor`, `name: zizmor (workflow audit)`, `entry: uv run zizmor .`, at the block's
      default commit stage. It runs in well under a second on five files and never rewrites a file.
    - `id: doc-truth-guard`, `name: doc-truth harness (naming guard, deployment claims)`,
      `entry: uv run pytest tests/test_deployment_config.py -q`,
      `stages: [pre-merge-commit, pre-push]`. This is D-08's third enforcement point, which
      RESEARCH found does not exist today (there is no pytest hook anywhere in this file). Scope it
      to the doc-truth harness rather than the whole suite: it is exactly the file the naming guard
      lives in and it costs about two seconds instead of twenty-seven. Write a comment saying so,
      and saying that the guard's other two enforcement points are CI and a plain `uv run pytest`.
      Do NOT restate D-08's original "three enforcement points" claim as though it were already
      true before this change.

    Zizmor will still report findings against `release.yml`, `docs.yml` and `dependabot.yml` after
    this task — Tasks 2 and 3 clear those. The gate for THIS task is that zizmor reports nothing
    against `ci.yml`.
  </action>
  <verify>
    <automated>uv run zizmor . 2>&1 | tee /tmp/zizmor-t1.txt; test "$(command grep -c 'ci\.yml' /tmp/zizmor-t1.txt)" = "0" && uv run prek run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - `uv run zizmor . 2>&1 | command grep -c 'ci\.yml'` returns 0 (no finding cites `ci.yml`)
    - `command grep -c 'workflow_call' .github/workflows/ci.yml` returns 1
    - `command grep -c 'persist-credentials: false' .github/workflows/ci.yml` returns 3
    - `command grep -c 'permissions:' .github/workflows/ci.yml` returns 4 (workflow level plus
      three jobs)
    - `command grep -c 'uv run zizmor .' .github/workflows/ci.yml` returns 1 and the line carries
      no `||`, no `continue-on-error`, no `--min-` flag
    - `command grep -c 'zizmor: ignore' .github/workflows/ci.yml` returns 0
    - `command grep -c 'zizmor' .pre-commit-config.yaml` is at least 2 and
      `command grep -c 'test_deployment_config' .pre-commit-config.yaml` returns 1
    - `uv run prek run --all-files` passes and `uv run prek run --stage pre-push --all-files` passes
    - Every `uses:` line in `ci.yml` still matches `@[0-9a-f]{40} # v` and the header comment
      carries today's resolution date
  </acceptance_criteria>
  <done>`ci.yml` is callable, fully scoped, credential-free and zizmor-clean; zizmor and the doc-truth harness both run from prek.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite `release.yml` — reusable CI gate, routed OIDC publish, provenanced image</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml in full (it is pre-Phase-20: floating major tags, unlocked
      `uv sync`, a bare `uv run pytest`, no `permissions:` on `test`, and an unconditional
      `type=raw,value=latest`)
    - .github/workflows/ci.yml as just edited — the style canon this file is brought up to
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md § 4 in full,
      especially "self-repository call syntax", "Pre-release routing and `latest` gating",
      "One job or two?", "`publish-docker` — provenance and permissions", and Pitfalls 1, 2, 3
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md D-16..D-23 and
      the "Verify-at-implementation (not settled)" block
  </read_first>
  <action>
    **First, confirm the self-repository call syntax before writing it.** zizmor 1.30.1 raises
    `self-repository` at High confidence against `uses: ./.github/workflows/ci.yml` and demands
    `uses: $/.github/workflows/ci.yml`, citing a GitHub changelog dated 2026-07-30 and runner
    >= 2.336.0. GitHub's own reusable-workflows documentation still shows only the `./` form, so
    this is UNCONFIRMED. Check GitHub's current documentation and that changelog entry. If the `$/`
    form is confirmed, use it. If it is NOT confirmed, write `./` and STOP — do not suppress the
    zizmor finding (`# zizmor: ignore` is forbidden by D-15 and by CLAUDE.md) and do not weaken the
    audit; escalate to the user that zizmor's rule and GitHub's documented syntax are in conflict
    and let them choose. Record which form you used and the evidence in the plan summary.

    Rewrite `.github/workflows/release.yml` with the header comment carrying today's SHA resolution
    date and the `ci.yml` conventions throughout (`uses: owner/repo@<40-hex> # vX.Y.Z`,
    `runs-on: ubuntu-latest`, `timeout-minutes:`, a `permissions:` block on every job, prose
    comments on every non-obvious choice):

    - Keep the existing tag trigger. Add a workflow-level `permissions: contents: read`, which
      clears one of the two `excessive-permissions` findings.
    - Replace the entire hand-written `test` job with a single `ci` job that calls
      `<confirmed-form>/.github/workflows/ci.yml` with `permissions: contents: read`. The current
      job is broken three independent ways — no `libsane-dev` so `uv sync` cannot build
      `python-sane`, an unlocked `uv sync`, and a bare `uv run pytest` that collects browser tests
      with no Chromium — and D-16 wants one definition of green so the release gate can never drift
      from the CI gate. Note in a comment that this brings the `browser` job into the release path.
    - `publish-pypi`: `needs: ci`, with `environment:` in OBJECT form —
      `environment:` then `name: ${{ contains(github.ref_name, '-') && 'testpypi' || 'pypi' }}`.
      The string form `environment: ${{ ... }}` fails with `Unrecognized named-value` (Pitfall 3).
      This is D-17's one code path and D-18's two environments in one job; the manual approval rule
      on the `pypi` environment gates only the runs where the expression resolves to `pypi`.
      `permissions: contents: read` plus `id-token: write`.
      Steps: checkout with `persist-credentials: false`; `astral-sh/setup-uv` with
      `enable-cache: false` (this clears the `cache-poisoning` finding, which fires because the
      workflow is tag-triggered and publishing); a step with `id: index` that writes
      `url=https://test.pypi.org/legacy/` to `$GITHUB_OUTPUT` when `$GITHUB_REF_NAME` contains a
      hyphen and `url=https://upload.pypi.org/legacy/` otherwise; `uv build`; then
      `pypa/gh-action-pypi-publish` with `attestations: true` and
      `repository-url: ${{ steps.index.outputs.url }}`. Always pass a non-empty explicit URL —
      whether the action treats an empty `repository-url` as "use the default" is unverified
      (assumption A4) and an explicit value removes the question.
    - `publish-docker`: `needs: ci`, with
      `permissions: contents: read, packages: write, id-token: write, attestations: write` — the
      last two are required by `provenance: true` and this exact set was verified zizmor-clean.
      Steps: checkout with `persist-credentials: false`; `docker/login-action` against `ghcr.io`
      with `${{ github.actor }}` and `${{ secrets.GITHUB_TOKEN }}`; `docker/metadata-action` with
      `images: ghcr.io/kdknigga/saneless` (the renamed owner — Plan 02 landed it); and
      `docker/build-push-action` with `push: true` and `provenance: true`.
    - **Delete the unconditional `type=raw,value=latest` line** from metadata-action's `tags:`,
      leaving only `type=semver,pattern={{version}}`. `docker/metadata-action`'s default
      `flavor: latest=auto` already skips `latest` for semver pre-releases, so deleting the line is
      the mechanism with the fewest moving parts. Do not re-add it with an `enable=` condition —
      that puts back the exact line D-23 exists to remove, now with a condition that has to stay
      correct. Write the rationale as a comment above `tags:`, naming D-23's hard constraint: a
      rehearsal must never leave GHCR's `latest`, or a plain `pip install saneless`, pointing at a
      pre-release.
    - Record in a comment that the pre-release half of that constraint is satisfied structurally on
      the PyPI side too: the RC only ever lands on TestPyPI and real PyPI stays empty until the
      final tag.
  </action>
  <verify>
    <automated>uv run zizmor . 2>&1 | tee /tmp/zizmor-t2.txt; test "$(command grep -c 'release\.yml' /tmp/zizmor-t2.txt)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run zizmor . 2>&1 | command grep -c 'release\.yml'` returns 0
    - `command grep -c 'type=raw,value=latest' .github/workflows/release.yml` returns 0
    - `command grep -c 'attestations: true' .github/workflows/release.yml` returns 1
    - `command grep -c 'provenance: true' .github/workflows/release.yml` returns 1
    - `command grep -c 'enable-cache: false' .github/workflows/release.yml` returns 1
    - `command grep -c 'persist-credentials: false' .github/workflows/release.yml` returns 2
    - `command grep -c '/.github/workflows/ci.yml' .github/workflows/release.yml` returns 1
    - `command grep -c 'ghcr.io/kdknigga/saneless' .github/workflows/release.yml` returns 1 and
      `command grep -c 'kris-knigga' .github/workflows/release.yml` returns 0
    - Every `uses:` line matches `@[0-9a-f]{40} # v`; no line contains `@v` as the ref
    - `environment:` appears in object form (`name:` on the following line), never as
      `environment: ${{`
    - The plan summary records which self-repository form was used and the documentation evidence
      for it
    - `python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))"` exits 0
  </acceptance_criteria>
  <done>`release.yml` gates on the reusable CI workflow, routes pre-release tags to TestPyPI and final tags to PyPI through their own environments, attaches attestations and provenance, and cannot tag a pre-release as `latest`.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite `docs.yml` to the Pages-artifact flow and extend Dependabot</name>
  <files>.github/workflows/docs.yml, .github/dependabot.yml</files>
  <read_first>
    - .github/workflows/docs.yml in full (`branches: [main]`, `permissions: contents: write`,
      `pip install mkdocs-material`, `mkdocs gh-deploy --force`, floating action tags)
    - .github/dependabot.yml in full
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md § 11.1 in
      full (the verified zero-finding docs workflow), § 4 "`dependabot.yml`", and Pitfalls 8 and 11
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md — the
      D-13 AMENDED and D-27 AMENDED blocks, both LOCKED
    - mkdocs.yml (to confirm `mkdocs build --strict` has a valid nav; it passes today)
  </read_first>
  <action>
    Rewrite `.github/workflows/docs.yml` as the Pages-artifact flow. This is the LOCKED D-13
    amendment, not a choice: `mkdocs gh-deploy` pushes using the credentials `actions/checkout`
    persists into `.git/config`, so `persist-credentials: false` breaks the deploy and omitting it
    is an `artipacked` finding — and suppression is forbidden. The artifact flow was measured at
    zero findings, exit 0.

    - `on: push: branches: [master]`. The current `main` is why the docs site has never deployed.
    - Workflow-level `permissions: contents: read` — `contents: write` disappears entirely, which
      is a real posture improvement.
    - `concurrency: group: pages` with `cancel-in-progress: false`.
    - A `build` job with `permissions: contents: read`: checkout with
      `persist-credentials: false`; `astral-sh/setup-uv`; `uv sync --locked --only-group dev
      --no-install-project`; `uv run mkdocs build --strict`; then `actions/upload-pages-artifact`
      with `path: site`. The `--only-group dev --no-install-project` flags are load-bearing: they
      install mkdocs-material without building `python-sane`, so this job needs NO `libsane-dev`
      apt step, unlike every `ci.yml` job. `--strict` is deliberate — it catches a broken nav or a
      dead internal link, and it passes today. Comment both choices.
    - A `deploy` job with `needs: build`, `environment: name: github-pages` and
      `url: ${{ steps.deployment.outputs.page_url }}`, `permissions: pages: write, id-token:
      write`, and a single `actions/deploy-pages` step with `id: deployment`.
    - Drop `actions/setup-python` entirely; the uv-based job replaces it.
    - Comment that this flow requires the repository setting **Settings -> Pages -> Source: GitHub
      Actions** (user blocker 5, amended — there is no `gh-pages` branch at all) and that GitHub
      auto-creates a third environment named `github-pages` beside D-18's `testpypi` and `pypi`.

    Rewrite `.github/dependabot.yml` with two `updates:` entries — the existing `github-actions`
    one and a new `docker` one, both at `directory: "/"`, both with a block-style
    `schedule: interval: "weekly"` matching the file's current style, and **both** carrying a
    `cooldown:` block with `default-days: 7`. Both are required: adding a second entry without a
    cooldown on each produces a second `dependabot-cooldown` finding that fails the new zizmor step
    (Pitfall 11).

    Add a comment recording the watch item: Dependabot's docker ecosystem also parses `image:` keys
    out of YAML manifests, so it may open PRs against `docker-compose.yml`'s
    `image: ghcr.io/kdknigga/saneless:latest` — our own image. If that happens the remedy is an
    `ignore:` entry for that dependency, not removing the ecosystem.

    This task's gate is the phase's real DLVR-03 gate: `uv run zizmor .` exits 0 with no findings
    at all.
  </action>
  <verify>
    <automated>uv run zizmor . && uv run mkdocs build --strict && uv run prek run --all-files</automated>
  </verify>
  <acceptance_criteria>
    - `uv run zizmor .` exits 0 and prints `No findings to report`
    - `command grep -c 'branches: \[master\]' .github/workflows/docs.yml` returns 1 and
      `command grep -c 'main' .github/workflows/docs.yml` returns 0
    - `command grep -c 'gh-deploy' .github/workflows/docs.yml` returns 0
    - `command grep -c 'contents: write' .github/workflows/docs.yml` returns 0
    - `command grep -c 'upload-pages-artifact' .github/workflows/docs.yml` returns 1 and
      `command grep -c 'deploy-pages' .github/workflows/docs.yml` returns 1
    - `command grep -c 'mkdocs build --strict' .github/workflows/docs.yml` returns 1
    - `command grep -c 'only-group dev --no-install-project' .github/workflows/docs.yml` returns 1
      and `command grep -c 'libsane-dev' .github/workflows/docs.yml` returns 0
    - `command grep -c 'package-ecosystem' .github/dependabot.yml` returns 2 and
      `command grep -c 'cooldown' .github/dependabot.yml` returns 2
    - `uv run mkdocs build --strict` exits 0
    - `uv run prek run --all-files` and `uv run prek run --stage pre-push --all-files` both pass
  </acceptance_criteria>
  <done>`uv run zizmor .` exits 0 across the whole repository; the docs workflow triggers on `master`, builds strictly with the pinned mkdocs, deploys via the Pages artifact, and needs no write token; Dependabot maintains both ecosystems.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party GitHub Action -> workflow runner with `GITHUB_TOKEN` | Every `uses:` executes attacker-controllable code inside a job that holds a token |
| untrusted ref (PR branch) -> Actions cache -> the publishing job | Cache entries written by a lower-privilege ref are readable by the tag-triggered publish |
| workflow OIDC identity -> PyPI / TestPyPI / GHCR | The identity that publishes irreversible, globally resolvable artefacts |
| repository tag -> automatic publish | Pushing a ref triggers an action whose result cannot be undone (a PyPI version number is never reusable) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Tampering / EoP | 11 unpinned `uses:` in `docs.yml` and `release.yml` | mitigate | Pin every `uses:` to a re-resolved full 40-hex commit SHA with a `# vX.Y.Z` comment (the `tj-actions/changed-files` class of attack), and keep `uv run zizmor .` in the `lint` job so `unpinned-uses` blocks any regression. Annotated tags are dereferenced to the commit SHA, not pinned as tag objects |
| T-31-03 | Information disclosure | `actions/checkout` persisting credentials (`artipacked`) | mitigate | `persist-credentials: false` on all seven checkouts across the three workflows; zizmor's `artipacked` rule keeps it that way. This is also why `mkdocs gh-deploy` had to go — it depends on the persisted credential |
| T-31-04 | Tampering | `astral-sh/setup-uv` cache in the tag-triggered publish job | mitigate | `enable-cache: false` on `setup-uv` in `release.yml`, so the publishing path never restores a cache a lower-privilege ref could have poisoned |
| T-31-05 | Elevation of privilege | over-broad `GITHUB_TOKEN` in the release and docs paths | mitigate | Workflow-level `permissions: contents: read` plus an explicit per-job block everywhere; `id-token: write` scoped only to the two publishing jobs; `packages: write` only to `publish-docker`; `pages: write` only to the deploy job. `docs.yml` loses `contents: write` entirely |
| T-31-06 | Tampering | an accidental tag push triggering an irreversible publish | mitigate | A manual-approval rule on the `pypi` environment (D-18, user blocker 3) gates every run whose environment expression resolves to `pypi`; D-20 puts every tag push in the user's hands and no task in this phase creates a tag |
| T-31-09 | Spoofing | substitution of the published wheel or image | mitigate | `attestations: true` on `gh-action-pypi-publish` and `provenance: true` on `docker/build-push-action`, both riding the workflow's existing OIDC identity. Neither is hand-rolled |
| T-31-21 | Tampering | a pre-release reaching real PyPI or GHCR `latest` | mitigate | One code path routes by `contains(github.ref_name, '-')`: pre-release tags publish to `https://test.pypi.org/legacy/` under the `testpypi` environment, final tags to PyPI under `pypi`. The unconditional `type=raw,value=latest` line is deleted so `metadata-action`'s `latest=auto` skips `latest` for semver pre-releases |
| T-31-22 | Tampering | release gate drifting from the CI gate | mitigate | `release.yml` calls `ci.yml` as a reusable workflow instead of redefining "green". The callee declares `contents: read`, matching the caller's restriction |
</threat_model>

<verification>
- `uv run zizmor .` — exit 0, `No findings to report` (this IS the DLVR-03 test)
- `uv run mkdocs build --strict` — exit 0
- `uv run prek run --all-files` and `uv run prek run --stage pre-push --all-files`
- `python -c "import yaml; [yaml.safe_load(open(p)) for p in ['.github/workflows/ci.yml','.github/workflows/release.yml','.github/workflows/docs.yml','.github/dependabot.yml']]"`
- `command grep -rn 'uses:' .github/workflows/ | command grep -v '@[0-9a-f]\{40\} # v'` — only the
  reusable-workflow call line may appear
- `uv run pytest -m "not browser and not sane_hardware" -q`

**Not verifiable in this plan (user blockers):** whether the release workflow actually runs green
(blockers 1-3, 6 — Plans 09/10) and whether the docs site actually deploys (blocker 5:
Settings -> Pages -> Source: GitHub Actions).
</verification>

<success_criteria>
- `uv run zizmor .` exits 0 with zero findings (DLVR-03, criterion 4)
- Every action is SHA-pinned with a version comment and Dependabot maintains both the
  github-actions and docker ecosystems (DLVR-03, criterion 4)
- Every job in all three workflows has a `permissions:` block (DLVR-03, criterion 4)
- The naming guard runs in CI via the `test` job's pytest run, so CI fails if the old slug
  reappears (CI-02, criterion 1)
- `release.yml` gates on `ci.yml`, routes pre-releases to TestPyPI, carries attestations and
  provenance, and can never tag a pre-release as `latest` (DLVR-02, criterion 2)
- `docs.yml` triggers on `master` and deploys through the Pages artifact with no write token
</success_criteria>

<output>
Create `.planning/phases/31-delivery-identity-and-documentation-accuracy/31-04-SUMMARY.md` when done
</output>
