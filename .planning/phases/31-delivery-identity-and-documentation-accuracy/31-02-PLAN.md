---
phase: 31-delivery-identity-and-documentation-accuracy
plan: 02
type: tdd
wave: 2
depends_on: ["31-01"]
files_modified:
  - README.md
  - docker-compose.yml
  - docs/getting-started/first-cli-scan.md
  - docs/getting-started/quick-start.md
  - docs/how-to/deploy-docker-compose.md
  - docs/how-to/scanner-host-discovery.md
  - docs/reference/docker.md
  - mkdocs.yml
  - pyproject.toml
  - tests/test_deployment_config.py
autonomous: true
requirements: [CI-02, DLVR-01, DOCS-02]

must_haves:
  truths:
    - "No tracked file outside `.planning/` contains the old owner slug (criterion 1)"
    - "A plain `uv run pytest` fails if the old slug reappears in any shipped file (criterion 1)"
    - "Every `<owner>.github.io/saneless/<path>/` link in README.md maps to a file that exists under `docs/` (criterion 5)"
    - "The README `saneless scan` example carries `--title` and its `source` value is a real SANE spelling (criterion 5)"
  artifacts:
    - path: "tests/test_deployment_config.py"
      provides: "the naming guard, the README deep-link test, the README example tests; the three Phase 30 artifacts removed"
      contains: "_shipped_files"
    - path: "README.md"
      provides: "kdknigga URLs, a --title-carrying scan example, a Flatbed source value, a resolving tutorial link"
    - path: "docker-compose.yml"
      provides: "ghcr.io/kdknigga/saneless image reference"
    - path: "mkdocs.yml"
      provides: "kdknigga site/repo URLs"
  key_links:
    - from: "tests/test_deployment_config.py"
      to: "git ls-files"
      via: "literal-argv subprocess with cwd=REPO_ROOT"
      pattern: "ls-files"
    - from: "README.md"
      to: "docs/getting-started/first-cli-scan.md"
      via: "the tutorial deep link, asserted to resolve"
      pattern: "first-cli-scan"
---

<objective>
Rename every shipped reference to the old GitHub owner and land the guard that keeps it that way
— in **one commit** — then pin the README's examples and deep links.

Purpose: `tests/test_deployment_config.py` is simultaneously an offender (its module docstring),
the home of the Phase 30 constant and test that *require* the compose file to still carry the old
name, and the file the new guard goes in. Guard-first makes the suite red; rename-first leaves a
window with no guard. One commit is the only ordering where the suite is green at every commit.

Output: 24 renamed lines across nine files, three Phase 30 artifacts deleted, a `git ls-files`-based
naming guard, and four README static assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-CONTEXT.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
@.planning/phases/31-delivery-identity-and-documentation-accuracy/31-01-SUMMARY.md

<interfaces>
The 24 lines to rename (verified 2026-09-18; re-derive with the guard before trusting these):

  README.md:37,72,74,75,76,77                (6)
  docker-compose.yml:15,25                   (2)
  docs/getting-started/first-cli-scan.md:38  (1)
  docs/getting-started/quick-start.md:21     (1)
  docs/how-to/deploy-docker-compose.md:57,88 (2)
  docs/how-to/scanner-host-discovery.md:35   (1)
  docs/reference/docker.md:9,141,163,181,199 (5)
  mkdocs.yml:3,4,5                           (3)
  pyproject.toml:42,43,44                    (3)

The three Phase 30 artifacts to DELETE (D-12 — delete, do not adjust):

  tests/test_deployment_config.py:28-31  the module-docstring sentence beginning
                                         "They also pin the ``kris-knigga`` occurrence count,"
  tests/test_deployment_config.py:781-787  the 6-line rationale comment plus
                                         `COMPOSE_KRIS_KNIGGA_COUNT = 2`
  tests/test_deployment_config.py:920-928  `test_phase_30_does_not_perform_the_dlvr_01_rename`

`pyproject.toml` `authors = [{ name = "Kris Knigga", ... }]` uses a SPACE, not a hyphen — the
guard's needle does not match it. Leave the authors block alone.

Harness helpers already present (reuse, do not reinvent):
  REPO_ROOT = Path(__file__).resolve().parents[1]        (line 77)
  def _read(path) -> tuple[str, Path]                    (lines 211-214)
  def _numbered(path) -> list[tuple[int, str]]           (lines 798-806)
  def _doc_pages() -> list[Path]                         (lines 100-105)

Source constant for the SANE source spelling, to derive from rather than hard-code:
  saneless.config.ProfileConfig.model_fields["source"].default == "Flatbed"
  (src/saneless/config.py:362)

Existing S603-clean subprocess shape to copy (tests/test_atomic_write.py:711-717,
tests/test_scanner.py:2623-2634): absolute binary path as argv[0], every argv element a string
literal, per-run values via `cwd=` / `env=`, always `timeout=`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The naming guard, the rename, and the Phase 30 deletions — one commit</name>
  <files>tests/test_deployment_config.py, README.md, docker-compose.yml, docs/getting-started/first-cli-scan.md, docs/getting-started/quick-start.md, docs/how-to/deploy-docker-compose.md, docs/how-to/scanner-host-discovery.md, docs/reference/docker.md, mkdocs.yml, pyproject.toml</files>
  <read_first>
    - tests/test_deployment_config.py — the whole module docstring (lines 1-36), the constant
      block (38-106), `_read` (211-214), `_numbered` (798-806), the Phase 30 banner and constant
      (779-790), and the test body at 918-930
    - tests/test_scanner.py lines 2620-2640 and tests/test_atomic_write.py lines 705-720 — the
      S603-clean subprocess shape and the comment that documents the rule
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md § 8 in full
      (the inventory, both ruff blockers, the working prototype, the sequencing note) and
      Pitfalls 6 and 7
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
      § "the naming guard subsection" and § "Shared Patterns" 1, 3, 4, 5
    - Each of the nine files being renamed, at the line numbers listed in `<interfaces>`
  </read_first>
  <behavior>
    One new test in `tests/test_deployment_config.py`, under a new section banner reading
    `Phase 31: the identity guard (CI-02, DLVR-01, D-08..D-12)`:

    `test_no_shipped_file_references_the_old_owner` — for every path returned by a new
    `_shipped_files()` helper, read it as UTF-8 (skipping `UnicodeDecodeError` and `OSError`) and
    collect `f"{name}:{number}: {line.strip()}"` for every line containing `FORBIDDEN_OWNER_SLUG`.
    Assert the offender list is empty, with the message naming DLVR-01 and instructing that every
    URL must use the kdknigga forms.

    RED expectation before the rename: the guard names 29 offenders across 10 files.
    GREEN expectation after the rename and the three deletions: zero offenders.
  </behavior>
  <action>
    Write the guard FIRST, run it, and confirm it reports 29 offenders in 10 files — that count is
    the evidence the file set and the needle are right. Then perform the rename and the deletions
    and confirm zero. Commit the guard, the rename and the deletions as **ONE commit**; this is a
    hard constraint, because `test_phase_30_does_not_perform_the_dlvr_01_rename` asserts the
    compose file still carries the old name and the guard asserts it does not. There is no
    ordering other than one commit in which the suite is green at every commit.

    Build the guard in `tests/test_deployment_config.py`, at module level, using this exact shape:

    - Module constants `_OWNER_GIVEN = "kris"`, `_OWNER_FAMILY = "knigga"` and
      `FORBIDDEN_OWNER_SLUG = f"{_OWNER_GIVEN}-{_OWNER_FAMILY}"`. The f-string form is mandatory:
      D-11 requires the forbidden string never to appear literally in the guard's own source, and
      ruff's `FLY002` rewrites `"-".join(("kris", "knigga"))` back into the literal, which would
      make the guard fail on itself. Write the D-11 comment above these constants in the house
      form — what the reader would otherwise assume, why it is wrong, what happens if you change
      it back — and name the rule (`FLY002`) explicitly.
    - Module constant `_GIT = "/usr/bin/git"` and `_EXCLUDED_PREFIX = ".planning/"`.
    - `def _shipped_files() -> list[str]` calling
      `subprocess.run([_GIT, "ls-files", "-z"], cwd=REPO_ROOT, capture_output=True, text=True,
      check=True, timeout=30)` and returning every non-empty NUL-separated name that does not
      start with `_EXCLUDED_PREFIX`. `cwd=` is load-bearing: putting `str(REPO_ROOT)` into argv via
      `git -C` trips ruff `S603` and suppression is forbidden. Carry the comment explaining the
      literal-argv rule, matching `tests/test_scanner.py`'s.
    - `git ls-files` is the definition of "shipped" per D-10, so a file added in a later phase is
      covered without anyone remembering. `site/` is gitignored (`.gitignore:207`) and therefore
      never reaches `git ls-files`, which is how the requirement's second exclusion is satisfied.
      Say both in the helper's Google-style docstring.
    - No file is exempt from the scan, including this one — that is the point of the runtime
      assembly.

    Rename all 24 lines listed in `<interfaces>` from the `kris-knigga` forms to the `kdknigga`
    forms: `github.com/kris-knigga/saneless` → `github.com/kdknigga/saneless`,
    `kris-knigga.github.io/saneless` → `kdknigga.github.io/saneless`,
    `ghcr.io/kris-knigga/saneless` → `ghcr.io/kdknigga/saneless`. Do not change `pyproject.toml`'s
    `authors` entry (it uses a space) and do not change `name = "saneless"`.

    Delete the three Phase 30 artifacts named in `<interfaces>`: the module-docstring sentence, the
    `COMPOSE_KRIS_KNIGGA_COUNT` constant with its rationale comment, and
    `test_phase_30_does_not_perform_the_dlvr_01_rename` in full. D-12 says delete, not adjust — the
    constant's whole purpose is gone. The docstring sentence must go too or the guard fires on the
    guard file's own docstring.

    Do not touch the module docstring's other sentences about the Phase 30 compose template, the
    consume-directory mount or `TZ` — those tests still exist.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -q && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check . && uv run ruff format --check . && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files | command grep -v '^.planning/' | xargs command grep -l 'kris-knigga'` prints
      nothing (exit status 1 from grep)
    - `uv run pytest tests/test_deployment_config.py -q -k old_owner` passes
    - `command grep -c 'COMPOSE_KRIS_KNIGGA_COUNT' tests/test_deployment_config.py` returns 0
    - `command grep -c 'test_phase_30_does_not_perform_the_dlvr_01_rename' tests/test_deployment_config.py` returns 0
    - `command grep -c 'ghcr.io/kdknigga/saneless' docker-compose.yml` returns 1
    - `command grep -c 'kdknigga' mkdocs.yml` returns 3 and `command grep -c 'kdknigga' pyproject.toml` returns 3
    - The guard, the rename and the deletions are in a single commit — `git show --stat HEAD`
      lists `tests/test_deployment_config.py` together with the nine renamed files
    - `uv run ruff check .` exits 0 with no `# noqa: S603` and no `# noqa: FLY002` anywhere
    - The full non-browser suite passes; no pre-existing test other than the three deleted Phase 30
      artifacts was modified
  </acceptance_criteria>
  <done>The old owner slug appears in no tracked file outside `.planning/`; a plain `uv run pytest` fails if one reappears; the three Phase 30 artifacts are gone; all of it is one commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the README's examples and deep links (rows 16, 17, 18)</name>
  <files>tests/test_deployment_config.py, README.md</files>
  <read_first>
    - README.md in full — especially the `saneless scan` example block near line 44, the
      `source = "flatbed"` config example near line 63, and the five documentation deep links at
      lines 72-77 (post-rename line numbers may shift by zero, but read the current file)
    - src/saneless/config.py line 362 — `source: str = "Flatbed"` on `ProfileConfig`
    - tests/test_deployment_config.py lines 980-1039 — the two
      expectation-derived-from-source patterns (route decorators; `WebConfig.model_fields`)
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-RESEARCH.md § 9 rows
      16, 17, 18 and § "D-45: pinning README examples statically"
    - .planning/phases/31-delivery-identity-and-documentation-accuracy/31-PATTERNS.md
      § "README's five dead deep links"
  </read_first>
  <behavior>
    Three new tests in `tests/test_deployment_config.py`, in the Phase 31 section:

    - `test_readme_scan_example_carries_a_title`: the fenced block in `README.md` containing
      `saneless scan` includes `--title`. (Row 16. `--title` is optional at runtime with a resolved
      default, but DOCS-02 asks the example to show it.)
    - `test_readme_source_values_are_real_sane_spellings`: every `source = "..."` value in
      `README.md` equals `ProfileConfig.model_fields["source"].default` (i.e. `"Flatbed"`), derived
      by importing the model rather than hard-coding the word — so a change to the default cannot
      leave the README silently wrong. The docstring must say the comparison in saneless is
      case-sensitive, which is exactly why `"flatbed"` was a real bug (row 17).
    - `test_every_readme_docs_link_resolves_to_a_page`: for every
      `kdknigga.github.io/saneless/<path>/` URL in `README.md`, assert `docs/<path>.md` exists, with
      an offender list of `f"{lineno}: {url}"`. This generalises row 18 so a future dead deep link
      cannot ship. The expectation is derived from the filesystem, not a hard-coded list.

    All three use the offender-list failure-message convention and one-line present-tense docstrings.
  </behavior>
  <action>
    Write the three tests first and observe them fail on the current README: the scan example has
    no `--title`, `source = "flatbed"` is lowercase, and
    `kdknigga.github.io/saneless/tutorials/scan-your-first-document/` has no corresponding
    `docs/tutorials/scan-your-first-document.md`.

    Then correct `README.md`:
    - Row 16: add `--title "Some Document"` to the `saneless scan` example line so the example
      shows the flag DOCS-02 asks for. Keep the trailing `# Scan a document` comment style.
    - Row 17: change `source = "flatbed"` to `source = "Flatbed"` — the spelling
      `saneless.toml.example:16` already uses and the value SANE reports. The comparison is
      case-sensitive.
    - Row 18: change the tutorial deep link from
      `https://kdknigga.github.io/saneless/tutorials/scan-your-first-document/` to
      `https://kdknigga.github.io/saneless/getting-started/first-cli-scan/`, which corresponds to
      the real page `docs/getting-started/first-cli-scan.md`. Update the link text if it still says
      "Scan Your First Document" so it names the page it now points at.
    - Check the other four deep links at lines 72-77 against the test and fix any that do not
      resolve to a `docs/<path>.md`.

    Do NOT add maturity prose, hedges or a "this project is young" note anywhere — D-07 forbids it;
    this phase removes claims, it does not add caveats.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -q -k "readme" && uv run pytest -m "not browser and not sane_hardware" -q</automated>
  </verify>
  <acceptance_criteria>
    - `command grep -c 'saneless scan' README.md` is unchanged and the matching line also contains
      `--title`
    - `command grep -c 'source = "flatbed"' README.md` returns 0 and
      `command grep -c 'source = "Flatbed"' README.md` returns at least 1
    - `command grep -c 'tutorials/scan-your-first-document' README.md` returns 0
    - `command grep -c 'getting-started/first-cli-scan' README.md` returns at least 1
    - For every `kdknigga.github.io/saneless/<path>/` URL in README.md, `docs/<path>.md` exists on
      disk
    - `uv run pytest tests/test_deployment_config.py -q -k readme` passes with 3 tests
    - The naming guard from Task 1 still passes (the new links use the kdknigga form)
    - ruff, ruff format --check, ty and pyrefly all clean with no suppressions
  </acceptance_criteria>
  <done>README rows 16, 17 and 18 are corrected and each is defended by a test whose expectation is derived from source or from the filesystem.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository content → published artefacts (GHCR image ref, docs site URLs, PyPI project URLs) | A wrong owner slug points users and tooling at an account this project does not control |
| test process → `git` child process | The guard shells out; argv content crosses into a subprocess |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-14 | Spoofing | `ghcr.io/<owner>/saneless` in `docker-compose.yml` and the docs | mitigate | Rename every reference to `kdknigga` and pin it with `test_no_shipped_file_references_the_old_owner`, whose file set is `git ls-files` minus `.planning/` — a file added later is covered without anyone remembering. The old-owner namespace has never been published (RESEARCH Runtime State Inventory: no GHCR package, no Pages deploy), so there is nothing already resolving under it, but an unrenamed reference would invite exactly that squat |
| T-31-15 | Tampering | the guard's own source | mitigate | The forbidden slug is assembled at runtime from `_OWNER_GIVEN` and `_OWNER_FAMILY` via an f-string, so no file is exempt from the scan and there is no exempt file where a real reference could hide (D-11). `"-".join((...))` is forbidden because ruff `FLY002` rewrites it back into the literal |
| T-31-16 | Tampering | `subprocess.run` in the guard | mitigate | Literal argv only (`["/usr/bin/git", "ls-files", "-z"]`), repository path travels as `cwd=`, `check=True`, `timeout=30` — the shape `tests/test_scanner.py` and `tests/test_atomic_write.py` already established. No shell, no interpolation, and no `# noqa: S603` |
| T-31-17 | Information disclosure | README deep links | accept | A dead docs link leaks nothing; the harm is a reader landing on a 404. Mitigated for correctness by `test_every_readme_docs_link_resolves_to_a_page` rather than treated as a security control |
</threat_model>

<verification>
- `uv run pytest -m "not browser and not sane_hardware" -q` — full non-browser suite green
- `git ls-files | command grep -v '^.planning/' | xargs command grep -n 'kris-knigga'` — no output
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
- `uv run prek run --all-files` and `uv run prek run --stage pre-push --all-files`
- `git log --oneline -1 --stat` — the rename, the deletions and the guard are one commit
</verification>

<success_criteria>
- No shipped file references `kris-knigga/saneless`, `kris-knigga.github.io/saneless` or
  `ghcr.io/kris-knigga/saneless`, and a plain `uv run pytest` fails if one reappears (criterion 1,
  CI-02, DLVR-01)
- The PyPI distribution name is still `saneless`
- README's `saneless scan` example carries `--title`, its `source` value is `"Flatbed"`, and every
  documentation deep link resolves to a page that exists (criterion 5, DOCS-02)
- The three Phase 30 artifacts are deleted, not weakened
</success_criteria>

<output>
Create `.planning/phases/31-delivery-identity-and-documentation-accuracy/31-02-SUMMARY.md` when done
</output>
