---
phase: 18-automatic-scanned-page-size-detection-or-user-specified-paper-size-to-avoid-capturing-the-full-scanner-bed
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - docs/reference/configuration.md
  - src/saneless/auto_profiles.py
autonomous: true
requirements:
  - PS-05
  - PS-06

must_haves:
  truths:
    - "Configuration reference docs list the paper_size field with all preset values"
    - "Auto-generated profiles do not write paper_size to TOML (default is full, per D-08)"
  artifacts:
    - path: "docs/reference/configuration.md"
      provides: "paper_size field documentation"
      contains: "paper_size"
    - path: "src/saneless/auto_profiles.py"
      provides: "Unchanged auto profile generation (paper_size defaults to full)"
  key_links:
    - from: "docs/reference/configuration.md"
      to: "src/saneless/config.py"
      via: "Documents ProfileConfig.paper_size field"
      pattern: "paper_size"
---

<objective>
Update configuration reference documentation with the new paper_size field and verify auto-profile generation handles the default correctly.

Purpose: Users need documentation to discover and use the paper_size feature. Auto-profiles must not write paper_size (default "full" is implicit per D-08).
Output: Updated docs and verified auto-profile behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-automatic-scanned-page-size-detection-or-user-specified-paper-size-to-avoid-capturing-the-full-scanner-bed/18-CONTEXT.md
@.planning/phases/18-automatic-scanned-page-size-detection-or-user-specified-paper-size-to-avoid-capturing-the-full-scanner-bed/18-01-SUMMARY.md

<interfaces>
From src/saneless/paper_sizes.py (created in Plan 01):
```python
PaperSize = Literal["full", "a3", "a4", "a5", "letter", "legal"]

PAPER_SIZES_MM: dict[str, tuple[float, float]] = {
    "a3": (297.0, 420.0),
    "a4": (210.0, 297.0),
    "a5": (148.0, 210.0),
    "letter": (215.9, 279.4),
    "legal": (215.9, 355.6),
}
```

From src/saneless/config.py (modified in Plan 01):
```python
class ProfileConfig(BaseModel):
    paper_size: PaperSize = "full"
```

From src/saneless/auto_profiles.py (write_profiles_to_config):
```python
def write_profiles_to_config(
    profiles: dict[str, ProfileConfig],
    config_path: Path,
) -> None:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update configuration docs and verify auto-profile default behavior</name>
  <files>docs/reference/configuration.md, tests/test_auto_profiles.py</files>
  <read_first>docs/reference/configuration.md, src/saneless/auto_profiles.py, src/saneless/config.py, tests/test_auto_profiles.py</read_first>
  <action>
    1. In `docs/reference/configuration.md`, in the `[profiles.NAME]` table (around line 60), add a row for `paper_size` after the `auto_source_mode` row:
       ```
       | `paper_size` | string | `"full"` | Constrain scan area to a standard paper size. Presets: `full` (entire scanner bed), `a3`, `a4`, `a5`, `letter`, `legal`. Sets SANE geometry options when supported; falls back to post-scan crop otherwise. |
       ```

    2. In the Complete Example section (around line 78), add `paper_size = "a4"` to one of the example profiles to demonstrate usage. For example, in the `[profiles.receipts]` section, add:
       ```toml
       paper_size = "letter"
       ```

    3. In `tests/test_auto_profiles.py`, add a test verifying that auto-generated profiles do NOT explicitly set paper_size (it should be absent from the TOML output since "full" is the default and per Phase 16 convention, defaults are not written):
       - Test name: `test_auto_generated_profiles_omit_paper_size`
       - Call `generate_profiles()` with sample capabilities
       - Call `write_profiles_to_config()` with the result
       - Read the written TOML file content
       - Assert `"paper_size"` is NOT in the TOML text (default omitted per D-08)

    4. Verify docs build: `uv run mkdocs build --strict` must exit 0.
  </action>
  <verify>
    <automated>cd /home/kris/git/saneless && uv run mkdocs build --strict && uv run pytest tests/test_auto_profiles.py -x -v -k "paper_size"</automated>
  </verify>
  <acceptance_criteria>
    - docs/reference/configuration.md contains `paper_size` in the profiles table
    - docs/reference/configuration.md contains `full`, `a3`, `a4`, `a5`, `letter`, `legal` in the paper_size description
    - docs/reference/configuration.md Complete Example contains `paper_size =`
    - tests/test_auto_profiles.py contains `test_auto_generated_profiles_omit_paper_size` or `paper_size`
    - `uv run mkdocs build --strict` exits 0
    - `uv run pytest tests/test_auto_profiles.py -x -k "paper_size"` exits 0
  </acceptance_criteria>
  <done>Configuration reference documents paper_size field with all presets. Auto-generated profiles verified to omit paper_size (default "full" is implicit). Docs build passes strict mode.</done>
</task>

</tasks>

<verification>
1. `uv run mkdocs build --strict` -- docs build clean
2. `uv run pytest tests/test_auto_profiles.py -x` -- auto-profile tests pass
3. `uv run pytest` -- full suite green
4. `uv run ruff check docs/` -- no lint issues in docs (if applicable)
</verification>

<success_criteria>
- Configuration reference page lists paper_size field with description covering all presets
- Example TOML in docs shows paper_size usage
- Auto-generated profiles do not write paper_size to TOML (verified by test)
- Docs build passes strict mode
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-automatic-scanned-page-size-detection-or-user-specified-paper-size-to-avoid-capturing-the-full-scanner-bed/18-02-SUMMARY.md`
</output>
