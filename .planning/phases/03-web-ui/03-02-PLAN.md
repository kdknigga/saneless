---
phase: 03-web-ui
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/saneless/web/templates/base.html
  - src/saneless/web/templates/index.html
  - src/saneless/web/templates/partials/status.html
  - src/saneless/web/templates/partials/flip.html
  - src/saneless/web/templates/partials/history.html
  - src/saneless/web/templates/partials/tags.html
  - src/saneless/web/templates/partials/correspondents.html
  - src/saneless/web/static/app.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-07
  - UI-08
  - PROF-03
  - PLSS-04
  - LOG-03

must_haves:
  truths:
    - "User can open the web UI and see profile dropdown, title field, tags multi-select, correspondent dropdown, and Scan button"
    - "Status area shows live job state with spinner for active states, checkmark for done, X for error"
    - "Status area polls every 1s during active job states and stops polling when idle/done/error"
    - "For manual duplex, status area shows flip prompt with exact PRD wording, inline SVG illustration, Continue and Cancel buttons"
    - "First-page thumbnail appears inline in status area as base64 JPEG img tag"
    - "Scan button is disabled (aria-busy=true or disabled attribute) while job is in progress"
    - "Tag and correspondent dropdowns have refresh icons that trigger per-resource cache invalidation"
    - "Error messages from failed jobs are displayed in the status area"
    - "Profile dropdown is populated from settings.profiles keys"
  artifacts:
    - path: "src/saneless/web/templates/base.html"
      provides: "HTML shell with PicoCSS, HTMX, app.css"
      contains: "picocss/pico@2"
    - path: "src/saneless/web/templates/index.html"
      provides: "Main page with form, status, history"
      contains: "hx-post"
    - path: "src/saneless/web/templates/partials/status.html"
      provides: "Job status with conditional polling"
      contains: "hx-trigger"
    - path: "src/saneless/web/templates/partials/flip.html"
      provides: "Flip prompt with SVG illustration"
      contains: "flip the stack over the long edge"
    - path: "src/saneless/web/static/app.css"
      provides: "App-specific layout styles"
      min_lines: 10
  key_links:
    - from: "src/saneless/web/templates/index.html"
      to: "/api/scan"
      via: "hx-post on scan form"
      pattern: "hx-post.*api/scan"
    - from: "src/saneless/web/templates/partials/status.html"
      to: "/api/jobs/current/status"
      via: "hx-get with every 1s trigger"
      pattern: "hx-get.*api/jobs/current/status"
    - from: "src/saneless/web/templates/partials/flip.html"
      to: "/api/flip/continue"
      via: "hx-post on Continue button"
      pattern: "hx-post.*api/flip/continue"
---

<objective>
Complete Jinja2 templates and HTMX interactions for the saneless web UI.

Purpose: Replace the template stubs from Plan 01 with full, styled, interactive templates using PicoCSS for styling and HTMX for dynamic behavior. This implements the user-facing web UI: scan form with profile/metadata fields, live status polling, manual duplex flip prompt with SVG illustration, thumbnail display, job history table, and scan button disabling.

Output: Fully functional web UI templates that render correctly with PicoCSS, handle all HTMX interactions, and satisfy all UI requirements.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-RESEARCH.md
@.planning/phases/03-web-ui/03-01-SUMMARY.md

<interfaces>
<!-- Route endpoints from Plan 01 that templates must target -->

From src/saneless/web/routes.py (created in Plan 01):
- GET / -- serves index.html with context: {profiles: list[str], tags: list[dict], correspondents: list[dict], job: Job|None, jobs: list[Job]}
- POST /api/scan -- form: profile (str), title (str), tags (list[int]), correspondent (int|None) -- returns partials/status.html
- GET /api/jobs/current/status -- returns partials/status.html with {job: Job|None}
- GET /api/tags -- returns partials/tags.html with {tags: list[dict]}
- GET /api/correspondents -- returns partials/correspondents.html with {correspondents: list[dict]}
- POST /api/cache/invalidate?resource=tags -- invalidates cache, returns fresh partial
- POST /api/cache/invalidate?resource=correspondents -- same
- GET /api/jobs/history -- returns partials/history.html with {jobs: list[Job]}
- POST /api/flip/continue -- signals worker, returns partials/status.html
- POST /api/flip/abort -- signals worker, returns partials/status.html

From src/saneless/job.py:
- JobState values: PENDING, SCANNING, AWAITING_FLIP, ASSEMBLING, UPLOADING, DONE, ERROR
- Job fields: id, profile, title, state (JobState), error (str|None), created_at (datetime), tags (list[int]), correspondent (int|None), thumbnail (str|None)

Active states that need polling: SCANNING, AWAITING_FLIP, ASSEMBLING, UPLOADING
Terminal states (stop polling): DONE, ERROR, PENDING (or no job)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create complete Jinja2 templates with HTMX interactions</name>
  <files>
    src/saneless/web/templates/base.html
    src/saneless/web/templates/index.html
    src/saneless/web/templates/partials/status.html
    src/saneless/web/templates/partials/flip.html
    src/saneless/web/templates/partials/history.html
    src/saneless/web/templates/partials/tags.html
    src/saneless/web/templates/partials/correspondents.html
  </files>
  <read_first>
    src/saneless/web/templates/base.html
    src/saneless/web/templates/index.html
    src/saneless/web/templates/partials/status.html
    src/saneless/web/templates/partials/flip.html
    src/saneless/web/templates/partials/history.html
    src/saneless/web/templates/partials/tags.html
    src/saneless/web/templates/partials/correspondents.html
    src/saneless/web/routes.py
    .planning/phases/03-web-ui/03-CONTEXT.md
    .planning/phases/03-web-ui/03-RESEARCH.md
    docs/PRD.md
  </read_first>
  <action>
**base.html** -- HTML5 shell:
```html
<!DOCTYPE html>
<html lang="en" data-theme="auto">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>saneless</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css">
    <link rel="stylesheet" href="/static/app.css">
    <script src="https://cdn.jsdelivr.net/npm/htmx.org@2.0.8/dist/htmx.min.js"></script>
</head>
<body>
    <header class="container">
        <h1>saneless</h1>
    </header>
    <main class="container">
        {% block content %}{% endblock %}
    </main>
</body>
</html>
```

**index.html** -- Main page extending base, single-page layout with three sections. Use semantic HTML for PicoCSS:

Section 1: Scan Form (`<article>`):
- `<form hx-post="/api/scan" hx-target="#status-area" hx-swap="outerHTML">`
- Profile dropdown: `<select name="profile">` populated from `{% for p in profiles %}<option value="{{ p }}">{{ p }}</option>{% endfor %}`
- Title input: `<input type="text" name="title" placeholder="Document title (auto-generated if empty)">`
- Tags multi-select: `<label>Tags <button type="button" hx-post="/api/cache/invalidate?resource=tags" hx-target="#tags-select" hx-swap="innerHTML" class="refresh-btn" title="Refresh tags">&#x21bb;</button></label>` followed by `<select name="tags" id="tags-select" multiple hx-get="/api/tags" hx-trigger="load" hx-swap="innerHTML">{% for tag in tags %}<option value="{{ tag.id }}">{{ tag.name }}</option>{% endfor %}</select>`
- Correspondent dropdown: `<label>Correspondent <button type="button" hx-post="/api/cache/invalidate?resource=correspondents" hx-target="#correspondent-select" hx-swap="innerHTML" class="refresh-btn" title="Refresh correspondents">&#x21bb;</button></label>` followed by `<select name="correspondent" id="correspondent-select" hx-get="/api/correspondents" hx-trigger="load" hx-swap="innerHTML"><option value="">-- None --</option>{% for c in correspondents %}<option value="{{ c.id }}">{{ c.name }}</option>{% endfor %}</select>`
- Submit button: `<button type="submit" id="scan-btn" {% if job and job.state.value in ["SCANNING", "AWAITING_FLIP", "ASSEMBLING", "UPLOADING"] %}disabled aria-busy="true"{% endif %}>{% if job and job.state.value in ["SCANNING", "AWAITING_FLIP", "ASSEMBLING", "UPLOADING"] %}Scanning...{% else %}Scan{% endif %}</button>`

Section 2: Status Area:
- `{% include "partials/status.html" %}`

Section 3: Job History (`<article>`):
- `<h2>Job History</h2>`
- `<table role="grid"><thead><tr><th>Time</th><th>Profile</th><th>Title</th><th>Status</th></tr></thead>{% include "partials/history.html" %}</table>`

**partials/status.html** -- Conditional polling and state rendering:
```html
{% set active_states = ["SCANNING", "ASSEMBLING", "UPLOADING", "AWAITING_FLIP"] %}
<div id="status-area"
     {% if job and job.state.value in active_states %}
     hx-get="/api/jobs/current/status"
     hx-trigger="every 1s"
     hx-swap="outerHTML"
     {% endif %}>
  {% if job %}
    {% if job.state.value == "SCANNING" %}
      <p aria-busy="true">Scanning...</p>
    {% elif job.state.value == "AWAITING_FLIP" %}
      {% include "partials/flip.html" %}
    {% elif job.state.value == "ASSEMBLING" %}
      <p aria-busy="true">Assembling PDF...</p>
    {% elif job.state.value == "UPLOADING" %}
      <p aria-busy="true">Uploading to paperless-ngx...</p>
    {% elif job.state.value == "DONE" %}
      <p class="status-done">&#10003; Done: {{ job.title }}</p>
      <script>
        var btn = document.getElementById("scan-btn");
        if (btn) { btn.disabled = false; btn.ariaBusy = "false"; btn.textContent = "Scan"; }
      </script>
    {% elif job.state.value == "ERROR" %}
      <p role="alert" class="status-error">&#10007; Error: {{ job.error }}</p>
      <script>
        var btn = document.getElementById("scan-btn");
        if (btn) { btn.disabled = false; btn.ariaBusy = "false"; btn.textContent = "Scan"; }
      </script>
    {% endif %}
    {% if job.thumbnail %}
      <img src="data:image/jpeg;base64,{{ job.thumbnail }}" alt="First page preview" class="thumbnail">
    {% endif %}
  {% else %}
    <p>Ready to scan.</p>
  {% endif %}
</div>
```

Also add an HTMX after-swap handler to refresh the history table after status transitions to DONE or ERROR. Use `hx-trigger="load"` on a hidden element inside the DONE/ERROR blocks:
```html
<div hx-get="/api/jobs/history" hx-target="#history-body" hx-swap="outerHTML" hx-trigger="load" style="display:none"></div>
```

**partials/flip.html** -- Manual duplex flip prompt with inline SVG:
- Exact wording from PRD: "Keep the pages in the same order, then flip the stack over the long edge (the left or right side, not the top or bottom)"
- Inline SVG showing correct long-edge flip (arrow curving over left/right edge of a page stack) and incorrect short-edge flip (X mark over top/bottom flip). SVG should be simple: two rectangles representing paper stack, curved arrow for correct flip, X for incorrect. Use currentColor for stroke so it works in both light and dark mode.
- Two buttons:
  - `<button hx-post="/api/flip/continue" hx-target="#status-area" hx-swap="outerHTML">Continue</button>`
  - `<button hx-post="/api/flip/abort" hx-target="#status-area" hx-swap="outerHTML" class="secondary">Cancel</button>`

**partials/history.html** -- Job history table body:
```html
<tbody id="history-body">
{% for job in jobs %}
<tr>
  <td>{{ job.created_at.strftime("%Y-%m-%d %H:%M") }}</td>
  <td>{{ job.profile }}</td>
  <td>{{ job.title }}</td>
  <td class="{% if job.state.value == 'DONE' %}status-done{% elif job.state.value == 'ERROR' %}status-error{% endif %}">
    {{ job.state.value }}
  </td>
</tr>
{% endfor %}
{% if not jobs %}
<tr><td colspan="4">No scan history yet.</td></tr>
{% endif %}
</tbody>
```

**partials/tags.html**:
```html
{% for tag in tags %}<option value="{{ tag.id }}">{{ tag.name }}</option>{% endfor %}
```

**partials/correspondents.html**:
```html
<option value="">-- None --</option>
{% for c in correspondents %}<option value="{{ c.id }}">{{ c.name }}</option>{% endfor %}
```
  </action>
  <verify>
    <automated>cd /home/kris/git/saneless && python -c "
from jinja2 import Environment, FileSystemLoader
env = Environment(loader=FileSystemLoader('src/saneless/web/templates'))
for t in ['base.html', 'index.html', 'partials/status.html', 'partials/flip.html', 'partials/history.html', 'partials/tags.html', 'partials/correspondents.html']:
    env.get_template(t)
    print(f'OK: {t}')
print('All templates parse successfully')
"</automated>
  </verify>
  <acceptance_criteria>
    - base.html contains `picocss/pico@2/css/pico.min.css`
    - base.html contains `htmx.org@2.0.8/dist/htmx.min.js`
    - base.html contains `<link rel="stylesheet" href="/static/app.css">`
    - index.html contains `hx-post="/api/scan"`
    - index.html contains `name="profile"`
    - index.html contains `name="tags"` and `multiple`
    - index.html contains `name="correspondent"`
    - index.html contains `id="scan-btn"`
    - index.html contains `hx-post="/api/cache/invalidate?resource=tags"`
    - index.html contains `hx-post="/api/cache/invalidate?resource=correspondents"`
    - partials/status.html contains `hx-get="/api/jobs/current/status"`
    - partials/status.html contains `hx-trigger="every 1s"`
    - partials/status.html contains `hx-swap="outerHTML"`
    - partials/status.html contains `id="status-area"`
    - partials/status.html contains `data:image/jpeg;base64`
    - partials/status.html contains `role="alert"`
    - partials/flip.html contains `flip the stack over the long edge`
    - partials/flip.html contains `<svg`
    - partials/flip.html contains `hx-post="/api/flip/continue"`
    - partials/flip.html contains `hx-post="/api/flip/abort"`
    - partials/history.html contains `id="history-body"`
    - All 7 templates parse without Jinja2 errors
  </acceptance_criteria>
  <done>All templates parse correctly. index.html contains profile dropdown, title, tags multi-select, correspondent dropdown, scan button. Status partial has conditional polling. Flip partial has PRD wording and SVG. History partial renders job table.</done>
</task>

<task type="auto">
  <name>Task 2: Create app-specific CSS and verify full render</name>
  <files>
    src/saneless/web/static/app.css
    src/saneless/web/routes.py
  </files>
  <read_first>
    src/saneless/web/static/app.css
    src/saneless/web/routes.py
    src/saneless/web/templates/index.html
    src/saneless/web/templates/partials/status.html
    src/saneless/web/templates/partials/flip.html
  </read_first>
  <action>
1. **src/saneless/web/static/app.css** -- App-specific CSS overrides for PicoCSS. Keep minimal, leverage PicoCSS classless styling:

```css
/* saneless -- app-specific layout overrides for PicoCSS */

/* Status area */
#status-area {
    margin: 1rem 0;
    padding: 1rem;
    border-left: 3px solid var(--pico-primary);
    border-radius: var(--pico-border-radius);
}

/* Status indicators */
.status-done {
    color: var(--pico-ins-color, green);
}

.status-error {
    color: var(--pico-del-color, red);
}

/* Thumbnail preview */
.thumbnail {
    max-width: 200px;
    max-height: 300px;
    margin-top: 0.5rem;
    border: 1px solid var(--pico-muted-border-color);
    border-radius: var(--pico-border-radius);
}

/* Flip illustration */
.flip-illustration {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin: 1rem 0;
}

.flip-illustration svg {
    width: 120px;
    height: auto;
}

/* Refresh buttons inline with labels */
.refresh-btn {
    display: inline;
    width: auto;
    padding: 0.2rem 0.5rem;
    margin-left: 0.5rem;
    font-size: 0.9rem;
    border: none;
    background: transparent;
    cursor: pointer;
    color: var(--pico-primary);
}

.refresh-btn:hover {
    color: var(--pico-primary-hover);
}

/* Job history */
#history-body td {
    white-space: nowrap;
}

/* Responsive: stack on mobile */
@media (max-width: 576px) {
    .flip-illustration {
        flex-direction: column;
        align-items: center;
        gap: 1rem;
    }

    .thumbnail {
        max-width: 100%;
    }
}
```

2. **src/saneless/web/routes.py** -- Minor adjustments if needed to ensure route handler template contexts match what templates expect. Verify that:
   - The `/` route passes `profiles` as a `list[str]` (keys from settings.profiles dict)
   - The `/` route passes `tags` and `correspondents` as lists of dicts
   - The `/` route passes `job` (current/latest Job or None)
   - The `/` route passes `jobs` (list of recent Jobs for history table)
   - Status partial routes pass `job` in context
   - History route passes `jobs` in context
   - Tags/correspondents routes pass `tags`/`correspondents` respectively

   If the route handler from Plan 01 already does this correctly, no changes needed. If adjustments are needed (e.g., the scan form submission should also return a script to disable the button, or the index route needs to include `active_states` list), make those adjustments.

   Specifically ensure the scan form POST handler (`/api/scan`) response also triggers the scan button to show as disabled. This can be handled purely in the template (the status partial already conditionally includes the disable script for DONE/ERROR states, and the form hx-post can use `hx-on::before-request` to disable the button client-side):

   Add to the form tag in index.html (if not already):
   ```
   hx-on::before-request="this.querySelector('#scan-btn').disabled=true; this.querySelector('#scan-btn').ariaBusy='true'; this.querySelector('#scan-btn').textContent='Scanning...'"
   ```

   This ensures the button disables immediately on click before the response arrives.
  </action>
  <verify>
    <automated>cd /home/kris/git/saneless && uv run ruff check src/saneless/web/ && uv run ruff format --check src/saneless/web/ && test -s src/saneless/web/static/app.css && echo "CSS exists and non-empty" && wc -l src/saneless/web/static/app.css</automated>
  </verify>
  <acceptance_criteria>
    - app.css contains `#status-area`
    - app.css contains `.status-done`
    - app.css contains `.status-error`
    - app.css contains `.thumbnail`
    - app.css contains `.flip-illustration`
    - app.css contains `.refresh-btn`
    - app.css contains `@media`
    - app.css is at least 10 lines
    - `uv run ruff check src/saneless/web/` exits 0
  </acceptance_criteria>
  <done>App CSS provides layout overrides for status area, thumbnails, flip illustration, refresh buttons, and responsive mobile layout. All route handler template contexts align with template expectations.</done>
</task>

</tasks>

<verification>
- All 7 templates parse without Jinja2 errors
- `uv run ruff check src/saneless/web/` exits 0
- `uv run ruff format --check src/saneless/web/` exits 0
- CSS file is non-empty and includes all required class selectors
- Template IDs match HTMX targets: status-area, tags-select, correspondent-select, history-body, scan-btn
</verification>

<success_criteria>
- Complete single-page UI with scan form, status area, and job history table
- HTMX polling starts during active states and stops on terminal states
- Flip prompt shows exact PRD wording with SVG illustration and Continue/Cancel buttons
- Scan button disabled during active job, re-enabled on completion
- Tag/correspondent dropdowns have inline refresh buttons
- Error messages displayed in status area
- Thumbnail rendered as base64 img when available
- PicoCSS provides base styling, app.css adds app-specific overrides
- Responsive: single-column on mobile
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-02-SUMMARY.md`
</output>
