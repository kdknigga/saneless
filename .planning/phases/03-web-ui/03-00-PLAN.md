---
phase: 03-web-ui
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_web.py
  - tests/test_cache.py
  - tests/test_job.py
  - pyproject.toml
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - PROF-03
  - PLSS-04
  - PLSS-05
  - HLTH-01
  - HLTH-02
  - LOG-03

must_haves:
  truths:
    - "All xfail test stubs exist and are collected by pytest"
    - "httpx dev dependency is installed for FastAPI TestClient"
    - "pytest discovers and skips all stubs without errors"
  artifacts:
    - path: "tests/test_web.py"
      provides: "xfail stubs for web endpoint tests"
      contains: "pytest.mark.xfail"
    - path: "tests/test_cache.py"
      provides: "xfail stubs for cache tests"
      contains: "pytest.mark.xfail"
    - path: "tests/test_job.py"
      provides: "xfail stubs for list_recent and prune tests"
      contains: "test_list_recent"
  key_links: []
---

<objective>
Wave 0 test scaffolding: create xfail test stubs for all Phase 3 requirements so that subsequent plans have Nyquist-compliant verification targets from the start.

Purpose: Establish the test file structure and install the httpx dev dependency needed by FastAPI TestClient. All stubs are `@pytest.mark.xfail(reason="stub")` so they are collected but do not block execution. Plans 01-03 will replace these stubs with real implementations.

Output: Three test files with xfail stubs covering all 14 Phase 3 requirements, httpx installed as dev dependency.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-ui/03-CONTEXT.md
@.planning/phases/03-web-ui/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install httpx and create xfail test stubs for all Phase 3 requirements</name>
  <files>
    tests/test_web.py
    tests/test_cache.py
    tests/test_job.py
    pyproject.toml
  </files>
  <read_first>
    tests/conftest.py
    tests/test_job.py
    pyproject.toml
  </read_first>
  <action>
1. **Install httpx dev dependency:**
   ```bash
   uv add --dev httpx
   ```

2. **tests/test_web.py** -- Create file with xfail stubs for all web endpoint tests. Each stub is a function with `@pytest.mark.xfail(reason="stub -- implemented in 03-03")` and a body of `pytest.fail("Not implemented")`. Include module docstring and import pytest.

   Stubs to create (one function each):
   - `test_page_loads` (UI-01) -- GET / returns 200 with form elements
   - `test_status_polling` (UI-02) -- GET /api/jobs/current/status returns status partial
   - `test_status_polling_active_job` (UI-02) -- Active job triggers hx-trigger polling attributes
   - `test_flip_prompt` (UI-03) -- AWAITING_FLIP state shows flip prompt with PRD wording
   - `test_thumbnail_display` (UI-04) -- Job with thumbnail shows base64 img tag
   - `test_job_history` (UI-05) -- GET /api/jobs/history returns job list
   - `test_scan_button_disabled` (UI-07) -- Scan button disabled during active job
   - `test_cache_invalidate` (UI-08) -- POST /api/cache/invalidate refreshes resource
   - `test_profile_dropdown` (PROF-03) -- Profile names from settings appear in dropdown
   - `test_scan_form_submit` (PLSS-04) -- POST /api/scan creates job and returns status
   - `test_health_endpoint` (HLTH-01) -- GET /health returns 200 {status: ok}
   - `test_health_no_auth` (HLTH-02) -- GET /health requires no authentication
   - `test_error_display` (LOG-03) -- Error state shows error message in status area

3. **tests/test_cache.py** -- Create file with xfail stubs for MetadataCache tests:
   - `test_cache_set_and_get` (PLSS-05)
   - `test_cache_ttl_expiry` (PLSS-05)
   - `test_cache_invalidate` (PLSS-05)
   - `test_cache_invalidate_nonexistent` (PLSS-05)
   - `test_cache_per_resource` (PLSS-05)

4. **tests/test_job.py** -- APPEND xfail stubs to existing file (do NOT overwrite existing tests). Add at the bottom:
   - `test_list_recent` (UI-05)
   - `test_list_recent_empty` (UI-05)
   - `test_prune_by_age` (UI-06)
   - `test_prune_by_count` (UI-06)
   - `test_prune_no_deletions` (UI-06)

   If `pytest` is not already imported in test_job.py, add the import.

All stubs must have Google-style docstrings. All must pass `uv run ruff check` and `uv run ruff format --check`.
  </action>
  <verify>
    <automated>cd /home/kris/git/saneless && uv run pytest tests/test_web.py tests/test_cache.py tests/test_job.py --collect-only -q 2>&1 | tail -5 && uv run ruff check tests/test_web.py tests/test_cache.py tests/test_job.py && echo "All stubs collected and lint-clean"</automated>
  </verify>
  <done>httpx installed as dev dependency. All xfail test stubs exist in tests/test_web.py, tests/test_cache.py, and tests/test_job.py. pytest collects all stubs. Ruff passes clean.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_web.py tests/test_cache.py tests/test_job.py --collect-only -q` shows all stubs
- `uv run pytest tests/ -x -q` passes (xfail stubs are collected but do not fail the suite)
- `uv run ruff check tests/` exits 0
- `python -c "import httpx; print('httpx ok')"` succeeds
</verification>

<success_criteria>
- tests/test_web.py has 13 xfail stubs covering UI-01 through UI-08, PROF-03, PLSS-04, HLTH-01, HLTH-02, LOG-03
- tests/test_cache.py has 5 xfail stubs covering PLSS-05
- tests/test_job.py has 5 new xfail stubs covering UI-05, UI-06 (appended to existing tests)
- httpx is in dev dependencies
- All stubs are collected by pytest without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui/03-00-SUMMARY.md`
</output>
