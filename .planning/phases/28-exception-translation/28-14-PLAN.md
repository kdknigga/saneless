---
phase: 28-exception-translation
plan: 14
type: execute
wave: 4
depends_on: [28-10, 28-11]
files_modified:
  - docs/reference/web-api.md
  - docs/how-to/set-up-adf-duplex.md
  - docs/explanation/architecture.md
  - docs/explanation/consume-directory-fallback.md
  - docs/explanation/empty-page-detection.md
autonomous: true
requirements: [EXC-01, EXC-03, EXC-04]
decisions: [D-01, D-02, D-03, D-10, D-11]

must_haves:
  truths:
    - "docs/reference/web-api.md lists CANCELLED among the job states, says polling stops and the Scan button re-enables on it, and says an Abort ends the job CANCELLED (D-01)"
    - "docs/how-to/set-up-adf-duplex.md says answering no, Ctrl-C or closing input cancels the scan with exit code 130, a terminal read error fails it with exit 1, a web Abort shows Cancelled, and a flip timeout still fails the job (D-02, D-03)"
    - "docs/explanation/architecture.md says an abort cancels the job while a timeout fails it, lists CANCELLED as a terminal state, and no longer claims an auth failure falls back to the consume directory (D-01, RESEARCH doc surface list)"
    - "docs/explanation/consume-directory-fallback.md says every transient network failure (connection refused or reset, timeouts, a proxy closing the connection) and server errors retry then fall back; a malformed URL is not retried but still falls back; a rejected upload (4xx) does not fall back; polling survives network blips; a retry after a lost response can create a duplicate document (D-10, D-11)"
    - "docs/explanation/empty-page-detection.md says that when detection removes every page the scan fails with 'All pages were blank', and that a scan returning no pages at all reports 'No pages were scanned' (EXC-03)"
    - "`uv run mkdocs build --strict` succeeds"
  artifacts:
    - path: "docs/reference/web-api.md"
      provides: "CANCELLED state documentation"
      contains: "CANCELLED"
    - path: "docs/explanation/consume-directory-fallback.md"
      provides: "retry and fallback rules after D-10"
      contains: "malformed"
  key_links:
    - from: "docs/how-to/set-up-adf-duplex.md"
      to: "exit code 130"
      via: "manual duplex CLI abort sentence"
      pattern: "130"
---

<objective>
Correct every explanation and how-to sentence outside the exit-code references that Phase 28 made
false: the CANCELLED state in the web API, the manual-duplex abort outcome, the architecture's
failure endings, which upload failures retry and fall back, and the zero-page / all-blank wording.

Purpose: the ROADMAP makes documentation cross-cutting -- sentences are corrected in the phase that
changes them. The exit-code tables and the troubleshooting page are plan 28-13.
Output: five corrected docs pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-exception-translation/28-CONTEXT.md
@.planning/phases/28-exception-translation/28-RESEARCH.md
@.planning/phases/28-exception-translation/28-06-SUMMARY.md
@.planning/phases/28-exception-translation/28-10-SUMMARY.md
@.planning/phases/28-exception-translation/28-11-SUMMARY.md
@CLAUDE.md

<interfaces>
Behaviour to document (plans 28-01, 28-03, 28-06, 28-07, 28-08, 28-10, 28-11):
- JobState gains terminal `CANCELLED`, label "Cancelled", rendered muted grey (not an alert); the status poll stops and the Scan button re-enables; the web Abort ends the job CANCELLED with the message "Manual duplex scan cancelled at the flip prompt"; a flip timeout and a shutdown-claimed abort stay ERROR
- CLI prompt: no / Ctrl-D / Ctrl-C -> one line `Manual duplex scan cancelled at the flip prompt`, exit 130; a terminal read error -> `Scan error: Flip prompt failed: <reason>`, exit 1, logged with traceback; timeout unchanged (exit 1)
- Upload: retries (3 attempts, exponential backoff) on every httpx transport error (connection refused, reset, timeouts, a reverse proxy closing the connection) and 5xx; a URL with no or an unsupported scheme is not retried but still falls back when `consume_dir` is set; a 4xx fails at once with Paperless's reason and does not fall back; `Upload to Paperless at <url> failed after 3 attempts: <reason>` when no fallback; a network error while polling an accepted task keeps polling until the deadline; a duplicate FAILURE warns to check Paperless before rescanning; on default paperless-ngx settings a retry after a lost response can create a second copy (accepted, D-10)
- Zero pages: "No pages were scanned" when the scanner returns no pages; "All pages were blank" only when detection removed every page; an empty feeder still reports "No paper detected in feeder"

Doc sites (RESEARCH § "Doc Surfaces to Change (exact)"): web-api.md:101 (states list), :161 (Abort "ends ERROR"); set-up-adf-duplex.md:95 (CLI abort sentence quoting `Scan error: Manual duplex scan aborted at the flip prompt` and exit code 1) and the web bullet above it; architecture.md:30 ("An abort or a timeout fails the job"), :46 ("network error, timeout, auth failure" fallback claim; a 4xx never fell back), :56 (failure list including "an aborted flip"); consume-directory-fallback.md:9-11 and :52-55 (retry conditions); empty-page-detection.md ~60-70 (no existing all-blank sentence; add one).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CANCELLED and the flip-prompt abort in web-api, set-up-adf-duplex and architecture</name>
  <files>docs/reference/web-api.md, docs/how-to/set-up-adf-duplex.md, docs/explanation/architecture.md</files>
  <read_first>
    - docs/reference/web-api.md (whole file)
    - docs/how-to/set-up-adf-duplex.md (manual duplex section)
    - docs/explanation/architecture.md (whole file)
    - src/saneless/pipeline.py flip match and src/saneless/worker.py _scan_job endings (as left by plans 28-07 and 28-11)
  </read_first>
  <action>
    Per D-01 / D-02 / D-03:
    - web-api.md: add `CANCELLED` to the possible-states list at ~101 (after `FALLBACK`) with a short clause that it is terminal, so polling stops and the Scan button re-enables; at ~161 change the Abort description so the job ends `CANCELLED` (not `ERROR`) with the cancel message; keep the "first answer is final" and job_id rules unchanged.
    - set-up-adf-duplex.md manual duplex section: web bullet gains that Abort scan ends the job as Cancelled (shown in grey, not as an error); rewrite the CLI sentence to: answering no, pressing Ctrl-C, or closing input (Ctrl-D) cancels the scan, prints `Manual duplex scan cancelled at the flip prompt` and exits with code 130; an error reading the terminal at the prompt fails the scan straight away with a `Scan error:` line and exit code 1, and the error is logged; nothing is uploaded in either case. Keep the timeout sentence (still a failure).
    - architecture.md: line ~30 -- an abort cancels the job (CANCELLED) and a timeout fails it; line ~46 -- the fallback is taken for network errors, timeouts and server errors after the retries, and for a malformed URL without retrying, never for a rejected (4xx, e.g. authentication) upload; line ~56 -- remove "an aborted flip" from the failure list and add one sentence for the third ending: an operator's abort is recorded CANCELLED and logged at INFO, a failure is recorded ERROR and logged with its traceback, a shutdown during a flip wait is recorded as a restart. Where the page lists terminal states, include CANCELLED.
  </action>
  <verify>
    <automated>grep -n "CANCELLED" docs/reference/web-api.md docs/explanation/architecture.md && grep -n "130" docs/how-to/set-up-adf-duplex.md && uv run mkdocs build --strict -d "$(mktemp -d)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "aborted at the flip prompt" docs` returns nothing
    - `grep -n "CANCELLED" docs/reference/web-api.md` matches at least twice (states list and Abort)
    - `grep -n "auth failure" docs/explanation/architecture.md` returns nothing
    - `grep -n "exit code 130" docs/how-to/set-up-adf-duplex.md` matches
    - `uv run mkdocs build --strict -d "$(mktemp -d)"` exits 0
  </acceptance_criteria>
  <done>The web API, duplex how-to and architecture pages describe a cancel as a cancel.</done>
</task>

<task type="auto">
  <name>Task 2: Retry and fallback rules, and the zero-page / all-blank wording</name>
  <files>docs/explanation/consume-directory-fallback.md, docs/explanation/empty-page-detection.md</files>
  <read_first>
    - docs/explanation/consume-directory-fallback.md (whole file)
    - docs/explanation/empty-page-detection.md (whole file)
    - src/saneless/paperless.py upload_document and poll_task docstrings (as left by plans 28-06 and 28-10)
    - src/saneless/pipeline.py _require_pages and _drop_empty_pages (plan 28-03)
  </read_first>
  <action>
    Per D-10 / D-11 / EXC-03:
    - consume-directory-fallback.md lines ~9-11: the upload is attempted up to 3 times with exponential backoff when paperless-ngx cannot be reached or answers with a server error -- connection refused or reset, a timeout, or a reverse proxy closing the connection. Conditions list ~52-55: (1) the upload fails with one of those, or `paperless.url` has no usable `http://`/`https://` scheme (not retried, since retrying cannot help, but still falls back so the scan is not lost); (2) the attempts are exhausted; (3) `consume_dir` is set. Add: a rejected upload (a 4xx, such as a bad token or an invalid title) is reported at once with Paperless's reason and does not fall back. Add a short "Network blips after the upload" paragraph: once paperless-ngx has accepted the upload, a network error while waiting for the task keeps waiting until the deadline instead of failing the scan. Add a "Duplicates" note: if the connection drops after paperless-ngx received the file, the retry can create a second copy of the document on default paperless-ngx settings; when paperless-ngx is set to reject duplicates, the failure says the document may already be in Paperless, so check before scanning again. Do not quote full error messages.
    - empty-page-detection.md near the enable/disable section (~60-70): add that when detection removes every page of a scan, the scan fails with "All pages were blank" and nothing is uploaded -- lower the thresholds or disable detection for documents that really are blank; and that a scanner that returns no pages at all is reported separately as "No pages were scanned" (an empty feeder still says no paper was detected), so the two are never confused (EXC-03, N-06).
  </action>
  <verify>
    <automated>grep -n "All pages were blank" docs/explanation/empty-page-detection.md && grep -n "No pages were scanned" docs/explanation/empty-page-detection.md && grep -in "malformed\|scheme" docs/explanation/consume-directory-fallback.md && uv run mkdocs build --strict -d "$(mktemp -d)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "All pages were blank" docs/explanation/empty-page-detection.md` matches once
    - `grep -n "No pages were scanned" docs/explanation/empty-page-detection.md` matches once
    - `grep -in "duplicate" docs/explanation/consume-directory-fallback.md` matches
    - `grep -in "4xx\|rejected" docs/explanation/consume-directory-fallback.md` matches
    - `uv run mkdocs build --strict -d "$(mktemp -d)"` exits 0
    - `uv run pytest tests/test_deployment_config.py -q` still passes (existing doc-truth tests read these pages)
  </acceptance_criteria>
  <done>Fallback and empty-page explanations match the new behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> operator decisions | Operators decide whether to rescan based on the docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-52 | Tampering (duplicate documents) | operator rescans after a lost-response retry | mitigate | consume-directory-fallback.md documents the duplicate risk and the check-before-rescanning message (D-10) |
| T-28-53 | Repudiation (false docs) | "auth failure falls back" claim | mitigate | Removed; 4xx documented as no fallback |
</threat_model>

<verification>
- `uv run mkdocs build --strict -d "$(mktemp -d)"`
- `uv run pytest tests/test_deployment_config.py -q`
- `grep -rn "aborted at the flip prompt\|detected as empty" docs` returns nothing
</verification>

<success_criteria>
- Five pages corrected for CANCELLED, the 130 abort, retry/fallback rules and zero-page wording
- Strict docs build green
</success_criteria>

<output>
Create `.planning/phases/28-exception-translation/28-14-SUMMARY.md` when done
</output>
