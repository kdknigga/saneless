---
phase: 28-exception-translation
plan: 08
type: tdd
wave: 2
depends_on: [28-01]
files_modified:
  - src/saneless/web/static/app.css
  - tests/test_browser.py
  - .planning/UI-SPEC.md
autonomous: true
requirements: [EXC-04]
decisions: [D-01]

must_haves:
  truths:
    - "A CANCELLED job's status line and history cell render in Pico's muted colour (var(--pico-muted-color)), not the error red, in the light scheme, the dark scheme and the forced-dark data-theme (D-01, Claude's Discretion: styling tokens)"
    - "The four status classes (done, error, fallback, cancelled) compute four distinct colours, and `.status-cancelled` meets WCAG AA 4.5:1 on the page/td surface and on the card in both schemes, measured in Chromium (D-01, UI-SPEC convention rule 5)"
    - "With a CANCELLED job as the current job, the Scan button is enabled, the status area stops polling and the history table refreshes to show the Cancelled row (D-01: terminal)"
    - "UI-SPEC records the CANCELLED status row and its measured ratios"
  artifacts:
    - path: "src/saneless/web/static/app.css"
      provides: ".status-cancelled rule using the Pico muted token"
      contains: ".status-cancelled"
    - path: "tests/test_browser.py"
      provides: "Playwright CANCELLED rendering, contrast and terminal-behaviour tests"
    - path: ".planning/UI-SPEC.md"
      provides: "CANCELLED state row and measured contrast rows"
  key_links:
    - from: "src/saneless/web/static/app.css .status-cancelled"
      to: "Pico --pico-muted-color"
      via: "direct token use, no app-owned override (UI-SPEC convention rule 4)"
      pattern: "var\\(--pico-muted-color\\)"
---

<objective>
Style the CANCELLED state neutral and muted (D-01), prove it with Playwright in light, dark and
forced-dark schemes (computed colour, AA contrast), and prove the state behaves as terminal in the
browser: polling stops, the Scan button re-enables and history refreshes.

Purpose: success criterion 4's "recorded as a cancelled job" must also be shown as one, never red.
Project rule: browser checks are automated with Playwright, never manual.
Output: one CSS rule, browser tests, UI-SPEC rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-exception-translation/28-CONTEXT.md
@.planning/phases/28-exception-translation/28-RESEARCH.md
@.planning/phases/28-exception-translation/28-PATTERNS.md
@.planning/UI-SPEC.md
@CLAUDE.md

<interfaces>
From plan 28-01: `JobState.CANCELLED` is terminal; status.html renders `<p class="status-cancelled">&#8856; Cancelled: {{ job.title }}</p>` plus the history-reload div; history.html gives the cell `class="status-cancelled"`.

From src/saneless/web/static/app.css (19-56): `.status-done { color: var(--pico-ins-color, green); }`, `.status-error { color: var(--pico-del-color, red); }`; the app-owned `--saneless-status-fallback` pair with its light `:root`, `@media (prefers-color-scheme: dark) :root:not([data-theme])` and `[data-theme="dark"]` blocks (only for colours Pico lacks).

Research measurement (to confirm in Chromium): vendored pico-2.1.1 `--pico-muted-color` is `#646b79` light and `#7b8495` dark; ratios 5.36:1 light page, 4.77:1 dark page/td, 4.53:1 dark card.

From tests/test_browser.py (line numbers approximate): `_PROBE_STATUS_COLOURS` JS (774-787) iterating `["status-done", "status-error", "status-fallback"]`; `_PROBE_CONTEXT_CONTRAST` (~842); `TestFallbackRendering` with `fallback_page` fixture (1028-1072: create job, `job_store.finish_job(job.id, JobState.FALLBACK, result=...)`, set `app.state.worker._current_job_id`, teardown clears the pointer AND deletes the job -- see its comment 1063-1070), `_goto` (1074-1078), distinct-colours assertion `len(set(colours.values())) == 3` (~1111), Scan-button re-enable (1120-1144) and history refresh (1146-1166) tests, AA contrast parametrisation (1214-1240, `Literal` class list), forced-dark test (1242-1255).
From .planning/UI-SPEC.md: colour table (~140-150), "Measured status contrast" table (152-163), convention rules (176-183), status state table (~250-262).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Playwright tests for CANCELLED rendering, then the muted style</name>
  <files>tests/test_browser.py, src/saneless/web/static/app.css</files>
  <read_first>
    - src/saneless/web/static/app.css (whole file)
    - tests/test_browser.py lines 770-860 (probes), 1020-1260 (TestFallbackRendering: fixture, _goto, colour, button, history, AA contrast, forced dark)
    - src/saneless/web/templates/partials/status.html and partials/history.html (as changed by plan 28-01)
    - .planning/UI-SPEC.md lines 136-190
  </read_first>
  <behavior>
    - A `cancelled_page` fixture (mirroring `fallback_page`) finishes a job with `job_store.finish_job(job.id, JobState.CANCELLED, error="Manual duplex scan cancelled at the flip prompt")`, adopts it as current, and on teardown clears the pointer and deletes the job (test class name contains `Cancel`)
    - `#status-area .status-cancelled` is visible with text `Cancelled: <title>`; no element in `#status-area` has `role="alert"`
    - Probe over `["status-done", "status-error", "status-fallback", "status-cancelled"]` returns four distinct computed colours; `.status-cancelled` computed colour equals the computed value of `--pico-muted-color` and differs from `.status-error`'s
    - Light scheme (`color_scheme="light"`) and dark scheme (`color_scheme="dark"`): `.status-cancelled` contrast >= 4.5 on the page/td surface and on the card, via the existing contrast helper (AA parametrisation gains `status-cancelled`)
    - Forced dark (`data-theme="dark"` on a light-scheme context): `.status-cancelled` resolves to the dark muted value
    - With the CANCELLED job current: `#scan-btn` is enabled (not `disabled`, no `aria-busy`), the status area carries no `hx-trigger="every 1s"`, and the history table shows a `td.status-cancelled` with text `Cancelled`
  </behavior>
  <action>
    RED (commit `test(28-08): ...`): add a `TestCancelledRendering` class (or extend the fallback class's parametrisations where they are already shared) covering the behaviors, marked `browser` like its neighbours; extend `_PROBE_STATUS_COLOURS` and the distinct-colour count to four and add `status-cancelled` to the AA contrast `Literal` list and the forced-dark test. Run `uv run pytest tests/test_browser.py -m browser -k "cancel or contrast or dark or colour"`: the cancelled colour assertions fail (unstyled text inherits the body colour).

    GREEN (commit `feat(28-08): ...`) in src/saneless/web/static/app.css per D-01: add `.status-cancelled { color: var(--pico-muted-color); }` next to `.status-done` / `.status-error`, with a comment: a deliberate stop, neutral not red (D-01, N-08); Pico's muted token already swaps per scheme, so it is used directly with no app-owned pair (UI-SPEC convention rule 4). Do not add a `--saneless-status-cancelled` variable unless the measured AA ratio fails; if it does, switch to an app-owned pair following convention rules 1-3 and record why.
  </action>
  <verify>
    <automated>uv run pytest tests/test_browser.py -m browser -k "cancel or Cancel or contrast or dark or colour or fallback" -x -q && uv run pytest tests/test_web_state_rendering.py -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.status-cancelled" src/saneless/web/static/app.css` matches once and the rule body contains `var(--pico-muted-color)`
    - `grep -n '"status-cancelled"' tests/test_browser.py` matches at least 2 lines (probe list and AA parametrisation)
    - `uv run pytest tests/test_browser.py -m browser -k cancel -q` selects at least 4 tests and passes
    - Full browser suite passes: `uv run pytest -m browser -q`
    - Gate commands exit 0; RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>CANCELLED is visibly muted and terminal in both schemes, proven in Chromium. VALIDATION row: EXC-04 (browser).</done>
</task>

<task type="auto">
  <name>Task 2: Record the CANCELLED state and its measured contrast in UI-SPEC</name>
  <files>.planning/UI-SPEC.md</files>
  <read_first>
    - .planning/UI-SPEC.md lines 136-190 and 245-265
    - The Playwright contrast output from Task 1 (run the AA test with `-s` or read the helper's computed values)
  </read_first>
  <action>
    Per D-01 and UI-SPEC convention rule 5, add to .planning/UI-SPEC.md: a colour-table row "Neutral (cancelled) | `--pico-muted-color` | <light computed> | <dark computed> | `.status-cancelled` text: Cancelled status line + CANCELLED history cell"; two rows in "Measured status contrast" for `.status-cancelled` light and dark with the Chromium-computed colour, the page/td ratio, the card ratio and PASS (use the values measured in Task 1, not the research estimates, if they differ); and a status state table row after FALLBACK: `CANCELLED | ⊘ Cancelled: {job.title} (U+2298) | .status-cancelled; no role="alert"`. One sentence noting a cancel is a deliberate stop (EXC-04), so it is neither red nor an alert.
  </action>
  <verify>
    <automated>grep -c "status-cancelled" .planning/UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status-cancelled" .planning/UI-SPEC.md` is at least 4 (colour row, two contrast rows, state row)
    - `grep -n "| CANCELLED |" .planning/UI-SPEC.md` matches once
    - The two contrast rows each contain `PASS` and a ratio of at least `4.5`
  </acceptance_criteria>
  <done>UI-SPEC documents the CANCELLED presentation with measured ratios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-rendered status -> operator perception | Colour and ARIA role communicate failure vs deliberate stop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-29 | Spoofing (UI) | CANCELLED shown as an error | mitigate | Muted token, distinct-colour assertion vs `.status-error`, no `role="alert"` asserted in Chromium |
| T-28-30 | Denial of service (accessibility) | low-contrast muted text | mitigate | AA 4.5:1 measured in Chromium in light, dark and forced dark; failing ratio switches to an app-owned pair |
| T-28-31 | Tampering | test pollution of the shared browser server store | mitigate | Fixture teardown clears the current-job pointer and deletes the job, as the fallback fixture does |
</threat_model>

<verification>
- `uv run pytest -m browser -q`
- `uv run pytest tests/test_web_state_rendering.py -q`
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
</verification>

<success_criteria>
- `.status-cancelled` muted, distinct from error, AA in every scheme, proven with Playwright
- CANCELLED behaves as terminal in the browser
- UI-SPEC updated with measured values
</success_criteria>

<output>
Create `.planning/phases/28-exception-translation/28-08-SUMMARY.md` when done
</output>
