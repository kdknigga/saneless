---
phase: 28-exception-translation
plan: 04
type: tdd
wave: 2
depends_on: [28-01]
files_modified:
  - src/saneless/pdf.py
  - tests/test_pdf.py
autonomous: true
requirements: [EXC-01, EXC-03]
decisions: [D-04, D-08]

must_haves:
  truths:
    - "Each of img2pdf's seven error classes (AlphaChannelError, ExifOrientationError, ImageOpenError, JpegColorspaceError, NegativeDimensionError, PdfTooLargeError, UnsupportedColorspaceError) raised during assembly surfaces as PdfError carrying the original message, with __cause__ the original instance (D-04, success criterion 1)"
    - "img2pdf's bare Exception, TypeError and ValueError, Pillow's OSError (CMYK PNG) and SystemError (0x0 image), and the defensive `convert returned None` path all surface as PdfError chained to their cause (D-04, RESEARCH § img2pdf and Pillow)"
    - "assemble_pdf([]) raises PdfError before img2pdf is called, so img2pdf's 'Unable to process empty list' ValueError is unreachable from any caller (EXC-03 defence in depth, N-06)"
    - "The one-line message names the page count and the target PDF path, then the original text via describe() (D-08)"
    - "Temporary PNG files are still cleaned up when assembly fails"
  artifacts:
    - path: "src/saneless/pdf.py"
      provides: "boundary catch -> PdfError; empty-list refusal"
      contains: "raise PdfError(msg) from exc"
  key_links:
    - from: "src/saneless/pdf.py assemble_pdf"
      to: "src/saneless/exceptions.py PdfError, describe"
      via: "import and narrow-span broad-type except"
      pattern: "from saneless.exceptions import PdfError, describe"
---

<objective>
Make `assemble_pdf` a module boundary that raises only `PdfError` (D-04). Every img2pdf class,
img2pdf's untyped raises, Pillow save failures and the "returned None" guard become `PdfError` with
the original message and chain; an empty page list is refused up front.

Purpose: EXC-01's img2pdf boundary and success criterion 1's seven parametrised rows; the job record
stops calling a full disk a scanner failure (ASSEMBLY category, exit 4 via plan 28-09).
Output: pdf.py boundary, tests/test_pdf.py rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-exception-translation/28-CONTEXT.md
@.planning/phases/28-exception-translation/28-RESEARCH.md
@.planning/phases/28-exception-translation/28-PATTERNS.md
@CLAUDE.md

<interfaces>
From plan 28-01 (src/saneless/exceptions.py): `class PdfError(SanelessError)` (single-message), `def describe(exc: BaseException) -> str` (str(exc) or class name).

From src/saneless/pdf.py `assemble_pdf(images: list[Image.Image], output_dir: Path, filename: str, dpi: int) -> Path` (126-202): body is `output_dir.mkdir(...)`; `with tempfile.TemporaryDirectory(dir=str(output_dir)) as tmp_dir:` saving each image as PNG, `pdf_path = output_dir / filename`, `img2pdf.convert(image_paths, layout_fun=img2pdf.get_fixed_dpi_layout_fun((dpi, dpi)))`, `if pdf_bytes is None: raise RuntimeError("img2pdf.convert returned None")`, `pdf_path.write_bytes(pdf_bytes)`. Docstring has no Raises section. Imports are absolute style for saneless modules elsewhere (`pipeline.py` uses `from saneless.exceptions import ...`).

Narrow-span broad-type precedent: `pipeline.py` `_preserving` docstring (397-405): "The caught type is Exception, deliberately ... always re-raised with the original chained on __cause__. KeyboardInterrupt and SystemExit derive from BaseException and pass through untouched."

Measured: the seven img2pdf classes are each a direct `Exception` subclass (no shared base); img2pdf also raises bare `Exception`, `TypeError`, `ValueError`; `img2pdf.convert([])` -> `ValueError('Unable to process empty list')`; Pillow CMYK PNG save -> `OSError('cannot write mode CMYK as PNG')`; 0x0 image save -> `SystemError('tile cannot extend outside image')`.

From tests/test_pdf.py: imports `import saneless.pdf as pdf_mod`; `test_temp_files_cleaned_on_error` (96-128) replaces `pdf_mod.img2pdf` with a fake whose convert raises RuntimeError("fake img2pdf error") and expects RuntimeError -- must become PdfError. No `from __future__ import annotations` in this test file; keep its import style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Every assembly failure becomes PdfError (seven img2pdf rows plus the untyped paths)</name>
  <files>src/saneless/pdf.py, tests/test_pdf.py</files>
  <read_first>
    - src/saneless/pdf.py (whole file)
    - tests/test_pdf.py lines 1-20 and 90-130 (imports, test_temp_files_cleaned_on_error)
    - src/saneless/pipeline.py lines 395-410 and 440-472 (_preserving docstring and catch)
    - .planning/phases/28-exception-translation/28-RESEARCH.md § "img2pdf 0.6.3 and Pillow 12.1.1", § "Parametrised boundary test shape", Anti-Patterns (tuple catch)
  </read_first>
  <behavior>
    - Parametrised over the seven real `img2pdf.<Class>` types (seven ids, test class name contains `Boundary`): monkeypatch `pdf_mod.img2pdf.convert` to raise `error_cls("boom from img2pdf")`; `assemble_pdf([Image.new("RGB", (100, 100), "white")], tmp_path, "x.pdf", dpi=300)` raises `PdfError` matching `boom from img2pdf`; `excinfo.value.__cause__ is original`; `not isinstance(excinfo.value, error_cls)`
    - Rows for img2pdf's untyped raises: convert raising `Exception("bare")`, `TypeError("typed")`, `ValueError("valued")` -> PdfError with the text and the cause
    - Real Pillow failures: a `Image.new("CMYK", (10, 10))` page -> PdfError whose message contains `cannot write mode CMYK as PNG` and cause is OSError; a `Image.new("RGB", (0, 0))` page -> PdfError whose cause is SystemError
    - convert returning `None` -> PdfError matching `img2pdf.convert returned None`
    - Message shape: `str(err).startswith(f"Could not assemble 1 page(s) into {tmp_path / 'x.pdf'}: ")`
    - `assemble_pdf([], tmp_path, "x.pdf", dpi=300)` raises PdfError matching `no pages` and a patched convert records zero calls
    - `test_temp_files_cleaned_on_error` expects PdfError and still finds no leftover temp files
    - `KeyboardInterrupt` raised from convert propagates as KeyboardInterrupt (caught explicitly in the test with `pytest.raises(KeyboardInterrupt)`, Pitfall 5)
  </behavior>
  <action>
    RED (commit `test(28-04): ...`): add a `TestPdfBoundary` class to tests/test_pdf.py with the rows above (module or class docstring citing M-17, D-04, success criterion 1) and switch `test_temp_files_cleaned_on_error` to PdfError. Run: they fail.

    GREEN (commit `feat(28-04): ...`) in src/saneless/pdf.py per D-04 / D-08:
    - `from saneless.exceptions import PdfError, describe`.
    - Before any filesystem work: `if not images:` raise `PdfError("Could not assemble a PDF: no pages were given")` (message in a variable). Comment: the pipeline's `_require_pages` already refuses an empty batch; this keeps img2pdf's empty-list ValueError unreachable from any caller of this public function (N-06).
    - Compute `pdf_path = output_dir / filename` before the try. Wrap everything from `output_dir.mkdir(...)` through the end of the `with tempfile.TemporaryDirectory(...)` block (so temp-dir cleanup errors are covered) in one `try`. Inside, replace the `RuntimeError` with `PdfError("img2pdf.convert returned None")`. Clauses: `except PdfError: raise` first (so the None-guard is not re-wrapped), then `except Exception as exc:` building `msg = f"Could not assemble {len(images)} page(s) into {pdf_path}: {describe(exc)}"` and `raise PdfError(msg) from exc`.
    - Add a docstring paragraph mirroring `_preserving`'s justification: the caught type is Exception deliberately, because img2pdf raises seven unrelated classes plus bare Exception, TypeError and ValueError, and Pillow raises OSError and SystemError, so a tuple would leak; the span is narrow (assembly only); the original is always chained; KeyboardInterrupt and SystemExit pass through. Add a Raises section naming PdfError.
    - Do not attempt to keep pages on failure (HARD-01, Phase 29).
  </action>
  <verify>
    <automated>uv run pytest tests/test_pdf.py -x -q && uv run pytest tests/test_pipeline.py tests/test_outcomes_e2e.py -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "raise RuntimeError" src/saneless/pdf.py` returns nothing
    - `grep -n "raise PdfError(msg) from exc" src/saneless/pdf.py` matches once
    - `grep -n "except PdfError:" src/saneless/pdf.py` matches once and appears before `except Exception as exc:`
    - `uv run pytest tests/test_pdf.py -k Boundary -q` selects at least 14 tests (7 img2pdf classes + untyped/Pillow/None/empty rows) and passes
    - Gate commands exit 0; RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>assemble_pdf raises only PdfError. VALIDATION row: EXC-01 (seven img2pdf classes + ValueError + OSError + SystemError + RuntimeError -> PdfError).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanner image data -> Pillow/img2pdf | Device-produced rasters of arbitrary mode and size |
| PdfError text -> job.error / CLI line | Library messages and the output path become user-visible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-12 | Denial of service | unhandled library exception killing a CLI run with a traceback | mitigate | Boundary catch of Exception around assembly only; every row tested |
| T-28-13 | Information disclosure | output path in the message | accept | The path is under the operator's own tmp_dir/data_dir and already appears in INFO logs; no secret is involved |
| T-28-14 | Tampering (masking) | broad catch hiding a cancel | mitigate | BaseException subclasses (KeyboardInterrupt, SystemExit) are not caught; test asserts KeyboardInterrupt propagates |
</threat_model>

<verification>
- `uv run pytest tests/test_pdf.py -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
</verification>

<success_criteria>
- Seven img2pdf class rows plus untyped, Pillow, None and empty-list rows all assert PdfError, message, cause
- No third-party exception leaves assemble_pdf
</success_criteria>

<output>
Create `.planning/phases/28-exception-translation/28-04-SUMMARY.md` when done
</output>
