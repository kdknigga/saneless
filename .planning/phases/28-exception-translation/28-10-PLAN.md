---
phase: 28-exception-translation
plan: 10
type: tdd
wave: 3
depends_on: [28-06]
files_modified:
  - src/saneless/paperless.py
  - tests/test_paperless.py
autonomous: true
requirements: [EXC-01]
decisions: [D-08, D-10, D-11]

must_haves:
  truths:
    - "A transport error (ConnectError, ReadError, ReadTimeout, RemoteProtocolError) while polling an accepted task does not fail the poll: polling continues within the same monotonic deadline and succeeds if the task later reports SUCCESS (D-11, M-17)"
    - "If the deadline expires after transport errors, PaperlessTimeoutError names the task id and `last error: <describe(last transport error)>` (D-11, OUTC-07)"
    - "A non-200 poll response still raises PaperlessError at once (OUTC-07 kept); a 200 poll body that is not JSON raises PaperlessError chained to the ValueError (EXC-01)"
    - "A FAILURE task whose text contains 'duplicate of' (any case), or whose result_data has `duplicate_of`, raises PaperlessError that says the document may already be in Paperless and to check before scanning again; a v10 duplicate with no message still gets that sentence (D-10)"
    - "get_tags and get_correspondents raise PaperlessError naming the endpoint and base URL with the original text for any httpx.HTTPError (including a non-2xx) and for a non-JSON body, chained to the cause (D-11, EXC-01)"
  artifacts:
    - path: "src/saneless/paperless.py"
      provides: "poll continuation through transport errors, duplicate wording, wrapped metadata fetches"
      contains: "last error: "
  key_links:
    - from: "src/saneless/paperless.py poll_task"
      to: "monotonic deadline"
      via: "transport error recorded then fall through to the remaining/sleep logic"
      pattern: "except httpx.TransportError as exc"
---

<objective>
Finish the Paperless client boundary on the read side (EXC-01): `poll_task` keeps polling through
transport errors until its deadline instead of failing a job whose upload already succeeded (D-11),
a duplicate-task failure tells the user the document may already be in Paperless (D-10), and the tag
and correspondent fetches raise only `PaperlessError`.

Purpose: M-17's "a transport blip after a successful upload failed the job and invited a duplicate
rescan", and no httpx type escaping `get_tags` / `get_correspondents`.
Output: paperless.py read-side changes, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-exception-translation/28-CONTEXT.md
@.planning/phases/28-exception-translation/28-RESEARCH.md
@.planning/phases/28-exception-translation/28-PATTERNS.md
@.planning/phases/28-exception-translation/28-06-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 28-06 (src/saneless/paperless.py): `self._base_url` (configured URL, no token); `_render_error_body(response) -> str` (one line); poll non-200 raises `PaperlessError(f"Paperless task poll failed ({status} {reason}): {body}")`.
From plan 28-01: `describe(exc)`.

From src/saneless/paperless.py (pre-28-06 line numbers):
- `_failure_message(task: dict[str, object]) -> str` (97-123): v9 `result` string, else v10 `result_data["error_message"|"reason"]`, else `_NO_FAILURE_MESSAGE`
- `poll_task(self, task_id: str, timeout: int | float = 300) -> dict[str, object]` (385-462): `deadline = time.monotonic() + timeout`, `delay = 0.5`; loop: `response = self._client.get("/api/tasks/", params={"task_id": task_id})` (no transport handling); non-200 raise; `task = _extract_task(response.json())`; SUCCESS returns; other terminal statuses raise `PaperlessError(f"Paperless task {task_id} ended {status}: {_failure_message(task)}")`; `remaining = deadline - time.monotonic()`; `if remaining <= 0:` warn + `PaperlessTimeoutError(f"Paperless task {task_id} did not finish within {timeout}s")`; `time.sleep(min(delay, remaining))`; `delay = min(delay * 2, 30.0)`
- `get_tags()` (509-523) and `get_correspondents()` (525-539): `self._client.get(...)`, `raise_for_status()`, `response.json()`, results-or-list tolerance

paperless-ngx duplicate shapes (RESEARCH, verified v3.1.3 and v2.17.1): v9 `result` = `Not consuming: It is a duplicate of document #N`; v2.x `Not consuming <file>: It is a duplicate of <title> (#id).`; v10 `result_data` = `{"duplicate_of": N, "duplicate_in_trash": bool}` with no error_message. Rule: `"duplicate of"` in the failure text case-insensitively, or `"duplicate_of"` key in `result_data`.

From tests/test_paperless.py: `_poll_client` (89-97); poll tests near the renderer tests; web routes.py metadata catch is unaffected (SWP-04 is Phase 32).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: poll_task continues through transport errors to its deadline; duplicate wording; non-JSON poll body</name>
  <files>src/saneless/paperless.py, tests/test_paperless.py</files>
  <read_first>
    - src/saneless/paperless.py (whole file as left by plan 28-06)
    - tests/test_paperless.py poll tests (grep `poll_task`) and `_poll_client`
    - .planning/phases/28-exception-translation/28-RESEARCH.md § Pattern 7, § "paperless-ngx task and error shapes"
  </read_first>
  <behavior>
    - Handler raising `httpx.ReadError("reset")` twice then returning a SUCCESS task list: `poll_task("t1", timeout=5)` returns the task; 3 handler calls; `time.sleep` patched to a recorder (test names contain `poll`)
    - Parametrised over ConnectError, ReadTimeout (empty message), RemoteProtocolError: handler always raises, `timeout=0.05` with real monotonic time and patched sleep advancing nothing -> `PaperlessTimeoutError` whose message contains `Paperless task t1 did not finish within 0.05s` and `last error: ` followed by `describe(exc)` (`ReadTimeout` for the empty one); the loop terminates (pytest timeout would catch a hang)
    - Deadline expiry with no transport error keeps today's message without a `last error` suffix
    - 401 poll response raises PaperlessError at once (1 call) -- unchanged OUTC-07
    - 200 poll body `<html>` -> PaperlessError matching `not JSON`, cause is ValueError
    - FAILURE with `result = "Not consuming: It is a duplicate of document #42"` -> PaperlessError containing the original text and `the document may already be in Paperless; check before scanning again`
    - FAILURE v2 text `Not consuming scan.pdf: It is a Duplicate of Invoice (#7).` -> same sentence (case-insensitive)
    - FAILURE v10 `result_data = {"duplicate_of": 42, "duplicate_in_trash": False}` with no message -> contains `_NO_FAILURE_MESSAGE` text and the duplicate sentence
    - FAILURE without any duplicate marker -> no duplicate sentence
  </behavior>
  <action>
    RED (commit `test(28-10): ...`): add the poll tests above (docstrings citing D-10, D-11, M-17, OUTC-07). Run: they fail.

    GREEN (commit `feat(28-10): ...`):
    - `poll_task`: before the loop set `last_transport_error: httpx.TransportError | None = None`. Wrap only the `self._client.get(...)` call: `except httpx.TransportError as exc:` record it, `logger.warning("Polling task %s failed, retrying until the deadline: %s", task_id, describe(exc))`, and set `response = None`; only when `response is not None` run the existing non-200 / JSON / status handling, so control always falls through to the unchanged `remaining` / sleep logic (no `continue` that skips the deadline check). Wrap `response.json()` so a ValueError raises `PaperlessError(f"Paperless at {self._base_url} returned a task response that is not JSON: {describe(exc)}") from exc`. On expiry append `f"; last error: {describe(last_transport_error)}"` when one was recorded, raising `PaperlessTimeoutError(msg) from last_transport_error` in that case. Keep the monotonic deadline and the clamped sleep. Update the docstring: transport errors while polling an accepted task keep polling (the upload succeeded, and failing now invites a duplicate rescan, D-11); a non-200 still ends the poll at once.
    - Add `_DUPLICATE_HINT = "the document may already be in Paperless; check before scanning again"` and `def _is_duplicate_failure(task: dict[str, object], message: str) -> bool` (text contains "duplicate of" case-insensitively, or `result_data` is a dict containing `duplicate_of`). In the terminal-status branch append `f"; {_DUPLICATE_HINT}"` to the message when it matches. Docstring cites D-10's accepted risk: on current paperless-ngx a lost-response retry usually creates a silent second copy; only with CONSUMER_DELETE_DUPLICATES is it reported, and then the user is told to check.
  </action>
  <verify>
    <automated>uv run pytest tests/test_paperless.py -k "poll or duplicate" -x -q && uv run pytest tests/test_paperless.py tests/test_outcomes_e2e.py -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "except httpx.TransportError as exc" src/saneless/paperless.py` matches inside `poll_task` (and the upload loop from plan 28-06)
    - `grep -n "last error: " src/saneless/paperless.py` matches once
    - `grep -n "_DUPLICATE_HINT" src/saneless/paperless.py` matches at least twice (definition and use)
    - `grep -n "time.monotonic()" src/saneless/paperless.py` still anchors the poll deadline (no wall-clock `time.time()` introduced)
    - `uv run pytest tests/test_paperless.py -k "poll or duplicate" -q` selects at least 9 tests and passes in under 30 s
    - Gate commands exit 0; RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>A transient network failure after a successful upload no longer fails the job; duplicates are flagged. VALIDATION row: EXC-01 (poll continues to deadline).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: get_tags and get_correspondents raise only PaperlessError</name>
  <files>src/saneless/paperless.py, tests/test_paperless.py</files>
  <read_first>
    - src/saneless/paperless.py get_tags / get_correspondents (end of file)
    - src/saneless/web/routes.py lines 60-100 (the metadata-fetch catch that consumes these; unchanged)
    - tests/test_paperless.py existing get_tags / get_correspondents tests (grep `get_tags`)
  </read_first>
  <behavior>
    - Parametrised over method (`get_tags`, `get_correspondents`) x failure (ConnectError, ReadTimeout, 500 response, 403 response, 200 non-JSON body): each raises PaperlessError whose message starts `Could not fetch tags from Paperless at http://paperless.test:` or `Could not fetch correspondents from ...` and ends with `describe(cause)`; `__cause__` is the httpx exception or ValueError; not an httpx type (test names contain `metadata`)
    - Successful list and paginated-dict responses still return the results unchanged
  </behavior>
  <action>
    RED (commit `test(28-10): ...`): add the parametrised metadata tests. Run: they fail.

    GREEN (commit `feat(28-10): ...`): extract one private helper, e.g. `_fetch_collection(self, path: str, noun: str) -> list[dict[str, object]]`, used by both methods: `try` the GET plus `raise_for_status()`; `except httpx.HTTPError as exc` -> `PaperlessError(f"Could not fetch {noun} from Paperless at {self._base_url}: {describe(exc)}") from exc`; then `response.json()` with `except ValueError as exc` -> same prefix; keep the results-or-list tolerance and each public method's docstring, adding a Raises section (D-11, EXC-01). Leave routes.py untouched (its cause logging is SWP-04, Phase 32).
  </action>
  <verify>
    <automated>uv run pytest tests/test_paperless.py -x -q && uv run pytest tests/test_web.py tests/test_cache.py -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Could not fetch {noun} from Paperless at" src/saneless/paperless.py` matches once
    - `grep -c "raise_for_status()" src/saneless/paperless.py` is at most 2 (upload and the shared helper)
    - `uv run pytest tests/test_paperless.py -k metadata -q` selects at least 10 tests and passes
    - Full non-browser suite passes; gate commands exit 0; RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>No httpx exception type escapes the Paperless client. VALIDATION row: EXC-01 (get_tags/correspondents).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paperless task API -> job outcome | Task status and failure text decide whether a scan is reported failed |
| network -> poll loop | Transient failures and hostile slow responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-38 | Denial of service | poll loop spinning forever on transport errors | mitigate | Transport errors fall through to the unchanged monotonic deadline and clamped sleep; parametrised expiry test |
| T-28-39 | Tampering (duplicate document) | user rescans after a job failed only because a poll blipped | mitigate | Poll continues to the deadline (D-11); duplicate FAILURE carries the check-first hint (D-10) |
| T-28-40 | Information disclosure | failure text from Paperless in job.error | accept | Paperless's own task message, already shown before this phase; the token is never in it |
| T-28-41 | Spoofing (UI text) | malicious long task `result` | accept | Existing behaviour; the web template autoescapes and Phase 30 (APPL-04) reframes messages |
</threat_model>

<verification>
- `uv run pytest tests/test_paperless.py -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
</verification>

<success_criteria>
- poll_task survives transport errors within its deadline and names the last one on expiry
- Duplicate failures carry the check-before-rescanning hint for v2, v9 and v10 shapes
- get_tags/get_correspondents raise only PaperlessError
</success_criteria>

<output>
Create `.planning/phases/28-exception-translation/28-10-SUMMARY.md` when done
</output>
