---
phase: 28-exception-translation
plan: 07
type: tdd
wave: 2
depends_on: [28-01]
files_modified:
  - src/saneless/worker.py
  - tests/test_worker.py
autonomous: true
requirements: [EXC-04, EXC-05]
decisions: [D-01, D-02]

must_haves:
  truths:
    - "A job whose pipeline raises ScanCancelledError is recorded JobState.CANCELLED with the cancel message in error and no error_category, logged once at INFO with no exc_info (D-01, N-08)"
    - "A job whose flip answer was claimed by shutdown is still recorded ERROR with RESTART_REASON and logged at INFO, even though the pipeline raised ScanCancelledError: the shutdown check runs first (D-02, Phase 26 WR-06)"
    - "Every other job failure -- a classified category (ScanError, PaperlessError, ConfigError, PdfError) or UNKNOWN (RuntimeError) -- is recorded ERROR with its category and logged at ERROR with exc_info whose value is the raised exception (EXC-05)"
    - "The three endings (shutdown, cancel, failure) are three explicit branches; the old `category is None` sentinel is gone (D-01)"
    - "Cancelled jobs never count toward degraded worker health (D-01, Phase 26 D-10)"
  artifacts:
    - path: "src/saneless/worker.py"
      provides: "three explicit job endings with exc_info failure logging"
      contains: "JobState.CANCELLED"
  key_links:
    - from: "src/saneless/worker.py _scan_job"
      to: "saneless.exceptions.ScanCancelledError"
      via: "isinstance branch after the shutdown check"
      pattern: "isinstance\\(exc, ScanCancelledError\\)"
    - from: "src/saneless/worker.py _scan_job failure branch"
      to: "logging exc_info"
      via: "logger.exception inside the except block"
      pattern: "logger\\.exception\\("
---

<objective>
Give the web worker three explicit job endings: shutdown (ERROR, RESTART_REASON, INFO), cancel
(CANCELLED, INFO, no traceback), and failure (ERROR with category, logged with `exc_info`).

Purpose: EXC-04's worker half (a cancel is recorded as cancelled) and EXC-05 (every failure logged
with its traceback so the operator can find the cause). The pipeline starts raising
`ScanCancelledError` for a flip abort in plan 28-11; this plan makes the worker ready for it and
proves it with a stubbed pipeline.
Output: worker.py `_scan_job` except block, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-exception-translation/28-CONTEXT.md
@.planning/phases/28-exception-translation/28-RESEARCH.md
@.planning/phases/28-exception-translation/28-PATTERNS.md
@CLAUDE.md

<interfaces>
From plan 28-01: `saneless.exceptions.ScanCancelledError(SanelessError)`; `JobState.CANCELLED` (terminal); `classify_error(ScanCancelledError(...))` returns UNKNOWN (so it must be tested before classifying).

From src/saneless/worker.py:
- imports (18-43) relative: `from .exceptions import ConfigError`
- `_OwedWrite(state, error=..., category=...)` (96-118): frozen, state-agnostic; its attribute docstring says "for an ERROR write"
- `WorkerFlipCoordinator.aborted_by_shutdown` property (~255-261)
- `ScanWorker._failure_record(exc, coordinator=None) -> tuple[str, ErrorCategory | None]` (941-965): returns `(RESTART_REASON, None)` when `coordinator.aborted_by_shutdown`, else `(str(exc), classify_error(exc))`; also used by `_best_effort_fail` (~1003-1005)
- `_record_loop_failure` (~967-979): degraded counting is loop-level only
- `_scan_job` except block (1313-1336): `error, category = self._failure_record(exc, coordinator)`; long comment about owed writes (keep it); `self._finish_or_owe(job.id, _OwedWrite(JobState.ERROR, error=error, category=category))`; `if category is None: logger.info("Job %s ended by shutdown: %s", ...)` else `logger.error("Job %s failed (%s): %s", job.id, category.value.lower(), exc)`

From tests/test_worker.py: `test_worker_sets_error_on_failure` (255-290) patches `saneless.worker.run_pipeline` with a raising function -- the seam for new tests; `_worker_records(caplog, level, text)` (1988-1998); degraded-health tests (3194-3309); shutdown RESTART_REASON tests (1709-1801) must stay green; abort-via-real-pipeline tests (422, 1201-1229, 1840-1873) assert ERROR today and stay green here (the pipeline still raises ScanError until plan 28-11).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Worker records CANCELLED for ScanCancelledError and logs every failure with exc_info</name>
  <files>src/saneless/worker.py, tests/test_worker.py</files>
  <read_first>
    - src/saneless/worker.py lines 18-45, 90-120, 180-270 (WorkerFlipCoordinator shutdown marker), 925-1015 (_failure_record, _record_loop_failure, _best_effort_fail), 1240-1360 (_scan_job)
    - tests/test_worker.py lines 1-110 (imports, fixtures), 250-295, 1700-1810, 1980-2000, 3190-3310
    - .planning/phases/28-exception-translation/28-RESEARCH.md § Pattern 5 and its caplog test pattern
  </read_first>
  <behavior>
    - Stubbed `saneless.worker.run_pipeline` raising `ScanCancelledError("Manual duplex scan cancelled at the flip prompt")`: job ends `JobState.CANCELLED`, `error == "Manual duplex scan cancelled at the flip prompt"`, `error_category is None`; exactly one `saneless.worker` INFO record containing `cancelled` and the job id, with `exc_info is None`; no ERROR record for that job (test names contain `cancel`)
    - Stubbed pipeline raising `ScanError("Paper jam")`: ERROR, category SCANNER; one ERROR record for the job whose `exc_info[1] is` the raised instance (test names contain `exc_info`)
    - Parametrised over `PaperlessError`, `ConfigError`, `PdfError`, `RuntimeError`: ERROR with categories UPLOAD, CONFIG, ASSEMBLY, UNKNOWN respectively; each ERROR record has `exc_info[1] is` the raised instance
    - A job whose WorkerFlipCoordinator was answered by `abort_for_shutdown` and whose pipeline stub raises ScanCancelledError: ERROR with `error == RESTART_REASON`, INFO "ended by shutdown" record, no CANCELLED (construct via the existing shutdown-test setup or by driving the coordinator directly)
    - Submitting at least `_DEGRADED_AFTER` consecutive jobs that each end CANCELLED leaves `worker.health` not degraded and the next submit accepted
    - Existing shutdown, abort-through-real-pipeline, timeout and degraded tests stay green unchanged
  </behavior>
  <action>
    RED (commit `test(28-07): ...`): add the tests above to tests/test_worker.py (class or functions following the file's style, docstrings citing D-01, N-08, EXC-05, Phase 26 D-10), reusing `_worker_records` and the `run_pipeline` monkeypatch seam. Run: they fail.

    GREEN (commit `feat(28-07): ...`) in src/saneless/worker.py:
    - Import `ScanCancelledError` next to `ConfigError`.
    - Rewrite `_scan_job`'s `except Exception as exc:` block into three explicit branches, keeping the existing owed-write comment above them: (1) `if coordinator is not None and coordinator.aborted_by_shutdown:` -> `_OwedWrite(JobState.ERROR, error=RESTART_REASON)`, `logger.info("Job %s ended by shutdown: %s", job.id, exc)`; (2) `elif isinstance(exc, ScanCancelledError):` -> `_OwedWrite(JobState.CANCELLED, error=str(exc))`, `logger.info("Job %s cancelled: %s", job.id, exc)` with no exc_info (N-08: a cancel is not a failure and is not logged at ERROR); (3) `else:` -> `category = classify_error(exc)`, `_OwedWrite(JobState.ERROR, error=str(exc), category=category)`, and `logger.exception("Job %s failed (%s): %s", job.id, category.value.lower(), exc)` called inside the except block (EXC-05; it reads sys.exc_info()). Each branch calls `self._finish_or_owe(job.id, owed)` then the block returns. Comment: the shutdown check stays first because a shutdown-claimed Abort also reaches the pipeline as ScanCancelledError (D-02, WR-06).
    - Keep `_failure_record` for `_best_effort_fail` (it still returns the shutdown or failure record there); update its docstring to say `_scan_job` now distinguishes the cancel ending itself. If `_scan_job` no longer calls it, keep behaviour identical for `_best_effort_fail`.
    - Widen `_OwedWrite`'s docstring from "for an ERROR write" to cover a CANCELLED write.
    - No change to degraded counting: job endings never call `_record_loop_failure`; the new test proves a cancel cannot count.
  </action>
  <verify>
    <automated>uv run pytest tests/test_worker.py -k "cancel or exc_info or shutdown or degraded or abort" -x -q && uv run pytest tests/test_worker.py tests/test_app_lifespan.py tests/test_outcomes_e2e.py -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "if category is None:" src/saneless/worker.py` returns nothing
    - `grep -n "isinstance(exc, ScanCancelledError)" src/saneless/worker.py` matches once
    - `grep -n 'logger.exception("Job %s failed' src/saneless/worker.py` matches once
    - `grep -n "_OwedWrite(JobState.CANCELLED" src/saneless/worker.py` matches once
    - `uv run pytest tests/test_worker.py -k "cancel or exc_info" -q` selects at least 7 tests and passes
    - Full non-browser suite passes; gate commands exit 0; RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>The worker has three explicit endings and every failure carries its traceback in the log. VALIDATION rows: EXC-04 (worker CANCELLED, INFO, not degraded, shutdown still ERROR), EXC-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline exception -> job store / log | Exception text is persisted and tracebacks are written to the operator's log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-25 | Repudiation | a cancel recorded as a scanner failure (N-08) | mitigate | Explicit ScanCancelledError branch writes CANCELLED; test pins state, category None and INFO level |
| T-28-26 | Repudiation | a shutdown-claimed abort recorded as an operator cancel | mitigate | Shutdown branch first; test drives abort_for_shutdown with a ScanCancelledError pipeline |
| T-28-27 | Information disclosure | tracebacks in the log (EXC-05) | accept | Log is operator-owned; saneless exceptions never carry the token (CFG-05), TOML/validation causes are token-free (plan 28-02, Phase 27 D-14) |
| T-28-28 | Denial of service | cancels degrading the worker and rejecting scans | mitigate | Job endings never touch loop-failure counting; test submits `_DEGRADED_AFTER` cancels and asserts healthy |
</threat_model>

<verification>
- `uv run pytest tests/test_worker.py -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
</verification>

<success_criteria>
- Shutdown, cancel and failure are three explicit, tested endings
- Every failure log record carries exc_info; a cancel logs INFO without it
- Cancels never degrade the worker
</success_criteria>

<output>
Create `.planning/phases/28-exception-translation/28-07-SUMMARY.md` when done
</output>
