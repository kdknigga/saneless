---
phase: 28-exception-translation
plan: 12
type: tdd
wave: 4
depends_on: [28-05, 28-06, 28-11]
files_modified:
  - src/saneless/cli.py
  - tests/test_cli.py
autonomous: true
requirements: [EXC-02]
decisions: [D-03, D-05, D-07]

must_haves:
  truths:
    - "`scan`, `devices`, `auto-profiles` and `serve` call require_sane() as their first statement: with python-sane absent (ModuleNotFoundError) or its libsane extension unloadable (ImportError) each prints one line containing the import's reason and the libsane-dev / sane-backends-devel install hint and exits 2, before loading config or touching the scanner (D-05, EXC-02)"
    - "`saneless jobs` works with python-sane unavailable, and `saneless <cmd> --help` exits 0 without importing sane (D-05, CFG-10)"
    - "`serve` port-bind failure prints one line `Cannot bind to <host>:<port>: <reason>` and exits 2 (D-07 amendment, was ClickException exit 1)"
    - "`serve` when uvicorn.run exits non-zero (startup failure, uvicorn's own exit 3) prints one line and exits 2, never 3 (D-07 amendment)"
    - "`serve` when uvicorn.run returns normally (Ctrl-C graceful stop) exits 0, not 130 (D-03 exception)"
    - "`serve` with a malformed paperless.url exits 3 with one `Paperless error: Paperless URL ... is not valid: ...` line (D-07 row 3)"
    - "`saneless jobs` on a fresh install with no data_dir prints an empty history and exits 0 (Claude's Discretion: jobs on a fresh install)"
  artifacts:
    - path: "src/saneless/cli.py"
      provides: "require_sane first in four commands; serve bind/startup failures as ConfigError (exit 2)"
      contains: "require_sane()"
  key_links:
    - from: "src/saneless/cli.py scan/devices/auto_profiles/serve"
      to: "saneless.scanner.sane_backend.require_sane"
      via: "module-global import, first statement"
      pattern: "from \\.scanner\\.sane_backend import SaneBackend, require_sane"
    - from: "src/saneless/cli.py serve"
      to: "ExitCode.CONFIG"
      via: "ConfigError raised for bind and uvicorn startup failures, mapped by _GuardedGroup"
      pattern: "except SystemExit as exc"
---

<objective>
Finish the CLI side of EXC-02: every SANE command refuses at once with exit 2 and an install hint
when python-sane cannot be imported (D-05), and `serve` joins the shared exit-code table -- a port
bind failure and a uvicorn startup failure both exit 2, a normal Ctrl-C stop still exits 0 (D-07
amendment, D-03).

Purpose: success criterion 2's "a missing python-sane import prints an install hint", and removing
`serve`'s collisions with exit 1 ("scan error") and uvicorn's 3 ("Paperless error").
Output: cli.py command preambles and serve handling, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-exception-translation/28-CONTEXT.md
@.planning/phases/28-exception-translation/28-RESEARCH.md
@.planning/phases/28-exception-translation/28-PATTERNS.md
@.planning/phases/28-exception-translation/28-05-SUMMARY.md
@.planning/phases/28-exception-translation/28-09-SUMMARY.md
@CLAUDE.md

<interfaces>
From plan 28-05: `saneless.scanner.sane_backend.require_sane() -> None` raises `ConfigError("python-sane cannot be imported (<reason>). Install the SANE development package (libsane-dev on Debian/Ubuntu, sane-backends-devel on Fedora/RHEL) and reinstall saneless; see Install on Bare Metal in the documentation") from exc`; it calls `_ensure_sane()`, which imports into the module global `sane_backend.sane`.
From plan 28-06: `PaperlessClient.__init__` raises `PaperlessError("Paperless URL <url> is not valid: <httpx text>")` for httpx.InvalidURL; `create_app` constructs the client.
From plan 28-09: `_GuardedGroup` maps ConfigError -> `str(exc)`, exit 2; PaperlessError -> `Paperless error: ...`, exit 3; ClickException re-raised (click's own exit). `ExitCode` members only, no literals. `_patch_cli` stubs `load_settings`, `configure_logging`, `SaneBackend`, `PaperlessClient`.

From src/saneless/cli.py (as left by plans 28-09 and 28-11):
- `from .scanner.sane_backend import SaneBackend` (module-global import; tests patch `saneless.cli.SaneBackend`)
- `scan` first statement `settings = _load_cli_settings(ctx)`; `devices` `_settings = _load_cli_settings(ctx)`; `auto_profiles` `settings = _load_cli_settings(ctx)`; `jobs` must NOT call require_sane
- `serve(ctx, host, port)`: `_load_cli_settings`; `scanner = SaneBackend(...)`; `app = create_app(settings, scanner)`; bind check `sock.bind(...)` with `except OSError as e: raise click.ClickException(f"Cannot bind to {actual_host}:{actual_port}: {e}") from e`; `click.echo("Serving on ...")`; `uvicorn.run(app, host=..., port=..., log_config=None, log_level=..., access_log=True)`

Measured uvicorn 0.42.0: `uvicorn.run` catches KeyboardInterrupt itself and returns (process exits 0); on a startup failure it calls `sys.exit(STARTUP_FAILURE)` with `STARTUP_FAILURE = 3`, a SystemExit invisible to `except Exception`.

From tests/test_cli.py: `TestServeCommand` (~1525+) with `_capture_uvicorn` patching `saneless.cli.uvicorn.run` (~1535-1553) and the bind test patching `saneless.cli.socket.socket` (~1532), which today expects exit 1. Missing-sane seam: `monkeypatch.setattr(saneless.scanner.sane_backend, "sane", None)` plus `monkeypatch.setitem(sys.modules, "sane", None)` gives ModuleNotFoundError; patching `saneless.scanner.sane_backend._ensure_sane` to raise `ImportError("libsane.so.1: cannot open shared object file: No such file or directory")` gives the extension case.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: require_sane() first in scan, devices, auto-profiles and serve; jobs and --help unaffected</name>
  <files>src/saneless/cli.py, tests/test_cli.py</files>
  <read_first>
    - src/saneless/cli.py (whole file)
    - src/saneless/scanner/sane_backend.py require_sane and _ensure_sane (as added by plan 28-05)
    - tests/test_cli.py lines 81-200 (_patch_cli), the jobs tests (grep `"jobs"`), and TestServeCommand
    - .planning/phases/28-exception-translation/28-RESEARCH.md § Pattern 3
  </read_first>
  <behavior>
    - `_patch_cli` now also patches `saneless.cli.require_sane` to a no-op recorder, so existing tests never import real sane (hermetic)
    - Parametrised over `scan`, `devices`, `auto-profiles`, `serve` x {ModuleNotFoundError seam, ImportError libsane seam} with `require_sane` NOT stubbed: exit 2; `result.stderr` is exactly one line containing `python-sane cannot be imported`, the reason (`import of sane halted` or `libsane.so.1`), `libsane-dev` and `sane-backends-devel`; `load_settings` stub and the scanner class record zero calls (the check runs before anything else) (test names contain `python_sane`)
    - `jobs` with the ModuleNotFoundError seam and real settings pointing `data_dir` at a fresh `tmp_path / "new"`: exit 0, output is the header with no rows, and the directory now exists (fresh-install behaviour pinned)
    - `scan --help`, `devices --help`, `auto-profiles --help`, `serve --help` with the ModuleNotFoundError seam: exit 0, usage printed, the require_sane recorder records no call, and `sys.modules.get("sane")` is still None
  </behavior>
  <action>
    RED (commit `test(28-12): ...`): extend `_patch_cli` with the `require_sane` stub and add the tests above (docstrings citing D-05, CFG-10, M-17). Run: they fail.

    GREEN (commit `feat(28-12): ...`) per D-05: change the import to `from .scanner.sane_backend import SaneBackend, require_sane` (module-global so tests patch `saneless.cli.require_sane`). Make `require_sane()` the first statement of `scan`, `devices`, `auto_profiles` and `serve`, each with a one-line comment: python-sane is mandatory, so a command that needs it refuses before loading config or touching the device, exit 2 through the guard (D-05); `--help` never reaches the body (CFG-10). Do not add it to `jobs`. Do not import sane anywhere at module level.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "python_sane or help or jobs" -x -q && uv run pytest tests/test_cli.py -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "    require_sane()" src/saneless/cli.py` is 4
    - `grep -n "from .scanner.sane_backend import SaneBackend, require_sane" src/saneless/cli.py` matches once
    - `grep -n "^import sane\|^from sane" -r src` returns nothing
    - `uv run pytest tests/test_cli.py -k python_sane -q` selects at least 8 tests and passes
    - Gate commands exit 0; RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>A missing python-sane is a one-line exit 2 with an install hint on every SANE command. VALIDATION row: EXC-02 (python-sane missing 2 + hint, --help without sane 0).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: serve joins the exit-code table -- bind and startup failures exit 2, Ctrl-C stays 0</name>
  <files>src/saneless/cli.py, tests/test_cli.py</files>
  <read_first>
    - src/saneless/cli.py serve command
    - tests/test_cli.py TestServeCommand (whole class)
    - .planning/phases/28-exception-translation/28-RESEARCH.md § "uvicorn 0.42.0"; 28-CONTEXT.md D-07 amendment
  </read_first>
  <behavior>
    - Bind failure (`saneless.cli.socket.socket` stub whose `bind` raises `OSError(98, "Address already in use")`): exit 2; stderr exactly one line `Cannot bind to 127.0.0.1:8080: [Errno 98] Address already in use` (host/port from the test settings); `uvicorn.run` not called (rewrite of the existing bind test, which expected exit 1) (test names contain `serve`)
    - `saneless.cli.uvicorn.run` stub raising `SystemExit(3)`: exit 2; one stderr line containing `web server could not start` and the host:port; not exit 3
    - `uvicorn.run` stub raising `SystemExit(0)`: exit 0
    - `uvicorn.run` stub returning None (what uvicorn does after Ctrl-C): exit 0, no `Cancelled` in output
    - `create_app` raising `PaperlessError("Paperless URL http://host:abc is not valid: Invalid port: 'abc'")` (or the real client via settings `paperless.url = "http://host:abc"` if TestServeCommand builds the real app): exit 3, one line starting `Paperless error: Paperless URL http://host:abc is not valid`
    - Existing serve tests (host/port override, log level passthrough) stay green
  </behavior>
  <action>
    RED (commit `test(28-12): ...`): rewrite the bind test to exit 2 and add the rows above. Run: they fail.

    GREEN (commit `feat(28-12): ...`) in `serve` per the D-07 amendment:
    - Bind check: replace `click.ClickException` with `raise ConfigError(msg) from e` where `msg = f"Cannot bind to {actual_host}:{actual_port}: {describe(e)}"` (the guard prints it and exits 2: "can't start, fix your setup").
    - Wrap `uvicorn.run(...)` in `try`/`except SystemExit as exc:` -- when `exc.code` is None or 0, re-raise unchanged; otherwise `msg = f"The web server could not start on {actual_host}:{actual_port} (uvicorn exit status {exc.code}); the cause is in the log"` and `raise ConfigError(msg) from exc`. Comment: uvicorn exits 3 on a startup failure, which would read as "Paperless error" in saneless's table; every command shares one table (D-07 amendment). Ctrl-C needs no handling because uvicorn.run swallows KeyboardInterrupt and returns (measured), so a normal stop keeps exit 0 (D-03).
    - Remove the now-unused `click.ClickException` usage only; keep `import click`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "serve or Serve" -x -q && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ClickException" src/saneless/cli.py` returns nothing
    - `grep -n "except SystemExit as exc" src/saneless/cli.py` matches once
    - `grep -n "Cannot bind to" src/saneless/cli.py` matches once, in a `ConfigError` message
    - `uv run pytest tests/test_cli.py -k serve -q` passes, including the SystemExit(3) -> 2 and return -> 0 cases
    - Full non-browser suite passes; gate commands exit 0; RED commit precedes GREEN commit
  </acceptance_criteria>
  <done>serve follows the shared exit-code table. VALIDATION row: EXC-02 (serve bind/startup failure 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host environment (installed libraries, ports) -> CLI start | Setup failures before any scan |
| CLI exit status -> scripts / systemd | Automation branches on the code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-46 | Denial of service | `serve` starting half-working without python-sane | mitigate | require_sane first in serve; test asserts exit 2 before config load or app creation |
| T-28-47 | Spoofing (automation) | uvicorn's exit 3 misread as a Paperless error by scripts | mitigate | SystemExit with non-zero code mapped to exit 2; test pins it |
| T-28-48 | Repudiation | a normal Ctrl-C stop reported as a cancel/failure to systemd | mitigate | uvicorn return path untouched; test asserts exit 0 |
| T-28-49 | Information disclosure | import error text in the install hint | accept | Module or shared-object names only |
</threat_model>

<verification>
- `uv run pytest tests/test_cli.py -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
</verification>

<success_criteria>
- Four SANE commands refuse with exit 2 and the install hint when python-sane is missing; jobs and --help unaffected
- serve: bind 2, startup failure 2, Ctrl-C 0, malformed Paperless URL 3
</success_criteria>

<output>
Create `.planning/phases/28-exception-translation/28-12-SUMMARY.md` when done
</output>
