---
phase: 28-exception-translation
plan: 13
type: execute
wave: 5
depends_on: [28-12]
files_modified:
  - docs/how-to/troubleshoot-a-failed-scan.md
  - mkdocs.yml
  - docs/how-to/cli-scripting.md
  - docs/reference/cli-commands.md
  - docs/how-to/install-bare-metal.md
  - docs/how-to/scanner-host-discovery.md
  - tests/test_deployment_config.py
autonomous: true
requirements: [EXC-02, EXC-04]
decisions: [D-03, D-05, D-06, D-07, D-13]

must_haves:
  truths:
    - "A doc-truth test fails if the exit-code table in docs/how-to/cli-scripting.md or the global exit-code table in docs/reference/cli-commands.md lists a code that is not an ExitCode member or omits one (D-13, D-07)"
    - "Every per-command exit-code table in cli-commands.md lists only ExitCode values; `scan` lists 0, 1, 2, 3, 4, 5, 130; `serve` lists 0, 2, 3, 5 and neither 1 nor 130 (D-07 amendment, D-03)"
    - "docs/how-to/troubleshoot-a-failed-scan.md exists, is in the How-To Guides nav, starts with the exit-code table, then has symptom sections for scanner errors, Paperless errors, PDF assembly errors, configuration, python-sane missing, cancelled scans, and unexpected error 5 telling the user to attach the log file to a bug report (D-13)"
    - "The docs say an abort at the flip prompt (n, Ctrl-D, Ctrl-C) exits 130 and a flip timeout or terminal read failure exits 1 (D-02, D-03)"
    - "Exit 2 is documented as covering a job database saneless cannot use (in the global tables, the `jobs` and `serve` command tables, and the troubleshooting page's configuration section); no exit-5 row or the troubleshooting page's unexpected-error section mentions the job database, pinned by doc-truth tests (D-07 amendment)"
    - "install-bare-metal.md's Troubleshooting describes the python-sane-missing refusal (exit 2 with an install hint) and links to the new page; scanner-host-discovery.md's Troubleshooting links to it too (D-05, D-13)"
    - "`uv run mkdocs build --strict` succeeds"
  artifacts:
    - path: "docs/how-to/troubleshoot-a-failed-scan.md"
      provides: "symptom-organised troubleshooting how-to"
      contains: "| 130 |"
    - path: "tests/test_deployment_config.py"
      provides: "exit-code doc-truth tests"
      contains: "ExitCode"
    - path: "mkdocs.yml"
      provides: "nav entry"
      contains: "how-to/troubleshoot-a-failed-scan.md"
  key_links:
    - from: "tests/test_deployment_config.py"
      to: "saneless.vocabulary.ExitCode"
      via: "set of documented first-cell codes == {int(c) for c in ExitCode}"
      pattern: "for code in ExitCode"
---

<objective>
Make every exit-code sentence in the docs true for Phase 28 and pin the tables to the code (D-13):
a new symptom-organised troubleshooting how-to, rows 4, 5 and 130 in both exit-code tables and in
each command's table (with `serve`'s amended codes), the flip-prompt abort documented as 130, and
cross-links from the existing Troubleshooting sections.

Purpose: ROADMAP goal "with the troubleshooting docs updated in-phase"; the doc-truth test keeps the
tables honest as codes change.
Output: one new page, nav entry, four corrected pages, doc-truth tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-exception-translation/28-CONTEXT.md
@.planning/phases/28-exception-translation/28-RESEARCH.md
@.planning/phases/28-exception-translation/28-PATTERNS.md
@.planning/phases/28-exception-translation/28-09-SUMMARY.md
@.planning/phases/28-exception-translation/28-11-SUMMARY.md
@.planning/phases/28-exception-translation/28-12-SUMMARY.md
@CLAUDE.md

<interfaces>
Code truth (plans 28-01, 28-09, 28-11, 28-12): `ExitCode` SUCCESS 0, SCAN 1, CONFIG 2, PAPERLESS 3, PDF 4, UNEXPECTED 5, CANCELLED 130.
- 1: ScanError / FeederEmptyError (e.g. "No pages were scanned", "No paper detected in feeder"), flip wait timed out, flip prompt read failure; `auto-profiles` finding no scanners
- 2: ConfigError: invalid config incl. TOML syntax error with line/column, unknown profile, missing `--config` file, non-TTY manual duplex, "No scanner found" mid-scan, python-sane not importable (install hint), `serve` port bind failure, `serve` web server startup failure, `auto-profiles` cannot write the config; StorageError: the job database cannot be used (the file cannot be opened or is not SQLite, or its jobs table is an unsupported shape) -- `jobs` and `serve`, one line starting `Job database error:` that names the database path and the reason (D-07 amendment)
- 3: PaperlessError: unreachable after retries with no consume directory, rejected upload (4xx), malformed `paperless.url` (including at `serve` start)
- 4: PdfError: the pages could not be assembled into a PDF (disk full, unwritable output directory, image the PDF library rejects)
- 5: an unexpected error -- an exception that is not a saneless type, i.e. a saneless bug (never a job database problem, which is 2): one line `Unexpected error (<Type>): <message>`, traceback in the log file (`Full details in <log_file>`), `-v` shows it on stderr
- 130: cancelled by the operator: n / Ctrl-D / Ctrl-C at the flip prompt; Ctrl-C anywhere in a one-shot command (`scan`, `devices`, `auto-profiles`, `jobs`); NOT `serve`, whose Ctrl-C is a graceful stop (exit 0)
- Prefixes: `Scan error:`, `Paperless error:`, `PDF error:`, `Job database error:`; config errors print their own `Configuration error in <file>:` header
- Per-command reachable codes: scan {0,1,2,3,4,5,130}; devices {0,1,2,5,130}; jobs {0,2,5,130}; serve {0,2,3,5}; auto-profiles {0,1,2,5,130}

From tests/test_deployment_config.py: constants `CLI_REFERENCE`, `CLI_SCRIPTING`, `INSTALL_BARE_METAL`, `DOCS_DIR`, `REPO_ROOT` (27-46); `_read(path) -> tuple[str, Path]` (187-189); `_table_row(text, first_cell) -> str` (259-263, rows start with `| <first cell> |`); section-scoped pattern `test_scripting_exit_code_two_examples` (323-330: `text.partition("## Exit codes")`, `rest.split("\n## ", 1)[0]`); module docstring (1-20) lists the phases it pins.

Docs today: cli-scripting.md "## Exit codes" table `| Exit Code | Meaning | Example |` with rows 0-3 and a warning admonition saying an abort or unconfirmed flip exits 1; state list (~56-58) naming nine states and labels. cli-commands.md: global note line 14; per-command `**Exit codes:**` tables at ~31-38 (scan, row 1 says "manual duplex aborted at the flip prompt"), ~57-61 (devices: 0 only), ~100-104 (jobs: 0 only), ~121-126 (serve: 0 and "1 Port bind error"), ~142-148 (auto-profiles); line 40 describes abort/Ctrl-C/EOF and "Flip prompt failed; treating it as an abort". install-bare-metal.md § Troubleshooting (~75-95); scanner-host-discovery.md § Troubleshooting (~70-95, bold-symptom format). mkdocs.yml How-To Guides block (~44-50).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Doc-truth tests for the exit-code tables, then the tables and prompt wording</name>
  <files>tests/test_deployment_config.py, docs/how-to/cli-scripting.md, docs/reference/cli-commands.md</files>
  <read_first>
    - tests/test_deployment_config.py (whole file)
    - docs/how-to/cli-scripting.md (whole file)
    - docs/reference/cli-commands.md (whole file)
    - src/saneless/vocabulary.py ExitCode
    - src/saneless/cli.py (the guard, the four commands, serve)
  </read_first>
  <action>
    First add the tests (commit `test(28-13): ...`), then fix the docs (commit `docs(28-13): ...`).

    Tests in tests/test_deployment_config.py (module docstring gains a sentence: the Phase 28 exit-code tables and troubleshooting page, D-07, D-13):
    - A helper `_documented_codes(section: str) -> set[int]` collecting the integer first cells of Markdown table rows (lines matching `^\| (\d+) \|`) in a section.
    - `test_scripting_exit_code_table_matches_exit_code_enum`: the `## Exit codes` section of CLI_SCRIPTING documents exactly `{int(code) for code in ExitCode}`.
    - `test_cli_reference_global_exit_code_table_matches_exit_code_enum`: CLI_REFERENCE has a `## Exit codes` section documenting exactly that set.
    - `test_cli_reference_command_exit_codes_are_real`: for each `## \`saneless <cmd>\`` section, its `**Exit codes:**` table codes are a subset of ExitCode; `scan` is exactly {0,1,2,3,4,5,130}; `serve` is exactly {0,2,3,5} (no 1, no 130: D-07 amendment, D-03); `jobs` contains 5 and 130.
    - `test_job_database_documented_under_exit_code_two`: in CLI_SCRIPTING's `## Exit codes` section, CLI_REFERENCE's global `## Exit codes` section, and the `saneless jobs` and `saneless serve` command tables, the `_table_row(section, "2")` row contains `job database` (case-insensitive) and the `_table_row(section, "5")` row does not (D-07 amendment: StorageError exits 2, exit 5 only for non-saneless exceptions).
    - `test_flip_prompt_abort_documented_as_cancelled`: CLI_SCRIPTING and CLI_REFERENCE each contain `130` in the sentence about the flip prompt and no longer contain "aborted at the flip prompt or not confirmed within `flip_timeout_seconds` exits with code 1" / "manual duplex aborted at the flip prompt or flip wait timed out".
    Import `ExitCode` from `saneless.vocabulary`. Run them: they fail.

    Docs per D-07 / D-03 / D-13:
    - cli-scripting.md `## Exit codes`: keep the three-column table; rows 1-3 keep their meanings with examples updated from the interfaces list (row 2 adds a TOML syntax error, python-sane not installed, and a job database saneless cannot use -- unreadable or an unsupported schema; row 3 adds a malformed `paperless.url`); add rows `| 4 | PDF assembly error | Disk full while writing the PDF, unwritable output directory |`, `| 5 | Unexpected error (a saneless bug) | Prints one line; the traceback is in the log file -- attach it to a bug report |`, `| 130 | Cancelled by the operator | Answered no, Ctrl-D or Ctrl-C at the flip prompt; Ctrl-C during a one-shot command |`. Add a sentence after the table linking to [Troubleshoot a Failed Scan](troubleshoot-a-failed-scan.md). Rewrite the warning admonition's last paragraph: an abort at the flip prompt exits 130; a flip wait that times out, or a terminal that fails while prompting, exits 1. Update the `state` list to include `CANCELLED` and the label list to include "Cancelled".
    - cli-commands.md: after the global options, add a `## Exit codes` section with a `| Code | Meaning |` table for all seven codes (row 2 names configuration, profile or setup errors including a job database saneless cannot use; row 5 is only an unexpected error, a saneless bug), a note that every command exits 5 on an unexpected error and 130 on Ctrl-C except `serve` (graceful stop, 0), and a link to [Troubleshoot a Failed Scan](../how-to/troubleshoot-a-failed-scan.md). Replace line 14's "Every command exits with code 2 when the configuration cannot be loaded" sentence so it also mentions a TOML syntax error names its line and column. Per-command tables: scan {0,1,2,3,4,5,130} with row 1 no longer mentioning an abort (timeouts and prompt read failures remain) and row 2 adding "no scanner found" and python-sane missing; devices {0,1,2,5,130}; jobs {0,2 (invalid config, job database unreadable or with an unsupported schema),5,130}; serve {0,2 (port already in use, web server failed to start, python-sane missing, invalid config, job database unreadable or with an unsupported schema),3 (malformed Paperless URL),5}; auto-profiles {0,1,2,5,130}. Rewrite line 40: yes scans the backs; no, Ctrl-D (end of input) or Ctrl-C cancels the scan, prints one line and exits 130; a terminal read error at the prompt fails the scan with exit 1 and is logged with its traceback.
    Never quote full error messages beyond the documented prefixes (Phase 30 rewords them).
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_deployment_config.py -k "exit_code or flip_prompt" -q` selects at least 5 tests and passes
    - `uv run pytest tests/test_deployment_config.py -k job_database -q` selects at least 1 test and passes
    - `grep -n "| 130 |" docs/how-to/cli-scripting.md docs/reference/cli-commands.md` matches in both files
    - `grep -n "Port bind error" docs/reference/cli-commands.md` returns nothing
    - `grep -n "CANCELLED" docs/how-to/cli-scripting.md` matches
    - git log shows `test(28-13):` before `docs(28-13):`
  </acceptance_criteria>
  <done>Both exit-code tables and every command's table match ExitCode, pinned by tests.</done>
</task>

<task type="auto">
  <name>Task 2: The troubleshooting how-to, nav entry and cross-links</name>
  <files>docs/how-to/troubleshoot-a-failed-scan.md, mkdocs.yml, docs/how-to/install-bare-metal.md, docs/how-to/scanner-host-discovery.md, tests/test_deployment_config.py</files>
  <read_first>
    - docs/how-to/scanner-host-discovery.md (whole file: H1 + purpose sentence format, bold-symptom Troubleshooting format)
    - docs/how-to/install-bare-metal.md (whole file)
    - docs/explanation/consume-directory-fallback.md (to link, not edit)
    - mkdocs.yml (nav)
    - tests/test_deployment_config.py (after Task 1)
  </read_first>
  <action>
    Add a test `test_troubleshooting_page_is_linked_and_covers_every_exit_code` (constant `TROUBLESHOOTING = DOCS_DIR / "how-to" / "troubleshoot-a-failed-scan.md"`): the file exists; its first table documents exactly the ExitCode set; `mkdocs.yml` contains `how-to/troubleshoot-a-failed-scan.md`; the page has headings containing each of `Scanner`, `Paperless`, `PDF`, `Configuration`, `python-sane`, `Cancelled`, `Unexpected`; the Unexpected section mentions `log file` and `bug`; the Configuration errors section mentions `job database` and the Unexpected section does not (case-insensitive, sections split on `\n## ` as in `test_scripting_exit_code_two_examples`; D-07 amendment); INSTALL_BARE_METAL and `docs/how-to/scanner-host-discovery.md` link to `troubleshoot-a-failed-scan.md`. Commit with the page in one `docs(28-13): ...` commit after confirming the test fails first.

    Write docs/how-to/troubleshoot-a-failed-scan.md per D-13, organised by symptom and never quoting full messages (Phase 30 rewords them): H1 `# Troubleshoot a Failed Scan` and a one-sentence purpose; `## Start with the exit code` with the seven-row `| Exit code | What happened | Where to look |` table linking to the sections below; then `## Scanner errors (exit 1)` (lines start `Scan error:`; the message names what saneless was doing and the device, then the scanner's own words; empty feeder vs every sheet unreadable vs "No pages were scanned" vs "All pages were blank" and what each means; flip wait timed out; run `saneless devices`; link Scanner Host Discovery); `## Configuration errors (exit 2)` (header naming the file, one line per problem, a TOML syntax error names line and column; unknown profile; non-interactive manual duplex; no scanner found; `serve` port already in use or web server failed to start; the job database cannot be used -- a `Job database error:` line names its path and the reason (the file cannot be opened or is not a SQLite database, or its jobs table has a shape this version does not recognise; do not claim a cause such as a downgrade -- an older saneless opening a newer database is not detected today): check the path and its permissions and that the file is saneless's job database; moving it aside lets saneless start with an empty job history, while the moved file is kept for inspection or restoring); `## python-sane is not installed (exit 2)` (every scanner command refuses at once; install `libsane-dev` or `sane-backends-devel` and reinstall; `jobs` and `--help` still work; link Install on Bare Metal); `## Paperless errors (exit 3)` (unreachable after three attempts; which failures retry and fall back to the consume directory and that a malformed URL is not retried; a rejected upload shows Paperless's own reason on one line; a duplicate warning means check Paperless before scanning again; a retry after a lost response can create a second copy on default paperless-ngx settings; link Consume Directory Fallback); `## PDF assembly errors (exit 4)` (disk space and permissions of the output directory; the scanned pages are not kept yet); `## Cancelled scans (exit 130)` (no, Ctrl-D or Ctrl-C at the flip prompt; Ctrl-C during a command; the web UI shows Cancelled in grey, not an error; a timeout is not a cancel); `## Unexpected errors (exit 5)` (a saneless bug -- an error saneless did not anticipate, never a setup problem (those exit 2; do not write the words "job database" in this section, the doc-truth test forbids them); the line names the exception type; the traceback is in the log file named on that line, or run again with `-v`; attach the log file when reporting a bug).

    mkdocs.yml: add `- Troubleshoot a Failed Scan: how-to/troubleshoot-a-failed-scan.md` to the How-To Guides block.
    install-bare-metal.md § Troubleshooting: add a bold symptom entry for "saneless says python-sane cannot be imported" (every scanner command exits 2 with an install hint naming the missing library; install the SANE development package and reinstall), and a closing line linking to the new page. scanner-host-discovery.md § Troubleshooting: add a closing line linking to the new page for failures after discovery.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -x -q && uv run mkdocs build --strict -d "$(mktemp -d)"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/how-to/troubleshoot-a-failed-scan.md` succeeds
    - `grep -n "how-to/troubleshoot-a-failed-scan.md" mkdocs.yml` matches once
    - `grep -n "troubleshoot-a-failed-scan.md" docs/how-to/install-bare-metal.md docs/how-to/scanner-host-discovery.md docs/how-to/cli-scripting.md docs/reference/cli-commands.md` matches in all four files
    - `uv run pytest tests/test_deployment_config.py -q` passes
    - `uv run mkdocs build --strict -d "$(mktemp -d)"` exits 0
  </acceptance_criteria>
  <done>The troubleshooting how-to exists, is navigable and linked, and is pinned by a doc-truth test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> operator scripts | Scripts branch on documented exit codes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-50 | Repudiation (false docs) | exit-code tables drifting from ExitCode | mitigate | Doc-truth tests compare documented code sets to the enum and per-command expectations |
| T-28-51 | Information disclosure | troubleshooting advice to post logs publicly | mitigate | The page says to attach the log file to a bug report, never the config file, and to check a log recorded with `log_level = "DEBUG"` for the Paperless token before sharing it (httpx debug output can carry the Authorization header, T-27-23) |
</threat_model>

<verification>
- `uv run pytest tests/test_deployment_config.py -q`
- `uv run mkdocs build --strict -d "$(mktemp -d)"`
- `uv run pytest -m "not browser and not sane_hardware" -q`
</verification>

<success_criteria>
- Exit-code tables in both references and every command table match the code, enforced by tests
- New symptom-organised how-to in nav, cross-linked from install and discovery pages
- Strict docs build passes
</success_criteria>

<output>
Create `.planning/phases/28-exception-translation/28-13-SUMMARY.md` when done
</output>
