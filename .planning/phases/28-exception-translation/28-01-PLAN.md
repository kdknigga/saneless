---
phase: 28-exception-translation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/saneless/exceptions.py
  - src/saneless/vocabulary.py
  - src/saneless/web/templates/partials/status.html
  - src/saneless/web/templates/partials/history.html
  - tests/test_exceptions.py
  - tests/test_vocabulary.py
  - tests/test_web_state_rendering.py
autonomous: true
requirements: [EXC-01, EXC-02, EXC-04]
decisions: [D-01, D-04, D-07, D-08]

must_haves:
  truths:
    - "PdfError and ScanCancelledError are direct SanelessError subclasses; ScanCancelledError is NOT a ScanError, so no existing `except ScanError` can turn a cancel into exit 1 or ERROR (D-01, D-04, Claude's Discretion: hierarchy)"
    - "describe(exc) returns str(exc), or the exception's class name when str(exc) is empty (D-08)"
    - "JobState has ten members; CANCELLED is terminal, labelled 'Cancelled' by state_label and progress_label (D-01)"
    - "ErrorCategory.ASSEMBLY exists; classify_error(PdfError(...)) is ASSEMBLY; classify_error(ScanCancelledError(...)) is UNKNOWN (a cancel is not a category) (D-04, D-01)"
    - "ExitCode is one IntEnum with exactly 0 SUCCESS, 1 SCAN, 2 CONFIG, 3 PAPERLESS, 4 PDF, 5 UNEXPECTED, 130 CANCELLED, and exit_code_for maps every ErrorCategory through a total match with assert_never (D-07)"
    - "classify_error(StorageError(...)) stays UNKNOWN, so no job record's category changes; StorageError's exit code 2 is assigned by exception type in the CLI guard (plan 28-09), never by reclassifying it, and the ExitCode/exit_code_for docstrings say so (D-07 amendment: StorageError exits 2, exit 5 only for non-saneless exceptions)"
    - "A CANCELLED job renders a non-alert `status-cancelled` line and the history-refresh hook in the status partial, and a `status-cancelled` history cell (D-01)"
  artifacts:
    - path: "src/saneless/exceptions.py"
      provides: "PdfError, ScanCancelledError, describe()"
      contains: "class ScanCancelledError(SanelessError)"
    - path: "src/saneless/vocabulary.py"
      provides: "JobState.CANCELLED, ErrorCategory.ASSEMBLY, ExitCode, exit_code_for"
      contains: "class ExitCode(IntEnum)"
    - path: "src/saneless/web/templates/partials/status.html"
      provides: "CANCELLED branch"
      contains: "JobState.CANCELLED"
    - path: "tests/test_exceptions.py"
      provides: "hierarchy and describe() tests"
  key_links:
    - from: "src/saneless/vocabulary.py exit_code_for"
      to: "ErrorCategory"
      via: "match + assert_never"
      pattern: "def exit_code_for\\(category: ErrorCategory\\) -> ExitCode"
    - from: "src/saneless/vocabulary.py classify_error"
      to: "src/saneless/exceptions.py PdfError"
      via: "isinstance branch"
      pattern: "isinstance\\(exc, PdfError\\)"
---

<objective>
Lay the shared vocabulary every other Phase 28 plan builds on: the two new exception types and the
`describe()` message helper, the terminal `JobState.CANCELLED` (with the template branches that keep
the state-rendering suite green), the PDF `ErrorCategory`, and the single `ExitCode` definition.

Purpose: EXC-01/02/04 all need a saneless type to translate into, a place where exit codes live once
(D-07), and a cancel state that is not ERROR (D-01). Wave 2 plans import these names.
Output: exceptions.py and vocabulary.py additions, CANCELLED template branches, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/28-exception-translation/28-CONTEXT.md
@.planning/phases/28-exception-translation/28-RESEARCH.md
@.planning/phases/28-exception-translation/28-PATTERNS.md
@CLAUDE.md

<interfaces>
From src/saneless/exceptions.py (whole file, 44 lines): `SanelessError(Exception)`, `ConfigError`, `ScanError`,
`FeederEmptyError(ScanError)`, `PaperlessError`, `PaperlessTimeoutError(PaperlessError)`, `StorageError`; sorted
`__all__`; no constructors (pipeline.py:469-470 `_preserving` rebuilds `type(exc)(msg)`, so every class stays
single-message).

From src/saneless/vocabulary.py:
- imports: `from enum import StrEnum`, `from typing import Final, assert_never`, `from saneless.exceptions import (ConfigError, FeederEmptyError, PaperlessError, ScanError)`; leaf module (imports only exceptions)
- `class JobState(StrEnum)` lines 50-60: PENDING, SCANNING, AWAITING_FLIP, SCANNING_REVERSE, ASSEMBLING, UPLOADING, DONE, ERROR, FALLBACK
- `class ErrorCategory(StrEnum)` lines 64-83: FEEDER, CONFIG, SCANNER, UPLOAD, UNKNOWN, REJECTED
- `TERMINAL_STATES` line ~253: frozenset({DONE, ERROR, FALLBACK})
- `state_label(state: JobState) -> str` (~274-312), `progress_label(state: JobState) -> str` (~315-363): total match, assign-then-return
- `error_message(category: ErrorCategory) -> str` (~438-482): total match; deliberately unwired (Phase 30). CONTEXT calls it `user_message`; the real name is `error_message` (RESEARCH Pitfall 11)
- `rejection_status_code` (~621-658): the enum -> int total-match shape to copy
- `classify_error(exc: Exception) -> ErrorCategory` (~661-686): ordered isinstance chain with five returns today (PLR0911 limit 6)
- IntEnum precedent: `scanner/sane_backend.py` `class GeometryUnit(IntEnum)` (~269-294)

From templates: status.html FALLBACK branch lines 21-26 (non-alert line + history-reload div); history.html line 7 class chain.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PdfError, ScanCancelledError, describe(), ErrorCategory.ASSEMBLY, ExitCode</name>
  <files>src/saneless/exceptions.py, src/saneless/vocabulary.py, tests/test_exceptions.py, tests/test_vocabulary.py</files>
  <read_first>
    - src/saneless/exceptions.py (whole file)
    - src/saneless/vocabulary.py lines 1-90 (imports, __all__, JobState, ErrorCategory), 430-486 (error_message), 610-686 (rejection_status_code, classify_error)
    - src/saneless/scanner/sane_backend.py lines 265-296 (GeometryUnit IntEnum docstring style)
    - tests/test_vocabulary.py lines 80-100 (ErrorCategory name set), 660-760 (unrecognised-value tests, classify_error tests)
    - .planning/phases/28-exception-translation/28-RESEARCH.md § "ScanCancelledError Placement", § Pattern 2
  </read_first>
  <behavior>
    - `issubclass(PdfError, SanelessError)` and `issubclass(ScanCancelledError, SanelessError)`; `not issubclass(ScanCancelledError, ScanError)`; `not issubclass(PdfError, ScanError)`
    - For each of PdfError and ScanCancelledError: `type(exc)("rebuilt")` works and `str(...) == "rebuilt"` (single-message constructor, Pitfall 9)
    - `describe(ValueError("boom")) == "boom"`; `describe(httpx.ReadTimeout("")) == "ReadTimeout"`; `describe(RuntimeError()) == "RuntimeError"`
    - `{c.name for c in ErrorCategory}` includes `"ASSEMBLY"`; `error_message(ErrorCategory.ASSEMBLY)` is a non-empty constant sentence
    - `classify_error(PdfError("disk full")) is ErrorCategory.ASSEMBLY`; `classify_error(ScanCancelledError("x")) is ErrorCategory.UNKNOWN`; `classify_error(StorageError("x")) is ErrorCategory.UNKNOWN` (pinned so nobody "fixes" the storage exit code by reclassifying it, D-07 amendment); existing FEEDER/CONFIG/SCANNER/UPLOAD/UNKNOWN results unchanged
    - `{(m.name, int(m)) for m in ExitCode} == {("SUCCESS", 0), ("SCAN", 1), ("CONFIG", 2), ("PAPERLESS", 3), ("PDF", 4), ("UNEXPECTED", 5), ("CANCELLED", 130)}`
    - `exit_code_for`: FEEDER -> SCAN, SCANNER -> SCAN, CONFIG -> CONFIG, UPLOAD -> PAPERLESS, ASSEMBLY -> PDF, UNKNOWN -> UNEXPECTED, REJECTED -> UNEXPECTED; parametrised over `list(ErrorCategory)` every member returns an `ExitCode`; `exit_code_for(cast("ErrorCategory", "UNRECOGNISED"))` raises AssertionError (test names contain `exit_code`)
  </behavior>
  <action>
    RED (commit `test(28-01): ...`): create tests/test_exceptions.py (module docstring citing M-17, N-08, D-08) with the hierarchy, single-message and describe() tests; extend tests/test_vocabulary.py with the ErrorCategory name set (add ASSEMBLY), classify_error PdfError/ScanCancelledError tests, and an `ExitCode` / `exit_code_for` test class copying the parametrised and `cast(...)` unrecognised-value shapes already in that file. Run them: they fail on import.

    GREEN (commit `feat(28-01): ...`):
    - src/saneless/exceptions.py: add `class PdfError(SanelessError)` (docstring: PDF assembly failure; a sibling of ScanError so a full disk is never recorded as a scanner failure, D-04) and `class ScanCancelledError(SanelessError)` (docstring: the operator deliberately stopped the scan at the flip prompt, N-08, D-01; deliberately not a ScanError so no `except ScanError` can absorb it). No constructors. Extend the existing `StorageError` docstring (no code change): the job database cannot be used -- the file cannot be opened or read as SQLite, or its jobs table is a shape this build does not recognise; every message names the job database path; the CLI reports it as a setup problem with exit 2, not as an unexpected error (D-07 amendment). Add `def describe(exc: BaseException) -> str` returning `str(exc) or type(exc).__name__`, Google docstring citing D-08 (httpx ReadTimeout can stringify empty). Add all three names to `__all__`, kept sorted.
    - src/saneless/vocabulary.py: `from enum import IntEnum, StrEnum`; import `PdfError`. Add `ASSEMBLY = "ASSEMBLY"` to ErrorCategory with a docstring sentence (the scanned pages could not be assembled into a PDF: img2pdf, Pillow, or the output directory, D-04). `error_message` gains the arm `"The scanned pages could not be assembled into a PDF."`. `classify_error` gains `PdfError -> ASSEMBLY` (after PaperlessError is fine since PdfError shares no base with the others); convert the chain to assign-once-return-once if ruff PLR0911 fires, and extend the docstring to say ScanCancelledError is deliberately left UNKNOWN because a cancel is not a failure category and callers test for it first (D-01).
    - Add `class ExitCode(IntEnum)` with members SUCCESS=0, SCAN=1, CONFIG=2, PAPERLESS=3, PDF=4, UNEXPECTED=5, CANCELLED=130 and a docstring stating: this is the one definition of the CLI exit codes (D-07); the documentation tables are pinned to it by a doc-truth test; dispatch to it is a match so a new ErrorCategory fails the type gate. Add `def exit_code_for(category: ErrorCategory) -> ExitCode` using the `rejection_status_code` shape: `FEEDER | SCANNER -> SCAN`, `CONFIG -> CONFIG`, `UPLOAD -> PAPERLESS`, `ASSEMBLY -> PDF`, `UNKNOWN | REJECTED -> UNEXPECTED`, `case _: assert_never(category)`, assign then return once; docstring notes two things callers resolve by exception type before classifying: a cancel is not a category, so ScanCancelledError / KeyboardInterrupt map to CANCELLED; and StorageError, which classifies UNKNOWN, is a setup problem the CLI guard maps to CONFIG (exit 2) by type (D-07 amendment, user decision 2026-09-15, RESEARCH Open Question 3 RESOLVED). The mapping is by type rather than by making classify_error return CONFIG because ErrorCategory is persisted on job records and a store that cannot open is not a job's configuration failure; adding an ErrorCategory.STORAGE member was rejected for the same reason (a new persisted value no job could ever carry). UNKNOWN -> UNEXPECTED is therefore reached only by exceptions that are not saneless types (and a bare SanelessError, itself a bug). Add `"ExitCode"` and `"exit_code_for"` to `__all__` alphabetically.
    - Do NOT add JobState.CANCELLED in this task (Task 2 owns it with its template branches).
  </action>
  <verify>
    <automated>uv run pytest tests/test_exceptions.py tests/test_vocabulary.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "^class ScanCancelledError(SanelessError):" src/saneless/exceptions.py` matches once
    - `grep -n "^class PdfError(SanelessError):" src/saneless/exceptions.py` matches once
    - `grep -n "^def describe(exc: BaseException) -> str:" src/saneless/exceptions.py` matches once
    - `grep -n "class ExitCode(IntEnum):" src/saneless/vocabulary.py` matches once; `grep -n "CANCELLED = 130" src/saneless/vocabulary.py` matches once
    - `grep -n "def exit_code_for(category: ErrorCategory) -> ExitCode:" src/saneless/vocabulary.py` matches once
    - `uv run pytest tests/test_vocabulary.py -k exit_code -q` selects at least 8 tests and passes
    - `grep -n -A 10 "^class StorageError(SanelessError):" src/saneless/exceptions.py` shows a docstring naming the job database path and exit 2; `uv run pytest tests/test_vocabulary.py -k "classify and storage" -q` selects at least 1 test and passes (name the test e.g. `test_classify_error_storage_error_is_unknown`)
    - Gate commands exit 0; `git log --oneline` shows `test(28-01):` before `feat(28-01):`
  </acceptance_criteria>
  <done>The new exception types, describe(), ASSEMBLY category and ExitCode exist and are tested. VALIDATION rows: EXC-01 (types), EXC-02 (ExitCode definition).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JobState.CANCELLED as a terminal state with its status and history rendering</name>
  <files>src/saneless/vocabulary.py, src/saneless/web/templates/partials/status.html, src/saneless/web/templates/partials/history.html, tests/test_vocabulary.py, tests/test_web_state_rendering.py</files>
  <read_first>
    - src/saneless/vocabulary.py lines 50-62 (JobState), 245-365 (state sets, state_label, progress_label and its docstring naming "DONE, ERROR and FALLBACK")
    - src/saneless/web/templates/partials/status.html (whole file)
    - src/saneless/web/templates/partials/history.html (whole file)
    - tests/test_vocabulary.py lines 55-70 (member count guard), 120-200 (partition/terminal membership, state_label table), 215-240 (progress_label terminal prose)
    - tests/test_web_state_rendering.py lines 175-270
    - src/saneless/job.py lines 310-330 (jobs table schema: `state TEXT NOT NULL`, confirm no CHECK constraint)
  </read_first>
  <behavior>
    - `len(list(JobState)) == 10`; `JobState.CANCELLED.value == "CANCELLED"`
    - `TERMINAL_STATES == frozenset({DONE, ERROR, FALLBACK, CANCELLED})`; ACTIVE_STATES and TERMINAL_STATES still partition JobState; CANCELLED not in BUSY_STATES
    - `state_label(JobState.CANCELLED) == "Cancelled"`; `progress_label(JobState.CANCELLED) == "Cancelled"`
    - History partial for a CANCELLED job contains `<td class="status-cancelled">` and no other state has that class; DONE/ERROR/FALLBACK classes unchanged
    - Status partial for a CANCELLED job titled "Render Test" contains `<p class="status-cancelled">&#8856; Cancelled: Render Test</p>`, contains no `role="alert"`, contains the `hx-get="/api/jobs/history"` hook (already asserted for every TERMINAL_STATES member), and does not poll (`hx-trigger="every 1s"` absent)
  </behavior>
  <action>
    RED (commit `test(28-01): ...`): bump the member-count guard to ten, add CANCELLED to the terminal-membership expectation, the state_label table ("Cancelled") and the progress_label terminal prose; in tests/test_web_state_rendering.py add the `status-cancelled` assertion to `test_history_cell_css_class` (docstring "three" -> "four terminal history cells") and a CANCELLED block to `test_status_area_prose` copying the FALLBACK block (exact line above plus `role="alert"` absent). Run: they fail.

    GREEN (commit `feat(28-01): ...`) per D-01:
    - vocabulary.py: `CANCELLED = "CANCELLED"` after FALLBACK in JobState; add it to TERMINAL_STATES; `state_label` and `progress_label` arms returning `"Cancelled"`; update the progress_label docstring to name the four terminal states. Let `uv run ty check` / `uv run pyrefly check src tests` list any other `match` on JobState and add the arm there (research found only these two).
    - status.html: add `{% elif job.state == JobState.CANCELLED %}` after the FALLBACK branch, rendering `<p class="status-cancelled">&#8856; Cancelled: {{ job.title }}</p>` and the same hidden `hx-get="/api/jobs/history" ... hx-trigger="load"` div the other terminal branches carry. No `role="alert"`: a cancel is a deliberate stop, not a failure. Without this branch a CANCELLED job renders nothing (the if-chain has no else).
    - history.html line 7: add `{% elif job.state == JobState.CANCELLED %}status-cancelled` to the class chain.
    - Styling (the `.status-cancelled` rule) is plan 28-08; an unstyled class inherits body text colour, never red, so this interim state is safe.
    - Confirm by reading job.py's CREATE TABLE that `state` is TEXT with no CHECK constraint; no migration is written. Record the confirmation and the rollback note (an older build reading a CANCELLED or ASSEMBLY row raises ValueError in `JobState(...)` / `ErrorCategory(...)`) in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_vocabulary.py tests/test_web_state_rendering.py tests/test_job.py tests/test_web.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'CANCELLED = "CANCELLED"' src/saneless/vocabulary.py` matches once
    - `grep -n "JobState.CANCELLED" src/saneless/web/templates/partials/status.html src/saneless/web/templates/partials/history.html` matches at least once in each file
    - `grep -c 'role="alert"' src/saneless/web/templates/partials/status.html` is unchanged from before the task (1)
    - `grep -n "CHECK" src/saneless/job.py` shows no CHECK constraint on `state`
    - `uv run pytest tests/test_web_state_rendering.py -k CANCELLED -q` selects tests (parametrised ids include CANCELLED) and passes
    - Full non-browser suite passes: `uv run pytest -m "not browser and not sane_hardware" -q`
    - Gate commands exit 0
  </acceptance_criteria>
  <done>CANCELLED is a terminal, labelled, rendered state. VALIDATION row: EXC-04 (JobState ten members; partition; labels).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job store row -> status/history templates | `job.title` and `job.error` are user/upstream-derived text rendered into HTML |
| exception text -> describe() -> user-visible messages | Third-party `str(exc)` flows into one-line messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering (XSS) | status.html CANCELLED branch rendering `job.title` | mitigate | Jinja autoescape stays on (no `|safe`); the new branch uses `{{ job.title }}` exactly as DONE/FALLBACK do |
| T-28-02 | Spoofing (UI) | CANCELLED rendered as an alert | mitigate | No `role="alert"` on the branch; test asserts its absence |
| T-28-03 | Information disclosure | describe() | accept | Returns only `str(exc)` or a class name; callers decide what to pass (the token never reaches an exception message, CFG-05) |
| T-28-04 | Denial of service (rollback) | new persisted enum values CANCELLED / ASSEMBLY | accept | Forward-compatible TEXT column; an older build cannot read those rows. Noted in SUMMARY for the changelog |
</threat_model>

<verification>
- `uv run pytest tests/test_exceptions.py tests/test_vocabulary.py tests/test_web_state_rendering.py -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
</verification>

<success_criteria>
- PdfError, ScanCancelledError (not a ScanError), describe() exported
- ExitCode and exit_code_for defined once, total over ErrorCategory
- JobState.CANCELLED terminal with labels and non-alert rendering
- Full non-browser suite and all gates green
</success_criteria>

<output>
Create `.planning/phases/28-exception-translation/28-01-SUMMARY.md` when done
</output>
