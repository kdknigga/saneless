---
phase: 27-configuration-strictness
plan: 05
type: execute
wave: 2
depends_on: ["27-02"]
files_modified:
  - tests/test_deployment_config.py
  - docker-compose.yml
  - docs/how-to/deploy-docker-compose.md
  - docs/reference/docker.md
  - docs/getting-started/quick-start.md
  - docs/how-to/configure-scan-profiles.md
autonomous: true
requirements: [CFG-09, CFG-07, CFG-06, CFG-03]
decisions: [D-09, D-01, D-02, D-03, D-04, D-08, D-15, D-16]

must_haves:
  truths:
    - "docker-compose.yml mounts the config directory read-write as ./config:/etc/saneless (no :ro), and no longer mounts config.toml as a single file (D-09, CFG-09)"
    - "The compose comments, docs/how-to/deploy-docker-compose.md and docs/reference/docker.md say a missing config.toml means defaults plus environment variables and the container still starts; the 'will fail to start' sentence is gone (D-09, doc row 20)"
    - "The docs explain why the directory must be writable (profile rewrites create a temp file there and rename it; a single-file bind mount fails with EBUSY) and give existing single-file deployments a migration note (D-08, D-09)"
    - "docs/how-to/configure-scan-profiles.md describes --force as a merge: generated keys of auto_generated profiles are refreshed in place, default_tags, title and comments are kept, profiles without auto_generated are skipped and reported (D-01..D-04, doc row 8)"
    - "docs/how-to/configure-scan-profiles.md describes title as a literal default title used when the typed title is blank, falling back to 'Scan <time>' (D-15, D-16, doc row 6)"
    - "A static text test fails if any doc or the compose file reintroduces a single-file config.toml mount or the fail-to-start claim"
  artifacts:
    - path: "tests/test_deployment_config.py"
      provides: "CFG-09 static text assertions over docker-compose.yml and docs/**/*.md"
      min_lines: 40
    - path: "docker-compose.yml"
      provides: "directory mount"
      contains: "./config:/etc/saneless"
  key_links:
    - from: "tests/test_deployment_config.py"
      to: "docker-compose.yml and docs/"
      via: "REPO_ROOT = Path(__file__).resolve().parents[1]"
      pattern: "parents\\[1\\]"
---

<objective>
Ship the config-*directory* mount in the compose example and every deployment doc, in the same wave that
plan 27-04 routes rewrites through the atomic replace (CFG-09, D-09; ROADMAP ordering note 4: `os.replace`
over a bind-mounted file is EBUSY). Correct the profile how-to for this phase's merge and title
semantics. A static text test locks the result.

Boundaries: do not touch the compose `image:` line or its URL owner (Phase 31), the compose
`environment:` block or the Paperless service in the examples (DOCS-06 / APPL-11), or README `--title`
wording (DOCS-02, Phase 31).

Output: `tests/test_deployment_config.py` (Wave 0 file from VALIDATION.md), compose and doc edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-configuration-strictness/27-CONTEXT.md
@.planning/phases/27-configuration-strictness/27-RESEARCH.md
@.planning/phases/27-configuration-strictness/27-PATTERNS.md
@CLAUDE.md

<interfaces>
Current offending lines (grep-verified at HEAD 5d78599):
- docker-compose.yml:4 `#   - Volume mount: bind config.toml to /etc/saneless/config.toml (read-only)`; :16-19 `# WARNING: config.toml must exist on the host before first run.` / `# Create it with at minimum: [profiles.default] and [paperless] sections.` / `# See: https://github.com/kris-knigga/saneless#configuration` / `- ./config.toml:/etc/saneless/config.toml:ro`. Leave :12 (image) and :23-28 (environment) as they are; keep the URL on :18 unchanged if the comment keeps a link.
- docs/getting-started/quick-start.md:19 `-v ./config.toml:/etc/saneless/config.toml:ro \`; :65 search order `--config PATH`, `./saneless.toml`, `~/.config/saneless/config.toml`, `/etc/saneless/config.toml`
- docs/how-to/deploy-docker-compose.md:14 "Create a `config.toml` on the host"; :27-28 `!!! warning` "... (mounted read-only) and will fail to start if the file is missing."; :52 `- ./config.toml:/etc/saneless/config.toml:ro`; :71 `- **Config mount:** ... -- read-only bind mount`; upgrade section ~:175-215 (bold-lead-in paragraphs plus a "### What else changed in this release" bullet list)
- docs/reference/docker.md:31 volumes table row `| /etc/saneless/config.toml | Configuration file (mount read-only) | **Yes** |`; :97, :121, :137, :155 `- ./config.toml:/etc/saneless/config.toml:ro`
- docs/how-to/configure-scan-profiles.md:60 table row `title` "Default title template for scanned documents"; :145 "Use `--force` to overwrite existing auto-generated profiles:"; :153 "Profiles you wrote yourself are never touched."; :165 "The config file cannot be written -- for example, because it is mounted read-only, as in the Docker Compose examples"; :178 example `title = "Receipt"` (keep the example; fix any "template" prose near it)

Behaviour these docs must describe (from plans 27-01, 27-02, 27-04):
- CLI grouped output lines: `Added: ...`, `Refreshed: ...`, `Skipped (not auto-generated): ...` (reason: not created by auto-profiles, no `auto_generated = true`; rename or delete it to regenerate), `Skipped (already exists; use --force to refresh): ...`, `Removed (scanner no longer offers it): ...`
- Owned keys refreshed under --force: `source, resolution, mode, auto_source_mode, duplex, auto_generated`; a stale owned key the new generation omits is removed; a hand edit to an owned key is overwritten while `auto_generated = true` is set
- EBUSY message: "Cannot replace <file>: it is bind-mounted as a single file. Mount its directory instead (see docs/how-to/deploy-docker-compose.md)." CLI exits 2; the server logs a warning and uses the profiles for that run only
- A new config file created by `auto-profiles` gets mode 0600
- With an empty mounted directory the server does not create `config.toml`; generated profiles are used in memory and the log tells the operator to create the file (Phase 26 D-16..D-18)
- Title rule: typed title, else the profile's `title`, else `Scan <time>`

Analog for the new test: tests/test_vendor_assets.py (module docstring explaining the contract, module-level path constants, module-level `test_` functions, `read_text(encoding="utf-8")`, non-empty guard such as `assert templates`, failure messages naming the file). Plain-text assertions only; no PyYAML.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Static deployment test, then the directory mount in compose and the Docker docs</name>
  <files>tests/test_deployment_config.py, docker-compose.yml, docs/how-to/deploy-docker-compose.md, docs/reference/docker.md, docs/getting-started/quick-start.md</files>
  <read_first>
    - tests/test_vendor_assets.py (lines 1-40 and 125-175, the analog)
    - docker-compose.yml (whole file)
    - docs/how-to/deploy-docker-compose.md (whole file)
    - docs/reference/docker.md lines 1-160
    - docs/getting-started/quick-start.md lines 1-70
    - .planning/phases/27-configuration-strictness/27-RESEARCH.md § "Docs and Compose Change List" and § Runtime State Inventory (Live service config row)
  </read_first>
  <behavior>
    - `test_compose_mounts_the_config_directory`: docker-compose.yml has a volume line whose stripped text is exactly `- ./config:/etc/saneless` (no `:ro`)
    - `test_no_single_file_config_mount_anywhere`: neither docker-compose.yml nor any `docs/**/*.md` contains the substring `config.toml:/etc/saneless/config.toml`; the docs glob is non-empty
    - `test_no_fail_to_start_claim`: no `docs/**/*.md` and not docker-compose.yml contains `fail to start` (case-insensitive) in a sentence about a missing config file (implement as: the phrase `fail to start` does not appear within the same line as `config`)
    - `test_deploy_docs_use_the_directory_mount`: docs/how-to/deploy-docker-compose.md, docs/reference/docker.md and docs/getting-started/quick-start.md each contain `./config:/etc/saneless`
    - `test_deploy_doc_explains_missing_config_and_migration`: deploy-docker-compose.md contains `defaults` and `environment variables` near the missing-file statement, contains `EBUSY`, and contains a migration heading or bullet mentioning moving `config.toml` into `./config/`
  </behavior>
  <action>
    RED (commit `test(27-05): ...`): create tests/test_deployment_config.py with a module docstring (CFG-09 / M-30 / D-09: the documented deployment must be one where atomic config rewrites succeed; a single-file bind mount makes the rename fail with EBUSY), constants `REPO_ROOT = Path(__file__).resolve().parents[1]`, `COMPOSE = REPO_ROOT / "docker-compose.yml"`, `DOCS_DIR = REPO_ROOT / "docs"`, and the five tests above with failure messages naming the file and line. Run: they fail against the current files.

    GREEN (commit `docs(27-05): ...`):
    - docker-compose.yml: line 4 becomes a bullet saying the config *directory* is mounted read-write at /etc/saneless and holds config.toml. Replace the three WARNING comment lines and the volume line with comments stating: `./config` holds `config.toml`; the directory must be writable because saneless rewrites profiles by writing a temp file beside `config.toml` and renaming it (a single-file bind mount fails with EBUSY); if `config.toml` is missing, saneless starts with defaults plus environment variables and does not create the file; keep the existing `See:` URL line verbatim. Volume line: `- ./config:/etc/saneless`. Touch nothing else (image, environment, data volume).
    - docs/how-to/deploy-docker-compose.md: Step 1 creates `./config/config.toml` (show `mkdir config` before the TOML block). Replace the warning admonition with a `!!! note` stating the container reads `/etc/saneless/config.toml` from the mounted `./config` directory, a missing file means defaults plus environment variables and the container still starts, and the directory must be writable for `auto-profiles` and startup profile generation. Compose snippet line 52 -> `- ./config:/etc/saneless`. Key details bullet line 71 -> `**Config mount:** ./config:/etc/saneless -- a read-write directory mount. saneless replaces config.toml atomically (temp file in the same directory, then rename); a single-file bind mount makes that rename fail with EBUSY, and saneless reports "Mount its directory instead".` Add, next to the upgrade section, a subsection (e.g. `### Moving from a single-file config mount`) with steps: stop the stack, `mkdir config && mv config.toml config/`, change the volume line to `./config:/etc/saneless`, start; note that with the old mount profile writes fail (EBUSY or read-only) and profiles are used for that run only.
    - docs/reference/docker.md: volumes table row -> `/etc/saneless` | Configuration directory holding `config.toml` (mount read-write; a missing `config.toml` means defaults plus environment variables) | Recommended; add one sentence after the table explaining the EBUSY reason. Replace the four compose snippet lines (:97, :121, :137, :155) with `- ./config:/etc/saneless`.
    - docs/getting-started/quick-start.md: line 19 -> `-v ./config:/etc/saneless \` (leave the relative-path/`$(pwd)` question to DOCS-06) and add a sentence that `config.toml` goes inside `./config`; line 65 search order -> `--config PATH`, `./saneless.toml`, `$XDG_CONFIG_HOME/saneless/config.toml` (default `~/.config/saneless/config.toml`), `/etc/saneless/config.toml` (CFG-03 wording, implemented by plan 27-07 in wave 3).
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "config.toml:/etc/saneless/config.toml" docker-compose.yml docs` returns nothing
    - `grep -n -- "- ./config:/etc/saneless$" docker-compose.yml` matches once
    - `grep -rni "fail to start" docs docker-compose.yml` returns nothing
    - Image and environment untouched: `grep -c "image: ghcr.io/kris-knigga/saneless:latest" docker-compose.yml` is 1, `grep -c "SANELESS_PAPERLESS__TOKEN=changeme" docker-compose.yml` is 1, and `grep -c "saneless-data:/var/lib/saneless" docker-compose.yml` is 1
    - `uv run pytest tests/test_deployment_config.py -q` passes with 5 tests
    - Gate commands exit 0
  </acceptance_criteria>
  <done>The compose example and every Docker doc mount the config directory read-write and tell the truth about a missing file. VALIDATION row: CFG-09.</done>
</task>

<task type="auto">
  <name>Task 2: Profile how-to: merge semantics, literal title, and the unwritable-config example</name>
  <files>docs/how-to/configure-scan-profiles.md, tests/test_deployment_config.py</files>
  <read_first>
    - docs/how-to/configure-scan-profiles.md (whole file)
    - tests/test_deployment_config.py (as written in Task 1)
    - .planning/phases/27-configuration-strictness/27-CONTEXT.md § decisions D-01..D-04, D-15, D-16
  </read_first>
  <action>
    - Add tests to tests/test_deployment_config.py first (commit together with the doc change is acceptable for prose; run them red before editing): `test_profile_howto_describes_force_as_a_merge` (configure-scan-profiles.md contains `auto_generated`, `default_tags`, `Refreshed`, `Skipped (not auto-generated)`, and does not contain `to overwrite existing auto-generated profiles`); `test_profile_howto_title_is_literal` (the file does not contain `title template` case-insensitive, and contains `Scan <`); `test_profile_howto_unwritable_example_is_current` (does not contain `as in the Docker Compose examples`).
    - docs/how-to/configure-scan-profiles.md edits:
      - Field table `title` row: "Default document title, used as written when you leave the title blank in the web UI or omit `--title` on the CLI. A title you type always wins; with neither, the title is `Scan <date time>`." (D-15, D-16)
      - Replace the `--force` sentence (around :145) and the "Profiles you wrote yourself are never touched." sentence (around :153) with a short merge explanation: `--force` refreshes profiles that carry `auto_generated = true`; only the generated keys (`source`, `resolution`, `mode`, `auto_source_mode`, `duplex`, `auto_generated`) are rewritten in place, and a generated key the new run no longer writes is removed; everything else in the table (`default_tags`, `default_correspondent`, `title`, `paper_size`, thresholds, comments) is kept; a hand edit to a generated key is overwritten, so delete `auto_generated` from a profile to keep your edits; a profile without `auto_generated = true` is never changed, even with `--force`, and is listed as `Skipped (not auto-generated)` -- rename or delete it to let auto-profiles regenerate it. Show the grouped output lines (`Added:`, `Refreshed:`, `Skipped (not auto-generated):`, `Skipped (already exists; use --force to refresh):`, `Removed (scanner no longer offers it):`) in a small text block. Keep the existing sentences about renames, the `default` profile, and pruning.
      - Around :165 replace "because it is mounted read-only, as in the Docker Compose examples" with "for example, a read-only mount, or `config.toml` bind-mounted as a single file (the rename fails with EBUSY; mount its directory instead, see [Deploy with Docker Compose](deploy-docker-compose.md))".
      - Mention once, near the auto-profiles section, that a config file `auto-profiles` creates from scratch is created with mode 0600 because it may hold your paperless-ngx token.
    Commit as `docs(27-05): ...`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_deployment_config.py -x -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -ni "title template" docs/how-to/configure-scan-profiles.md` returns nothing
    - `grep -n "as in the Docker Compose examples" docs/how-to/configure-scan-profiles.md` returns nothing
    - `grep -n "Skipped (not auto-generated)" docs/how-to/configure-scan-profiles.md` matches
    - `uv run pytest tests/test_deployment_config.py -q` passes with 8 tests
  </acceptance_criteria>
  <done>The profile how-to states the merge and title rules this phase implements. VALIDATION rows: CFG-07 (doc), CFG-06 (doc).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation -> operator deployment | Operators copy the compose example; its mount shape decides whether rewrites are durable and who owns the file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-18 | Denial of service | single-file bind mount in docs | mitigate | directory mount in compose and all docs; static test forbids `config.toml:/etc/saneless/config.toml` anywhere under docs/ or in compose |
| T-27-19 | Tampering / Elevation | writable config directory | accept | The directory must be writable for atomic replace (D-09). Docs state it holds `config.toml` only and must not be writable by untrusted users; saneless writes via mkstemp (O_EXCL) and preserves owner/mode (plan 27-02) |
| T-27-20 | Information disclosure | new config file permissions | mitigate | documented 0600 creation mode (implemented in plan 27-02) so operators know a generated file is private |
</threat_model>

<verification>
- `uv run pytest tests/test_deployment_config.py -q`
- `grep -rn "config.toml:/etc/saneless/config.toml\|fail to start" docker-compose.yml docs` returns nothing
</verification>

<success_criteria>
- The documented Docker Compose mount is `./config:/etc/saneless`, read-write (SC3 deployment half, CFG-09)
- No doc claims the container fails to start without `config.toml`
- The profile how-to matches D-01..D-04 and D-15/D-16
</success_criteria>

<output>
Create `.planning/phases/27-configuration-strictness/27-05-SUMMARY.md` when done
</output>
