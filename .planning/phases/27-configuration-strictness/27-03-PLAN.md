---
phase: 27-configuration-strictness
plan: 03
type: tdd
wave: 2
depends_on: ["27-01"]
files_modified:
  - src/saneless/config.py
  - tests/test_config.py
autonomous: true
requirements: [CFG-01, CFG-02, CFG-05, CFG-11]
decisions: [D-10, D-11, D-12, D-13, D-14]

must_haves:
  truths:
    - "ScannerConfig, PaperlessConfig, OutputConfig and ProfileConfig all forbid extra keys, and Settings forbids them explicitly; ProfileConfig keeps populate_by_name=True (CFG-01)"
    - "Every pydantic validation error from load_settings becomes one line under a header 'Configuration error in <file>:' (or 'Configuration error (defaults and environment):'), rendered from loc and msg only, as a ConfigError (D-10)"
    - "An unknown key gets a difflib close-match suggestion from field names plus aliases and the valid-key list derived from model_fields (title shown, not default_title); a key that belongs to a different section says 'it belongs in [<section>]'; '[Paperless]' says 'did you mean [paperless]?' and is not suggested as [profiles.Paperless] (D-11)"
    - "An error whose value came from a SANELESS_* variable names that variable as spelled in the environment (D-12)"
    - "A SANELESS_* variable whose first segment is not scanner, paperless, output or profiles (including SANELESS_CONFIG_PATH and SANELESS_PAPERLES__TOKEN) is rejected at load with a hint, exit-2 class ConfigError; valid nested and JSON-valued variables are accepted (D-13)"
    - "No config error ever contains an input value: a token-shaped value under a mistyped key is absent from str(err), repr(err), and the exception chain (raised from None) (D-14, CFG-05)"
    - "An explicit --config path that does not exist or is not a regular file raises ConfigError naming the ~-expanded path; auto-discovery skips a directory at a search path (CFG-02)"
    - "env_sourced_keys() returns dotted key names supplied by the environment, and log_config_sources(settings) logs one INFO line naming the loaded file (or 'no config file; defaults + environment') and those names, never values (CFG-11)"
  artifacts:
    - path: "src/saneless/config.py"
      provides: "extra='forbid' on all models; error renderer; unknown-env scan; env_sourced_keys; log_config_sources; CFG-02 load path"
      contains: "def log_config_sources"
    - path: "tests/test_config.py"
      provides: "renderer, env attribution, unknown env, never-echoes, missing-config, config-sources tests"
  key_links:
    - from: "load_settings / _build_settings"
      to: "pydantic_settings.EnvSettingsSource"
      via: "env contribution oracle for D-12 attribution and CFG-11 names"
      pattern: "EnvSettingsSource\\(Settings\\)"
    - from: "_build_settings"
      to: "ConfigError"
      via: "rendered lines, chain suppressed"
      pattern: "raise ConfigError\\(msg\\) from None"
---

<objective>
Make a wrong config fail at load with a message that names the right place and never echoes a value:
nested `extra="forbid"`, a pure renderer over `ValidationError.errors()` (D-10, D-11, D-14),
environment attribution and unknown-`SANELESS_*` rejection (D-12, D-13), the CFG-02 missing/non-file
`--config` error, and the CFG-11 source-reporting functions (called by the CLI in plan 27-06).

Purpose: M-18, M-19, N-15 (error half), U-01 (logging half). The CLI already turns any load exception
into exit 2; plan 27-06 stops it doubling the "Configuration error" prefix.

Output: config.py loader/renderer changes and tests. No CLI changes here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/phases/27-configuration-strictness/27-CONTEXT.md
@.planning/phases/27-configuration-strictness/27-RESEARCH.md
@.planning/phases/27-configuration-strictness/27-PATTERNS.md
@.planning/phases/27-configuration-strictness/27-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From src/saneless/config.py after plan 27-01:
- Models: `ScannerConfig(host, device)`, `PaperlessConfig(url, token: SecretStr, consume_dir)`, `ProfileConfig` (`model_config = ConfigDict(populate_by_name=True)`, `default_title` alias `title`, `_translate_legacy_manual_duplex` before-validator), `OutputConfig` (`log_level: LogLevel`), `Settings(BaseSettings)` with `SettingsConfigDict(env_prefix="SANELESS_", env_nested_delimiter="__")`, sources `(init, env, toml)`.
- `_VALID_SECTIONS = ("scanner", "paperless", "output", "profiles")` (line ~336) -- to be removed (D-11).
- `_SettingsFactory` Protocol + `_build_settings(toml_file: Path | None = None) -> Settings` (353-380): today renders only top-level `extra_forbidden` as "Unknown config section ... Did you mean [profiles.X]?" and `raise ConfigError(msg) from exc`; other ValidationErrors re-raise raw.
- `config_search_paths() -> tuple[Path, ...]` (430-446); `load_settings(config_path: str | None = None) -> Settings` (449-477): `Path(config_path) if config_path else next((p for p in config_search_paths() if p.exists()), None)`; sets `settings._config_path = path`.
- `warn_on_legacy_duplex_sources(settings)` (286-333): the post-logging function pattern to copy for `log_config_sources`.
- `logger = logging.getLogger(__name__)` exists (name `saneless.config`).

Verified pydantic behaviour (RESEARCH § Pattern 1 table): `[paperless] tokne` -> type `extra_forbidden`, loc `('paperless','tokne')`, msg `Extra inputs are not permitted`; `[profiles.default] resoluton` -> loc `('profiles','default','resoluton')`; top-level `[Paperless]` -> loc `('Paperless',)`; `[output] web_port = "abc"` -> `int_parsing` loc `('output','web_port')`; `default_tags = ["x"]` -> loc `(..., 'default_tags', 0)`; `log_level="TRACE"` -> `literal_error` loc `('output','log_level')`; missing default -> `value_error` loc `('profiles',)` msg `Value error, A 'default' profile must be defined in config`. No `msg` contains the input; `input` and `ctx` do. `str(ValidationError)` DOES contain inputs.

Verified pydantic-settings behaviour (RESEARCH § Pattern 3): `EnvSettingsSource(Settings)()` returns the case-folded nested dict of env contributions, JSON-valued sections exploded; unknown top-level names (`SANELESS_PAPERLES__TOKEN`, `SANELESS_CONFIG_PATH`, `SANELESS_SCANNER_HOST`) are silently ignored by it; invalid JSON for a complex field (`SANELESS_PAPERLESS=notjson`) raises `pydantic_settings.exceptions.SettingsError` (a ValueError, message names field and source, not value) from both the source and `Settings()`.

Existing tests to flip (PATTERNS § test_config.py): :78 missing default -> `ConfigError` keep `match="default"`; :174-182 missing explicit path -> `ConfigError` naming the path; :203-216 `test_env_var_cannot_set_config_path` -> rejected naming `SANELESS_CONFIG_PATH`; :442 flip timeout via TOML -> `ConfigError` keep `match="flip_timeout_seconds"`; :305-311 `test_wrong_section_name_gives_helpful_error` (`[default] title=...`) must still match `profiles\.default`. :253 invalid TOML syntax keeps `ValueError` (Phase 28 owns tomllib wrapping; do NOT catch TOMLDecodeError).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nested forbid, the loc/msg renderer, and the CFG-02 load path</name>
  <files>src/saneless/config.py, tests/test_config.py</files>
  <read_first>
    - src/saneless/config.py (whole file, ~500 lines after 27-01)
    - tests/test_config.py lines 60-90, 140-260, 295-345, 430-450
    - .planning/phases/27-configuration-strictness/27-RESEARCH.md § Pattern 1, Pattern 2, Pattern 6 (CFG-02 part), Pitfalls 1, 11
    - .planning/phases/27-configuration-strictness/27-PATTERNS.md § Shared Patterns S-1, S-4; § config.py
  </read_first>
  <behavior>
    - `[paperless] tokne = "x"` -> ConfigError; message's first line is `Configuration error in <file>:`; a line contains `[paperless]`, `'tokne'`, `did you mean 'token'`, and `valid keys: url, token, consume_dir` (test name contains `unknown_key`)
    - `[profiles.default] resoluton = 600` -> a line with `[profiles.default]`, `'resoluton'`, `did you mean 'resolution'`, and a valid-key list containing `title` and not `default_title` (test name contains `profile_unknown_key`)
    - `[paperless] web_port = 9` -> line `[paperless] unknown key 'web_port'; it belongs in [output]`; top-level `web_port = 9` -> line naming `it belongs in [output]` (test names contain `wrong_section`)
    - `[Paperless] url = "x"` -> line contains `did you mean [paperless]` and does not contain `profiles.Paperless`; `[default] title = "x"` still matches `profiles\.default` (existing test)
    - A TOML with `[paperless] tokne`, `[output] web_port = "abc"`, and `[output] log_level = "TRACE"` produces all three lines in one message, one per line, the type errors as `[output] web_port: Input should be a valid integer...` and `[output] log_level: Input should be 'DEBUG', ...` (test name contains `renders_every_error`)
    - `[profiles.default] default_tags = ["x"]` renders the key path as `default_tags[0]`
    - Missing default profile and out-of-range `flip_timeout_seconds` now raise ConfigError (flipped tests :78, :442)
    - Token-shaped value `tok-SECRET-7c2a` under `[paperless] tokne` and under `[output] web_port`: absent from `str(err)` and `repr(err)`; `err.__cause__ is None` and `err.__suppress_context__ is True` (test name contains `never_echoes`)
    - A key containing a control character (TOML quoted key `"tok\nne"`) is rendered escaped via repr, so the message has no raw newline inside that line
    - `load_settings("/nope/missing.toml")` -> ConfigError containing `/nope/missing.toml`; `load_settings(str(a_directory))` -> ConfigError naming it; `load_settings("~/cfg.toml")` with HOME redirected and no file -> message contains the expanded `<HOME>/cfg.toml` (test names contain `missing_config` or `not_a_file`)
    - A directory named `saneless.toml` in the cwd is skipped by discovery: `load_settings()` returns settings with `config_path is None` (test name contains `not_a_file`)
  </behavior>
  <action>
    RED (commit `test(27-03): ...`): add classes `TestUnknownKeyRendering` (D-10, D-11, CFG-01), `TestConfigErrorsNeverEchoValues` (D-14, CFG-05), and `TestExplicitConfigPath` (CFG-02) to tests/test_config.py; flip the existing tests listed in the interfaces block (:78, :174-182, :442). Reuse the `empty_cwd_and_home` fixture shape from `TestLoadedConfigPath`. Run: new and flipped tests fail.

    GREEN (commit `feat(27-03): ...`) in src/saneless/config.py:
    - Add `model_config = ConfigDict(extra="forbid")` to `ScannerConfig`, `PaperlessConfig`, `OutputConfig`; change `ProfileConfig` to `ConfigDict(extra="forbid", populate_by_name=True)`; add `extra="forbid"` to `Settings`' `SettingsConfigDict`. Comment on ProfileConfig: the legacy-duplex before-validator only adds `duplex`, a real field, so it stays valid under forbid.
    - Remove `_VALID_SECTIONS`. Add a private `_SECTION_MODELS: Final[dict[str, type[BaseModel]]] = {"scanner": ScannerConfig, "paperless": PaperlessConfig, "output": OutputConfig}` and helpers (Google docstrings, all private): `_valid_keys(model) -> list[str]` (display names: `field.alias or name`), `_match_candidates(model) -> list[str]` (field names plus aliases), `_section_owning(key, *, exclude) -> str | None` (the other plain section whose display or field names contain `key`), `_format_loc_path(parts) -> str` (strings joined by `.`, ints as `[i]`), and `_render_error_lines(errors: Sequence[ErrorDetails]) -> list[str]` implementing RESEARCH Pattern 2 rules 1-5: unknown top-level name (casefold match to a section -> `unknown section [X!r-escaped] (did you mean [section]?)`; a key of some section -> `unknown key 'k'; it belongs in [section]`; else `unknown section 'X' (did you mean [profiles.X]?)` plus a difflib hint over section names); plain-section `extra_forbidden` -> D-11 unknown-key line (`did you mean` only when difflib finds a match with `n=1`); `profiles` with `len(loc) >= 3` -> label `profiles.<name>` model `ProfileConfig`; any other error -> `[<label>] <key path>: <msg>` (a bare `('profiles',)` renders `[profiles]: <msg>`). Every user-controlled name (keys, section names, profile names) is formatted with `!r` or passed through `repr` so control characters are escaped (log-forgery defence, web/errors.py precedent). Read only `err["loc"]`, `err["type"]`, `err["msg"]`; never `input` or `ctx` (comment: D-14, str(ValidationError) embeds inputs). Sort lines for stable output. Import `ErrorDetails` from `pydantic_core` under TYPE_CHECKING, `difflib` at runtime.
    - Rewrite `_build_settings(toml_file)` to: try construction; `except ValidationError as exc:` build `lines = [header, *("  " + line for line in _render_error_lines(exc.errors()))]` where header is `f"Configuration error in {toml_file}:"` when a file is given else `"Configuration error (defaults and environment):"`; `msg = "\n".join(lines)`; `raise ConfigError(msg) from None` with a comment that chaining would print the ValidationError (and its inputs) in any traceback (Pitfall 1). Update its docstring. Task 2 extends this function with env attribution and the unknown-env scan; structure it so both can be added (for example, render into a list first, raise at the end).
    - `load_settings`: when `config_path` is truthy, `path = Path(config_path).expanduser()`; if `not path.is_file()`, `msg = f"Config file not found or not a regular file: {path}"` and raise ConfigError (CFG-02); else discovery uses `p.is_file()`. Delete the "CFG-02 (Phase 27) owns making that an error" comment; update the docstring's Returns/Raises.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -k "unknown_key or wrong_section or renders_every_error or never_echoes or missing_config or not_a_file" -x -q && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'extra="forbid"' src/saneless/config.py` is at least 5
    - `grep -n "_VALID_SECTIONS" src/saneless/config.py` returns nothing
    - `grep -n "from None" src/saneless/config.py` matches in `_build_settings`; `grep -n '\["input"\]\|\["ctx"\]\|get("input")\|get("ctx")' src/saneless/config.py` returns nothing
    - `grep -n "is_file()" src/saneless/config.py` matches at least twice; `grep -n "p.exists()" src/saneless/config.py` returns nothing
    - `uv run pytest tests/test_config.py -k "unknown_key or wrong_section or renders_every_error or never_echoes or missing_config or not_a_file" -q` selects at least 12 tests and passes
    - Full non-browser suite passes; gate commands exit 0
  </acceptance_criteria>
  <done>Every TOML-sourced validation error is rendered value-free, one per line, with suggestions; a bad --config path is a named ConfigError. VALIDATION rows: CFG-01, CFG-01 (D-14) + CFG-05, CFG-02 (unit half).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Environment attribution, unknown SANELESS_* rejection, SettingsError, and the CFG-11 source functions</name>
  <files>src/saneless/config.py, tests/test_config.py</files>
  <read_first>
    - src/saneless/config.py (as left by Task 1)
    - tests/test_config.py lines 1-60 (imports, env tests) and 560-620 (caplog helper `_warnings` pattern)
    - tests/conftest.py lines 125-160 (clean_env removes SANELESS_* only)
    - .planning/phases/27-configuration-strictness/27-RESEARCH.md § Pattern 3, Pitfalls 2, 9; § Code Examples "CFG-11 startup line"
    - .planning/phases/27-configuration-strictness/27-PATTERNS.md § Shared Patterns S-3, S-10
  </read_first>
  <behavior>
    - `SANELESS_SCANNER__HOSTNAME=x` (no file) -> ConfigError with a line `environment variable 'SANELESS_SCANNER__HOSTNAME': unknown key 'hostname' in [scanner] (did you mean 'host'?); valid keys: host, device`; a lower-case spelling `saneless_scanner__hostname` is named exactly as spelled (test name contains `attributes_env`)
    - `SANELESS_OUTPUT__WEB_PORT=abc` with a valid file loaded -> the web_port line names the variable, not the file; the same typo in the TOML file alone names no variable (test name contains `attributes_env`)
    - A TOML `[profiles.Receipt]` error is not attributed to an env var `SANELESS_PROFILES__RECEIPT__TITLE` unless that variable's path matches the error loc after case folding exactly as pydantic-settings folds it (document the chosen check in the test docstring)
    - `SANELESS_PAPERLES__TOKEN=t` -> ConfigError naming `SANELESS_PAPERLES__TOKEN` with `did you mean SANELESS_PAPERLESS__TOKEN`; `SANELESS_OUTPUT_WEB_PORT=9` -> hint `SANELESS_OUTPUT__WEB_PORT`; `SANELESS_CONFIG_PATH=/tmp/x` -> rejected naming it (rewritten test :203-216, strictly stronger for T-26-05) (test names contain `unknown_env`)
    - Accepted without error: `SANELESS_OUTPUT__DATA_DIR=<tmp>`, `SANELESS_PROFILES__RECEIPT__TITLE=R` (with default profile present), `SANELESS_OUTPUT='{"web_port": 9}'` (test name contains `unknown_env`)
    - Unknown env names and TOML errors appear together in one message when both exist
    - `SANELESS_PAPERLESS=notjson` -> ConfigError (not SettingsError) naming `paperless`; the string `notjson` is absent from the message; chain suppressed
    - `SANELESS_PAPERLESS__TOKNE=tok-SECRET-9f8e` -> the value is absent from the message (test name contains `never_echoes`)
    - `env_sourced_keys()` with `SANELESS_PAPERLESS__TOKEN=t`, `SANELESS_PAPERLESS__URL=u`, `SANELESS_PROFILES__RECEIPT__TITLE=R` returns `["paperless.token", "paperless.url", "profiles.receipt.title"]`; with none set returns `[]`
    - `log_config_sources(settings)` under `caplog.at_level(logging.INFO, logger="saneless.config")` emits exactly one INFO record from `saneless.config` containing the loaded path (or `no config file; defaults + environment`) and `paperless.token`, and not the token value (test name contains `config_sources` and `secret`)
  </behavior>
  <action>
    RED (commit `test(27-03): ...`): add classes `TestEnvironmentAttribution` (D-12), `TestUnknownEnvironmentVariables` (D-13; rewrite `test_env_var_cannot_set_config_path` into it), extend `TestConfigErrorsNeverEchoValues` (D-14 env case, SettingsError case), and `TestConfigSources` (CFG-11). Use `monkeypatch.setenv`; clean_env already strips SANELESS_*. Run: they fail.

    GREEN (commit `feat(27-03): ...`) in src/saneless/config.py:
    - `_env_contribution() -> dict[str, object]`: `EnvSettingsSource(Settings)()` (import from pydantic_settings). Comment: this is pydantic-settings' own prefix/delimiter/case-fold/JSON logic, so attribution and CFG-11 cannot drift from what was actually loaded (RESEARCH Don't Hand-Roll).
    - `_env_variable_for(loc: tuple[str | int, ...], env_data: Mapping[str, object]) -> str | None`: walk `env_data` by the string elements of `loc`; for the longest prefix present, look for an `os.environ` name equal (casefold) to `"SANELESS_" + "__".join(parts)`, trying longest to shortest; return the name as spelled in `os.environ`, else None. `_render_error_lines` gains an `env_data` parameter: when a variable is found, the line is prefixed `environment variable {name!r}: ` and the section is phrased `in [section]` (e.g. `environment variable 'SANELESS_SCANNER__HOSTNAME': unknown key 'hostname' in [scanner] (did you mean 'host'?); valid keys: host, device`); otherwise the Task 1 file-attributed form is kept (D-12).
    - `_unknown_env_lines(environ: Mapping[str, str]) -> list[str]`: for names whose casefold starts with `saneless_`, take the first `__` segment casefolded; if not in `Settings.model_fields`, emit `environment variable {name!r}: unknown section {first!r}` plus a hint: when `first` starts with `<section>_`, suggest `SANELESS_<SECTION>__<REST>` upper-cased; else a difflib match over section names gives `did you mean SANELESS_<MATCH>__<rest-of-name>?`; always append `; valid sections: scanner, paperless, output, profiles` (derived from `Settings.model_fields`). Comment: pydantic-settings silently ignores these (D-13), and this runs in the loader, never in a Settings validator, so direct `Settings(...)` construction in tests is unaffected (Pitfall 9, S-10).
    - `_build_settings`: compute `env_lines = _unknown_env_lines(os.environ)`; wrap the `EnvSettingsSource` call and construction so `SettingsError` becomes a rendered line `environment: {exc}` (message verified value-free) and raises ConfigError `from None`; on ValidationError render with `env_data`; if construction succeeded but `env_lines` is non-empty, still raise. All lines share the Task 1 header.
    - Public `env_sourced_keys() -> list[str]`: flatten `_env_contribution()` to sorted dotted leaves (a non-empty Mapping recurses; anything else is a leaf). Names only.
    - Public `log_config_sources(settings: Settings) -> None`: one `logger.info("Configuration: %s; from environment: %s", source, keys_or_none)` where source is `str(settings.config_path)` or `"no config file; defaults + environment"` and keys is `", ".join(env_sourced_keys()) or "(none)"`. Docstring states it is called by the CLI after `configure_logging` (S-10, CFG-11) and never logs values. If `env_sourced_keys` raises `SettingsError` here it cannot, because load already succeeded; do not add a handler.
    - Export `env_sourced_keys` and `log_config_sources` in `__all__`.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config.py -k "attributes_env or unknown_env or never_echoes or config_sources or secret" -x -q && uv run pytest -m "not browser and not sane_hardware" -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "EnvSettingsSource(Settings)" src/saneless/config.py` matches once
    - `grep -n "def env_sourced_keys() -> list\[str\]" src/saneless/config.py` and `grep -n "def log_config_sources(settings: Settings) -> None" src/saneless/config.py` each match once
    - `grep -n "SettingsError" src/saneless/config.py` matches
    - No `logger.` call in config.py passes an f-string (`grep -n 'logger\.[a-z]*(f"' src/saneless/config.py` returns nothing)
    - `uv run pytest tests/test_config.py -k "attributes_env or unknown_env or config_sources" -q` selects at least 10 tests and passes
    - Full non-browser suite passes (Dockerfile-style `SANELESS_OUTPUT__DATA_DIR` still accepted); gate commands exit 0
  </acceptance_criteria>
  <done>Env-sourced errors name their variable, unknown SANELESS_* names are rejected, SettingsError is a value-free ConfigError, and the CFG-11 functions exist. VALIDATION rows: CFG-01 (D-12/D-13), CFG-01 (D-14) + CFG-05, CFG-11 (function half).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TOML file / process environment -> loader | Operator-controlled keys, values and variable names |
| loader -> stderr / log file / tracebacks | Rendered errors and the CFG-11 line leave the process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-token | Information disclosure | `_build_settings` error rendering | mitigate | Lines built from `loc`/`type`/`msg` only; `raise ConfigError(msg) from None`; tests assert token absent from str, repr, `__cause__`, and that `__suppress_context__` is True |
| T-27-env | Tampering / Repudiation | unknown `SANELESS_*` names (e.g. `SANELESS_PAPERLES__TOKEN`) silently ignored | mitigate | `_unknown_env_lines` scan in the loader rejects them with a hint (D-13) |
| T-27-10 | Elevation / Tampering | forged write target via `SANELESS_CONFIG_PATH` | mitigate | `_config_path` stays a PrivateAttr (Phase 26) and D-13 now rejects the variable outright |
| T-27-11 | Spoofing / Tampering | control characters in TOML quoted keys or profile names forging stderr/log lines | mitigate | every user-controlled name rendered through `repr` / `!r`; test with an embedded newline |
| T-27-12 | Information disclosure | CFG-11 startup line | mitigate | `env_sourced_keys` returns dotted names only; caplog test asserts the token value is absent |
| T-27-13 | Information disclosure | `SettingsError` for invalid JSON env values | mitigate | wrapped into ConfigError `from None`; test asserts the raw value is absent (message verified value-free) |
</threat_model>

<verification>
- `uv run pytest tests/test_config.py -q`
- `uv run pytest -m "not browser and not sane_hardware" -q`
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
</verification>

<success_criteria>
- A typo under any section, in TOML or env, is rejected with section, key, suggestion and valid keys (SC1 first half)
- A missing or non-file `--config` is a ConfigError naming the path (SC1 second half, unit level)
- No config error or CFG-11 record contains a value (SC4 error/log half)
</success_criteria>

<output>
Create `.planning/phases/27-configuration-strictness/27-03-SUMMARY.md` when done
</output>
