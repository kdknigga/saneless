---
phase: 20-ci-gate
plan: 05
type: execute
wave: 5
depends_on: ["20-04"]
files_modified:
  - pyproject.toml
  - uv.lock
  - src/saneless/config.py
  - tests/test_scanner.py
autonomous: true
requirements: [CI-01]

must_haves:
  truths:
    - "ty and pyrefly are bumped to current and both report zero errors"
    - "All three `# type: ignore[...]` suppressions are gone, replaced by real typing changes, and no new suppression of any kind was introduced"
    - "The full non-browser suite still passes and behaviour is unchanged"
    - "The pull request has a green CI run on the bumped toolchain and is still open and unmerged"
  artifacts:
    - path: "pyproject.toml"
      provides: "Bumped ty and pyrefly lower bounds"
      contains: "pyrefly>="
    - path: "src/saneless/config.py"
      provides: "Suppression-free construction of Settings with the private _toml_file kwarg"
    - path: "tests/test_scanner.py"
      provides: "Pluggable get_options on MockSaneDev, replacing two lambda-over-method assignments"
  key_links:
    - from: "tests/test_scanner.py MockSaneDev.get_options"
      to: "the existing _snap_impl delegation pattern at tests/test_scanner.py:69,74-79"
      via: "an _options_impl attribute plus a delegating method"
      pattern: "_options_impl"
    - from: "src/saneless/config.py:168"
      to: "the cast(\"Name\", value) form already used at src/saneless/config.py:134"
      via: "string-literal cast bridging a runtime-true, statically-invisible init kwarg"
      pattern: "cast\\("
---

<objective>
Bump `ty` (0.0.24 → current) and `pyrefly` (0.57.1 → current, crossing 1.0) and fix the type errors
the newer checkers surface — with real typing changes, never suppressions — then push the result to
the open pull request and confirm CI is still green (D-16).

Purpose: D-16 is explicit that this overrides the ROADMAP's "zero source changes in this phase" note.
These are planned work, not an execution deviation. D-17 is equally explicit that this lands as a
separate, later commit than `ci.yml`: the gate was proven green on the locked versions first
precisely so that a red run here unambiguously implicates the bump rather than the workflow.

Output: bumped `pyproject.toml`/`uv.lock`, three suppressions removed, a green run on the PR.

Not TDD despite `workflow.tdd_mode`: no new behaviour is introduced. Task 2 is a typing refactor of
code already covered by the 332-test suite plus two type checkers, which is exactly the coverage a
RED phase would be manufacturing. The gate is `uv run ty check && uv run pyrefly check src tests && uv run
pytest -m "not browser"`, per VALIDATION.md's D-16 row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/20-ci-gate/20-CONTEXT.md
@.planning/phases/20-ci-gate/20-PATTERNS.md
@.planning/phases/20-ci-gate/20-VALIDATION.md
@.planning/phases/20-ci-gate/20-03-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
The three failure sites and their in-repo fix precedents, extracted in PATTERNS.md so no codebase
exploration is needed:

- `src/saneless/config.py:168` — `return Settings(_toml_file=toml_file)  # type: ignore[call-arg]`.
  `_toml_file` is not a declared field on `Settings` (`config.py:99-114`); it is a private init kwarg
  popped at runtime by the class's own `settings_customise_sources` override (`config.py:133-141`).
  Precedent four lines above the failure: `config.py:134` already uses
  `cast("InitSettingsSource", init_settings)`, and `cast` is already imported at `config.py:14`.
  `from __future__ import annotations` is present (`config.py:9`) and there is a `TYPE_CHECKING`-only
  import block at `config.py:26-27`.

- `tests/test_scanner.py:706` and `:723` — `mock_dev.get_options = lambda: [...]  # type: ignore[assignment]`,
  assigning a plain callable over the bound method declared at `tests/test_scanner.py:95` as
  `def get_options(self) -> list[tuple]:`. A genuine type error, not a checker quirk.
  Precedent in the same class: `MockSaneDev` already solves this for `snap()` via
  `self._snap_impl: MagicMock | None = None` (`:69`) plus a delegating `snap()` (`:74-79`), and
  callers assign `mock_dev._snap_impl = ...` with no suppression (`:688`, `:728-730`).
</interfaces>

<hard_constraints>
- **No suppressions.** Not `# type: ignore`, not `# ty: ignore[...]`, not `# noqa`, not disabling a
  rule in `pyproject.toml`. CLAUDE.md forbids all four; converting the existing mypy-coded ignores
  into ty-coded ignores is explicitly the wrong fix (D-16, PATTERNS).
- Do not merge the pull request (D-21). Do not create a tag.
- Bump ONLY `ty` and `pyrefly`. `ruff`, `pytest`, `playwright`, `pytest-playwright`, `prek`,
  `mkdocs-material` and the runtime pins are Phase 32's sweep (CONTEXT § Deferred).
- Do not touch `release.yml` or `docs.yml` (D-06).
- Do not reorder or alphabetize the `[dependency-groups].dev` list.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Bump ty and pyrefly and capture the fallout</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    - `pyproject.toml` lines 152-163 (the `[dependency-groups].dev` list)
    - `.planning/phases/20-ci-gate/20-RESEARCH.md` § Package Legitimacy Audit (both packages `[OK]`, Approved) and § Pitfall 8 (why ty stopped honouring bracketed `# type: ignore` codes) and § Pitfall 9 (pyrefly 1.x renamed `--python-interpreter`)
    - `.planning/phases/20-ci-gate/20-CONTEXT.md` D-16, D-17
  </read_first>
  <action>
    Run `uv add --dev "ty@latest" "pyrefly@latest"`, which raises the `ty>=0.0.21` and
    `pyrefly>=0.55.0` lower bounds and regenerates `uv.lock` in one step. Record the resolved versions
    (research saw `ty` 0.0.80 and `pyrefly` 1.2.0 on PyPI).

    Then run `uv run ty check` and `uv run pyrefly check src tests` and capture the complete diagnostic output
    verbatim before changing any source. Research predicts exactly three `ty` diagnostics — `ty` no
    longer honours a `# type: ignore` carrying any bracketed code, and the three existing ones carry
    *mypy* codes — and zero new `pyrefly` findings. If the real output differs, record the difference;
    it changes the scope of Task 2 and must be reported rather than absorbed silently.

    Add no CLI flags to either checker. `.pre-commit-config.yaml` invokes them bare as `uv run ty
    check` / `uv run pyrefly check src tests`, `ci.yml` mirrors that, and pyrefly 1.x's flag rename only matters
    if flags are introduced.

    This task's commit is the bump alone — the source fixes are Task 2's commit, so the diff that
    caused any behaviour change is attributable. Both commits are pushed together in Task 3.

    Expect this commit alone to leave `ty` red. That is the point of D-17's ordering and is not a
    reason to revert.
  </action>
  <verify>
    <automated>uv run ty --version; uv run pyrefly --version</automated>
    <automated>uv run pyrefly check src tests</automated>
    <automated>uv run pytest -m "not browser" -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty --version` reports a version greater than 0.0.24; `uv run pyrefly --version` reports >= 1.0.
    - `pyproject.toml`'s `dev` list shows raised lower bounds for exactly `ty` and `pyrefly`; `git diff pyproject.toml` shows no other dependency line changed and no reordering.
    - `uv.lock` is regenerated and `uv sync --locked` exits 0 (no lockfile drift).
    - `uv run pytest -m "not browser" -q` still exits 0 — no new import-time `DeprecationWarning`, which `filterwarnings = ["error"]` would turn into a failure.
    - `uv run ruff check .` and `uv run ruff format --check .` still exit 0.
    - The complete `uv run ty check` and `uv run pyrefly check src tests` output is captured verbatim in the SUMMARY, and any divergence from the predicted "3 ty diagnostics, 0 pyrefly" is called out explicitly.
    - `git show --stat HEAD` lists exactly `pyproject.toml` and `uv.lock`.
  </acceptance_criteria>
  <done>`ty` and `pyrefly` are current, the lockfile is consistent, the suite is green, and the exact new diagnostics are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Remove all three suppressions with real typing fixes</name>
  <files>src/saneless/config.py, tests/test_scanner.py</files>
  <read_first>
    - `src/saneless/config.py` lines 9-30 (imports, `__future__`, `TYPE_CHECKING` block), 95-145 (the `Settings` fields and `settings_customise_sources`), 160-175 (`_build_settings`, the failure site)
    - `tests/test_scanner.py` lines 60-100 (`MockSaneDev.__init__`, `_snap_impl`, `snap`, `get_options`) and 680-735 (the two lambda assignment sites and their `_snap_impl` neighbours)
    - `.planning/phases/20-ci-gate/20-PATTERNS.md` § "The three `# type: ignore[...]` sites" — both fix precedents, spelled out with line numbers
    - `pyproject.toml` lines 125-140 (the `tests/**` per-file-ignores — they cover only `S101`, `ARG`, `S104/5/6`, so the `D` docstring rules still apply to any new or modified test method)
  </read_first>
  <action>
    Fix all three sites so both checkers pass with zero suppressions.

    Site 1 — `src/saneless/config.py:168`. The suppression hides that `_toml_file` is a runtime-only
    private init kwarg consumed by the class's own `settings_customise_sources`. Bridge that gap with
    the `cast("Name", value)` string-literal form the file already uses at `config.py:134` (e.g. cast
    the `Settings` constructor to a callable type that accepts the private kwarg), keeping the runtime
    call identical. `cast` is already imported; add any type-only name to the existing
    `TYPE_CHECKING` block rather than importing it at runtime. Do not declare `_toml_file` as a public
    field — that changes the settings schema and is a behaviour change, not a typing fix.

    Sites 2 and 3 — `tests/test_scanner.py:706` and `:723`. Apply the pluggable-impl pattern
    `MockSaneDev` already uses twice for `snap()`: add an `_options_impl` attribute initialised to
    `None` in `__init__` alongside `_snap_impl` (`:69`), and have `get_options` return it when set and
    fall through to the existing default otherwise. Both call sites then become plain attribute
    assignments with no suppression, exactly like the neighbouring
    `mock_dev._snap_impl = MagicMock(...)` lines at `:688` and `:728-730`. Preserve the option tuples
    at each site byte-for-byte — the tests assert on their contents. The modified `get_options` needs a
    docstring; every method in these mock classes has one and ruff's `D` rules apply to `tests/`.

    Both files are one commit, separate from Task 1's bump.

    If a diagnostic appears that neither precedent covers, stop and report it rather than reaching for
    a suppression. Suppressing is out of bounds under CLAUDE.md regardless of how narrow it looks.
  </action>
  <verify>
    <automated>uv run ty check</automated>
    <automated>uv run pyrefly check src tests</automated>
    <automated>uv run pytest -m "not browser"</automated>
    <automated>uv run ruff check . &amp;&amp; uv run ruff format --check .</automated>
  </verify>
  <acceptance_criteria>
    - `uv run ty check` exits 0 with "All checks passed"; `uv run pyrefly check src tests` reports 0 errors.
    - `uv run pytest -m "not browser"` reports 332 passed, 8 deselected — the same counts as the pre-bump baseline, so no test was deleted, skipped, or xfailed to get here.
    - `git grep -c 'type: ignore' -- src tests` returns no matches (all three suppressions gone).
    - `git grep -cE '# (ty|pyrefly): ignore|# noqa' -- src tests` returns no matches — none were introduced.
    - `git diff pyproject.toml` in this task is empty — no lint or type rule was disabled or added to `per-file-ignores` to make the checks pass.
    - `git grep -c '_options_impl' -- tests/test_scanner.py` returns >= 3 (the `__init__` attribute, the delegation, and at least the two call sites).
    - `git grep -c 'get_options = lambda' -- tests/test_scanner.py` returns no matches.
    - `git grep -c 'cast(' -- src/saneless/config.py` is greater than its pre-task value.
    - `uv run ruff check .` and `uv run ruff format --check .` exit 0 (the `D` docstring rules cover the modified test method).
    - `uv run prek run` exits 0.
    - `git show --stat HEAD` lists exactly `src/saneless/config.py` and `tests/test_scanner.py`.
  </acceptance_criteria>
  <done>Both type checkers pass clean on the bumped versions with zero suppressions anywhere in `src/` or `tests/`, and the suite counts are unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the bump to the pull request and confirm it is still green</name>
  <files>(no working-tree files; remote ref and CI run only)</files>
  <read_first>
    - `.planning/phases/20-ci-gate/20-02-SUMMARY.md` (the filtered branch name and how it was built)
    - `.planning/phases/20-ci-gate/20-03-SUMMARY.md` (the pull request number and URL)
    - `.planning/phases/20-ci-gate/20-CONTEXT.md` D-21, D-22
  </read_first>
  <action>
    Extend the pushed filtered branch with the two commits from Tasks 1 and 2. Both touch only
    `pyproject.toml`, `uv.lock`, `src/`, and `tests/` — no `.planning/` paths — so cherry-picking them
    onto the filtered branch keeps the push a fast-forward. Prefer that over re-running the
    `/gsd-pr-branch` filter, which would rewrite history and require a force-push. D-22's standing
    consequence still applies: every future push needs the same filtering, so re-verify rather than
    assume.

    Push, then watch the resulting `pull_request` run to completion. Because Plan 03 proved the
    workflow green on the locked toolchain and Plan 04 proved a break turns it red, a red run here
    unambiguously implicates the bump — that is exactly the attribution D-17's ordering was designed
    to produce. If it is red, fix the real cause; do not weaken the gate and do not revert the bump
    without reporting.

    **Do not merge the pull request.** The phase's terminal state is "PR open, green, handed to the
    user" (D-21). End the plan by reporting the PR URL and its state to the user.
  </action>
  <verify>
    <automated>gh run list --workflow=ci.yml --limit 1 --json conclusion,event --jq '{conclusion,event}'</automated>
    <automated>gh pr view --json state,mergedAt,url --jq '{state,mergedAt,url}'</automated>
    <automated>git ls-tree -r --name-only origin/"$PR_BRANCH" | grep -c '^\.planning/' ; true</automated>
  </verify>
  <acceptance_criteria>
    - The push was a fast-forward (no `--force` / `--force-with-lease` used); if a force-push was unavoidable, the reason is recorded in the SUMMARY.
    - `git ls-tree -r --name-only origin/<PR_BRANCH> | grep -c '^\.planning/'` returns `0`, and every newly pushed commit touches zero `.planning/` paths.
    - `gh run list --workflow=ci.yml --limit 1 --json conclusion,event` returns `success` / `pull_request`, and the run's head SHA equals `gh pr view --json headRefOid --jq .headRefOid`.
    - Both required check contexts report success on the new head commit (`gh api repos/kdknigga/saneless/commits/$SHA/check-runs`), so the ruleset's requirements are satisfied by the bumped tree.
    - `gh pr view --json state,mergedAt --jq '{state,mergedAt}'` returns `OPEN` / `null` (D-21).
    - `git ls-remote origin refs/heads/master` still points at the original root commit.
    - `git ls-remote --tags origin` is empty.
    - The PR URL and final state are reported to the user in the plan's closing message.
  </acceptance_criteria>
  <done>The bumped, suppression-free tree is on the pull request with a green run on both required checks, and the pull request is open and unmerged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → developer machine and CI runner | `uv add --dev "ty@latest" "pyrefly@latest"` pulls new executable code |
| Type-checker output → source changes | Pressure to silence a diagnostic rather than fix it |
| Local commits → the gated pull request | New code enters the published branch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-22 | Tampering | `ty` / `pyrefly` package legitimacy at the bumped versions | mitigate | RESEARCH.md § Package Legitimacy Audit covers both at the target versions (`ty` 0.0.80 astral-sh, `pyrefly` 1.2.0 facebook upstream), `slopcheck` `[OK]`, both already project dependencies. No `[ASSUMED]`/`[SUS]`/`[SLOP]` package in this phase, so no blocking legitimacy checkpoint is required |
| T-20-23 | Repudiation | Type errors hidden by suppressions instead of fixed, making the gate report a safety it does not have | mitigate | Zero-suppression acceptance criteria enforced by `git grep` for `type: ignore`, `ty: ignore`, `pyrefly: ignore`, and `noqa`, plus an assertion that `pyproject.toml` is unmodified in Task 2 (no rule disabled, no `per-file-ignores` widened) |
| T-20-24 | Tampering | Tests deleted, skipped, or xfailed to reach green | mitigate | Suite counts asserted equal to the measured 332 passed / 8 deselected baseline |
| T-20-25 | Tampering | Published history diverging or picking up `.planning/` on a later push | mitigate | Fast-forward-preferred cherry-pick onto the filtered branch, with the `.planning/` count re-verified on the pushed ref (D-22's standing consequence) |
| T-20-26 | Elevation of Privilege | Bypassing the just-installed gate to land the bump | accept then mitigate | The bump goes through the same pull request and the same two required checks as any other change; no bypass actor is used and the PR is not merged |
</threat_model>

<verification>
Full five-check sequence on the bumped toolchain:

`uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests && uv run pytest -m "not browser"`

All five exit 0, with zero suppressions anywhere in `src/` or `tests/`, and the same 332/8 suite
counts as the pre-bump baseline. Then the same five checks green in CI on the pull request.
</verification>

<success_criteria>
- `ty` and `pyrefly` are at current versions and both report zero errors.
- All three pre-existing `# type: ignore[...]` suppressions are removed via real typing changes, and no suppression of any kind was added.
- The suite still reports 332 passed / 8 deselected.
- The pull request has a green run on the bumped toolchain, both required contexts satisfied.
- The pull request is OPEN and unmerged, `master` has not advanced, and no tag exists.
</success_criteria>

<output>
Create `.planning/phases/20-ci-gate/20-05-SUMMARY.md` when done. Record the resolved `ty` and
`pyrefly` versions, the verbatim pre-fix diagnostics, the fix applied at each of the three sites, and
the final pull request URL and state. Close the phase by handing the open pull request to the user —
do not merge it.
</output>
