---
phase: 20-ci-gate
plan: 01
subsystem: infra
tags: [github-actions, dependabot, pytest-timeout, uv, ci, supply-chain]

# Dependency graph
requires:
  - phase: none
    provides: first plan of the phase -- no in-phase dependencies
provides:
  - ".github/workflows/ci.yml -- five checks across two parallel jobs on push-to-master and pull_request"
  - ".github/dependabot.yml -- weekly github-actions SHA-pin bumps"
  - "CONTRIBUTING.md at the repo root documenting the gate"
  - "pytest-timeout hang guard (timeout = 60, timeout_method = signal) live in pyproject.toml"
affects: [20-02 filtered PR branch, 20-03 push and PR, 20-04 seeded break, 20-05 toolchain bump, 31-delivery-and-identity]

# Tech tracking
tech-stack:
  added: [pytest-timeout>=2.4.0]
  patterns:
    - "GitHub Actions pinned to full 40-hex commit SHAs with a trailing full-version comment"
    - "libsane-dev apt step before uv sync --locked in every CI job"
    - "uv sync --locked everywhere -- a stale lockfile must turn CI red"

key-files:
  created: [.github/workflows/ci.yml, .github/dependabot.yml, CONTRIBUTING.md]
  modified: [pyproject.toml, uv.lock]

key-decisions:
  - "pytest-timeout ini keys and the dependency landed in one commit -- --strict-config makes an unknown ini key a hard error on every pytest invocation"
  - "timeout_method = signal, not thread -- thread calls os._exit(1) and kills teardown plus the rest of the suite"
  - "Workflow-level permissions: contents: read; no job escalates (T-20-01)"
  - "concurrency cancel-in-progress only on pull_request, so master runs are never cancelled mid-flight"
  - "No cache or python-version inputs to setup-uv -- enable-cache defaults to auto and the default cache-dependency-glob already covers pyproject.toml and uv.lock"
  - "CONTRIBUTING.md follows README.md's GitHub markdown dialect, not the mkdocs-material dialect used under docs/"

patterns-established:
  - "The five checks are spelled identically in .pre-commit-config.yaml, ci.yml and CONTRIBUTING.md"
  - "Action pins are paired with a Dependabot ecosystem entry in the same commit, so pins are maintained rather than frozen"

requirements-completed: [CI-01, TEST-07]

# Metrics
duration: 6min
completed: 2026-09-10
---

# Phase 20 Plan 01: CI Gate -- Local Half Summary

**Five-check GitHub Actions gate on `master` and every PR, SHA-pinned with Dependabot upkeep, plus a `pytest-timeout` hang guard and a root `CONTRIBUTING.md` that reproduces the gate command-for-command.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-09-10T13:08:59Z
- **Completed:** 2026-09-10T13:15:20Z
- **Tasks:** 3
- **Files modified:** 5

## Accomplishments

- `pytest-timeout` 2.4.0 installed as a plain dev requirement with `timeout = 60` / `timeout_method = "signal"` live under `--strict-config`; suite green at 332 passed / 8 deselected.
- `.github/workflows/ci.yml` created: `push: branches: [master]` plus a bare `pull_request:`, two parallel jobs (`lint`, `test`) covering all five checks, `libsane-dev` apt step before `uv sync --locked` in both jobs, both actions pinned to full commit SHAs, workflow-level `permissions: contents: read`, `timeout-minutes` on both jobs, PR-only `cancel-in-progress`.
- `.github/dependabot.yml` landed in the same commit as `ci.yml` so the pins stay maintained (D-18).
- `CONTRIBUTING.md` (97 lines) at the repo root documents all five D-04 content requirements plus the `bypass_actors: []` consequence and the expected Dependabot PRs against `release.yml` / `docs.yml`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Install and configure pytest-timeout** - `bef7ab4` (chore) -- `pyproject.toml`, `uv.lock` only
2. **Task 2: Create ci.yml and dependabot.yml** - `38541c9` (ci) -- both files, one commit
3. **Task 3: Write the root CONTRIBUTING.md** - `a8f9d97` (docs)

## Files Created/Modified

- `.github/workflows/ci.yml` - The five-check gate: `lint` job (ruff check, ruff format --check, ty, pyrefly) and `test` job (pytest -m "not browser")
- `.github/dependabot.yml` - Weekly `github-actions` ecosystem updates from `/`
- `CONTRIBUTING.md` - Contributor-facing description of the gate, setup, pre-flight and PR policy
- `pyproject.toml` - `pytest-timeout>=2.4.0` in `[dependency-groups].dev`; `timeout` / `timeout_method` appended to `[tool.pytest.ini_options]`
- `uv.lock` - Regenerated by `uv add --dev pytest-timeout`

## TEST-07 evidence -- hang guard demonstration (success criterion 3)

Captured verbatim from a throwaway two-test file (one 30s sleep, one trivial pass) run under
the session scratchpad with `uv run pytest <file> -o timeout=3`. The file was deleted
immediately afterwards and is not committed:

```text
============================= test session starts ==============================
platform linux -- Python 3.14.2, pytest-9.0.2, pluggy-1.6.0
rootdir: /tmp/claude-1000/-home-kris-git-saneless/.../scratchpad
plugins: anyio-4.12.1, playwright-0.7.2, base-url-2.1.0, timeout-2.4.0
timeout: 3.0s
timeout method: signal
timeout func_only: False
collected 2 items

.../scratchpad/test_hang_demo.py F                                       [ 50%]
.                                                                        [100%]

=================================== FAILURES ===================================
__________________________________ test_hangs __________________________________

    def test_hangs() -> None:
        """Sleep far longer than the timeout so pytest-timeout kills it."""
>       time.sleep(30)
E       Failed: Timeout (>3.0s) from pytest-timeout.

.../scratchpad/test_hang_demo.py:8: Failed
=========================== short test summary info ============================
FAILED .../scratchpad/test_hang_demo.py::test_hangs
========================= 1 failed, 1 passed in 3.01s ==========================
```

The hung test failed with a per-test traceback pointing at the exact `time.sleep(30)` line,
`timeout method: signal` is confirmed in the header, and `1 failed, 1 passed` proves the
second test still ran after the kill -- which is precisely what `timeout_method = "thread"`
would have prevented.

## Verification

Full five-check sequence, run locally on the locked `ruff` 0.15.7 / `ty` 0.0.24 / `pyrefly` 0.57.1:

```text
uv run ruff check .            -> No issues found
uv run ruff format --check .   -> 37 files already formatted
uv run ty check                -> All checks passed!
uv run pyrefly check src tests           -> INFO 0 errors
uv run pytest -m "not browser" -> 332 passed, 8 deselected in 26.78s
```

`uv run prek run` exits 0. Every Task 2 grep acceptance criterion passed: `main`=0,
`branches: [master]`=1, each of the five commands=1, `libsane-dev`=2 (both before their job's
`uv sync --locked` at lines 26<28 and 43<45), `uv sync --locked`=2, bare `uv sync`=0, SHA pins=4,
version-tag pins=0, `contents: read`=1, `contents: write`=0, `continue-on-error`/`|| true`=0,
`timeout-minutes`=2, both files parse as YAML. `git status --porcelain` on `release.yml` and
`docs.yml` prints nothing -- both are byte-identical to their pre-plan state (D-06).

## Decisions Made

- Stripped the `# D-15` inline comment from the RESEARCH example's dev-list entry and the
  `# D-12` / `# D-13` comments from the ini keys: neither matches the file's existing
  no-inline-comment convention, and `uv add --dev` does not emit one.
- Kept `uv add --dev`'s natural append position at the end of the dev list rather than
  re-sorting -- the list is deliberately unsorted and a sort would produce an unrelated diff.
- `CONTRIBUTING.md` links to `README.md#system-requirements` for the distro table instead of
  reproducing it a third time.
- `CONTRIBUTING.md` avoids the literal string `pre-commit run` entirely (the phrase "drop-in
  replacement for the older hook runner" is used instead) so `prek` is the only documented form.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. `pytest-timeout` installed cleanly into the project `.venv` as research predicted, and
the ini keys were accepted on the first `--strict-config` run.

## Threat Model Coverage

- T-20-01 (EoP, token scope): mitigated -- workflow-level `permissions: { contents: read }`, verified by grep.
- T-20-02 (Tampering, action supply chain): mitigated -- 4 full-SHA pins with version comments plus `dependabot.yml` in the same commit.
- T-20-03 (DoS, hung test): mitigated -- `timeout = 60` fires identically locally and in CI, `timeout-minutes` bounds apt and `uv sync` too.
- T-20-04 / T-20-SC (package legitimacy): `pytest-timeout` 2.4.0 is the pytest-dev project's own plugin, audited `[OK]` in RESEARCH.md; installed 2.4.0 exactly as audited, so no legitimacy checkpoint was required.
- T-20-05 (Info disclosure): accepted as planned -- `pull_request`, not `pull_request_target`; no secrets referenced.

No new security surface was introduced beyond the threat register, so there are no threat flags.

## Known Stubs

None.

## User Setup Required

None in this plan. The branch ruleset (`bypass_actors: []`) that `CONTRIBUTING.md` describes is
created out-of-tree in Plan 02 -- the document deliberately describes the end state of the phase.

## Next Phase Readiness

- `ci.yml` exists in the tree, which is Plan 02's precondition: the filtered PR branch must be
  built on a commit that already carries the workflow, or the pull request has no workflow on its
  head to trigger.
- All five checks are green on the currently-locked toolchain, which is the precondition D-17
  depends on before the Plan 05 bump is allowed to move.
- Nothing was pushed to any remote and no tags were created, per this plan's hard constraints.

---
*Phase: 20-ci-gate*
*Completed: 2026-09-10*

## Self-Check: PASSED

All four artifact paths exist on disk and all three task commits are present in `git log`.
