---
phase: 19-write-user-facing-docs
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - mkdocs.yml
  - docs/index.md
  - docs/how-to/configure-scan-profiles.md
  - docs/how-to/set-up-adf-duplex.md
autonomous: false
requirements:
  - DOC-GS-04
  - DOC-FW-01
  - DOC-FW-02

must_haves:
  truths:
    - "Getting Started is the first nav section after Home in mkdocs.yml"
    - "auto_source_mode is documented in configure-scan-profiles.md and set-up-adf-duplex.md"
    - "paper_size is documented in configure-scan-profiles.md"
    - "Tutorials section is removed from nav"
    - "Old tutorial file is deleted"
    - "mkdocs build --strict passes with zero errors"
  artifacts:
    - path: "mkdocs.yml"
      provides: "Updated nav with Getting Started first"
      contains: "Getting Started"
    - path: "docs/index.md"
      provides: "Updated landing page featuring Getting Started"
      contains: "getting-started"
    - path: "docs/how-to/configure-scan-profiles.md"
      provides: "auto_source_mode and paper_size documentation"
      contains: "auto_source_mode"
  key_links:
    - from: "mkdocs.yml"
      to: "docs/getting-started/quick-start.md"
      via: "nav entry"
      pattern: "getting-started/quick-start"
    - from: "docs/index.md"
      to: "docs/getting-started/quick-start.md"
      via: "markdown link"
      pattern: "getting-started/quick-start"
    - from: "docs/how-to/configure-scan-profiles.md"
      to: "docs/reference/configuration.md"
      via: "see also link"
      pattern: "reference/configuration"
---

<objective>
Wire the Getting Started section into navigation, weave auto_source_mode and paper_size into existing how-to guides, update the landing page, clean up the old tutorial, and validate the build.

Purpose: Per D-01 Getting Started must be first nav item. Per D-05/D-06 features must be documented in existing guides. The old Tutorials section is removed per D-04.
Output: Updated mkdocs.yml, index.md, two how-to guides, deleted old tutorial, passing mkdocs build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-write-user-facing-docs-including-a-full-getting-started-section-that-walks-a-new-user-through-setup-and-first-scan-using-the-di-taxis-model/19-CONTEXT.md
@.planning/phases/19-write-user-facing-docs-including-a-full-getting-started-section-that-walks-a-new-user-through-setup-and-first-scan-using-the-di-taxis-model/19-RESEARCH.md
@.planning/phases/19-write-user-facing-docs-including-a-full-getting-started-section-that-walks-a-new-user-through-setup-and-first-scan-using-the-di-taxis-model/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weave auto_source_mode and paper_size into how-to guides</name>
  <files>docs/how-to/configure-scan-profiles.md, docs/how-to/set-up-adf-duplex.md</files>
  <read_first>
    - docs/how-to/configure-scan-profiles.md (current content to extend)
    - docs/how-to/set-up-adf-duplex.md (current content to extend)
    - docs/reference/configuration.md (verify field names and defaults for accuracy)
    - src/saneless/config.py (verify auto_source_mode values: "flatbed", "adf")
    - src/saneless/paper_sizes.py (verify paper size presets: full, a3, a4, a5, letter, legal)
  </read_first>
  <action>
**configure-scan-profiles.md** (per D-05, D-06):

1. Add `auto_source_mode` and `paper_size` rows to the "Profile field reference" table. Insert after the `auto_generated` row:
   ```
   | `auto_source_mode` | string | `"flatbed"` | When source is `"Auto"`: `"flatbed"` for single-page or `"adf"` for multi-page feeder |
   | `paper_size` | string | `"full"` | Constrain scan area: `"full"`, `"a3"`, `"a4"`, `"a5"`, `"letter"`, `"legal"` |
   ```

2. Add a new "## Auto source" section AFTER the "## Source values" section. Content:
   ```markdown
   ## Auto source

   Some scanners expose only an `Auto` source instead of separate `Flatbed` and `ADF` entries.
   When you set `source = "Auto"`, saneless needs to know whether to treat it as a single-page
   flatbed scan or a multi-page ADF scan. The `auto_source_mode` field controls this:

   | `auto_source_mode` | Behavior |
   |---|---|
   | `"flatbed"` (default) | Single page, like Flatbed |
   | `"adf"` | Multi-page feeder, like ADF |

   ```toml
   [profiles.auto-scan]
   source = "Auto"
   auto_source_mode = "adf"
   resolution = 300
   mode = "Color"
   ```

   See [Configuration reference](../reference/configuration.md) for all profile fields.
   ```

3. Add a new "## Paper size" section AFTER the "## Auto source" section (and before "## Auto-generated profiles"). Content:
   ```markdown
   ## Paper size

   By default, saneless scans the entire scanner bed. If your documents are a standard size,
   set `paper_size` to crop the scan area automatically:

   ```toml
   [profiles.letters]
   source = "ADF"
   resolution = 300
   mode = "Color"
   paper_size = "letter"
   ```

   Available presets: `full` (default -- entire bed), `a3`, `a4`, `a5`, `letter`, `legal`.

   When your scanner supports SANE geometry options, saneless sets the scan area at the hardware
   level. Otherwise, it crops the image after scanning.

   See [Configuration reference](../reference/configuration.md) for all profile fields.
   ```

**set-up-adf-duplex.md** (per D-05):

Add an admonition at the END of the "## The three ADF modes" section, before "## Empty page detection":

```markdown
!!! info "Auto source scanners"
    If your scanner reports only an `Auto` source instead of `ADF` or `ADF Duplex`, you can
    route it to multi-page ADF behavior by setting `auto_source_mode = "adf"` in your profile.
    See [Configure Scan Profiles](configure-scan-profiles.md#auto-source) for details.
```
  </action>
  <verify>
    <automated>grep -q "auto_source_mode" docs/how-to/configure-scan-profiles.md && grep -q "paper_size" docs/how-to/configure-scan-profiles.md && grep -q "auto_source_mode" docs/how-to/set-up-adf-duplex.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/how-to/configure-scan-profiles.md contains "## Auto source" as a section heading
    - docs/how-to/configure-scan-profiles.md contains "## Paper size" as a section heading
    - docs/how-to/configure-scan-profiles.md contains "auto_source_mode" in the profile field reference table
    - docs/how-to/configure-scan-profiles.md contains "paper_size" in the profile field reference table
    - docs/how-to/configure-scan-profiles.md contains '"flatbed"' and '"adf"' as auto_source_mode values
    - docs/how-to/configure-scan-profiles.md contains '"a4"' and '"letter"' as paper_size presets
    - docs/how-to/configure-scan-profiles.md contains 'source = "Auto"' in a TOML example
    - docs/how-to/set-up-adf-duplex.md contains "auto_source_mode" somewhere in the file
    - docs/how-to/set-up-adf-duplex.md contains "Auto source scanners" (admonition title)
  </acceptance_criteria>
  <done>Both how-to guides document auto_source_mode and paper_size with TOML examples, value tables, and cross-links to the configuration reference.</done>
</task>

<task type="auto">
  <name>Task 2: Update nav, landing page, delete old tutorial, validate build</name>
  <files>mkdocs.yml, docs/index.md</files>
  <read_first>
    - mkdocs.yml (current nav to replace)
    - docs/index.md (current landing page to update)
    - docs/getting-started/quick-start.md (verify it exists from Plan 01)
    - docs/getting-started/first-cli-scan.md (verify it exists from Plan 01)
    - docs/getting-started/first-web-ui-scan.md (verify it exists from Plan 01)
  </read_first>
  <action>
**mkdocs.yml** (per D-01, D-04):

Replace the `nav:` section with this exact content (Getting Started FIRST after Home, Tutorials section REMOVED):

```yaml
nav:
  - Home: index.md
  - Getting Started:
    - Quick Start: getting-started/quick-start.md
    - First CLI Scan: getting-started/first-cli-scan.md
    - First Web UI Scan: getting-started/first-web-ui-scan.md
  - How-To Guides:
    - Install on Bare Metal: how-to/install-bare-metal.md
    - Deploy with Docker Compose: how-to/deploy-docker-compose.md
    - Configure Scan Profiles: how-to/configure-scan-profiles.md
    - Set Up ADF Duplex Scanning: how-to/set-up-adf-duplex.md
    - Scanner Host Discovery (Containers): how-to/scanner-host-discovery.md
    - Use the CLI for Scripting: how-to/cli-scripting.md
  - Reference:
    - CLI Commands: reference/cli-commands.md
    - Configuration (TOML): reference/configuration.md
    - Environment Variables: reference/environment-variables.md
    - Web API: reference/web-api.md
    - Docker: reference/docker.md
  - Explanation:
    - Architecture Overview: explanation/architecture.md
    - Empty Page Detection: explanation/empty-page-detection.md
    - Consume Directory Fallback: explanation/consume-directory-fallback.md
```

Keep ALL content above the `nav:` key unchanged (site_name, theme, markdown_extensions, etc.).

**docs/index.md:**

Replace the entire content with an updated landing page that features Getting Started prominently. The structure:

1. Title and description (keep existing opening paragraph)
2. New "## Getting Started" section (FIRST, replacing old Tutorials section):
   ```markdown
   ## Getting Started

   **New to saneless? Start here.** Get from installation to your first scan in minutes.

   - [Quick Start](getting-started/quick-start.md) -- Five-minute path from install to your first document in paperless-ngx.
   - [First CLI Scan](getting-started/first-cli-scan.md) -- Step-by-step CLI tutorial: install, configure, scan, verify.
   - [First Web UI Scan](getting-started/first-web-ui-scan.md) -- Walk through the web interface to scan a document.
   ```
3. Keep "## How-To Guides" section as-is (no changes to guide list)
4. Keep "## Reference" section as-is
5. Keep "## Explanation" section as-is

**Delete old tutorial:**

Delete `docs/tutorials/scan-your-first-document.md` and remove the `docs/tutorials/` directory.

**Validate build:**

Run `uv run mkdocs build --strict` and confirm it exits 0 with no warnings about broken links or missing files.
  </action>
  <verify>
    <automated>cd /home/kris/git/saneless && uv run mkdocs build --strict 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mkdocs.yml contains "Getting Started:" as a nav section
    - mkdocs.yml contains "getting-started/quick-start.md" in nav
    - mkdocs.yml contains "getting-started/first-cli-scan.md" in nav
    - mkdocs.yml contains "getting-started/first-web-ui-scan.md" in nav
    - mkdocs.yml does NOT contain "Tutorials:" as a nav section
    - mkdocs.yml does NOT contain "scan-your-first-document"
    - docs/index.md contains "## Getting Started" section
    - docs/index.md contains "getting-started/quick-start.md" (link to Quick Start)
    - docs/index.md does NOT contain "tutorials/scan-your-first-document"
    - docs/tutorials/scan-your-first-document.md does NOT exist
    - `uv run mkdocs build --strict` exits 0
  </acceptance_criteria>
  <done>Nav has Getting Started first, Tutorials removed. Landing page updated. Old tutorial deleted. mkdocs build --strict passes clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify documentation site</name>
  <files>none</files>
  <action>
Human verification of the rendered documentation site. Start the dev server and visually confirm all pages render correctly.

What was built: Complete Getting Started section with 3 pages, feature documentation for auto_source_mode and paper_size woven into how-to guides, updated navigation and landing page.

How to verify:
1. Run `uv run mkdocs serve` and open http://127.0.0.1:8000
2. Verify "Getting Started" is the first nav section (after Home)
3. Click Quick Start -- should show Docker-first install with tabbed content
4. Click First CLI Scan -- should mention Python 3.14 and pipx (NOT 3.12 or pip)
5. Click First Web UI Scan -- should describe profile dropdown, title, tags, correspondent, scan button
6. Navigate to How-To > Configure Scan Profiles -- scroll to "Auto source" and "Paper size" sections
7. Navigate to How-To > Set Up ADF Duplex -- look for "Auto source scanners" info box
8. Verify no "Tutorials" section exists in the nav

Resume signal: Type "approved" or describe issues.
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually confirmed all documentation pages render correctly and navigation is properly structured.</done>
</task>

</tasks>

<verification>
- `uv run mkdocs build --strict` exits 0 with zero warnings
- Getting Started appears as first nav section after Home
- All three getting-started pages render correctly
- auto_source_mode documented in configure-scan-profiles.md and set-up-adf-duplex.md
- paper_size documented in configure-scan-profiles.md
- No broken cross-links
- No references to Python 3.12 or `pip install saneless` anywhere in docs/getting-started/
</verification>

<success_criteria>
- mkdocs build --strict passes clean
- Getting Started is first nav section with 3 pages
- Feature documentation woven into 2 existing how-to guides
- Old tutorial deleted, no Tutorials nav section
- Landing page features Getting Started prominently
</success_criteria>

<output>
After completion, create `.planning/phases/19-write-user-facing-docs-including-a-full-getting-started-section-that-walks-a-new-user-through-setup-and-first-scan-using-the-di-taxis-model/19-02-SUMMARY.md`
</output>
