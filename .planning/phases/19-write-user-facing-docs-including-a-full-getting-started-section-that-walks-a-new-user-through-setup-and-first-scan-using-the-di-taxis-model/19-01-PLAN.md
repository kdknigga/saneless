---
phase: 19-write-user-facing-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/getting-started/quick-start.md
  - docs/getting-started/first-web-ui-scan.md
  - docs/getting-started/first-cli-scan.md
autonomous: true
requirements:
  - DOC-GS-01
  - DOC-GS-02
  - DOC-GS-03

must_haves:
  truths:
    - "Quick Start page exists with Docker + Web UI as primary path and bare metal as tab alternative"
    - "First Web UI Scan page describes all web UI form elements with realistic examples"
    - "First CLI Scan page is the relocated tutorial with Python 3.14, pipx, and mentions of auto_source_mode and paper_size"
  artifacts:
    - path: "docs/getting-started/quick-start.md"
      provides: "5-minute Docker-first getting started"
      contains: "docker run"
    - path: "docs/getting-started/first-web-ui-scan.md"
      provides: "Web UI scan walkthrough"
      contains: "profile dropdown"
    - path: "docs/getting-started/first-cli-scan.md"
      provides: "Relocated and refreshed CLI tutorial"
      contains: "Python 3.14"
  key_links:
    - from: "docs/getting-started/quick-start.md"
      to: "docs/getting-started/first-cli-scan.md"
      via: "relative link"
      pattern: "first-cli-scan"
    - from: "docs/getting-started/quick-start.md"
      to: "docs/getting-started/first-web-ui-scan.md"
      via: "relative link"
      pattern: "first-web-ui-scan"
---

<objective>
Create the three Getting Started pages: Quick Start (new), First Web UI Scan (new), and First CLI Scan (relocated + refreshed tutorial).

Purpose: Per D-01 and D-02, the Getting Started section is the first thing new users see. These pages provide the 5-minute onboarding path and detailed walkthroughs for both interfaces.
Output: Three markdown files in docs/getting-started/ ready for nav wiring in Plan 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-write-user-facing-docs-including-a-full-getting-started-section-that-walks-a-new-user-through-setup-and-first-scan-using-the-di-taxis-model/19-CONTEXT.md
@.planning/phases/19-write-user-facing-docs-including-a-full-getting-started-section-that-walks-a-new-user-through-setup-and-first-scan-using-the-di-taxis-model/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Quick Start and First Web UI Scan pages</name>
  <files>docs/getting-started/quick-start.md, docs/getting-started/first-web-ui-scan.md</files>
  <read_first>
    - docs/how-to/deploy-docker-compose.md (Docker run/compose patterns to align with)
    - docs/how-to/install-bare-metal.md (bare metal install patterns)
    - docs/reference/configuration.md (config field names for accuracy)
    - src/saneless/web/templates/index.html (actual Web UI elements to document accurately)
    - docs/how-to/scanner-host-discovery.md (for network scanner tip in Quick Start)
  </read_first>
  <action>
Create `docs/getting-started/` directory and two new pages.

**quick-start.md** (per D-01, D-02, D-03):
- Title: "Quick Start"
- Opening line: "Get from zero to your first scanned document in paperless-ngx in under five minutes."
- Section: "## Prerequisites" -- one-liner list: SANE-compatible scanner with saned running, a running paperless-ngx instance with API token, Docker or Python 3.14
- Section: "## Install" -- tabbed content (per D-03, Docker tab FIRST, bare metal tab second):
  - Docker tab: `docker run -p 8080:8080 -v ./config.toml:/etc/saneless/config.toml:ro -e SANELESS_SCANNER__HOST=192.168.1.50 ghcr.io/kris-knigga/saneless:latest` with note about scanner host
  - Bare metal tab: system headers install (Debian + Fedora snippets), then `pipx install saneless` (NOT pip -- per research pitfall), then `saneless serve`
- Section: "## Configure" -- minimal `saneless.toml` with `[paperless]` section only (url + token), mention config search paths briefly with link to Configuration reference
- Section: "## Scan" -- two sub-paths:
  - "### Web UI" (primary per D-03): "Open `http://localhost:8080` in your browser. Select a profile, enter a title, and click **Scan**." Link to [First Web UI Scan](first-web-ui-scan.md) for the full walkthrough.
  - "### CLI": `saneless scan --title "My First Scan"`. Link to [First CLI Scan](first-cli-scan.md) for the full walkthrough.
- Section: "## Verify" -- open paperless-ngx, search for title, confirm document arrived
- Section: "## Next steps" -- links to First CLI Scan, First Web UI Scan, Configure Scan Profiles, Deploy with Docker Compose (use relative links: `first-cli-scan.md`, `first-web-ui-scan.md`, `../how-to/configure-scan-profiles.md`, `../how-to/deploy-docker-compose.md`)
- Admonition: `!!! tip "Network scanners"` after the Docker install tab about SANELESS_SCANNER__HOST, linking to `../how-to/scanner-host-discovery.md`
- No screenshots (D-08). All commands copy-pasteable (D-09). Realistic values (D-10): IP 192.168.1.50, token "abc123def456".

**first-web-ui-scan.md** (per D-02):
- Title: "First Web UI Scan"
- Opening: "Walk through every element of the saneless web interface to scan a document and verify it in paperless-ngx."
- Section: "## Prerequisites" (per D-11): saneless running (`saneless serve` or Docker container), browser on the same network, paperless-ngx accessible with a valid API token in config
- Section: "## Open the web UI" -- navigate to `http://<host>:8080` (replace with actual host IP or localhost)
- Section: "## Fill in scan details" -- describe each form element based on actual index.html template:
  1. **Profile dropdown** (`<select name="profile">`) -- select from configured profiles, defaults to "default"
  2. **Title** (`<input name="title">`) -- document title, auto-generated if left empty
  3. **Tags** (`<select name="tags" multiple>`) -- multi-select loaded from paperless-ngx, click refresh icon to reload
  4. **Correspondent** (`<select name="correspondent">`) -- single select with "No correspondent" default, click refresh icon to reload
- Section: "## Start the scan" -- click Scan button, button disables with "Scanning..." text while job runs
- Section: "## Monitor progress" -- status area shows states: scanning, assembling, uploading, done (or error). First-page thumbnail appears once first page is scanned. For manual duplex: flip prompt with Continue/Cancel.
- Section: "## Check job history" -- job history table at the bottom shows Time, Profile, Title, Status columns
- Section: "## Verify in paperless-ngx" -- open paperless-ngx, search for the document title
- Section: "## Next steps" -- links to Configure Scan Profiles, Set Up ADF Duplex, CLI Scripting
- No screenshots (D-08). Text descriptions of UI elements only. Realistic example: title "Electricity Bill March 2026", profile "default".
  </action>
  <verify>
    <automated>test -f docs/getting-started/quick-start.md && test -f docs/getting-started/first-web-ui-scan.md && grep -q "docker run" docs/getting-started/quick-start.md && grep -q "profile dropdown" docs/getting-started/first-web-ui-scan.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/getting-started/quick-start.md exists and contains "docker run"
    - docs/getting-started/quick-start.md contains '=== "Docker"' (Docker tab first)
    - docs/getting-started/quick-start.md contains '=== "Bare metal"' (bare metal tab second)
    - docs/getting-started/quick-start.md contains "pipx install saneless" (NOT "pip install saneless")
    - docs/getting-started/quick-start.md contains "Python 3.14"
    - docs/getting-started/quick-start.md contains "first-web-ui-scan.md" (cross-link)
    - docs/getting-started/quick-start.md contains "first-cli-scan.md" (cross-link)
    - docs/getting-started/quick-start.md does NOT contain any image/screenshot references
    - docs/getting-started/first-web-ui-scan.md exists and contains "Profile dropdown" or "profile dropdown"
    - docs/getting-started/first-web-ui-scan.md contains "Tags" and "Correspondent"
    - docs/getting-started/first-web-ui-scan.md contains "## Prerequisites"
    - docs/getting-started/first-web-ui-scan.md contains "8080"
  </acceptance_criteria>
  <done>Both new Getting Started pages exist with accurate content matching the actual web UI and Docker deployment patterns. Quick Start has Docker-first tabbed install. Web UI Scan describes all form elements.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate and refresh the CLI tutorial</name>
  <files>docs/getting-started/first-cli-scan.md</files>
  <read_first>
    - docs/tutorials/scan-your-first-document.md (source file to relocate)
    - docs/reference/configuration.md (verify config field names for accuracy)
    - src/saneless/config.py (verify auto_source_mode and paper_size field names)
    - src/saneless/paper_sizes.py (verify paper size preset names for accuracy)
  </read_first>
  <action>
Copy `docs/tutorials/scan-your-first-document.md` to `docs/getting-started/first-cli-scan.md` and apply these changes (per D-04):

1. **Title:** Change from "Scan Your First Document" to "First CLI Scan"
2. **Opening paragraph:** Update to focus on CLI workflow specifically: "This tutorial walks you through scanning your first document using the saneless command line."
3. **Prerequisites:** Change "Python 3.12 or later" to "Python 3.14 or later"
4. **Step 1 bare metal tab:** Change `pip install saneless` to `pipx install saneless` (matching the install how-to guide pattern)
5. **Relative links:** Update all `../how-to/` links to stay as `../how-to/` (same relative depth from getting-started/ as from tutorials/). Update `../reference/` links similarly. These paths are the SAME since both tutorials/ and getting-started/ are one level deep under docs/.
6. **Add auto_source_mode mention** (natural insertion point -- after the "Next steps" section or in Step 4 as a tip):
   Add an admonition after Step 4:
   ```
   !!! tip "Auto source scanners"
       If your scanner only exposes an "Auto" source, you can control whether it behaves as flatbed or ADF by setting `auto_source_mode` in your profile. See [Configure Scan Profiles](../how-to/configure-scan-profiles.md).
   ```
7. **Add paper_size mention** (natural insertion point -- in Step 4 or as a tip):
   Add an admonition:
   ```
   !!! tip "Paper size"
       To avoid scanning the entire scanner bed, set `paper_size` in your profile (e.g., `paper_size = "a4"` or `paper_size = "letter"`). See [Configure Scan Profiles](../how-to/configure-scan-profiles.md).
   ```
8. **Next steps:** Add link to First Web UI Scan: `- [First Web UI Scan](first-web-ui-scan.md) -- Scan documents from the browser interface.` as the first item.

Do NOT delete `docs/tutorials/scan-your-first-document.md` yet -- Plan 02 handles deletion and nav cleanup together.
  </action>
  <verify>
    <automated>grep -q "Python 3.14" docs/getting-started/first-cli-scan.md && grep -q "pipx install saneless" docs/getting-started/first-cli-scan.md && grep -q "auto_source_mode" docs/getting-started/first-cli-scan.md && grep -q "paper_size" docs/getting-started/first-cli-scan.md && grep -q "first-web-ui-scan" docs/getting-started/first-cli-scan.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/getting-started/first-cli-scan.md exists
    - docs/getting-started/first-cli-scan.md contains "First CLI Scan" as title (# First CLI Scan)
    - docs/getting-started/first-cli-scan.md contains "Python 3.14" (NOT "Python 3.12")
    - docs/getting-started/first-cli-scan.md does NOT contain "Python 3.12"
    - docs/getting-started/first-cli-scan.md contains "pipx install saneless" (NOT "pip install saneless")
    - docs/getting-started/first-cli-scan.md does NOT contain "pip install saneless" (without pipx prefix)
    - docs/getting-started/first-cli-scan.md contains "auto_source_mode"
    - docs/getting-started/first-cli-scan.md contains "paper_size"
    - docs/getting-started/first-cli-scan.md contains "first-web-ui-scan" (cross-link to Web UI page)
    - docs/getting-started/first-cli-scan.md contains "../how-to/configure-scan-profiles.md" (valid relative link)
  </acceptance_criteria>
  <done>CLI tutorial relocated to getting-started/ with Python 3.14, pipx, auto_source_mode/paper_size mentions, and cross-link to Web UI Scan page. Original file still exists for Plan 02 cleanup.</done>
</task>

</tasks>

<verification>
All three Getting Started pages exist in docs/getting-started/ with correct content, cross-links between pages, and no references to Python 3.12 or pip.
</verification>

<success_criteria>
- Three files exist: docs/getting-started/quick-start.md, docs/getting-started/first-web-ui-scan.md, docs/getting-started/first-cli-scan.md
- Quick Start has Docker-first tabbed install with pipx for bare metal
- Web UI Scan describes all 7 form elements from the actual template
- CLI Scan has Python 3.14, pipx, auto_source_mode mention, paper_size mention
- All cross-links use correct relative paths (../how-to/, sibling .md files)
</success_criteria>

<output>
After completion, create `.planning/phases/19-write-user-facing-docs-including-a-full-getting-started-section-that-walks-a-new-user-through-setup-and-first-scan-using-the-di-taxis-model/19-01-SUMMARY.md`
</output>
