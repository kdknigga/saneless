---
phase: 22-job-store-hardening
plan: 06
type: execute
wave: 6
depends_on: ["22-05"]
files_modified:
  - docs/reference/configuration.md
  - docs/explanation/architecture.md
autonomous: true
requirements: [STOR-01, STOR-02, STOR-03, STOR-04, STOR-05]

must_haves:
  truths:
    - "The docs no longer claim prune() keeps completed job history; they say it keeps all job history by age and row cap"
    - "docs/explanation/architecture.md has a persistence section describing the schema version ladder and the single-connection lock"
    - "No documentation sentence claims any of the six new columns has a consumer"
    - "The whole non-browser suite is green and all four static gates report zero findings with zero suppressions"
  artifacts:
    - path: "docs/reference/configuration.md"
      provides: "corrected history_retention_days / history_max_rows descriptions"
      contains: "history_retention_days"
    - path: "docs/explanation/architecture.md"
      provides: "a Job Storage section between Worker Thread Model and Configuration Layer"
      contains: "Job Storage"
  key_links:
    - from: "docs/explanation/architecture.md"
      to: "the behaviour shipped in plans 22-01..22-05"
      via: "prose describing the migration ladder and the RLock, with no claimed consumer for the six new columns"
      pattern: "user_version|schema version"
---

<objective>
Correct the documentation sentences this phase made false, add the persistence section the docs never had, and prove the whole phase green.

Purpose: the ROADMAP goal's "with the storage docs updated in-phase" clause, and the phase-level integration gate from `22-VALIDATION.md`'s last verification row. D-31 established that the "storage documentation" the goal promises does not exist as a page — what exists is four scattered sentences plus a structural gap in `architecture.md`, and `docs/reference/configuration.md:48-49`'s "completed job history" is the one sentence that is actually wrong today.

Output: two corrected/extended doc pages and a recorded full-suite + four-gate green run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-job-store-hardening/22-CONTEXT.md
@.planning/phases/22-job-store-hardening/22-PATTERNS.md
@.planning/phases/22-job-store-hardening/22-VALIDATION.md
@.planning/phases/22-job-store-hardening/22-01-SUMMARY.md
@.planning/phases/22-job-store-hardening/22-05-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct the storage prose and add the persistence section</name>
  <files>docs/reference/configuration.md, docs/explanation/architecture.md</files>
  <read_first>
    - docs/reference/configuration.md lines 40-55 (the `[output]` field table — note the `| field | type | default | description |` shape, sentence-case descriptions, and no trailing period on any row)
    - docs/explanation/architecture.md lines 43-62 (§ Worker Thread Model and § Configuration Layer — the new section slots between them, and `:49` is currently the whole of what the page says about SQLite)
    - .planning/phases/22-job-store-hardening/22-PATTERNS.md § "`docs/reference/configuration.md` — the one wrong sentence" and § "`docs/explanation/architecture.md` — the structural gap" (the house style list there is binding: `##` for a subsystem, prose not bullets, ` -- ` and NEVER an em dash, backticked identifiers, explain why not what, relative markdown cross-links)
    - .planning/phases/22-job-store-hardening/22-CONTEXT.md D-31 and § "Claude's Discretion" last bullet (the exact prose is the planner's, subject to one hard constraint)
    - docs/reference/docker.md line 30 and docs/reference/cli-commands.md line 85 (read to CONFIRM whether either is now false; only edit what is actually wrong)
    - .planning/phases/22-job-store-hardening/22-04-SUMMARY.md (for what `prune()` actually does now, so the corrected sentence describes the shipped behaviour rather than the plan's intent)
  </read_first>
  <action>
    In `docs/reference/configuration.md`, fix the two rows at `:48-49`. `history_retention_days` and
    `history_max_rows` currently say "completed job history" and "job history entries in SQLite",
    but `prune()` deletes by `created_at` regardless of terminal state, so it does not keep
    *completed* history -- it keeps *all* history. Rewrite both description cells to say so. Keep
    the four-column table shape, sentence case, and no trailing period, matching every other row in
    the file.

    In `docs/explanation/architecture.md`, add a `## Job Storage` section between
    `## Worker Thread Model` and `## Configuration Layer`. Cover, in prose paragraphs:
    the single SQLite connection owned exclusively by `JobStore`; the `threading.RLock` that
    serialises every public method, and why it exists -- the web request threads and the worker
    thread share one connection, so writes must be serialised; and the `PRAGMA user_version` schema
    version ladder, which builds the schema from nothing on a fresh database, joins partway on an
    older one, and refuses to open a database whose shape it does not recognise rather than
    silently continuing until the next write crashes. Explain WHY the loud refusal is the right
    default -- that is the register this page is written in (`:5` -- "This page explains how
    saneless is structured and why it works the way it does").

    Use ` -- ` for parenthetical dashes and no em dashes anywhere. Backtick identifiers.
    Cross-link `[Configuration Reference](../reference/configuration.md)` for the retention settings
    if it reads naturally.

    HARD CONSTRAINT: do not describe `outcome`, `pages_scanned`, `pages_removed`, `pages_uploaded`,
    `warning` or `owner_token` as having a consumer. They are columns that exist and that nothing
    writes yet. Either omit them from the prose entirely, or name them once as reserved for the
    result-recording work that follows. Do not imply the UI shows page counts, do not imply
    `owner_token` authenticates anything -- REQUIREMENTS.md § Out of Scope is explicit that it is a
    footgun guard for a later flip prompt, not an auth mechanism.

    Also do NOT document the database path as moving, and do NOT centralize or describe a shared
    database-path helper. `tmp_dir / "saneless.db"` is still computed in two places
    (`src/saneless/cli.py:227`, `src/saneless/web/app.py:59`) and moving it onto `output.data_dir` is
    Phase 23's, deliberately (D-18). The docs must keep describing `tmp_dir` as where the database
    lives, because that is still true.

    Check `docs/reference/docker.md:30` and `docs/reference/cli-commands.md:85` against the shipped
    behaviour. Edit them ONLY if this phase made them false; if they are still accurate, leave them
    alone and say so in the SUMMARY. Phase 31's DOCS-01 is the milestone-wide documentation audit
    and this phase is not it.

    Commit: `docs(22-06): correct job history retention prose and document job storage`
  </action>
  <verify>
    <automated>uv run mkdocs build --strict 2>/dev/null || echo "mkdocs not configured for strict build; falling back to link/style checks"; command grep -c '—' docs/explanation/architecture.md docs/reference/configuration.md</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `command grep -c 'completed job history' docs/reference/configuration.md` returns 0.
    - Source assertion: `command grep -c '## Job Storage' docs/explanation/architecture.md` returns 1, and it appears between the `## Worker Thread Model` and `## Configuration Layer` headings.
    - Source assertion: `command grep -c '—' docs/explanation/architecture.md docs/reference/configuration.md` returns 0 for both files -- no em dashes (house style).
    - Source assertion: `command grep -in 'owner_token\|pages_scanned\|pages_removed\|pages_uploaded' docs/ -r` returns either nothing, or only lines that describe them as not yet written by anything. No line may claim a consumer.
    - Source assertion: `command grep -rn 'data_dir' docs/reference/configuration.md` returns nothing new -- the Phase 23 database move is not pre-announced (D-18).
    - CLI output: `uv run mkdocs build --strict` completes without error if the project builds docs that way; otherwise record in the SUMMARY that no docs build gate exists.
    - Git: a commit whose subject matches `^docs\(22-06\)` exists.
  </acceptance_criteria>
  <done>The one false retention sentence is corrected, `architecture.md` has a Job Storage section in house style, and no doc claims a consumer for any of the six new columns.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-level green gate and requirement traceability check</name>
  <files>(no source changes -- verification only)</files>
  <read_first>
    - .planning/phases/22-job-store-hardening/22-VALIDATION.md § "Per-Task Verification Map" (every row must now be reachable and green) and § "Validation Sign-Off"
    - .planning/ROADMAP.md § "Phase 22: Job Store Hardening" (the five success criteria this run is proving)
    - .planning/phases/22-job-store-hardening/22-01-SUMMARY.md through 22-05-SUMMARY.md (the commit lists and any recorded divergences)
  </read_first>
  <action>
    Run the full gate exactly as `22-VALIDATION.md`'s last verification row specifies, in one
    invocation so a failure anywhere fails the whole thing:
    `uv run pytest -m "not browser" -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`.

    Then run each named command from the per-task verification map individually and record the
    result against its row: `-k migration`, `-k migration_legacy`, `-k migration_guard`,
    `-k migration_idempotent`, `-k journal_mode`, `-k columns`, `-k outcome_roundtrip`,
    `-k locked_coverage`, `-k no_public_self_call`, `-k stress`, `-k prune`, `-k prune_shuffled`,
    `-k prune_concurrent`, `-k single_mapping`, `-k fail_active`, `-k list_pending`. Every one must
    select at least one test -- a `-k` expression that matches nothing exits zero and would report a
    false green, so check the collected count, not just the exit status.

    Confirm the suppression count did not grow:
    `command grep -rn 'noqa\|type: ignore' src/ tests/ | command grep -vc '^$'` against the
    pre-phase baseline recorded in `.planning/STATE.md` / Phase 20's notes. This phase must add zero.
    SWP-08 is separately committed to REMOVING the tree's existing suppressions, so any increase here
    is a regression against a live commitment.

    Confirm scope containment: `git diff --stat` for the whole phase must touch only
    `src/saneless/job.py`, `src/saneless/exceptions.py`, `tests/test_job.py`, the two doc pages, and
    `.planning/`. Any change to `worker.py`, `web/app.py`, `web/routes.py` or `cli.py` means
    something out of scope was folded in -- flag it rather than accepting it.

    Confirm requirement traceability: each of STOR-01..STOR-05 is claimed by at least one plan's
    frontmatter and has at least one green test. Update the `Status` column in
    `22-VALIDATION.md`'s verification map, set `nyquist_compliant: true` and `wave_0_complete: true`
    in its frontmatter, and update `.planning/ROADMAP.md`'s traceability table rows for STOR-01..05
    from Pending to Complete.

    If any check fails, do not paper over it: record the failure and stop. This task's value is that
    it is allowed to say no.

    Commit: `docs(22-06): record phase 22 verification results`
  </action>
  <verify>
    <automated>uv run pytest -m "not browser" -q && uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: `uv run pytest -m "not browser" -q` reports zero failures and a collected count at or above the Phase 20 baseline of 332.
    - CLI output: `uv run ruff check .`, `uv run ruff format --check .`, `uv run ty check` and `uv run pyrefly check src tests` each report zero findings.
    - CLI output: every `-k` expression in `22-VALIDATION.md`'s map collects at least one test (verified from the pytest collection summary, not from exit status alone).
    - Source assertion: `command grep -rc 'noqa\|type: ignore' src/saneless/job.py src/saneless/exceptions.py tests/test_job.py` returns 0 for all three.
    - Source assertion: `git diff --stat $(git merge-base HEAD master)..HEAD -- src/ tests/ docs/` lists only `src/saneless/job.py`, `src/saneless/exceptions.py`, `tests/test_job.py`, `docs/reference/configuration.md` and `docs/explanation/architecture.md`.
    - Source assertion: `22-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`, and every Status cell in its map is green.
    - Source assertion: `.planning/ROADMAP.md`'s traceability rows for STOR-01..STOR-05 read Complete.
  </acceptance_criteria>
  <done>The full suite and all four static gates are green with zero added suppressions, every VALIDATION map row is recorded, scope containment is confirmed, and STOR-01..STOR-05 are traceably complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → operator expectations | A false sentence about what the job store retains or what a column means is a defect with the same consequences as a code defect: the operator plans around it. |
| phase output → later phases | Phases 23, 26 and 30 build on the columns and methods this phase ships; a doc that overstates them invites a later phase to assume behaviour that does not exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-18 | Spoofing (of capability) | prose describing `owner_token` in `docs/` | mitigate | No documentation sentence may present `owner_token` as an authentication or authorisation mechanism. REQUIREMENTS.md § Out of Scope fixes it as a footgun guard for the Phase 30 flip prompt on a trusted LAN. An operator who read it as auth would deploy the appliance on an untrusted network. Enforced by the grep in Task 1's acceptance criteria. |
| T-22-19 | Repudiation | `docs/reference/configuration.md` retention rows | mitigate | The current text claims `prune()` keeps *completed* job history. It deletes by `created_at` regardless of terminal state, so an operator relying on the documented behaviour would lose in-flight and errored job records they expected to keep. Corrected in Task 1. |
| T-22-20 | Tampering | the phase-level green gate | mitigate | The gate is one chained invocation, and every `-k` expression is checked for a non-zero collected count -- a `-k` that matches nothing exits zero and would report a false green. Suppression count and diff scope are both asserted, so a green achieved by adding a `# noqa` or by silently editing an out-of-scope module cannot pass. |
| T-22-SC | Tampering | npm/pip/cargo installs | accept | This phase installs no packages. Everything used is CPython stdlib or already pinned (RESEARCH § Package Legitimacy Audit); `slopcheck` was correctly not run and no legitimacy checkpoint is required. |
</threat_model>

<verification>
- `uv run pytest -m "not browser" -q`
- `uv run ruff check . && uv run ruff format --check . && uv run ty check && uv run pyrefly check src tests`
- Every `-k` command in `.planning/phases/22-job-store-hardening/22-VALIDATION.md` § Per-Task Verification Map
- `command grep -rn 'noqa\|type: ignore' src/saneless/job.py src/saneless/exceptions.py tests/test_job.py`
</verification>

<success_criteria>
- The ROADMAP goal's "with the storage docs updated in-phase" clause is satisfied, and the one sentence that was actually wrong is corrected.
- No doc claims a consumer for any of the six new columns, and no doc pre-announces Phase 23's database move.
- All five ROADMAP success criteria for Phase 22 are demonstrated by named, green, automated tests.
- Zero suppressions added across the whole phase.
</success_criteria>

<output>
Create `.planning/phases/22-job-store-hardening/22-06-SUMMARY.md` when done, including the recorded results for every VALIDATION map row and an explicit note of any doc page checked and deliberately left unchanged.
</output>
