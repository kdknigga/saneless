---
phase: 09-enable-pytest-strict-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_browser.py
  - tests/test_job.py
  - tests/test_logging.py
  - tests/test_scanner.py
  - pyproject.toml
autonomous: true
requirements:
  - TQUAL-01
  - TQUAL-02

must_haves:
  truths:
    - "ty check runs on tests/ and passes clean"
    - "pyrefly check runs on tests/ and passes clean"
    - "All 6 type: ignore comments in test_scanner.py are eliminated"
    - "All 226 existing tests still pass"
  artifacts:
    - path: "pyproject.toml"
      provides: "ty and pyrefly exclusion removal"
      contains: "no exclude tests/ lines"
    - path: "tests/test_scanner.py"
      provides: "Concrete stub classes replacing mock reassignment"
      contains: "class _FakeSaneDevice"
    - path: "tests/test_browser.py"
      provides: "Fixed method override signatures"
      contains: "def get_capabilities(self, device_id: str)"
    - path: "tests/test_job.py"
      provides: "None-narrowing assertions"
      contains: "assert fetched is not None"
    - path: "tests/test_logging.py"
      provides: "Explicit logging.handlers import"
      contains: "import logging.handlers"
  key_links:
    - from: "pyproject.toml"
      to: "tests/"
      via: "ty and pyrefly now include tests"
      pattern: "no exclude.*tests"
---

<objective>
Fix all type checker errors in test files and remove ty/pyrefly exclusions so both type checkers cover tests/ alongside src/.

Purpose: This is the foundation for test quality parity -- type checkers must see test code before annotations and docstrings can be enforced.
Output: ty and pyrefly run clean on all test files; zero type: ignore comments in tests.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-enable-pytest-strict-mode-and-ensure-all-tests-are-properly-type-annotated-and-pass-all-of-the-same-linting-and-type-checking-quality-checks-as-the-normal-code/09-CONTEXT.md
@.planning/phases/09-enable-pytest-strict-mode-and-ensure-all-tests-are-properly-type-annotated-and-pass-all-of-the-same-linting-and-type-checking-quality-checks-as-the-normal-code/09-RESEARCH.md

<interfaces>
<!-- ScannerBackend ABC signatures that test stubs must match exactly -->

From src/saneless/scanner/base.py:
```python
class ScannerBackend(ABC):
    @abstractmethod
    def get_devices(self) -> list[DeviceInfo]: ...

    @abstractmethod
    def get_capabilities(self, device_id: str) -> DeviceCapabilities: ...

    @abstractmethod
    def scan_pages(self, device_id: str, settings: ScanSettings) -> Iterator[Image.Image]: ...
```

Key: parameter names are `device_id` and `settings` (not underscore-prefixed).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix type checker errors in test_browser.py, test_job.py, test_logging.py</name>
  <read_first>
    - tests/test_browser.py
    - tests/test_job.py
    - tests/test_logging.py
    - src/saneless/scanner/base.py
  </read_first>
  <files>tests/test_browser.py, tests/test_job.py, tests/test_logging.py</files>
  <action>
**test_browser.py** -- Fix invalid-method-override errors (ty line 56, 64; pyrefly line 56, 64):
- Line 56: Rename `_device_id` to `device_id` in `get_capabilities(self, device_id: str) -> DeviceCapabilities:`
- Line 64-65: Rename `_device_id` to `device_id` and `_settings` to `settings` in `scan_pages(self, device_id: str, settings: ScanSettings) -> Iterator[Image.Image]:`
- The parameters are unused in the stub but MUST match the ABC parent signatures exactly (no underscore prefix).

**test_job.py** -- Fix missing-attribute errors (pyrefly lines 114, 145):
- Line 114: Add `assert fetched is not None` before `assert fetched.error_category == ErrorCategory.SCANNER`
- Line 145: Add `assert fetched is not None` before `assert fetched.error_category == ErrorCategory.CONFIG`
- This narrows the `JobRecord | None` return type from `get_job()` so pyrefly sees the `.error_category` attribute.

**test_logging.py** -- Fix implicit-import warnings (pyrefly lines 30, 70, 100):
- Add `import logging.handlers` as a top-level import after `import logging` (line 3).
- This makes the `logging.handlers` submodule explicitly available rather than relying on implicit import.
  </action>
  <verify>
    <automated>cd /home/kris/git/saneless && uv run ty check tests/test_browser.py tests/test_job.py tests/test_logging.py && uv run pyrefly check tests/test_browser.py tests/test_job.py tests/test_logging.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_browser.py contains `def get_capabilities(self, device_id: str)` (no underscore prefix)
    - tests/test_browser.py contains `def scan_pages(self, device_id: str, settings: ScanSettings)` (no underscore prefix)
    - tests/test_job.py contains `assert fetched is not None` on lines before both `.error_category` assertions
    - tests/test_logging.py contains `import logging.handlers` as a top-level import
    - `uv run ty check tests/test_browser.py tests/test_job.py tests/test_logging.py` exits 0
    - `uv run pyrefly check tests/test_browser.py tests/test_job.py tests/test_logging.py` exits 0
  </acceptance_criteria>
  <done>Three test files pass both ty and pyrefly with zero errors or warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Replace mock patterns in test_scanner.py with concrete stubs, remove ty/pyrefly exclusions</name>
  <read_first>
    - tests/test_scanner.py
    - src/saneless/scanner/base.py
    - src/saneless/scanner/sane_backend.py
    - pyproject.toml
  </read_first>
  <files>tests/test_scanner.py, pyproject.toml</files>
  <action>
**test_scanner.py** -- Eliminate all 6 `type: ignore` comments by replacing mock reassignment with concrete fake classes:

1. Create a `_FakeSaneDevice` class near the top of the file (after the existing mock helpers section):
```python
class _FakeSaneDevice:
    """Fake SANE device for testing scan_pages behavior."""

    def __init__(
        self,
        *,
        multi_scan: Callable[[], Iterator[Image.Image]] | None = None,
        cancel: Callable[[], None] | None = None,
    ) -> None:
        self.multi_scan = multi_scan or (lambda: iter([]))
        self.cancel = cancel or (lambda: None)
```

2. Replace each `mock_dev.multi_scan = _raising_multi_scan  # type: ignore[assignment]` pattern:
   - Line 471: Create `_FakeSaneDevice(multi_scan=_raising_multi_scan)` instead of assigning to mock
   - Line 612: Create `_FakeSaneDevice(multi_scan=_blocking_iterator)` instead of assigning to mock
   - Line 658: Create `_FakeSaneDevice(multi_scan=_error_iterator)` instead of assigning to mock
   - Lines 705-706: Create `_FakeSaneDevice(multi_scan=_tracking_multi_scan, cancel=_tracking_cancel)` instead of two separate assignments

3. For each replacement, update the `mock_sane_module._mock_dev` assignment to use the new `_FakeSaneDevice` instance:
   - Pattern: Where test creates `mock_dev = mock_sane_module._mock_dev`, replace with creating a new `_FakeSaneDevice` and assigning `mock_sane_module._mock_dev = fake_dev`

4. Line 225 (`ScannerBackend()  # type: ignore[abstract]`): This tests that the ABC cannot be instantiated. Replace with a comment explaining it's testing ABC enforcement -- the type: ignore is justified here but per CONTEXT.md user decision, eliminate ALL type: ignore. Use `object.__new__(ScannerBackend)` or simply accept the expected TypeError by calling `ScannerBackend()` and letting the executor determine the cleanest approach that satisfies both ty and pyrefly without a type: ignore comment.

**pyproject.toml** -- Remove type checker exclusions:
- Delete line 138: `exclude = ["tests/"]` from `[tool.ty.src]` section (leave the section header if other keys exist, otherwise the empty section is fine)
- Delete line 141: `project_excludes = ["tests/"]` from `[tool.pyrefly]` section

After removal, the sections should look like:
```toml
[tool.ty.src]

[tool.pyrefly]
```

Run `uv run ty check` and `uv run pyrefly check` (full project, not just test files) to verify both pass clean on entire codebase including tests/. Fix any additional errors that surface when type checkers first see all test files.
  </action>
  <verify>
    <automated>cd /home/kris/git/saneless && uv run ty check && uv run pyrefly check && uv run pytest -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_scanner.py contains `class _FakeSaneDevice` with `multi_scan` and `cancel` attributes
    - tests/test_scanner.py contains zero occurrences of `type: ignore`
    - pyproject.toml `[tool.ty.src]` section does NOT contain `exclude = ["tests/"]`
    - pyproject.toml `[tool.pyrefly]` section does NOT contain `project_excludes = ["tests/"]`
    - `uv run ty check` exits 0 (full project including tests/)
    - `uv run pyrefly check` exits 0 (full project including tests/)
    - `uv run pytest -x` exits 0 with all 226 tests passing
  </acceptance_criteria>
  <done>All type: ignore comments eliminated from test_scanner.py. Both ty and pyrefly run on tests/ and pass clean. All 226 tests pass.</done>
</task>

</tasks>

<verification>
- `uv run ty check` passes clean (includes tests/)
- `uv run pyrefly check` passes clean (includes tests/)
- `uv run pytest` passes with all 226 tests green
- `grep -r "type: ignore" tests/` returns zero matches
- `grep "exclude.*tests" pyproject.toml` returns zero matches
</verification>

<success_criteria>
Both type checkers cover tests/ directory. Zero type: ignore comments in test files. All 226 existing tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-enable-pytest-strict-mode-and-ensure-all-tests-are-properly-type-annotated-and-pass-all-of-the-same-linting-and-type-checking-quality-checks-as-the-normal-code/09-01-SUMMARY.md`
</output>
