---
phase: 01-core-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/saneless/__init__.py
  - src/saneless/exceptions.py
  - src/saneless/config.py
  - src/saneless/logging_config.py
  - tests/conftest.py
  - tests/test_config.py
  - tests/test_logging.py
autonomous: true
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - PROF-01
  - PROF-02
  - LOG-01
  - LOG-02

must_haves:
  truths:
    - "Settings load from a TOML file with env var overrides using SANELESS_ prefix"
    - "Invalid config fails at startup with clear parse errors listing missing required fields"
    - "A default profile must always be present; missing it raises a validation error"
    - "Rotating log file is created at configurable path with configurable level"
    - "No credentials are hardcoded; API token comes from config file or SANELESS_PAPERLESS__TOKEN env var"
  artifacts:
    - path: "src/saneless/config.py"
      provides: "Pydantic Settings with TOML + env var loading"
      exports: ["Settings", "ScannerConfig", "PaperlessConfig", "OutputConfig", "ProfileConfig", "load_settings"]
    - path: "src/saneless/exceptions.py"
      provides: "Custom exception hierarchy"
      exports: ["SanelessError", "ConfigError", "ScanError", "PaperlessError"]
    - path: "src/saneless/logging_config.py"
      provides: "Logging setup with RotatingFileHandler"
      exports: ["configure_logging"]
    - path: "tests/conftest.py"
      provides: "Shared test fixtures for all test modules"
    - path: "tests/test_config.py"
      provides: "Config loading and validation tests"
    - path: "tests/test_logging.py"
      provides: "Logging configuration tests"
  key_links:
    - from: "src/saneless/config.py"
      to: "pydantic-settings"
      via: "BaseSettings with SettingsConfigDict"
      pattern: "class Settings\\(BaseSettings\\)"
    - from: "src/saneless/config.py"
      to: "TOML config file"
      via: "toml_file parameter"
      pattern: "toml_file"
    - from: "src/saneless/logging_config.py"
      to: "src/saneless/config.py"
      via: "reads OutputConfig for log path and level"
      pattern: "RotatingFileHandler"
---

<objective>
Install project dependencies, create the configuration layer (pydantic-settings with TOML + env vars), exception hierarchy, and logging setup. This is the foundation every other module imports.

Purpose: All subsequent plans depend on Settings for configuration and the exception hierarchy for error handling. Building this first eliminates circular dependency risk.
Output: Working config loading with validation, logging setup, exception classes, and test infrastructure.
</objective>

<execution_context>
@/home/kris/.claude/get-shit-done/workflows/execute-plan.md
@/home/kris/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-pipeline/01-CONTEXT.md
@.planning/phases/01-core-pipeline/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create exception hierarchy + config module</name>
  <files>pyproject.toml, src/saneless/__init__.py, src/saneless/exceptions.py, src/saneless/config.py, tests/conftest.py, tests/test_config.py</files>
  <read_first>
    - pyproject.toml (current dependency state)
    - src/saneless/__init__.py (current entry point stub)
    - .planning/phases/01-core-pipeline/01-RESEARCH.md (Pattern 1: pydantic-settings config, Pattern 5: Click CLI config loading)
    - .planning/codebase/CONVENTIONS.md (naming, imports, docstrings)
  </read_first>
  <behavior>
    - test_load_settings_from_toml: Write a TOML file with scanner.host="192.168.1.50", paperless.url="http://paperless:8000", paperless.token="abc123" -> Settings object has those values
    - test_env_var_override: Set SANELESS_SCANNER__HOST="10.0.0.1" env var -> overrides TOML value for scanner.host
    - test_env_prefix: Set SANELESS_PAPERLESS__TOKEN="envtoken" -> settings.paperless.token == "envtoken"
    - test_default_profile_required: Config with profiles dict missing "default" key -> raises ValidationError with message containing "default"
    - test_default_profile_present: Config with profiles containing "default" -> loads successfully
    - test_profile_fields: Profile with source="Flatbed", resolution=600, mode="color" -> ProfileConfig has those values
    - test_config_file_search_explicit: Pass explicit config_path -> loads from that path, ignores search paths
    - test_config_file_search_fallback: No explicit path, file exists at ./saneless.toml -> loads from there
    - test_no_config_file: No config files exist -> loads with defaults + env vars only
    - test_invalid_toml: Malformed TOML content -> raises error (not silent failure)
    - test_settings_defaults: No config, no env -> scanner.host=="", paperless.url=="", output.tmp_dir=="/tmp/saneless", output.log_level=="INFO"
    - test_nested_env_delimiter: SANELESS_OUTPUT__LOG_LEVEL="DEBUG" -> settings.output.log_level == "DEBUG"
    - test_exception_hierarchy: ScanError, ConfigError, PaperlessError all inherit from SanelessError which inherits from Exception
  </behavior>
  <action>
    1. Install runtime dependencies:
       `uv add click "pydantic-settings[toml]" python-sane img2pdf Pillow httpx`

    2. Create `src/saneless/exceptions.py` with custom exception hierarchy:
       - `SanelessError(Exception)` — base class for all saneless errors
       - `ConfigError(SanelessError)` — configuration loading/validation failures
       - `ScanError(SanelessError)` — scanner operation failures
       - `PaperlessError(SanelessError)` — paperless-ngx API failures
       Each class has a Google-style docstring. Module has `__all__` list.

    3. Create `src/saneless/config.py` with pydantic-settings models:
       - `ScannerConfig(BaseModel)`: host: str = "", device: str = ""
       - `PaperlessConfig(BaseModel)`: url: str = "", token: str = "", consume_dir: str = ""
       - `ProfileConfig(BaseModel)`: source: str = "Flatbed", resolution: int = 300, mode: str = "color", default_tags: list[int] = [], default_correspondent: int | None = None, default_title_template: str = ""
       - `OutputConfig(BaseModel)`: tmp_dir: str = "/tmp/saneless", log_file: str = "/var/log/saneless/saneless.log", log_level: str = "INFO", log_max_bytes: int = 10_485_760, log_backup_count: int = 5, history_retention_days: int = 7, history_max_rows: int = 500, paperless_task_timeout: int = 300
       - `Settings(BaseSettings)` with:
         - `model_config = SettingsConfigDict(env_prefix="SANELESS_", env_nested_delimiter="__")`
         - Note: Do NOT put toml_file in model_config. Handle TOML file loading in `load_settings()` function.
         - Fields: scanner: ScannerConfig, paperless: PaperlessConfig, output: OutputConfig, profiles: dict[str, ProfileConfig] with default {"default": ProfileConfig()}
         - `@field_validator("profiles")` that checks "default" in profiles dict, raises ValueError("A 'default' profile must be defined in config") if missing
       - `load_settings(config_path: str | None = None) -> Settings` function:
         - If config_path provided: load from that path using `Settings(_toml_file=Path(config_path))`
         - Otherwise search: Path("./saneless.toml"), Path.home() / ".config/saneless/config.toml", Path("/etc/saneless/config.toml")
         - First existing file wins: `Settings(_toml_file=path)`
         - No file found: `Settings()` (defaults + env vars only)
       Module has `__all__` list with all public symbols.

    4. Create `tests/conftest.py` with shared fixtures:
       - `tmp_config_dir(tmp_path)` fixture: creates a temp directory for config files
       - `sample_toml(tmp_config_dir)` fixture: writes a minimal valid TOML config to tmp_config_dir/saneless.toml with scanner.host, paperless.url, paperless.token, and a default profile
       - `clean_env(monkeypatch)` autouse fixture: removes all SANELESS_* env vars before each test

    5. Create `tests/test_config.py` with all behaviors listed above.

    6. Update `src/saneless/__init__.py`: keep `main()` stub but add import of Settings from config for convenience re-export.
  </action>
  <verify>
    <automated>cd /home/kris/git/saneless && uv run pytest tests/test_config.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml `dependencies` list contains "click", "pydantic-settings[toml]", "python-sane", "img2pdf", "Pillow", "httpx"
    - src/saneless/exceptions.py contains `class SanelessError(Exception):`
    - src/saneless/exceptions.py contains `class ConfigError(SanelessError):`
    - src/saneless/exceptions.py contains `class ScanError(SanelessError):`
    - src/saneless/exceptions.py contains `class PaperlessError(SanelessError):`
    - src/saneless/config.py contains `class Settings(BaseSettings):`
    - src/saneless/config.py contains `env_prefix="SANELESS_"`
    - src/saneless/config.py contains `env_nested_delimiter="__"`
    - src/saneless/config.py contains `def load_settings(`
    - src/saneless/config.py contains `"A 'default' profile must be defined`
    - tests/conftest.py exists and contains `def clean_env(`
    - tests/test_config.py exists and `uv run pytest tests/test_config.py -x` exits 0
  </acceptance_criteria>
  <done>All config tests pass. Settings loads from TOML with env var overrides. Default profile validation works. Exception hierarchy established.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create logging configuration module with tests</name>
  <files>src/saneless/logging_config.py, tests/test_logging.py</files>
  <read_first>
    - src/saneless/config.py (OutputConfig fields: log_file, log_level, log_max_bytes, log_backup_count)
    - .planning/codebase/CONVENTIONS.md (logging patterns)
  </read_first>
  <behavior>
    - test_configure_logging_creates_file_handler: Call configure_logging with a tmp log path -> RotatingFileHandler attached to root logger writing to that path
    - test_log_level_from_config: configure_logging with level="DEBUG" -> root logger level is DEBUG (10)
    - test_log_level_info: configure_logging with level="INFO" -> root logger level is INFO (20)
    - test_log_rotation_params: configure_logging with max_bytes=1000, backup_count=3 -> handler.maxBytes == 1000, handler.backupCount == 3
    - test_log_format_includes_timestamp_and_module: Write a log message -> log file content matches pattern r"\d{4}-\d{2}-\d{2}.*\w+.*INFO"
    - test_verbose_adds_stderr_handler: configure_logging with verbose=True -> StreamHandler writing to stderr attached
  </behavior>
  <action>
    Create `src/saneless/logging_config.py`:
    - `configure_logging(log_file: str, log_level: str = "INFO", max_bytes: int = 10_485_760, backup_count: int = 5, verbose: bool = False) -> None`
    - Creates parent directories for log_file path using `Path(log_file).parent.mkdir(parents=True, exist_ok=True)`
    - Creates `RotatingFileHandler(log_file, maxBytes=max_bytes, backupCount=backup_count)`
    - Sets formatter: `logging.Formatter("%(asctime)s %(levelname)-8s %(name)s %(message)s")`
    - Sets root logger level to `getattr(logging, log_level.upper())`
    - Adds file handler to root logger
    - If verbose=True, also adds a `StreamHandler(sys.stderr)` with same formatter to root logger
    - Module has `__all__ = ["configure_logging"]`

    Create `tests/test_logging.py` with all behaviors listed above. Use tmp_path for log file locations. Clean up handlers after each test to avoid leaking between tests.
  </action>
  <verify>
    <automated>cd /home/kris/git/saneless && uv run pytest tests/test_logging.py -x --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - src/saneless/logging_config.py contains `def configure_logging(`
    - src/saneless/logging_config.py contains `RotatingFileHandler`
    - src/saneless/logging_config.py contains `%(asctime)s`
    - tests/test_logging.py exists and `uv run pytest tests/test_logging.py -x` exits 0
  </acceptance_criteria>
  <done>Logging setup works with configurable file path, level, rotation params, and optional verbose stderr output. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /home/kris/git/saneless && uv run pytest tests/test_config.py tests/test_logging.py -v --tb=short
```
All config and logging tests green. Dependencies installed. Foundation modules importable.
</verification>

<success_criteria>
- `uv run python -c "from saneless.config import Settings, load_settings"` succeeds
- `uv run python -c "from saneless.exceptions import SanelessError, ConfigError, ScanError, PaperlessError"` succeeds
- `uv run python -c "from saneless.logging_config import configure_logging"` succeeds
- All tests in tests/test_config.py and tests/test_logging.py pass
- pyproject.toml has all 6 runtime dependencies listed
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-pipeline/01-01-SUMMARY.md`
</output>
